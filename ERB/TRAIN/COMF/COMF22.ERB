;-------------------------------------------------
;会话
;日常系指令、レベル2
;-------------------------------------------------
@COM22
PRINTL 聊天
STR:0 = 聊天


;-------------------------------------------------
;ソース追加処理
;-------------------------------------------------
;消費体力、气力
LOSEBASE:0 += 10
LOSEBASE:1 += 20

SOURCE:12 = 1000
SOURCE:17 = 100


;-------------------------------------------------
;经验の処理
;-------------------------------------------------
;爱情经验
IF (TALENT:3 == 1 || TALENT:6 == 1) && ASSIPLAY == 0
	PRINTS EXPNAME:23
	PRINTL +1
	EXP:23 += 1
ENDIF

;依赖度ベクトル
TFLAG:33 = 4

;依赖度補正値(恋慕で1、相亲相爱で2）
IF TALENT:3 && ASSIPLAY == 0
	TFLAG:30 += 1
ELSEIF TALENT:6 && ASSIPLAY == 0
	TFLAG:30 += 2
ENDIF


RETURN 1

