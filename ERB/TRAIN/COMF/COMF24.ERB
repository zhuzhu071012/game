;-------------------------------------------------
;身体接触
;日常系指令、レベル2
;-------------------------------------------------
@COM24

PRINTL 亲肤接触
STR:0 = 亲肤接触


;-------------------------------------------------
;ソース追加処理
;-------------------------------------------------
;消費体力、气力
LOSEBASE:0 += 25
LOSEBASE:1 += 25

SOURCE:4 = 500
SOURCE:5 = 500
SOURCE:17 = 100

;-------------------------------------------------
;特殊ソース処理
;-------------------------------------------------
;喜欢身体接触だと情爱のソース追加
IF TALENT:65
	SOURCE:4 += 200
ENDIF
;厌恶身体接触だと情爱のソース減少、郁闷のソース追加
IF TALENT:66
	SOURCE:4 -= 100
	SOURCE:16 = 200
ENDIF
;-------------------------------------------------
;经验の処理
;-------------------------------------------------
;爱情经验
IF (TALENT:3 == 1 || TALENT:6 == 1) && ASSIPLAY == 0
	PRINTS EXPNAME:23
	PRINTL +1
	EXP:23 += 1
ENDIF

;依赖度ベクトル
TFLAG:33 = 4

;依赖度補正値(恋慕で1、相亲相爱で2）
IF TALENT:3 && ASSIPLAY == 0
	TFLAG:30 += 1
ELSEIF TALENT:6 && ASSIPLAY == 0
	TFLAG:30 += 2
ENDIF


RETURN 1

