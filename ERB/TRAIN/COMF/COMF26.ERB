;-------------------------------------------------
;家务
;日常系指令、レベル3
;-------------------------------------------------
@COM26
PRINTL 做家务
STR:0 = 做家务


;-------------------------------------------------
;ソース追加処理
;-------------------------------------------------
;消費体力、气力
LOSEBASE:0 += 50
LOSEBASE:1 += 50

SOURCE:5 = 500
SOURCE:6 = 500
SOURCE:18 = 100


;-------------------------------------------------
;经验の処理
;-------------------------------------------------
;三角巾着用中はボー茄子
A = ABL:12/3
A += 1
B = 1
IF TEQUIP:7 == 4
	A += 1
	B += 1
ENDIF

PRINTFORML ２人的%EXPNAME:61%＋{A}
EXP:61 += A
EXP:MASTER:61 += A
PRINTFORML %EXPNAME:23%＋{B}
EXP:23 += B

;依赖度ベクトル
TFLAG:33 = 4

;依赖度補正値(恋慕で1、相亲相爱で2）
IF TALENT:3 && ASSIPLAY == 0
	TFLAG:30 += 1
ELSEIF TALENT:6 && ASSIPLAY == 0
	TFLAG:30 += 2
ENDIF


RETURN 1


