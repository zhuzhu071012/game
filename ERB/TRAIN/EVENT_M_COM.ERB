;调教時テ接吻ト
;-------------------------------------------------
;0,爱抚
;-------------------------------------------------
@MESSAGE_COM0
IF ASSIPLAY
	PRINTFORM %CALLNAME:ASSI%
ELSE
    PRINTFORM %CALLNAME:PLAYER%
ENDIF
;接吻をする場合
;助手プ雷の場合、男奴隶で、助手に同性倾向が2未満だと接吻しない。
SIF KANRAKU(TARGET) > 0 && (STAIN:0 < 2 || STAIN:0 == 16 || STAIN:0 == 17 || TALENT:MASTER:64) && TEQUIP:20 == 0 && TEQUIP:25 == 0 && (!ASSIPLAY || ABL:PLAYER:同性倾向 > 1 || !MALE(TARGET))
	PRINTFORM 与%CALLNAME%唇齿相交的同时，
;着衣プ雷
;触手の有無で分岐
IF TEQUIP:25
	PRINT 触手不断玩弄着
ELSE
	SIF !ASSIPLAY || ABL:PLAYER:同性倾向 > 1 || !MALE(TARGET)
		PRINTFORM 细致地
	PRINTFORM 爱抚着
ENDIF
PRINTFORM %CALLNAME%
IF TALENT:PLAYER:58 && (TEQUIP:2 || TEQUIP:3 || TEQUIP:4 || TEQUIP:5 || TEQUIP:6)
    PRINTFORM 穿着
	IF TEQUIP:6
		A = 6
	ELSEIF TEQUIP:5
		A = 5
	ELSEIF TEQUIP:3
		A = 3
	ELSEIF TEQUIP:4
		A = 4
	ELSE
		A = 2
	ENDIF
	CALL CLOTHES_NAME
	PRINTFORM 的
ENDIF
;矮小体型
IF TALENT:矮小体型
	PRINT 纤细
;高大体型
ELSEIF TALENT:高大体型
	PRINT 健壮
ENDIF
PRINT 的身体
;-------------------------------------------------
;1,舔阴
;-------------------------------------------------
@MESSAGE_COM1
IF ASSIPLAY
	PRINTFORM %CALLNAME:ASSI%
ELSE
    PRINTFORM %CALLNAME:PLAYER%
ENDIF
IF TALENT:PLAYER:58 && (TEQUIP:2 || TEQUIP:4 > 1 || TEQUIP:6 == 7)
	;下身外衣か布偶装を着ている
	IF TEQUIP:4 > 1 || TEQUIP:6 == 7
	PRINTFORM 隔着
		IF TEQUIP:4
			A = 4
		ELSE
			A = 6
		ENDIF
		CALL CLOTHES_NAME
	;下身内衣を着ている
	ELSEIF TEQUIP:2
		A = 2
		CALL CLOTHES_NAME
	ENDIF
	PRINTFORM 轻轻咬住
	;下身内衣を着ていない
	SIF TEQUIP:2 == 0
	    PRINTFORM 真空状态的
	PRINTFORM %CALLNAME%的阴唇来回舔舐…
ELSE
	;丝袜系
	IF TEQUIP:1 == 5 || TEQUIP:1 == 6
		PRINTFORM 沿着
		SIF TEQUIP:1 == 5
			PRINT 丝袜
		SIF TEQUIP:1 == 6
			PRINT 网袜
		PRINT 舔上去，最后
	ENDIF
	PRINTFORM 轻轻咬住%CALLNAME%的阴唇来回舔舐…
	;扶她＋情欲のPLAMLV3以上
	IF (TALENT:双性 || TALENT:扶她) && PALAM:6 >= PALAMLV:3
		PRINT 舌尖偶尔挑逗似的扫过
		SIF TALENT:包茎
			PRINT 裹着包皮的
		PRINT 阴茎
	;扶她
	ELSEIF (TALENT:双性 || TALENT:扶她)
		PRINT 同时也不忘照顾一旁
		SIF TALENT:包茎
			PRINT 裹着包皮
		PRINT 的阴茎
	;情欲のPALAMLV3以上
	ELSEIF PALAM:6 >= PALAMLV:3
		PRINT 最后吮吸了阴唇上方尖尖的阴蒂作为结束
	ENDIF
	PRINT …
ENDIF
;-------------------------------------------------
;2,口交
;-------------------------------------------------
@MESSAGE_COM2
IF ASSIPLAY
	PRINTFORM %CALLNAME:ASSI%
ELSE
    PRINTFORM %CALLNAME:PLAYER%
ENDIF
IF TALENT:PLAYER:58 && (TEQUIP:2 || TEQUIP:4 > 1 || TEQUIP:6 == 7)
	;下身外衣か布偶装を着ている
	IF TEQUIP:4 > 1 || TEQUIP:6 == 7
		PRINTFORM 叼住%CALLNAME%
		IF TEQUIP:4
			A = 4
		ELSE
			A = 6
		ENDIF
		CALL CLOTHES_NAME
	;下身内衣を着ている
	ELSEIF TEQUIP:2
		A = 2
		CALL CLOTHES_NAME
	ENDIF
	PRINTFORM 下
	;下身内衣を着ていない
	SIF TEQUIP:2 == 0
		PRINT 毫无遮挡物
	PRINTFORM 的阴茎活动起舌头来回舔舐…
ELSE
	;丝袜系
	IF TEQUIP:1 == 5 || TEQUIP:1 == 6
		PRINTFORM 隔着
		SIF TEQUIP:1 == 5
			PRINT 丝袜
		SIF TEQUIP:1 == 6
			PRINT 网袜
		PRINTFORM 叼住%CALLNAME%
		;情欲のPALAMLV3以上
		IF PALAM:6 >= PALAMLV:3
			PRINTFORM 已完全挺立
	    	IF TALENT:包茎
			    PRINT ，裹着包皮
    		    IF TALENT:孩童尺寸
		    	    PRINT 的小小
		        ELSEIF TALENT:短小
		        	PRINT 的短小
		        ELSEIF TALENT:巨根
		    	    PRINT 的粗大
		        ELSEIF TALENT:尺寸傲人
		    	    PRINT ，尺寸傲人的
		    	ELSE
		    	    PRINT 的
		        ENDIF
		    ELSE
    		    IF TALENT:孩童尺寸
		    	    PRINT 的小小
		        ELSEIF TALENT:短小
		        	PRINT 的短小
		        ELSEIF TALENT:巨根
		    	    PRINT 的粗大
		        ELSEIF TALENT:尺寸傲人
		    	    PRINT ，尺寸傲人的
		    	ELSE
		    	    PRINT 的
		        ENDIF
		    ENDIF
		ELSE
	    	IF TALENT:包茎
			    PRINT 裹着包皮
    		    IF TALENT:孩童尺寸
		    	    PRINT 的小小
		        ELSEIF TALENT:短小
		        	PRINT 的短小
		        ELSEIF TALENT:巨根
		    	    PRINT 的粗大
		        ELSEIF TALENT:尺寸傲人
		    	    PRINT ，尺寸傲人的
		    	ELSE
		    	    PRINT 的
		        ENDIF
		    ELSE
    		    IF TALENT:孩童尺寸
		    	    PRINT 小小的
		        ELSEIF TALENT:短小
		        	PRINT 短小的
		        ELSEIF TALENT:巨根
		    	    PRINT 粗大的
		        ELSEIF TALENT:尺寸傲人
		    	    PRINT 尺寸傲人的
		    	ELSE
		    	    PRINT 的
		        ENDIF
		    ENDIF
		ENDIF
		PRINT 阴茎活动起舌头来回舔舐…
	ELSE
		;奴隶が男で、调教者の技巧が3以上のテ接吻ト
		IF TALENT:140 && ABL:PLAYER:2 >= 3
			PRINTFORM 一边揉搓着%CALLNAME%的睾丸一边叼住他
			;情欲のPALAMLV3以上
			IF PALAM:6 >= PALAMLV:3
				PRINTFORM 已完全挺立
			    IF TALENT:孩童尺寸
				    PRINT 的小小
			    ELSEIF TALENT:短小
				    PRINT 的短小
			    ELSEIF TALENT:巨根
				    PRINT 的粗大
			    ELSEIF TALENT:尺寸傲人
				    PRINT ，尺寸傲人的
				ELSE
				    PRINT 的
			    ENDIF
			ELSE
			    IF TALENT:孩童尺寸
				    PRINT 小小的
			    ELSEIF TALENT:短小
				    PRINT 短小的
			    ELSEIF TALENT:巨根
				    PRINT 粗大的
			    ELSEIF TALENT:尺寸傲人
				    PRINT 尺寸傲人的
				ELSE
				    PRINT 的
			    ENDIF
			ENDIF
			PRINTFORM 阴茎活动起舌头来回舔舐…
		ELSE
			PRINTFORM 叼住%CALLNAME%
	    	;情欲のPALAMLV3以上
		    IF PALAM:6 >= PALAMLV:3
			    PRINTFORM 已完全挺立
	    	    IF TALENT:包茎
			        PRINT ，裹着包皮
    		        IF TALENT:孩童尺寸
    		            SIF TALENT:矮小体型 == 0
    		                PRINT ，与其体型不符
		    	        PRINT 的小小
		            ELSEIF TALENT:短小
		                SIF TALENT:高大体型
		                    PRINT ，与其高大体型不符
		            	PRINT 的短小
		            ELSEIF TALENT:巨根
		                SIF TALENT:矮小体型
		                    PRINT ，与其体型不符
		    	        PRINT 的粗大
		            ELSEIF TALENT:尺寸傲人
		                SIF TALENT:矮小体型
		                    PRINT ，与其体型不符
		    	        PRINT ，尺寸傲人的
		        	ELSE
		    	        PRINT 的
		            ENDIF
		        ELSE
    		        IF TALENT:孩童尺寸
    		            SIF TALENT:矮小体型 == 0
    		                PRINT ，与其体型不符
		    	        PRINT 的小小
		            ELSEIF TALENT:短小
		                SIF TALENT:高大体型
		                    PRINT ，与其高大体型不符
		            	PRINT 的短小
		            ELSEIF TALENT:巨根
		                SIF TALENT:矮小体型
		                    PRINT ，与其体型不符
		    	        PRINT 的粗大
		            ELSEIF TALENT:尺寸傲人
		                SIF TALENT:矮小体型
		                    PRINT ，与其体型不符
		    	        PRINT ，尺寸傲人的
		        	ELSE
		    	        PRINT 的
		            ENDIF
		        ENDIF
	    	ELSE
	        	IF TALENT:包茎
			        PRINT 裹着包皮
    		        IF TALENT:孩童尺寸
    		            SIF TALENT:矮小体型 == 0
    		                PRINT ，与其体型不符
		    	        PRINT 的小小
		            ELSEIF TALENT:短小
		                SIF TALENT:高大体型
		                    PRINT ，与其高大体型不符
		        	    PRINT 的短小
		            ELSEIF TALENT:巨根
		                SIF TALENT:矮小体型
		                    PRINT ，与其体型不符
		    	        PRINT 的粗大
		            ELSEIF TALENT:尺寸傲人
		                SIF TALENT:矮小体型
		                    PRINT ，与其体型不符
		    	        PRINT ，尺寸傲人的
		    	    ELSE
		    	        PRINT 的
		            ENDIF
		        ELSE
    		        IF TALENT:孩童尺寸
    		            SIF TALENT:矮小体型 == 0
    		                PRINT 与其体型不符
		    	        PRINT 的小小
		            ELSEIF TALENT:短小
		                SIF TALENT:高大体型
		                    PRINT 与其高大体型不符
		            	PRINT 的短小
		            ELSEIF TALENT:巨根
		                SIF TALENT:矮小体型
		                    PRINT 与其体型不符
		    	        PRINT 的粗大
		            ELSEIF TALENT:尺寸傲人
		                SIF TALENT:矮小体型
		                    PRINT 与其体型不符，
		    	        PRINT 尺寸傲人的
		    	    ELSE
		    	        PRINT 的
		            ENDIF
		        ENDIF
		    ENDIF
			PRINTFORM 阴茎活动起舌头来回舔舐…
		ENDIF
	ENDIF
ENDIF
;-------------------------------------------------
;3,手指插入
;-------------------------------------------------
@MESSAGE_COM3
IF ASSIPLAY
	PRINTFORM %CALLNAME:ASSI%
ELSE
    PRINTFORM %CALLNAME:PLAYER%
ENDIF
IF TALENT:PLAYER:58 && (TEQUIP:2 || TEQUIP:4 > 1 || TEQUIP:6 == 7)
    PRINTFORM 将手指按压在
	;下身外衣か布偶装を着ている
	IF TEQUIP:4 > 1 || TEQUIP:6 == 7
		IF TEQUIP:4
			A = 4
		ELSE
			A = 6
		ENDIF
		CALL CLOTHES_NAME
	;下身内衣を着ている
	ELSEIF TEQUIP:2
		A = 2
		CALL CLOTHES_NAME
	ENDIF
	PRINTFORM 下%CALLNAME%
	;下身内衣を着ていない
	SIF TEQUIP:2 == 0
	PRINT 光溜溜的
		;处女
	IF TALENT:0
		PRINTFORM 尚未被男人触碰过的
	;润滑LV4以上
	ELSEIF PALAM:4 >= PALAMLV:4
		PRINTFORM 已经分泌出爱液的
	ENDIF
	;矮小体型の有無で分岐
	IF TALENT:100
		PRINTFORM 娇嫩小穴
	ELSE
		PRINTFORM 秘裂
	ENDIF
	PRINTFORM 上描摹着边缘的形状，缓缓陷了进去…
ELSE
	;丝袜系
	IF TEQUIP:1 == 5 || TEQUIP:1 == 6
		IF TFLAG:55
			PRINT 将
			SIF TEQUIP:1 == 5
            	PRINT 丝袜
            SIF TEQUIP:1 == 6
            	PRINT 网袜
			PRINT 撕出一条缝，
		ELSE
			PRINT 沿着
			SIF TEQUIP:1 == 5
            	PRINT 丝袜
            SIF TEQUIP:1 == 6
            	PRINT 网袜
			PRINT 的破损处撕大开口，
		ENDIF
	ENDIF
	;自慰棒の有無で分岐
    SIF TEQUIP:13
    	PRINTFORM 抽出里面妖艳起伏着的自慰棒，
	PRINTFORM 将手指缓缓插入了%CALLNAME%
	;处女
	IF TALENT:0
		PRINTFORM 尚未被男人触碰过
	;润滑LV4以上
	ELSEIF PALAM:4 >= PALAMLV:4
		PRINTFORM 溢满了爱液
	ENDIF
	;矮小体型の有無で分岐
	IF TALENT:100
		PRINTFORM 的娇嫩小穴…
	ELSE
		PRINTFORM 的秘裂…
	ENDIF

ENDIF
;-------------------------------------------------
;4,舔肛
;-------------------------------------------------
@MESSAGE_COM4
IF ASSIPLAY
	PRINTFORM %CALLNAME:ASSI%
ELSE
    PRINTFORM %CALLNAME:PLAYER%
ENDIF
IF TALENT:PLAYER:58 && (TEQUIP:2 || TEQUIP:4 > 1 || TEQUIP:6 == 7)
	;下身外衣か布偶装を着ている
	IF TEQUIP:4 > 1 || TEQUIP:6 == 7
		PRINTFORM 将舌头插入
		IF TEQUIP:4
			A = 4
		ELSE
			A = 6
		ENDIF
		CALL CLOTHES_NAME
	;下身内衣を着ている
	ELSEIF TEQUIP:2
		A = 2
		CALL CLOTHES_NAME
	ENDIF
	PRINTFORM 下%CALLNAME%
	;下身内衣を着ていない
	SIF TEQUIP:2 == 0
	PRINT 光溜溜
	PRINTFORM 的肛穴来回舔舐…
ELSE
	PRINTFORM 把舌头插入%CALLNAME%
	;Ａ经验10以下
	SIF EXP:1 <= 10
		PRINTFORM 紧闭
	PRINTFORM 的肛穴来回舔舐
	;Ａ经验100以上＋欲望のPALAMLV3以上
	SIF EXP:1 >= 100 && PALAM:6 >= PALAMLV:3
		PRINTFORM ，在蠕动的肠道里色情地搅拌
	PRINTFORM …
ENDIF
;-------------------------------------------------
;5,肛门爱抚
;-------------------------------------------------
@MESSAGE_COM5
IF TALENT:PLAYER:58 && (TEQUIP:2 || TEQUIP:4 > 1 || TEQUIP:6 == 7)
    IF ASSIPLAY
        PRINTFORM %CALLNAME:ASSI%
    ELSE
        PRINTFORM %CALLNAME:PLAYER%
    ENDIF
	;下身外衣か布偶装を着ている
	IF TEQUIP:4 > 1 || TEQUIP:6 == 7
	    PRINTFORM 揉动着
		IF TEQUIP:4
			A = 4
		ELSE
			A = 6
		ENDIF
		CALL CLOTHES_NAME
	;下身内衣を着ている
	ELSEIF TEQUIP:2
		A = 2
		CALL CLOTHES_NAME
	ENDIF
	PRINTFORM 下%CALLNAME%
	;下身内衣を着ていない
	SIF TEQUIP:2 == 0
		PRINT 暴露出
	PRINTFORM 的肛门…
ELSE
    ;触手
    IF TEQUIP:25
    	PRINTFORM 满是粘液的触手
    	;丝袜系
    	IF TEQUIP:1 == 5 || TEQUIP:1 == 6
    		IF TFLAG:55
    			PRINT 沿着
    			SIF TEQUIP:1 == 5
                	PRINT 丝袜
                SIF TEQUIP:1 == 6
                	PRINT 网袜
    			PRINT 的破损处撕大开口，
    		ELSE
    			PRINT 将
    			SIF TEQUIP:1 == 5
                	PRINT 丝袜
                SIF TEQUIP:1 == 6
                	PRINT 网袜
    			PRINT 撕出一条缝，
    		ENDIF
    	ENDIF
    	PRINTFORM 玩弄着%CALLNAME%的肛门…
   	;肛塞
    ELSEIF TEQUIP:14
        IF ASSIPLAY
            PRINTFORM %CALLNAME:ASSI%
        ELSE
            PRINTFORM %CALLNAME:PLAYER%
        ENDIF
    	IF TEQUIP:1 == 5 || TEQUIP:1 == 6
    		IF TFLAG:55
    			PRINT 沿着
    			SIF TEQUIP:1 == 5
                	PRINT 丝袜
                SIF TEQUIP:1 == 6
                	PRINT 网袜
    			PRINT 的破损处撕大开口，
    		ELSE
    			PRINT 将
    			SIF TEQUIP:1 == 5
                	PRINT 丝袜
                SIF TEQUIP:1 == 6
                	PRINT 网袜
    			PRINT 撕出一条缝，
    		ENDIF
    	ENDIF
    	PRINTFORM 前后抽插着%CALLNAME%肛穴里的肛塞…
    ;后庭拉珠
    ELSEIF TEQUIP:15
        IF ASSIPLAY
            PRINTFORM %CALLNAME:ASSI%
        ELSE
            PRINTFORM %CALLNAME:PLAYER%
        ENDIF
    	IF TEQUIP:1 == 5 || TEQUIP:1 == 6
    		IF TFLAG:55
    			PRINT 沿着
    			SIF TEQUIP:1 == 5
                	PRINT 丝袜
                SIF TEQUIP:1 == 6
                	PRINT 网袜
    			PRINT 的破损处撕大开口，
    		ELSE
    			PRINT 将
    			SIF TEQUIP:1 == 5
                	PRINT 丝袜
                SIF TEQUIP:1 == 6
                	PRINT 网袜
    			PRINT 撕出一条缝，
    		ENDIF
    	ENDIF
    	PRINTFORM 轻轻拉动了肛门拉珠的绳子…
    ;灌肠器＋塞头
    ELSEIF TEQUIP:21
        IF ASSIPLAY
            PRINTFORM %CALLNAME:ASSI%
        ELSE
            PRINTFORM %CALLNAME:PLAYER%
        ENDIF
    	IF TEQUIP:1 == 5 || TEQUIP:1 == 6
    		IF TFLAG:55
    			PRINT 沿着
    			SIF TEQUIP:1 == 5
                	PRINT 丝袜
                SIF TEQUIP:1 == 6
                	PRINT 网袜
    			PRINT 的破损处撕大开口，
    		ELSE
    			PRINT 将
    			SIF TEQUIP:1 == 5
                	PRINT 丝袜
                SIF TEQUIP:1 == 6
                	PRINT 网袜
    			PRINT 撕出一条缝，
    		ENDIF
    	ENDIF
    	PRINTFORM 用肛塞狠狠挖了一下因排泄感而颤抖的肛穴…
    ;扩张气球
    ELSEIF TEQUIP:22
        IF ASSIPLAY
            PRINTFORM %CALLNAME:ASSI%
        ELSE
            PRINTFORM %CALLNAME:PLAYER%
        ENDIF
    	IF TEQUIP:1 == 5 || TEQUIP:1 == 6
    		IF TFLAG:55
    			PRINT 沿着
    			SIF TEQUIP:1 == 5
                	PRINT 丝袜
                SIF TEQUIP:1 == 6
                	PRINT 网袜
    			PRINT 的破损处撕大开口，
    		ELSE
    			PRINT 将
    			SIF TEQUIP:1 == 5
                	PRINT 丝袜
                SIF TEQUIP:1 == 6
                	PRINT 网袜
    			PRINT 撕出一条缝，
    		ENDIF
    	ENDIF
    	PRINTFORM 前后移动着把%CALLNAME%肛穴撑到极限的气球…
    ;肛门电极
    ELSEIF TEQUIP:23
        IF ASSIPLAY
            PRINTFORM %CALLNAME:ASSI%
        ELSE
            PRINTFORM %CALLNAME:PLAYER%
        ENDIF
    	IF TEQUIP:1 == 5 || TEQUIP:1 == 6
    		IF TFLAG:55
    			PRINT 沿着
    			SIF TEQUIP:1 == 5
                	PRINT 丝袜
                SIF TEQUIP:1 == 6
                	PRINT 网袜
    			PRINT 的破损处撕大开口，
    		ELSE
    			PRINT 将
    			SIF TEQUIP:1 == 5
                	PRINT 丝袜
                SIF TEQUIP:1 == 6
                	PRINT 网袜
    			PRINT 撕出一条缝，
    		ENDIF
    	ENDIF
    	PRINTFORM 搅动着%CALLNAME%屁股里发出卡兹卡兹声音的电极…
    ELSE
        IF ASSIPLAY
            PRINTFORM %CALLNAME:ASSI%
        ELSE
            PRINTFORM %CALLNAME:PLAYER%
        ENDIF
    	PRINTFORM 玩弄着%CALLNAME%的肛穴
    	;Ａ经验100以上＋欲望のPALAMLV3以上
    	SIF PALAM:6 >= PALAMLV:3 && ABL:5 >= 3
    		PRINTFORM ，指头插入了直肠深处
    PRINTFORM …
    ENDIF
ENDIF
;-------------------------------------------------
;6,揉胸
;-------------------------------------------------
@MESSAGE_COM6
IF ASSIPLAY
	PRINTFORM %CALLNAME:ASSI%
ELSE
    PRINTFORM %CALLNAME:PLAYER%
ENDIF
PRINTFORM 的手掌
;着衣プ雷
IF TALENT:PLAYER:58 && (TEQUIP:3 || TEQUIP:5 || TEQUIP:6)
	PRINTFORM 隔着
	IF TEQUIP:6
		A = 6
	ELSEIF TEQUIP:5
		A = 5
	ELSEIF TEQUIP:3
		A = 3
	ENDIF
	CALL CLOTHES_NAME
ENDIF
PRINTFORM 在%CALLNAME%那
;情欲のPALAMLV3以上
SIF PALAM:6 > PALAMLV:3
	PRINTFORM 因期待而颤抖的
;矮小体型＋贫乳
IF TALENT:100 && TALENT:109
	PRINTFORM 仅能感觉到膨胀程度的胸脯
;贫乳
ELSEIF TALENT:109
	PRINTFORM 小小的胸部
;巨乳
ELSEIF TALENT:111
	IF TALENT:140
		PRINTFORM 乳肉从手指缝隙里溢出的结实胸肌
	ELSE
		PRINTFORM 乳肉从手指缝隙里溢出的巨乳
	ENDIF
;爆乳
ELSEIF TALENT:112
	IF TALENT:140
		PRINTFORM 大到双手都无法容纳的发达胸肌
	ELSE
		PRINTFORM 大到双手都无法容纳的豪乳
	ENDIF
ELSEIF TALENT:137 || TALENT:140
	PRINTFORM 平坦的胸脯
ELSE
	PRINTFORM 鼓起的胸部
ENDIF
;母乳体质
IF TALENT:149
	;情欲のPALAMLV3以上＋恋慕or相亲相爱
	IF PALAM:6 > PALAMLV:3 && (TALENT:3 || TALENT:6)
		PRINTFORML 上用力地挤压揉动
		PRINTFORM %CALLNAME%呻吟着，挺起了自己
		;贫乳
		SIF TALENT:109
			PRINTFORM 保守的
		;巨乳
		IF TALENT:111
			IF TALENT:140
			PRINTFORM 结实的
			ELSE
			PRINTFORM 丰满的
			ENDIF
		ENDIF
		;爆乳
		SIF TALENT:112
			PRINTFORM 有着强烈视觉冲击的
		PRINTFORML 胸部索求着更为强烈的刺激
		PRINTFORM 乳汁从两边的乳孔里流了出来…
		TFLAG:52 = 1
	;情欲のPALAMLV3以上
	ELSEIF PALAM:6 > PALAMLV:3
		PRINTFORM 上用力地挤压揉动…
	ELSE
		PRINTFORM 上挤压揉动…
	ENDIF
ELSE
	;情欲のPALAMLV3以上＋恋慕or相亲相爱＋矮小体型
	IF PALAM:6 > PALAMLV:3 && (TALENT:3 || TALENT:6) && TALENT:110
		PRINTFORML 上温柔地爱抚着，
		PRINTFORM %CALLNAME%有些害羞地挺起胸，寻求着更为激烈的欺负…
		TFLAG:52 = 1
	;矮小体型
	ELSEIF TALENT:110
		PRINTFORM 上轻柔地抚摸着…
	;情欲のPALAMLV3以上＋恋慕or相亲相爱
	ELSEIF PALAM:6 > PALAMLV:3 && (TALENT:3 || TALENT:6)
		PRINTFORML 上粗暴地揉搓着，
		PRINTFORM %CALLNAME%动情地娇喘着，强调似的挺起自己
		;贫乳
		SIF TALENT:109
			PRINTFORM 保守的贫乳
		;巨乳
		IF TALENT:111
			IF TALENT:140
				PRINTFORM 结实的胸肌
			ELSE
				PRINTFORM 丰满的巨乳
			ENDIF
		ENDIF
		;爆乳
		IF TALENT:112
			IF TALENT:140
				PRINTFORM 给人带来强烈视觉冲击的发达胸肌
			ELSE
				PRINTFORM 给人带来强烈视觉冲击的豪乳
			ENDIF
		ENDIF
		IF TALENT:137 || TALENT:140
    		PRINTFORM 平坦的胸脯
		ELSE
    		PRINTFORM 鼓起的胸部
		ENDIF
		PRINTFORM 索求着更为激烈的蹂躏…
		TFLAG:52 = 1
	;情欲のPALAMLV3以上
	ELSEIF PALAM:6 > PALAMLV:3
		PRINTFORM 上粗暴地揉搓着…
	ELSE
		PRINTFORM 上轻轻地抚摸着…
	ENDIF
ENDIF
;-------------------------------------------------
;7,玩弄乳头
;-------------------------------------------------
@MESSAGE_COM7
IF ASSIPLAY
	PRINTFORM %CALLNAME:ASSI%
ELSE
    PRINTFORM %CALLNAME:PLAYER%
ENDIF
;着衣プ雷
IF TALENT:PLAYER:58 && (TEQUIP:3 || TEQUIP:5 || TEQUIP:6)
    PRINTFORM 隔着
	IF TEQUIP:6
		A = 6
	ELSEIF TEQUIP:5
		A = 5
	ELSEIF TEQUIP:3
		A = 3
	ENDIF
	CALL CLOTHES_NAME
ENDIF
IF TALENT:PLAYER:58 && (TEQUIP:3 || TEQUIP:5 || TEQUIP:6)
	PRINTFORM 摩擦着
ELSE
	PRINTFORM 揉捏着
ENDIF
PRINTFORM %CALLNAME%
;绝壁
IF TALENT:110
		PRINTFORM 那等同于没有的胸部
;矮小体型＋贫乳
ELSEIF TALENT:100 && TALENT:109
	PRINTFORM 那仅能感觉到膨胀程度的胸脯
;贫乳
ELSEIF TALENT:109
	PRINTFORM 那小小的胸部
;巨乳
ELSEIF TALENT:111
	IF TALENT:140
	PRINTFORM 那乳肉从手指缝隙里溢出的结实胸肌
	ELSE
	PRINTFORM 那乳肉从手指缝隙里溢出的巨乳
	ENDIF
;爆乳
ELSEIF TALENT:112
	IF TALENT:140
	PRINTFORM 那大到双手都无法容纳的发达胸肌
	ELSE
	PRINTFORM 那大到双手都无法容纳的豪乳
	ENDIF
ELSEIF TALENT:137 || TALENT:140
	PRINTFORM 胸口
ELSE
    PRINTFORM 那鼓起的胸部
ENDIF
;情欲のPALAMLV3以上
IF PALAM:6 >= PALAMLV:3
	PRINTFORM 前端硬硬的突起…
ELSE
	PRINTFORM 前端尚且柔软的突起…
ENDIF
;-------------------------------------------------
;8,胸爱抚
;-------------------------------------------------
@MESSAGE_COM8
IF ASSIPLAY && !TEQUIP:25
	PRINTFORM %CALLNAME:ASSI%
ELSE
    PRINTFORM %CALLNAME:PLAYER%
ENDIF
;着衣プ雷
IF TALENT:PLAYER:58 && (TEQUIP:3 || TEQUIP:5 || TEQUIP:6)
    PRINTFORM 将手伸进
	IF TEQUIP:6
		A = 6
	ELSEIF TEQUIP:5
		A = 5
	ELSEIF TEQUIP:3
		A = 3
	ENDIF
	CALL CLOTHES_NAME
	PRINTFORM 下
ENDIF
PRINTFORM 爱抚着%CALLNAME%
;绝壁
IF TALENT:110
	PRINTFORM 那等同于没有的胸部…
;矮小体型＋贫乳
ELSEIF TALENT:100 && TALENT:109
	PRINTFORM 那仅能感觉到膨胀程度的胸脯…
;贫乳
ELSEIF TALENT:109
	PRINTFORM 那小小的胸部…
;巨乳
ELSEIF TALENT:111
	IF TALENT:140
	PRINTFORM 那乳肉从手指缝隙里溢出的结实胸肌…
	ELSE
	PRINTFORM 那乳肉从手指缝隙里溢出的巨乳…
	ENDIF
;爆乳
ELSEIF TALENT:112
	IF TALENT:140
	PRINTFORM 那大到双手都无法容纳的发达胸肌…
	ELSE
	PRINTFORM 那大到双手都无法容纳的豪乳…
	ENDIF
ELSEIF TALENT:140 || TALENT:137
	PRINTFORM 那平坦的胸部…
ELSE
	PRINTFORM 那鼓起的双峰…
ENDIF
;触手召喚中
IF TEQUIP:25
	PRINTFORM 与此同时，%CALLNAME%的胸还被触手们纠缠在一起挤压玩弄着…
;着衣プ雷
ELSEIF TALENT:PLAYER:58 && (TEQUIP:3 || TEQUIP:5 || TEQUIP:6)
	PRINTFORM %CALLNAME%的胸被像和面一样
	IF TALENT:140 || TALENT:137 || TALENT:110
	    PRINTFORM 摩擦着…
	ELSE
	    PRINTFORM 揉动着…
	ENDIF
;欲望のPALAMLV3以上＋
ELSEIF PALAM:6 >= PALAMLV:3 && TEQUIP:17 == 0 && TEQUIP:16 == 0
	PRINTFORM %CALLNAME%的乳珠渐渐挺立起来了…
;绝壁
ELSEIF TALENT:110 || MALE(TARGET)
	PRINTFORM 像是和面一样摩擦着…
ELSE
	PRINTFORM 像是和面一样揉动着…
ENDIF
;-------------------------------------------------
;9,秘穴张开
;-------------------------------------------------
@MESSAGE_COM9
PRINTFORM 听到%CALLNAME:PLAYER%的命令，%CALLNAME%
;着衣プ雷
IF TALENT:PLAYER:58 && TEQUIP:2
	A = 2
	;露出癖
	IF ABL:8 == 0
		;反抗
		IF TALENT:反抗
			PRINT 双目中写满反抗，极为勉强地
		;无动于衷、缺乏感情
		ELSEIF TALENT:无动于衷 || TALENT:缺乏感情
			PRINT 面无表情地
		;胆怯、おと无い
		ELSEIF TALENT:胆怯 || TALENT:老实
			PRINT 浑身微颤着
		;刚强
		ELSEIF TALENT:刚强
			PRINT 屈辱地咬着唇，
		;害羞
		ELSEIF TALENT:害羞
			PRINT 耳朵羞得通红，
		ELSE
			PRINT 满脸苦闷地
		ENDIF
		IF TALENT:男性
	    	PRINTFORML 一左一右拉开洞口边缘的嫩肉，展示着肛穴的内部
	    ELSEIF TALENT:双性 || TALENT:扶她
	        IF !RAND:3
	            PRINTFORML 一左一右拉开洞口边缘的嫩肉，展示着肛穴的内部
	        ELSEIF !RAND:2
	            PRINTFORML 一左一右分开阴唇，展示着小穴的内部
	        ELSE
	            PRINTFORML 将小穴和肛穴同时扒开，展示着两个洞口的内部
	        ENDIF
	    ELSE
		    PRINTFORML 一左一右分开阴唇，展示着小穴的内部
		ENDIF
	ELSEIF ABL:8 == 1
	    PRINTFORM 面红耳赤，颤颤巍巍地扒开了自己的
		IF TALENT:男性
	    	PRINTFORM 肛穴
	    ELSEIF TALENT:双性 || TALENT:扶她
	        IF !RAND:3
	            PRINTFORM 肛穴
	        ELSEIF !RAND:2
	            PRINTFORM 小穴
	        ELSE
	            PRINTFORM 两个洞口
	        ENDIF
	    ELSE
            PRINTFORM 小穴
		ENDIF
	    PRINTFORML ，展示出内里的风光
	ELSEIF ABL:8 == 2
		PRINTFORM 两根指头像是比了一个Ｖ一样拨开
		IF TALENT:男性
	    	PRINTFORM 后穴
	    ELSEIF TALENT:双性 || TALENT:扶她
	        IF !RAND:3
	            PRINTFORM 后穴
	        ELSEIF !RAND:2
	            PRINTFORM 阴唇
	        ELSE
	            PRINTFORM 两个洞口
	        ENDIF
	    ELSE
            PRINTFORM 阴唇
		ENDIF
		PRINTFORML 的褶皱
		PRINTFORML 向%CALLNAME:PLAYER%曝光了自己的秘所
		IF TFLAG:38 > 0
			PRINTFORM 可以很清楚地看见里面缓缓渗出的之前射进去的精液…
		ELSE
			PRINTFORM 可以很清楚地看见里面缓缓渗出的粘稠的汁液，周围的衣物都被打湿了…
		ENDIF
	ELSEIF ABL:8 == 3
	    PRINTFORM 满脸欢欣地
		IF TALENT:男性
	    	PRINTFORML 拉开后穴，另一只空闲的手插入其中开始浅浅地抽插
	    ELSEIF TALENT:双性 || TALENT:扶她
	        IF !RAND:3
	            PRINTFORML 拉开后穴，另一只空闲的手插入其中开始浅浅地抽插
	        ELSEIF !RAND:2
	            PRINTFORML 拨开阴唇，另一只空闲的手开始拨弄起阴核
	        ELSE
	            PRINTFORML 一只手拨开阴唇，一只手拉开后穴
	        ENDIF
	    ELSE
            PRINTFORML 拨开阴唇，另一只空闲的手开始拨弄起阴核
		ENDIF
		IF TFLAG:38 > 0
			CALL CLOTHES_NAME
			PRINTFORM 可以很清楚地看见穴口的皱褶里沾满了刚才射进去的精液…
		ELSE
			PRINTFORML 在%CALLNAME%手指的刺激下，穴口里渐渐渗出一汪汪淫水，将周围的衣物打湿了…
		ENDIF
	ELSEIF ABL:8 == 4
		IF TALENT:男性
	    	PRINTFORM 用两根指头扩大了肛穴
	    ELSEIF TALENT:双性 || TALENT:扶她
	        IF !RAND:3
	            PRINTFORM 用两根指头扩大了肛穴
	        ELSEIF !RAND:2
	            PRINTFORM 用两根指头扩大了阴道口
	        ELSE
	            PRINTFORM 一只手扯开阴唇，一只手拉开了肛穴
	        ENDIF
	    ELSE
            PRINTFORM 用两根指头扩大了阴道口
		ENDIF
		PRINTFORML 的入口
		IF TFLAG:38 > 0
			PRINTFORML 拨开层层褶皱后可以很清楚地看到刚才射进去的精液正缓缓渗出
			CALL CLOTHES_NAME
			PRINTFORM 被濡湿了…
		ELSE
			PRINTFORM 拨开层层褶皱后可以很清楚地看到内部分泌的汁液正不断溢出
			CALL CLOTHES_NAME
			PRINTFORML 被洇湿了一片暗色的痕迹
			PRINTFORM %CALLNAME%的脸颊染上了霞红…
		ENDIF
	ELSEIF ABL:8 >= 5 && TALENT:4 == 0 && TALENT:7 == 0
		IF TALENT:男性
	    	PRINTFORML 用手指拉开了闭合的肛穴口
	    ELSEIF TALENT:双性 || TALENT:扶她
	        IF !RAND:3
	            PRINTFORML 用手指拉开了闭合的肛穴口
	        ELSEIF !RAND:2
	            PRINTFORML 用手指扒开了两片闭合的阴唇
	        ELSE
	            PRINTFORML 用手指拉开了闭合的阴唇和肛穴口
	        ENDIF
	    ELSE
            PRINTFORML 用手指扒开了两片闭合的阴唇
		ENDIF
		PRINTFORML 为了让%CALLNAME:PLAYER%看得更清楚，%CALLNAME%尽量让穴口张到最大
		IF TFLAG:38 > 0
			PRINTFORM 因此可以很清楚地看到刚才射进去的精液混合着淫水不断溢出，将
			CALL CLOTHES_NAME
			PRINTFORM 洇湿了…
		ELSE
			PRINTFORML 因此可以很清楚地看到穴口里的淫水正不断溢出，将
			CALL CLOTHES_NAME
			PRINTFORM 连同指尖都打湿了…
		ENDIF
	;露出癖LV5以上＋淫乱or娼妇
	ELSEIF ABL:8 >= 5 && (TALENT:4 || TALENT:7)
		IF TALENT:男性
            PRINTFORML 为了让%CALLNAME:PLAYER%看清内部不断蠕动的媚肉，拼命拉开了紧闭的肛穴口
            PRINTFORML 随即像是在引诱%CALLNAME:PLAYER%侵犯似的，自己噗啾噗啾地抽插起屁眼来…
	    ELSEIF TALENT:双性 || TALENT:扶她
	        IF !RAND:3
                PRINTFORML 为了让%CALLNAME:PLAYER%看清内部不断蠕动的媚肉，拼命拉开了紧闭的肛穴口
                PRINTFORML 随即像是在引诱%CALLNAME:PLAYER%侵犯似的，自己噗啾噗啾地抽插起屁眼来…
	        ELSEIF !RAND:2
                PRINTFORML 一左一右分开了阴唇，为了让%CALLNAME:PLAYER%看清里面不断蠕动的媚肉而拼命拉大穴口
                PRINTFORML 随即像是在引诱%CALLNAME:PLAYER%侵犯似的，自己噗啾噗啾地玩弄起小穴来…
	        ELSE
                PRINTFORML 为了让%CALLNAME:PLAYER%看清内部不断蠕动的媚肉，拼命拉开了两个穴口
                PRINTFORML 随即像是在引诱%CALLNAME:PLAYER%侵犯似的，自己噗啾噗啾地抚弄起来…
	        ENDIF
	    ELSE
            PRINTFORML 一左一右分开了阴唇，为了让%CALLNAME:PLAYER%看清里面不断蠕动的媚肉而拼命拉大穴口
            PRINTFORML 随即像是在引诱%CALLNAME:PLAYER%侵犯似的，自己噗啾噗啾地玩弄起小穴来…
		ENDIF
	ENDIF
ELSE
	;露出癖
	IF ABL:8 == 0
		IF NPC回合 == 1
			PRINTFORML 十分犹豫，忍不住皱起了眉头
		 ELSE
			PRINTFORML 十分犹豫，眼泪在眼眶里打转
		ENDIF
		PRINTFORM 再次命令后，%CALLNAME%才勉强地插入自己
		IF TALENT:男性
	    	PRINTFORML 紧窄的肛穴，将其撑开一条扁扁的开口
	    ELSEIF TALENT:双性 || TALENT:扶她
	        IF !RAND:3
	            PRINTFORML 紧窄的肛穴，将其撑开一条扁扁的开口
	        ELSEIF !RAND:2
                ;矮小体型
                IF TALENT:100
                    PRINTFORM 娇嫩
                ELSE
                    PRINTFORM 私密
                ENDIF
                PRINTFORML 的小穴，将其扒开一条细细的缝
	        ELSE
                ;矮小体型
                IF TALENT:100
                    PRINTFORM 娇嫩
                ELSE
                    PRINTFORM 私密
                ENDIF
                PRINTFORML 的小穴和紧窄的肛穴，将其撑开到能看清里面的程度
	        ENDIF
	    ;女性
	    ELSE
            ;矮小体型
            IF TALENT:100
                PRINTFORM 娇嫩
            ELSE
                PRINTFORM 私密
            ENDIF
            PRINTFORML 的小穴，将其扒开一条细细的缝
		ENDIF
	ELSEIF ABL:8 == 1
		PRINTFORM 面红耳赤地，慢慢地把
		IF TALENT:男性
	    	PRINTFORM 肛穴拉开
	    ELSEIF TALENT:双性 || TALENT:扶她
	        IF !RAND:3
	            PRINTFORM 肛穴拉开
	        ELSEIF !RAND:2
	            PRINTFORM 小穴拨开
	        ELSE
	            PRINTFORM 两个洞口拉开
	        ENDIF
	    ELSE
            PRINTFORM 小穴拨开
		ENDIF
		PRINTFORML 展示给%CALLNAME:PLAYER%看
	ELSEIF ABL:8 == 2
		IF TALENT:男性
            PRINTFORML 像是比Ｖ字一样用两根手指撑开肛穴，向%CALLNAME:PLAYER%曝光了身上最肮脏的地方
            PRINTFORML 从张开的穴口向里看，可以看到被肠液润湿的媚肉正淫靡地蠕动
            PRINTFORML 如同等待着能够喂满这张小嘴的巨物…
	    ELSEIF TALENT:双性 || TALENT:扶她
	        IF !RAND:3
                PRINTFORML 像是比Ｖ字一样用两根手指撑开肛穴，向%CALLNAME:PLAYER%曝光了身上最肮脏的地方
                PRINTFORML 从张开的穴口向里看，可以看到被肠液润湿的媚肉正淫靡地蠕动
                PRINTFORML 如同等待着能够喂满这张小嘴的巨物…
	        ELSEIF !RAND:2
                PRINTFORML 像是比Ｖ字一样用两根手指扒开阴唇，向%CALLNAME:PLAYER%曝光了自己的秘所
                IF TFLAG:38 > 0
                    PRINTFORML 可以很清楚地看见之前射进去的精液正从里面缓缓渗出…
                ELSE
                    PRINTFORML 从张开的女阴向里看，可以看到被淫水润湿的媚肉正淫靡地蠕动
                    PRINTFORML 如同等待着能够喂满这张小嘴的巨物…
                ENDIF
	        ELSE
                PRINTFORML 用手指撑开两个穴口，向%CALLNAME:PLAYER%曝光了自己的秘所
                IF TFLAG:38 > 0
                    PRINTFORML 可以很清楚地看见之前射进小穴的精液正从里面缓缓渗出…
                ELSE
                    PRINTFORML 从张开的穴口向里看，可以看到被爱液润湿的媚肉正淫靡地蠕动
                    PRINTFORML 如同等待着能够喂满这两张小嘴的巨物…
                ENDIF
	        ENDIF
	    ELSE
            PRINTFORML 像是比Ｖ字一样用两根手指扒开阴唇，向%CALLNAME:PLAYER%曝光了自己的秘所
            IF TFLAG:38 > 0
                PRINTFORML 可以很清楚地看见之前射进去的精液正从里面缓缓渗出…
            ELSE
                PRINTFORML 从张开的女阴向里看，可以看到被淫水润湿的媚肉正淫靡地蠕动
                PRINTFORML 如同等待着能够喂满这张小嘴的巨物…
            ENDIF
		ENDIF
	ELSEIF ABL:8 == 3
		IF TALENT:男性
            PRINTFORML 带着欢喜的表情拉开肛穴附近的嫩肉，空出来的那只手开始浅浅地抽插刺激穴内褶皱
            PRINTFORML 随着%CALLNAME%手指的刺激，肛穴里不住溢出晶莹的肠液…
	    ELSEIF TALENT:双性 || TALENT:扶她
	        IF !RAND:3
                PRINTFORML 带着欢喜的表情拉开肛穴附近的嫩肉，空出来的那只手开始浅浅地抽插刺激穴内褶皱
                PRINTFORML 随着%CALLNAME%手指的刺激，肛穴里不住溢出晶莹的肠液…
	        ELSEIF !RAND:2
                PRINTFORML 带着欢喜的表情拉开两片阴唇，空出来的那只手拨弄着阴核
                IF TFLAG:38 > 0
                    PRINTFORML 从张开的穴口向内窥视，可以很清楚地看到刚才射进去的精液正淫靡地向外流出…
                ELSE
                    PRINTFORML 随着%CALLNAME%手指的刺激，阴道口不住溢出晶莹的液体…
                ENDIF
	        ELSE
                PRINTFORML 带着欢喜的表情拉开两个穴口，向%CALLNAME:PLAYER%曝光了自己最私密的场所
                IF TFLAG:38 > 0
                    PRINTFORML 向内窥视，可以很清楚地看到刚才射进小穴的精液正淫靡地向外流出…
                ELSE
                    PRINTFORML 随着%CALLNAME%手指的刺激，两个穴口不住溢出晶莹的液体…
                ENDIF
	        ENDIF
	    ELSE
            PRINTFORML 带着欢喜的表情拉开两片阴唇，空出来的那只手拨弄着阴核
            IF TFLAG:38 > 0
                PRINTFORML 从张开的穴口向内窥视，可以很清楚地看到刚才射进去的精液正淫靡地向外流出…
            ELSE
                PRINTFORML 随着%CALLNAME%手指的刺激，阴道口不住溢出晶莹的液体…
            ENDIF
		ENDIF
	ELSEIF ABL:8 == 4
		IF TALENT:男性
            PRINTFORML 用两根手指拉开肛穴附近的嫩肉，继而插进去扩大了洞口的张开程度
            PRINTFORML 因为最肮脏的地方被看得清清楚楚，%CALLNAME%的脸颊染成了一片红霞
	    ELSEIF TALENT:双性 || TALENT:扶她
	        IF !RAND:3
                PRINTFORML 用两根手指拉开肛穴附近的嫩肉，继而插进去扩大了洞口的张开程度
                PRINTFORML 因为最肮脏的地方被看得清清楚楚，%CALLNAME%的脸颊染成了一片红霞
	        ELSEIF !RAND:2
                PRINTFORML 用两根手指分开阴唇，继而插进去扩大了阴道口的缝隙
                IF TFLAG:38 > 0
                    PRINTFORML 向%CALLNAME:PLAYER%展示出里面缓缓渗出的刚才射进去的精液…
                ELSE
                    PRINTFORML 因为私密处的里面被看得清清楚楚，%CALLNAME%的脸颊染成了一片红霞
                ENDIF
	        ELSE
                PRINTFORML 用手指拉开两个穴口，继而插进去扩大了穴口的张开程度
                IF TFLAG:38 > 0
                    PRINTFORML 向%CALLNAME:PLAYER%展示出小穴里面缓缓渗出的刚才射进去的精液…
                ELSE
                    PRINTFORML 因为最私密的两个地方被看得清清楚楚，%CALLNAME%的脸颊染成了一片红霞
                ENDIF
	        ENDIF
	    ELSE
            PRINTFORML 用两根手指分开阴唇，继而插进去扩大了阴道口的缝隙
            IF TFLAG:38 > 0
                PRINTFORML 向%CALLNAME:PLAYER%展示出里面缓缓渗出的刚才射进去的精液…
            ELSE
                PRINTFORML 因为私密处的里面被看得清清楚楚，%CALLNAME%的脸颊染成了一片红霞
            ENDIF
		ENDIF
	ELSEIF ABL:8 >= 5 && TALENT:4 == 0 && TALENT:7 == 0
		IF TALENT:男性
            PRINTFORML 用两根手指把肛门一左一右拉开，为了让%CALLNAME:PLAYER%看清肠道内壁而卖力扩张自己的后穴
            PRINTFORML 穴内的媚肉随着%CALLNAME%的呼吸频率起伏着，肠液一点一滴地渗了出来…
	    ELSEIF TALENT:双性 || TALENT:扶她
	        IF !RAND:3
                PRINTFORML 用两根手指把肛门一左一右拉开，为了让%CALLNAME:PLAYER%看清肠道内壁而卖力扩张自己的后穴
                PRINTFORML 穴内的媚肉随着%CALLNAME%的呼吸频率起伏着，肠液一点一滴地渗了出来…
	        ELSEIF !RAND:2
                PRINTFORML 用两根手指把阴唇一左一右分开，为了让%CALLNAME:PLAYER%看清阴道内壁而卖力扩张自己的小穴
                IF TFLAG:38 > 0
                    PRINTFORML 大大张开的穴口里，刚才射进去的精液正不断渗出…
                ELSE
                    PRINTFORML 穴内的媚肉随着%CALLNAME%的呼吸频率起伏着，爱液一点一滴地渗了出来…
                ENDIF
	        ELSE
                PRINTFORML 用手指把两个穴口拉开，为了让%CALLNAME:PLAYER%看清内壁而卖力扩张着
                IF TFLAG:38 > 0
                    PRINTFORML 大大张开的小穴里，刚才射进去的精液正不断渗出…
                ELSE
                    PRINTFORML 穴内的媚肉随着%CALLNAME%的呼吸频率起伏着，淫水一点一滴地渗了出来…
                ENDIF
	        ENDIF
	    ELSE
            PRINTFORML 用两根手指把阴唇一左一右分开，为了让%CALLNAME:PLAYER%看清阴道内壁而卖力扩张自己的小穴
            IF TFLAG:38 > 0
                PRINTFORML 大大张开的穴口里，刚才射进去的精液正不断渗出…
            ELSE
                PRINTFORML 穴内的媚肉随着%CALLNAME%的呼吸频率起伏着，爱液一点一滴地渗了出来…
            ENDIF
		ENDIF
	;露出癖LV5以上＋淫乱or娼妇
	ELSEIF ABL:8 >= 5 && (TALENT:4 || TALENT:7)
		IF TALENT:男性
            PRINTFORML 用两根手指把肛穴附近突起的肉箍一左一右拉开，炫耀似的展示自己蠕动着的屁眼
            PRINTFORML 柔软的内壁如泣如诉地吐露着晶莹的肠液，渴求着什么去把它填满…
	    ELSEIF TALENT:双性 || TALENT:扶她
	        IF !RAND:3
                PRINTFORML 用两根手指把肛穴附近突起的肉箍一左一右拉开，炫耀似的展示自己蠕动着的屁眼
                PRINTFORML 柔软的内壁如泣如诉地吐露着晶莹的肠液，渴求着什么去把它填满…
	        ELSEIF !RAND:2
                PRINTFORML 用两根手指把肥厚的阴唇一左一右分开，炫耀似的展示自己蠕动着的小穴
                IF TFLAG:38  > 0
                    PRINTFORML 刚才射进去的精液正咕嘟咕嘟地从粉红色的洞口里涌出来…
                ELSE
                    PRINTFORML 柔软的内壁如泣如诉地吐露着晶莹的爱液，渴求着什么去把它填满…
                ENDIF
	        ELSE
                PRINTFORML 用手指把肥厚的阴唇和翕张着的肛穴一左一右分开，炫耀似的展示自己蠕动的内部
                IF TFLAG:38  > 0
                    PRINTFORML 刚才射进小穴的精液正咕嘟咕嘟地从那粉红色的洞口里涌出来…
                ELSE
                    PRINTFORML 柔软的内壁如泣如诉地吐露着晶莹的淫液，渴求着什么去把它们填满…
                ENDIF
	        ENDIF
	    ELSE
            PRINTFORML 用两根手指把肥厚的阴唇一左一右分开，炫耀似的展示自己蠕动着的小穴
            IF TFLAG:38  > 0
                PRINTFORML 刚才射进去的精液正咕嘟咕嘟地从粉红色的洞口里涌出来…
            ELSE
                PRINTFORML 柔软的内壁如泣如诉地吐露着晶莹的爱液，渴求着什么去把它填满…
            ENDIF
		ENDIF
	ENDIF
ENDIF
;助手调教、调教者が害羞か男＋调教经验200以下
IF ASSIPLAY && (TALENT:PLAYER:35 || TALENT:PLAYER:137 || TALENT:PLAYER:140) && EXP:PLAYER:70 < 200
	PRINTL
	PRINTFORML 看着因自己的命令展露出痴态的%CALLNAME%
	PRINTFORM %CALLNAME:PLAYER%因些许罪恶感与羞耻感双颊通红，转开了头
ENDIF
TFLAG:52 = 1
;-------------------------------------------------
;10,胸自慰
;-------------------------------------------------
@MESSAGE_COM10
PRINTFORM %CALLNAME%
;顺从LV3未満
IF ABL:0 < 3
	;反抗
	IF TALENT:反抗
		PRINT 的双目中写满反抗，极为勉强地
	;无动于衷、缺乏感情
	ELSEIF TALENT:无动于衷 || TALENT:缺乏感情
		PRINT 面无表情地
	;胆怯、おと无い
	ELSEIF TALENT:胆怯 || TALENT:老实
		PRINT 浑身微颤着
	;刚强
	ELSEIF TALENT:刚强
		PRINT 屈辱地咬着唇，
	;害羞
	ELSEIF TALENT:害羞
		PRINT 的耳朵羞得通红，
	ELSE
		PRINT 满脸苦闷地
	ENDIF
ENDIF
;摄像机摄影中＋露出癖LV3以上
IF TEQUIP:28 && ABL:8 >= 3
	PRINTFORM 在镜头正中央
;摄像机摄影中
ELSEIF TEQUIP:28
	PRINTFORM 在镜头面前
;露出癖LV3以上
ELSEIF ABL:8 >= 3
	PRINTFORM 炫耀似的
ENDIF
PRINTFORM 抚弄着自己
;贫乳
SIF TALENT:109
	PRINTFORM 保守
;巨乳
IF TALENT:111
	IF TALENT:140
		PRINTFORM 结实
	ELSE
		PRINTFORM 丰满
	ENDIF
ENDIF
;爆乳
SIF TALENT:112
	PRINTFORM 给人带来视觉冲击
;母乳体质
SIF TALENT:149
	PRINTFORM 滴落着母乳
IF PALAM:快Ｂ >= 3000
    PRINTFORM 的胸部…
ELSE
    PRINTFORM 的胸部，尽力寻找着快感…
   ENDIF
;-------------------------------------------------
;11,自慰
;-------------------------------------------------
@MESSAGE_COM11
;顺从LV3未満
IF ABL:0 < 3
	PRINTFORM 听到自慰的命令，%CALLNAME%
	;高傲
	SIF TALENT:15
		PRINTFORM 表现出极高的自尊心，
	;反抗
	IF TALENT:反抗
		PRINTL 的眼中写满了反抗
		PRINT 于是再次下达了同样的命令，
	;无动于衷、缺乏感情
	ELSEIF TALENT:无动于衷 || TALENT:缺乏感情
		PRINT 面无表情地沉默着，
	;胆怯、おと无い
	ELSEIF TALENT:胆怯 || TALENT:老实
		PRINT 指尖发颤，
	;刚强
	ELSEIF TALENT:刚强
		PRINT 屈辱地咬着唇，
	;害羞
	ELSEIF TALENT:害羞
		PRINT 羞得双耳通红，
	ELSE
		PRINT 神色苦闷，
	ENDIF
;陷落(恋慕以外)
ELSEIF !RAND:2 && (KANRAKU(TARGET) == 2 || KANRAKU(TARGET) == 3)
	PRINTFORML %CALLNAME%对自慰的命令没有表现出任何的反抗，完全就是一副牝犬的样子
	PRINTFORM %CALLNAME%
;未陷落
ELSEIF !KANRAKU(TARGET)
	PRINTFORM 调教的成果出来了，%CALLNAME%遵从命令，
ELSE
	PRINTFORM %CALLNAME%遵从命令，
ENDIF
;视频拍摄中
SIF TEQUIP:28
	PRINTFORM 在镜头前
;露出癖LV3以上
SIF ABL:8 >= 3
	PRINTFORM 高调地展示着身体，
;自慰棒＋肛塞
IF TEQUIP:13 && TEQUIP:14 && ABL:0 >= 3
	PRINTFORML 亲自抽插起埋在穴里的两根东西
;自慰棒
ELSEIF TEQUIP:13 && ABL:0 >= 3
	PRINTFORML 亲自抽插起埋在小穴里的自慰棒
;肛塞
ELSEIF TEQUIP:14 && ABL:0 >= 3
	PRINTFORML 亲自拔插起吞在肛穴里的肛塞
ENDIF
IF (TALENT:137 || TALENT:140)
    PRINTFORM 搓动着自己
    IF TALENT:包茎
        PRINT 裹着包皮
    	IF TALENT:孩童尺寸
		    SIF TALENT:矮小体型 == 0
				PRINT ，与体型不符
			PRINT 的小小
		ELSEIF TALENT:短小
			SIF TALENT:高大体型
				PRINT ，与其高大体型不符
			PRINT 的短小
		ELSEIF TALENT:巨根
			SIF TALENT:矮小体型
				PRINT ，与体型不符
			PRINT 的粗大
		ELSEIF TALENT:117
			SIF TALENT:矮小体型
				PRINT ，与体型不符，
			PRINT 尺寸傲人的
		ELSE
	   		PRINT 的
		ENDIF
	ELSE
    	IF TALENT:孩童尺寸
		    SIF TALENT:矮小体型 == 0
				PRINT 与体型不符
			PRINT 的小小
		ELSEIF TALENT:短小
			SIF TALENT:高大体型
				PRINT 与其高大体型不符
			PRINT 的短小
		ELSEIF TALENT:巨根
			SIF TALENT:矮小体型
				PRINT 与体型不符
			PRINT 的粗大
		ELSEIF TALENT:117
			SIF TALENT:矮小体型
				PRINT 与体型不符，
			PRINT 尺寸傲人的
		ELSE
	   		PRINT 的
		ENDIF
	ENDIF
	PRINTFORM 阴茎
	IF PALAM:快Ｃ >= 3000
		PRINT ，发麻的快感渐渐上涌…
	ELSE
		PRINT …
	ENDIF
ELSE
	IF PALAM:快Ｃ >= 3000
		PRINTFORM 手指拨弄着下身的秘所…
	ELSE
		PRINT 手指拨弄着下身的秘所，尽力寻求着快感…
	ENDIF
ENDIF

;自慰棒＋肛塞＋Ｖ感觉LV5以上＋Ａ感觉LV5以上＋Ｃ感觉LV5以上＋Ｂ感觉LV5以上＋情欲のPALAMLV4以上
IF TEQUIP:13 && TEQUIP:14 && ABL:4 >= 5 && ABL:5 >= 5 && ABL:3 >= 5 && ABL:6 >= 5 && PALAM:6 >= PALAMLV:4
	PRINTL 
	PRINTFORML %CALLNAME%的口中流着口涎，身体阵阵痉挛，口齿间不住溢出破碎的呻吟
	PRINTFORM 好像是沉醉在快感的巅峰，愈加干劲十足了…
	TFLAG:52 = 1
;自慰棒＋Ｖ感觉LV5以上＋Ｃ感觉LV5以上＋Ｂ感觉LV5以上＋情欲のPALAMLV4以上
ELSEIF TEQUIP:13 && ABL:4 >= 5 && ABL:3 >= 5 && ABL:6 >= 5 && PALAM:6 >= PALAMLV:4
	PRINTL 
	PRINTFORML 爱液四散飞溅，但%CALLNAME%已经一点都顾不上了
	PRINTFORM 现在的%CALLNAME%只知道拼命抽插自慰棒来慰藉自己了…
	TFLAG:52 = 1
;肛塞＋Ａ感觉LV5以上＋Ｃ感觉LV5以上＋Ｂ感觉LV5以上＋情欲のPALAMLV4以上
ELSEIF TEQUIP:14 && ABL:5 >= 5 && ABL:3 >= 5 && ABL:6 >= 5 && PALAM:6 >= PALAMLV:4
	PRINTL 
	PRINTFORML %CALLNAME%颤抖着身体不停用自慰棒攻击自己的肛穴
	PRINTFORM 每一次挖搅自己的后面都仿佛要高潮一样高调呻吟着…
	TFLAG:52 = 1
;自慰成瘾LV3以上＋Ｃ感觉LV5以上＋Ｂ感觉LV5以上＋情欲のPALAMLV4以上
ELSEIF ABL:15 >= 3 && ABL:3 >= 5 && ABL:6 >= 5 && PALAM:6 >= PALAMLV:4 && !RAND:2 && D < 2
	;男性の有無で分岐
	PRINTL 
	IF TALENT:140
		PRINTFORML %CALLNAME%一边娇喘着一边专心致志地玩弄自己的阴茎
		PRINTFORM 就算想要停下来，%CALLNAME%的手也不会允许…
	ELSE
		PRINTFORML %CALLNAME%一边娇喘着一边专心致志地探索着自己的秘部
		PRINTFORM 就算想要停下来，%CALLNAME%的手也不会允许…
	ENDIF
	TFLAG:52 = 1
ENDIF
;-------------------------------------------------
;12,くるん爱抚 09/09/20
;-------------------------------------------------
@MESSAGE_COM12
IF TALENT:呆毛
	PRINTFORML %CALLNAME:PLAYER%抚摸着%CALLNAME%的发丝，并试着按住那根不自然翘起来的呆毛
	PRINTFORML %CALLNAME%一脸诧异，等%CALLNAME:PLAYER%的手离开后，呆毛又翘了回来
	PRINTFORML 对此束手无策的%CALLNAME:PLAYER%索性转为爱抚起%CALLNAME%的身体…
ELSEIF TEQUIP:60
	PRINTFORML %CALLNAME:PLAYER%似乎想看清楚%CALLNAME%的表情，拨弄着%CALLNAME%垂落下来的发丝…
ELSE
	PRINTFORML %CALLNAME:PLAYER%像是在称赞乖宝宝那样，抚摸着%CALLNAME%的发丝
	PRINTFORML 一次次抚摸着%CALLNAME%柔顺的头发，让被摸头的%CALLNAME%露出了莫名其妙的表情
	PRINTFORML 乘着%CALLNAME%呆愣的时候，%CALLNAME:PLAYER%转而开始爱抚起%CALLNAME%的身体…
ENDIF
;-------------------------------------------------
;13,阴茎爱抚
;-------------------------------------------------
@MESSAGE_COM13
IF ASSIPLAY
	PRINTFORM %CALLNAME:ASSI%
ELSE
    PRINTFORM %CALLNAME:PLAYER%
ENDIF
;着衣プ雷
IF TALENT:PLAYER:58 && (TEQUIP:2 || TEQUIP:4 > 1 || TEQUIP:6 == 7)
    PRINTFORM 揉弄着
	;下身外衣か布偶装を着ている
	IF TEQUIP:4 > 1 || TEQUIP:6 == 7
		IF TEQUIP:4
			A = 4
		ELSE
			A = 6
		ENDIF
		CALL CLOTHES_NAME
	;下身内衣を着ている
	ELSEIF TEQUIP:2
		A = 2
		CALL CLOTHES_NAME
	ENDIF
	PRINTFORM 下%CALLNAME%的阴茎…
;飞机杯使用中
ELSEIF TEQUIP:12 && !ASSIPLAY
	PRINTFORML 摆弄着套在阴茎上的飞机杯给予刺激
	PRINTFORM 间或舔舐裸露出来的龟头尖端…
ELSE
    PRINTFORM 用指腹
	;丝袜系
	IF TEQUIP:1 == 5 || TEQUIP:1 == 6
	    PRINTFORM 隔着
		SIF TEQUIP:1 == 5
			PRINT 丝袜
		SIF TEQUIP:1 == 6
			PRINT 网袜
	ENDIF
	PRINTFORM 摩擦玩弄着%CALLNAME%
	;情欲のPALAMLV3以上
	IF PALAM:6 >= PALAMLV:3
		PRINTFORM 已完全挺立
	    IF TALENT:包茎
			PRINT ，裹着包皮
    		IF TALENT:孩童尺寸
    		    SIF TALENT:矮小体型 == 0
    		        PRINT ，与其体型不符
		    	PRINT 的小小
		    ELSEIF TALENT:短小
		        SIF TALENT:高大体型
		            PRINT ，与其高大体型不符
		        PRINT 的短小
		    ELSEIF TALENT:巨根
		        SIF TALENT:矮小体型
		            PRINT ，与其体型不符
		    	PRINT 的粗大
		    ELSEIF TALENT:尺寸傲人
		        SIF TALENT:矮小体型
		            PRINT ，与其体型不符
		    	PRINT ，尺寸傲人的
		    ELSE
		    	PRINT 的
		    ENDIF
	    ELSE
    		IF TALENT:孩童尺寸
    		    SIF TALENT:矮小体型 == 0
    		        PRINT ，与其体型不符
		    	PRINT 的小小
		    ELSEIF TALENT:短小
		        SIF TALENT:高大体型
		            PRINT ，与其高大体型不符
		        PRINT 的短小
		    ELSEIF TALENT:巨根
		        SIF TALENT:矮小体型
		            PRINT ，与其体型不符
		    	PRINT 的粗大
		    ELSEIF TALENT:尺寸傲人
		        SIF TALENT:矮小体型
		            PRINT ，与其体型不符
		    	PRINT ，尺寸傲人的
		    ELSE
		    	PRINT 的
		    ENDIF
		ENDIF
	ELSE
	    IF TALENT:包茎
		    PRINT 裹着包皮
    	    IF TALENT:孩童尺寸
    		    SIF TALENT:矮小体型 == 0
    		        PRINT ，与其体型不符
		    	PRINT 的小小
		    ELSEIF TALENT:短小
		        SIF TALENT:高大体型
		            PRINT ，与其高大体型不符
		        PRINT 的短小
		    ELSEIF TALENT:巨根
		        SIF TALENT:矮小体型
		            PRINT ，与其体型不符
		    	PRINT 的粗大
		    ELSEIF TALENT:尺寸傲人
		        SIF TALENT:矮小体型
		            PRINT ，与其体型不符
		        PRINT ，尺寸傲人的
		    ELSE
		    	PRINT 的
		    ENDIF
		ELSE
    		IF TALENT:孩童尺寸
    		    SIF TALENT:矮小体型 == 0
    		       PRINT 与其体型不符
		    	PRINT 的小小
		    ELSEIF TALENT:短小
		        SIF TALENT:高大体型
		            PRINT 与其高大体型不符
		        PRINT 的短小
		    ELSEIF TALENT:巨根
		        SIF TALENT:矮小体型
		            PRINT 与其体型不符
		    	PRINT 的粗大
		    ELSEIF TALENT:尺寸傲人
		        SIF TALENT:矮小体型
		            PRINT 与其体型不符，
		    	PRINT 尺寸傲人的
		    ELSE
		        PRINT 的
		    ENDIF
		ENDIF
    ENDIF
	PRINTFORM 阴茎…
ENDIF
;-------------------------------------------------
;14,アナニー
;-------------------------------------------------
@MESSAGE_COM14
;顺从LV3未満
IF ABL:顺从 < 3
    PRINTFORM 听到%CALLNAME:PLAYER%的命令，%CALLNAME%
	;高傲
	SIF TALENT:15
		PRINT 表现出极高的自尊心，
	;反抗
	IF TALENT:反抗
		PRINTL 的眼中写满了反抗
		PRINT 于是再次下达了同样的命令，
	;无动于衷、缺乏感情
	ELSEIF TALENT:无动于衷 || TALENT:缺乏感情
		PRINT 面无表情地沉默着，
	;胆怯、おと无い
	ELSEIF TALENT:胆怯 || TALENT:老实
		PRINT 指尖发颤，
	;刚强
	ELSEIF TALENT:刚强
		PRINT 屈辱地咬着唇，
	;害羞
	ELSEIF TALENT:害羞
		PRINT 羞得双耳通红，
	ELSE
		PRINT 神色苦闷，
	ENDIF
;陷落(恋慕以外)
ELSEIF !RAND:2 && (KANRAKU(TARGET) == 2 || KANRAKU(TARGET) == 3)
	PRINTFORML 对%CALLNAME:PLAYER%的命令%CALLNAME%没有表现出任何的反抗，完全就是一副牝犬的样子
	PRINTFORM %CALLNAME%
;未陷落
ELSEIF !KANRAKU(TARGET)
	PRINTFORM 调教的成果出来了，%CALLNAME%遵从指示，
ELSE
	PRINTFORM %CALLNAME%遵从指示，
ENDIF
;视频拍摄中
SIF TEQUIP:28
	PRINTFORM 在镜头前
;露出癖LV5以上
SIF ABL:8 >= 5
	PRINTFORM 高调地展示着身体，
IF TEQUIP:肛塞
	PRINTFORM 抽插着后穴里的肛塞…
ELSEIF TEQUIP:后庭拉珠
	PRINTFORM 将后庭拉珠拉出推回，脸上流露出些许动摇…
ELSEIF TEQUIP:灌肠器＋塞头
	PRINTFORM 慢慢地玩弄着堵住肛门的塞头…
ELSEIF TEQUIP:扩张气球
	PRINTFORM 摇动着填充在肛穴里的扩张气球…
ELSEIF ABL:Ａ感觉 > 4
	PRINTFORM 玩弄着柔软的肛穴媚肉…
ELSEIF ABL:Ａ感觉 > 2
	PRINTFORM 手指在尻穴里探索移动着，寻求着让自己快乐的地方…
ELSE
	PRINTFORM 慢慢地把手指插入了肛穴…
ENDIF
;自慰成瘾３以上＋Ａ感觉7以上
IF ABL:自慰成瘾 >= 3 && ABL:Ａ感觉 >= 7
	PRINTL 
	PRINTFORM %CALLNAME%的口涎顺着嘴角滑下，嘴里泄出破碎的呻吟，身体阵阵痉挛着…
	TFLAG:52 = 1
;Ａ感觉5以上
ELSEIF ABL:Ａ感觉 >= 5 && D < 2
	PRINTL 
	PRINTFORML %CALLNAME%的身体已经记住了后面被玩弄的快感，无暇再顾及周围的一切
	PRINTFORML 现在的%CALLNAME%就像是发情期的动物，只知道一个劲儿地自慰…
	TFLAG:52 = 1
;自慰成瘾３以上
ELSEIF ABL:自慰成瘾 >= 3 && D < 2
	PRINTL 
	PRINTFORML %CALLNAME%专心致志地玩弄着自己的屁股，看起来相当热衷于开发新的快感带…
	TFLAG:52 = 1
ENDIF
;-------------------------------------------------
;15,抚摸翅膀
;-------------------------------------------------
@MESSAGE_COM15
IF ASSIPLAY
	PRINTFORM %CALLNAME:ASSI%
ELSE
    PRINTFORM %CALLNAME:PLAYER%
ENDIF
PRINTFORM 细致地抚摸着%CALLNAME%
IF TALENT:羽翅
    PRINT 翅膀上的羽毛
ELSEIF TALENT:骨翅
    PRINT 翅膀上的骨节
ELSEIF TALENT:蝠翼
    PRINT 翅膀上的翼膜
ELSEIF TALENT:虫翼
    PRINT 翅膀上的轻薄纹路
ENDIF
SIF ABL:PLAYER:2 >= 3
    PRINT ，指尖在翅根描摹打转
IF ABL:22 >= 5
    PRINTL
    PRINTFORM %CALLNAME%小声喘息着，翅膀微微开合
ELSEIF ABL:22 >= 3
    PRINTL
    PRINTFORM %CALLNAME%浑身一凛，气息变得凌乱
ELSEIF ABL:22 >= 1
    PRINTL
    PRINTFORM %CALLNAME%咬住嘴唇，貌似产生了异样的感觉
ELSE
    PRINTL
    PRINTFORM %CALLNAME%微微扭动身躯，似乎很是不适
ENDIF
PRINT …
;-------------------------------------------------
;16,抚摸尾巴
;-------------------------------------------------
@MESSAGE_COM16
IF ASSIPLAY
	PRINTFORM %CALLNAME:ASSI%
ELSE
    PRINTFORM %CALLNAME:PLAYER%
ENDIF
IF TALENT:兽尾
    PRINTFORM 顺着%CALLNAME%
    SIF TALENT:多尾
        PRINTFORM 其中一根
    PRINTFORM 尾巴抚摸下去
ELSEIF TALENT:鱼尾
    PRINTFORM 一一抚过%CALLNAME%
    SIF TALENT:多尾
        PRINTFORM 其中一条
    PRINTFORM 鱼尾上光滑的鳞片
ELSEIF TALENT:蛇尾
    PRINTFORM 抚摸着%CALLNAME%
    SIF TALENT:多尾
        PRINTFORM 其中一条
    PRINTFORM 冰冷的蛇尾
ENDIF
SIF ABL:PLAYER:2 >= 3
    PRINT ，同时另一只手轻轻搔动尾巴根部
IF ABL:23 >= 5
    PRINTL
    IF TALENT:鱼尾
        PRINTFORM %CALLNAME%喘息着，像是很愉快一般上下摆动鱼尾
    ELSE
        PRINTFORM %CALLNAME%喘息着，尾巴柔顺地缠绕过来
    ENDIF
ELSEIF ABL:23 >= 3
    PRINTL
    IF TALENT:兽尾
        PRINTFORM %CALLNAME%不耐烦地晃动尾巴，喉咙里却发出轻微的呼噜噜声
    ELSEIF TALENT:鱼尾
        PRINTFORM %CALLNAME%微眯着眼睛，身体凑了过来
    ELSEIF TALENT:蛇尾
        PRINTFORM %CALLNAME%的尾巴变换着盘绕的形状，似乎很是愉快
    ENDIF
ELSEIF ABL:23 >= 1
    PRINTL
    PRINTFORM %CALLNAME%摇晃着尾巴，尾巴尖颤动着
ELSE
    PRINTL
    PRINTFORM %CALLNAME%甩动尾巴，似乎不太舒服
ENDIF
PRINT …
;-------------------------------------------------
;17,抚摸耳朵
;-------------------------------------------------
@MESSAGE_COM17
IF ASSIPLAY
	PRINTFORM %CALLNAME:ASSI%
ELSE
    PRINTFORM %CALLNAME:PLAYER%
ENDIF
PRINTFORM 抚摸着%CALLNAME%
IF TALENT:兽耳
    PRINTFORM 毛茸茸的兽耳
ELSEIF TALENT:尖耳
    PRINTFORM 尖尖的耳朵
ELSE
	PRINTFORM 的耳朵
ENDIF
IF ABL:24 >= 5
    PRINTL
    IF TALENT:兽耳
        PRINTFORM %CALLNAME%低低地呻吟着，蹭了蹭%CALLNAME:PLAYER%的手心
    ELSE
        PRINTFORM %CALLNAME%气息不稳地喘息着，慢慢闭上了眼睛
    ENDIF
ELSEIF ABL:24 >= 3
    PRINTL
    IF TALENT:兽耳
        PRINTFORM %CALLNAME%舒服得微眯双眼
    ELSE
        PRINTFORM %CALLNAME%的耳尖有些发红，不好意思地转移视线
    ENDIF
ELSEIF ABL:24 >= 1
    PRINTL
    IF TALENT:兽耳
        PRINTFORM %CALLNAME%没有反抗，好奇地看着%CALLNAME:PLAYER%的动作
    ELSE
        PRINTFORM %CALLNAME%眨了眨眼，不太明白%CALLNAME:PLAYER%的举动
    ENDIF
ELSE
    PRINTL
    IF TALENT:兽耳
        PRINTFORM %CALLNAME%的兽耳抖了抖，不满地发出警告
    ELSE
        PRINTFORM %CALLNAME%偏过头，不习惯地避开了
    ENDIF
ENDIF
PRINT …
;-------------------------------------------------
;18,舔耳朵
;-------------------------------------------------
@MESSAGE_COM18
IF ASSIPLAY
	PRINTFORM %CALLNAME:ASSI%
ELSE
    PRINTFORM %CALLNAME:PLAYER%
ENDIF
PRINTFORM 舔舐着%CALLNAME%
IF TALENT:兽耳
    PRINTFORM 毛茸茸的兽耳
ELSEIF TALENT:尖耳
    PRINTFORM 尖尖的耳朵
ELSE
    PRINTFORM 的耳朵
ENDIF
IF ABL:PLAYER:2 >= 3
    PRINT ，舌尖从耳道滑到耳廓，不放过任何一丝角落
ELSE
    PRINT …
ENDIF
;-------------------------------------------------
;20,接吻
;-------------------------------------------------
@MESSAGE_COM20
;助手调教中
IF ASSIPLAY
	;顺从LV3以上＋欲望LV2以上＋蔷薇经验1000以上＋ASSIもTARGETも男
	IF ABL:顺从 >= 3 && ABL:欲望 >= 2 && EXP:41 >= 2000 && MALE(ASSI) && MALE(TARGET)
		PRINTFORML %CALLNAME:PLAYER%的舌头在%CALLNAME%的口腔里来回逡巡
		PRINTFORM 两人唇齿相交，交换了彼此的唾液…
	ELSE
		PRINTFORM %CALLNAME:PLAYER%渐渐靠近%CALLNAME%的脸，唇瓣相叠…
	ENDIF
ELSEIF TEQUIP:PLAYER:60
		PRINTFORML 两人唇舌难分难舍地交缠在一起…
ELSE
	IF KANRAKU(TARGET) > 0
		;情欲のPALAMLV4以上
		IF PALAM:6 >= PALAMLV:4
			PRINTFORM 在%CALLNAME%口中逡巡时，%CALLNAME%的舌头积极地缠绕上来…
		ELSE
			PRINTFORM %CALLNAME:PLAYER%渐渐靠近%CALLNAME%的脸，
			PRINTFORM %CALLNAME%
			SELECTCASE KANRAKU(TARGET)
				CASE 1
					PRINT 投来有些可爱的视线，献上了自己的嘴唇…
				CASE 2
					PRINTFORM \@ BIND_CHECK_B(5) ? 磨蹭着%CALLNAME:PLAYER%的身体， # 用手挂上%CALLNAME:PLAYER%的脖子， \@舌头热情地缠了上来…
				CASEELSE
					PRINT 闭上双眼扬起了头…
			ENDSELECT
		ENDIF
	ELSE
		PRINTFORM 双唇相叠，舌头在%CALLNAME%口中肆虐逡巡
		;顺从LV3以上＋欲望LV3以上＋屈服2以上
		IF ABL:顺从 >= 3 && ABL:欲望 >= 3 && MARK:屈服刻印 > 1
			PRINTFORM ，%CALLNAME%的舌头缠绕了上来…
		ELSE
			PRINTFORM …
		ENDIF
	ENDIF
ENDIF
;-------------------------------------------------
;21,什么也不做
;-------------------------------------------------
@MESSAGE_COM21
TFLAG:52 = 1
TFLAG:51 = 1
;-------------------------------------------------
;22,会话
;-------------------------------------------------
@MESSAGE_COM22
PRINTFORM 与%CALLNAME%随意聊了聊…
TFLAG:52 = 1
TFLAG:51 = 1
;-------------------------------------------------
;23,礼物
;-------------------------------------------------
@MESSAGE_COM23
PRINTFORM 把
IF TFLAG:17 == 0
	PRINTFORM 笑容
ELSEIF TFLAG:17 == 1
    IF TALENT:318 && CFLAG:600 == 1
        PRINTFORM 一罐蛋白粉
    ELSE
        IF (TALENT:大人 || TALENT:大叔) && !RAND:2
            PRINTFORM 好喝的酒
        ELSE
            PRINTFORM 美味的点心
        ENDIF
    ENDIF
ELSEIF TFLAG:17 == 2
    IF TALENT:318 && CFLAG:600 == 1
        PRINTFORM 沙袋
    ELSE
	    PRINTFORM 轻便的日常用品
    ENDIF
ELSEIF TFLAG:17 == 3
    IF CFLAG:600 == 1 && CSTR:11 == "拳击"
	    PRINTFORM 拳击手套
	ELSE
		PRINTFORM %CSTR:TARGET:45%
    ENDIF
ELSEIF TFLAG:17 == 4
    IF CFLAG:600 == 1 && CSTR:11 == "篮球"
        PRINT 有签名的球衣
    ELSE
		PRINTFORM %CSTR:TARGET:40%
	ENDIF
;ELSEIF TFLAG:17 == 5
;	PRINTFORM 贵重的周年纪念酒
ELSEIF TFLAG:17 == 6
	PRINTFORM 誓约的戒指
ENDIF
PRINTFORM 送给了%CALLNAME%…
TFLAG:52 = 1
TFLAG:51 = 1
;-------------------------------------------------
;24,身体接触
;-------------------------------------------------
@MESSAGE_COM24
PRINTFORML 与%CALLNAME%进行了身体接触
PRINTFORM %CALLNAME%好像很高兴的样子…
TFLAG:52 = 1
TFLAG:51 = 1
;-------------------------------------------------
;25,悠闲度过
;-------------------------------------------------
@MESSAGE_COM25
PRINTFORM 与%CALLNAME%一起悠闲度过了…
TFLAG:52 = 1
TFLAG:51 = 1
;-------------------------------------------------
;26,家务
;-------------------------------------------------
@MESSAGE_COM26
PRINTFORM 与%CALLNAME%一起做了家务…
TFLAG:52 = 1
TFLAG:51 = 1
;-------------------------------------------------
;27,歌唱练习
;-------------------------------------------------
@MESSAGE_COM27
PRINTFORML 对%CALLNAME%进行了歌唱训练
;歌唱技能
IF ABL:14 == 0
	PRINTFORM 房间里回荡起不堪入耳的歌声…
ELSEIF ABL:14 == 1
	PRINTFORM 房间里回荡起%CALLNAME%的歌声…
ELSEIF ABL:14 == 2
	PRINTFORM 房间里回荡起悦耳的歌声…
ELSEIF ABL:14 == 3
	PRINTFORM 房间里回荡起悠扬动听的歌声…
ELSEIF ABL:14 == 4
	PRINTFORM 房间里回荡起令人着迷的歌声…
ELSE
	PRINTFORM 房间里回荡起夺人心弦的天籁之音…
ENDIF
TFLAG:52 = 1
TFLAG:51 = 1
;-------------------------------------------------
;28,痛骂
;-------------------------------------------------
@MESSAGE_COM28
;调教者が男性
IF TALENT:PLAYER:140 == 1
	PRINTFORML %CALLNAME:PLAYER%对%CALLNAME%劈头盖脸一顿痛骂
ELSE
	PRINTFORML %CALLNAME:PLAYER%辱骂了%CALLNAME%
ENDIF
PRINTFORM %CALLNAME%
;口塞球の有無
IF TEQUIP:20 == 1
	;受虐属性
	IF ABL:9 == 0
		PRINTFORM 虽然想要说些什么来反驳，但因为带着口枷，只能发出呜呜的声音…
	ELSEIF ABL:9 <= 3
		PRINTFORML 一瞬间做出了反抗的姿态，但碍于口塞球的存在，什么都没能说出口…
	ELSEIF ABL:9 <= 5
		PRINTFORM 似乎因为被骂而兴奋起来…
	ELSE
		PRINTL 面泛红潮，鼻息紊乱，似乎产生快感兴奋了起来…
		PRINT 口水从口枷跟嘴的缝隙间流出，
		IF TALENT:鱼尾 || TALENT:蛇尾
		    PRINT 下身中心
		ELSE
		    PRINT 两腿之间
		ENDIF
		SIF TALENT:140 || TALENT:139 || TALENT:137
			PRINT 顶起了帐篷，
		SIF TALENT:139 || TALENT:137
			PRINT 还
		SIF TALENT:140 == 0
			PRINT 湿了一片
		PRINTL ，显得极为狼狈
        IF TALENT:鱼尾 || TALENT:蛇尾 || TALENT:马身
            PRINTFORM %CALLNAME%身下汇聚起水坑，
            PRINTFORML 仿佛不知羞耻似的用硬挺起来的乳头蹭%CALLNAME:PLAYER%的腿
        ELSE
            PRINTFORM %CALLNAME%脚下汇聚起水坑，
            PRINTFORML 仿佛不知羞耻似的爬到%CALLNAME:PLAYER%脚下用硬挺起来的乳头蹭%CALLNAME:PLAYER%的腿
        ENDIF
		PRINTL 像是在期待更为过分的辱骂般带着淫靡的视线仰望过来…
	ENDIF
ELSE
	;受虐属性
	IF ABL:9 == 0
		IF TALENT:胆怯 || TALENT:直率
			PRINTFORM 的眼中泛起泪光，默默忍受%CALLNAME:PLAYER%恶劣难听的言辞…
		;反抗or高傲or自大
		ELSEIF TALENT:11 || TALENT:15 || TALENT:16
			PRINTFORM 满目凶狠地瞪着%CALLNAME:PLAYER%，牙齿咬得咯咯作响…
		ELSE
			PRINTFORM 满脸愤怒和屈辱地瞪着%CALLNAME:PLAYER%…
		ENDIF
	ELSEIF ABL:9 <= 3
		PRINTFORM 虽然狠狠地瞪着%CALLNAME:PLAYER%，神色间却有一丝快感…
	ELSEIF ABL:9 <= 5
		PRINTFORM 似乎在骂声中察觉到了快感…
	ELSE
		PRINTFORML 好像因为被骂而兴奋了起来…
		PRINTFORML %CALLNAME%满面潮红，眼含媚色地看向%CALLNAME:PLAYER%
		PRINTFORM 以哀求的语气乞求着新的更为恶劣的辱骂…
		;刚强か高傲がある
		IF (TALENT:12 || TALENT:15) && !RAND:4
			PRINTL 
			PRINTFORM 在辱骂中感到兴奋的%CALLNAME%身上已经找不到以前的面貌了…
		ENDIF
	ENDIF
ENDIF
TFLAG:52 = 1
TFLAG:51 = 1
;-------------------------------------------------
;29,观看视频
;-------------------------------------------------
@MESSAGE_COM29
PRINTFORML %CALLNAME:PLAYER%和%CALLNAME%一起观看了记录着%CALLNAME%痴态的录像带
PRINTFORM 看到画面上自己淫乱的身姿，%CALLNAME%脸红着别开了视线…
TFLAG:52 = 1
TFLAG:51 = 1
;-------------------------------------------------
;30,暴力を振るう;口上入れるときは地の文無視して下さい
;-------------------------------------------------
@MESSAGE_COM30
IF ASSIPLAY
	PRINTFORM %CALLNAME:ASSI%
ELSE
    PRINTFORM %CALLNAME:PLAYER%
ENDIF
IF PREVCOM == 81 || PREVCOM == 82 || PREVCOM == 86 || PREVCOM == 120 || PREVCOM == 123
	PRINTFORM 将%CALLNAME%一脚踹倒，毫不留情地践踏上去…
ELSEIF RAND:2 == 0
	PRINTFORM 把%CALLNAME%推倒，毫不留情地殴打了%CALLNAME%的面部…
ELSE
	PRINTFORM 狠狠地抓起了%CALLNAME%的脖子…
ENDIF
;-------------------------------------------------
;31,化形
;-------------------------------------------------
@MESSAGE_COM31
IF CFLAG:633
    PRINTFORM %CALLNAME%显现出形体…
ELSE
    PRINTFORM %CALLNAME%变换了身姿…
ENDIF
TFLAG:52 = 1
TFLAG:51 = 1
;-------------------------------------------------
;32,变化体型
;-------------------------------------------------
@MESSAGE_COM32
PRINTFORM %CALLNAME%变化了体型…
TFLAG:52 = 1
TFLAG:51 = 1
;-------------------------------------------------
;33,助手化形
;-------------------------------------------------
@MESSAGE_COM33
IF ASSIPLAY
	IF CFLAG:ASSI:633
        PRINTFORM %CALLNAME:ASSI%显现出形体…
    ELSE
        PRINTFORM %CALLNAME:ASSI%变换了身姿…
    ENDIF
ENDIF
TFLAG:52 = 1
TFLAG:51 = 1
;-------------------------------------------------
;34,助手变化体型
;-------------------------------------------------
@MESSAGE_COM34
SIF ASSIPLAY
	PRINTFORM %CALLNAME:ASSI%变化了体型…
TFLAG:52 = 1
TFLAG:51 = 1
;-------------------------------------------------
;40,跳蛋
;-------------------------------------------------
@MESSAGE_COM40
IF ASSIPLAY
	PRINTFORM %CALLNAME:ASSI%
ELSE
    PRINTFORM %CALLNAME:PLAYER%
ENDIF
;扶她＋情欲のPALAMLV3以上
IF (TALENT:137 || TALENT:139) && PALAM:6 >= PALAMLV:3
	PRINTFORM 将震动着的跳蛋按在了湿泞的秘唇上…
;扶她
ELSEIF (TALENT:137 || TALENT:139)
	PRINTFORM 将震动着的跳蛋按在了秘唇上…
;情欲のPALAMLV3以上
ELSEIF PALAM:6 >= PALAMLV:3
	;【男性】
	IF TALENT:140 
		PRINTFORM 将震动着的跳蛋按在了红肿的阴茎上…
	ELSE
		PRINTFORM 将震动着的跳蛋推入了湿泞的小穴…
	ENDIF
ELSE
	;【男性】
	IF TALENT:140
		PRINTFORM 将震动着的跳蛋按在了阴茎上…
	ELSE
		PRINTFORM 将震动着的跳蛋推入了小穴…
	ENDIF
ENDIF
;-------------------------------------------------
;41,电动按摩棒
;-------------------------------------------------
@MESSAGE_COM41
IF ASSIPLAY
	PRINTFORM %CALLNAME:ASSI%
ELSE
    PRINTFORM %CALLNAME:PLAYER%
ENDIF
IF TALENT:140
	PRINTFORM 将震动着的电动按摩器压在%CALLNAME%的阴茎上…
ELSE
	PRINTFORM 将震动着的电动按摩器压在%CALLNAME%的阴唇上…
ENDIF

;Ｃ感觉LV3未満＋情欲のPALAMLV3未満
IF ABL:3 < 3 && PALAM:6 < PALAMLV:3
;（この描写は汎用喘ぎと矛盾するので、コメントアウト）
;	PRINTL
;	PRINTFORM %CALLNAME%好像有点痒的样子…
;	TFLAG:52 = 1
;Ｃ感觉LV3未満＋情欲のPALAMLV3以上
ELSEIF ABL:3 < 3 && PALAM:6 >= PALAMLV:3
;（この描写は汎用喘ぎと矛盾するので、コメントアウト）
;	PRINTL
;	PRINTFORM %CALLNAME%好像什么都说不出来了…
;	TFLAG:52 = 1
;恋慕か相亲相爱＋Ｃ感觉LV4以上＋情欲のPALAMLV3未満＋助手调教中ではない
ELSEIF (TALENT:3 || TALENT:6) && ABL:3 >= 4 && PALAM:6 < PALAMLV:3 && ASSIPLAY == 0 && D < 2
	PRINTL
	IF TALENT:鱼尾 || TALENT:蛇尾 || TALENT:马身
	    PRINTFORM %CALLNAME%迫不及待地抬起身子接受着%CALLNAME:PLAYER%的蹂躏…
	ELSE
	    PRINTFORM %CALLNAME%迫不及待地张开脚接受着%CALLNAME:PLAYER%的蹂躏…
	ENDIF
	TFLAG:52 = 1
;恋慕か相亲相爱＋Ｃ感觉LV4以上＋情欲のPALAMLV3以上＋情欲のPALAMLV5未満＋助手调教中ではない
ELSEIF (TALENT:3 || TALENT:6) && ABL:3 >= 4 && PALAM:6 >= PALAMLV:3 && PALAM:6 < PALAMLV:5 && ASSIPLAY == 0 && D < 2
	PRINTL
	PRINTFORM %CALLNAME%一脸快乐地淌着口涎，数次呼唤%CALLNAME:PLAYER%的名字…
	TFLAG:52 = 1
;恋慕か相亲相爱＋Ｃ感觉LV4以上＋情欲のPALAMLV5以上＋助手调教中ではない
ELSEIF (TALENT:3 || TALENT:6) && ABL:3 >= 4 && PALAM:6 >= PALAMLV:5 && ASSIPLAY == 0 && D < 2
	PRINTL
	PRINTFORM %CALLNAME%紧紧搂住%CALLNAME:PLAYER%的身体，沉浸在绝顶的恍惚感中…
	TFLAG:52 = 1
;Ｃ感觉LV3以上＋情欲のPALAMLV3未満
ELSEIF ABL:3 >= 3 && PALAM:6 < PALAMLV:3 && D < 2
	PRINTL
	PRINTFORM %CALLNAME%似乎因强烈的快感满脸通红…
	TFLAG:52 = 1
;Ｃ感觉LV3以上＋情欲のPALAMLV3以上＋情欲のPALAMLV5未満
ELSEIF ABL:3 >= 3 && PALAM:6 >= PALAMLV:3 && PALAM:6 < PALAMLV:5 && D < 2
	PRINTL
	PRINTFORM %CALLNAME%两眼失神，沉醉在快感当中…
	TFLAG:52 = 1
;Ｃ感觉LV3以上＋情欲のPALAMLV5以上
ELSEIF ABL:3 >= 3 && PALAM:6 >= PALAMLV:5 && D < 2
	PRINTL
	PRINTFORM %CALLNAME%眼看就要高潮了…
	TFLAG:52 = 1
ENDIF



;-------------------------------------------------
;42,阴蒂夹
;-------------------------------------------------
@MESSAGE_COM42
;解除時
IF TEQUIP:11
	PRINTFORM 从%CALLNAME%的阴蒂上取下了电动阴蒂夹…
ELSE
	PRINTFORM 给%CALLNAME%装上了电动阴蒂夹…
ENDIF
TFLAG:51 = 1
TFLAG:52 = 1
;-------------------------------------------------
;43,飞机杯
;-------------------------------------------------
@MESSAGE_COM43
;解除時
IF TEQUIP:12
	PRINTFORM 从%CALLNAME%的阴茎上取下了飞机杯…
ELSE
	PRINTFORM 给%CALLNAME%
	;情欲のPALAMLV3以上
	IF PALAM:6 >= PALAMLV:3
		PRINTFORM 已完全挺立
	    IF TALENT:包茎
			PRINT ，裹着包皮
    		IF TALENT:孩童尺寸
    		    SIF TALENT:矮小体型 == 0
    		        PRINT ，与其体型不符
		    	PRINT 的小小
		    ELSEIF TALENT:短小
		        SIF TALENT:高大体型
		            PRINT ，与其高大体型不符
		        PRINT 的短小
		    ELSEIF TALENT:巨根
		        SIF TALENT:矮小体型
		            PRINT ，与其体型不符
		    	PRINT 的粗大
		    ELSEIF TALENT:尺寸傲人
		        SIF TALENT:矮小体型
		            PRINT ，与其体型不符
		    	PRINT ，尺寸傲人的
		    ELSE
		    	PRINT 的
		    ENDIF
	    ELSE
    		IF TALENT:孩童尺寸
    		    SIF TALENT:矮小体型 == 0
    		        PRINT ，与其体型不符
		    	PRINT 的小小
		    ELSEIF TALENT:短小
		        SIF TALENT:高大体型
		            PRINT ，与其高大体型不符
		        PRINT 的短小
		    ELSEIF TALENT:巨根
		        SIF TALENT:矮小体型
		            PRINT ，与其体型不符
		    	PRINT 的粗大
		    ELSEIF TALENT:尺寸傲人
		        SIF TALENT:矮小体型
		            PRINT ，与其体型不符
		    	PRINT ，尺寸傲人的
		    ELSE
		    	PRINT 的
		    ENDIF
		ENDIF
	ELSE
	    IF TALENT:包茎
		    PRINT 裹着包皮
    	    IF TALENT:孩童尺寸
    		    SIF TALENT:矮小体型 == 0
    		        PRINT ，与其体型不符
		    	PRINT 的小小
		    ELSEIF TALENT:短小
		        SIF TALENT:高大体型
		            PRINT ，与其高大体型不符
		        PRINT 的短小
		    ELSEIF TALENT:巨根
		        SIF TALENT:矮小体型
		            PRINT ，与其体型不符
		    	PRINT 的粗大
		    ELSEIF TALENT:尺寸傲人
		        SIF TALENT:矮小体型
		            PRINT ，与其体型不符
		        PRINT ，尺寸傲人的
		    ELSE
		    	PRINT 的
		    ENDIF
		ELSE
    		IF TALENT:孩童尺寸
    		    SIF TALENT:矮小体型 == 0
    		       PRINT 与其体型不符
		    	PRINT 的小小
		    ELSEIF TALENT:短小
		        SIF TALENT:高大体型
		            PRINT 与其高大体型不符
		        PRINT 的短小
		    ELSEIF TALENT:巨根
		        SIF TALENT:矮小体型
		            PRINT 与其体型不符
		    	PRINT 的粗大
		    ELSEIF TALENT:尺寸傲人
		        SIF TALENT:矮小体型
		            PRINT 与其体型不符，
		    	PRINT 尺寸傲人的
		    ELSE
		        PRINT 的
		    ENDIF
		ENDIF
    ENDIF
	PRINT 阴茎
	IF TALENT:尺寸傲人
		PRINT 勉强地套上了飞机杯…
	ELSE
		PRINT 套上了飞机杯…
	ENDIF
ENDIF
TFLAG:51 = 1
TFLAG:52 = 1
;-------------------------------------------------
;44,自慰棒
;-------------------------------------------------
@MESSAGE_COM44
;解除時
IF TEQUIP:13
	PRINTFORML 从%CALLNAME%身体里抽出了蠢动着的自慰棒
	;Ｖ经验200以上＋(恋慕ルートで助手でないor=淫乱ルート)
	IF EXP:0 > 200 && ((KANRAKU(TARGET) == 1 && !ASSIPLAY ) || KANRAKU(TARGET) == 2)
		PRINTFORM %CALLNAME%不太适应阴道里空荡荡的失落感，眼泪跟口涎不自觉地流淌着
	;Ｖ经验150以上＋(恋慕ルートで助手でないor=淫乱ルート)
	ELSEIF EXP:0 > 150 && ((KANRAKU(TARGET) == 1 && !ASSIPLAY ) || KANRAKU(TARGET) == 2)
		PRINTFORM %CALLNAME%阴道里积蓄着的液体被连带着挤出，滴滴答答的在地上形成了一个水坑
	;Ｖ经验120以上＋(恋慕ルートで助手でないor=淫乱ルート)
	ELSEIF EXP:0 > 120 && ((KANRAKU(TARGET) == 1 && !ASSIPLAY ) || KANRAKU(TARGET) == 2)
		PRINTFORM 被拔走自慰棒后，%CALLNAME%的内壁流出了晶莹的爱液
	;Ｖ经验70以上＋恋慕ルート
	ELSEIF EXP:0 > 70 && (KANRAKU(TARGET) == 1 || KANRAKU(TARGET) == 2)
		PRINTFORM %CALLNAME%扭动身子，一边看着%PLAYER_NAME(2)%一边
		IF TALENT:鱼尾 || TALENT:蛇尾
		    PRINT 扭动着腰肢
		ELSEIF TALENT:马身
		    PRINT 扭动着臀部
        ELSE
		    PRINT 摩擦起双腿
		ENDIF
	;Ｖ经验40以上
	ELSEIF EXP:0 > 40
		PRINTFORM %CALLNAME%吐出了热切的呼吸
	;Ｖ经验20以上
	ELSEIF EXP:0 > 20
		PRINTFORM %CALLNAME%拉开了松弛的阴道口
	;Ｖ经验20未満
	ELSE
		PRINTFORM %CALLNAME%露出松了一口气的表情
	ENDIF
	PRINTFORM …
	TFLAG:52 = 1
ELSE
	PRINTFORM %CALLNAME:PLAYER%把自慰棒的前端贴在
	;润滑LV5以上でさらに追加
	SIF PALAM:4 >= PALAMLV:5
		PRINTFORM 沾满
	;润滑LV3以上でさらに追加
	SIF PALAM:4 >= PALAMLV:3
		PRINTFORM 粘液的
	;矮小体型
	IF TALENT:100
		PRINTFORM 娇嫩小穴
	ELSE
		PRINTFORM 秘裂
	ENDIF
	PRINTFORML 上待其适应

	PRINTFORM %CALLNAME%
	;处女＋娼妇+今も男
	IF TALENT:0 && TALENT:7 && MALE(TARGET)
		PRINTFORM 想到自己即将结束处女生涯，不由展现出淫靡的笑容，已经把曾是男人的过去抛在脑后了
	;处女＋娼妇+性転換した
	ELSEIF TALENT:0 && TALENT:7 && !MALE(TARGET)
		PRINTFORM 想到自己即将结束处女生涯，不由展现出淫靡的笑容，已经把曾是男人的过去抛在脑后了
	;处女＋恋慕か相亲相爱
	ELSEIF TALENT:0 && (TALENT:3 || TALENT:6)
		PRINTFORM 有点寂寞地看向%PLAYER_NAME(1)%
	;处女＋淫乱
	ELSEIF TALENT:0 && TALENT:4
		PRINTFORM 毫不在乎是否会破处，只一个劲儿地扭摆腰部
	;处女＋难以越过的底线
	ELSEIF TALENT:0 && TALENT:27
		PRINTFORM 像是已经放弃了什么似的，眼含泪水
	;处女
	ELSEIF TALENT:0
		PRINTFORM 脸色青白，浑身战栗，畏惧着失去处女的那一刻
	;Ｖ经验40以上＋恋慕ルートか淫乱ルート
	ELSEIF EXP:0 > 40 && (KANRAKU(TARGET) == 1 || KANRAKU(TARGET) == 2)
		;Ｖ经验200以上
		IF EXP:0 > 200
			PRINTFORM 身心都期待着被插入，嘎吱嘎吱地晃动着腰
		;Ｖ经验150以上
		ELSEIF EXP:0 > 150
			PRINTFORM 用像是要融化了似的眼神看着%CALLNAME:PLAYER%，自己抚弄起小穴边缘
		;Ｖ经验120以上
		ELSEIF EXP:0 > 120
			PRINTFORM 一脸恍惚，迫不及待地晃动着腰
		;Ｖ经验70以上
		ELSEIF EXP:0 > 70
			PRINTFORM 用湿润的眼睛看着%PLAYER_NAME(2)%
		;Ｖ经验40以上
		ELSE
			PRINTFORM 沉浸在自慰棒的摩擦中，吐露出浅碎的呻吟
		ENDIF
	;Ｖ经验20以上
	ELSEIF EXP:0 > 20
		PRINTFORM 在自慰棒的摩擦下分泌出爱液
	;Ｖ经验0以上
	ELSEIF EXP:0 >= 0
		PRINTFORM 稍微挺起了下身迎接自慰棒
	ENDIF
	PRINTFORML …
	PRINTFORM %CALLNAME:PLAYER%往%CALLNAME%
	;润滑LV4以上
	SIF PALAM:4 >= PALAMLV:4
		PRINTFORM 湿漉漉
	PRINTFORM 的性器里插入了自慰棒…
	TFLAG:52 = 1
ENDIF
TFLAG:51 = 1
TFLAG:52 = 1
;-------------------------------------------------
;45,肛塞
;-------------------------------------------------
@MESSAGE_COM45
;解除時
IF TEQUIP:14
	PRINTFORML 从%CALLNAME%的屁股里拔出了肛塞
	PRINTFORM %CALLNAME%
	;Ａ经验180以上＋淫乱ルート
	IF EXP:1 > 180 && KANRAKU(TARGET) == 2
		PRINTFORM 的肛穴里流出粘液，穴口寂寞难耐地翕张着
	;Ａ经验140以上＋淫乱ルート
	ELSEIF EXP:1 > 140 && KANRAKU(TARGET) == 2
		PRINTFORM 投来湿润的眼神，摇动着屁股
	;Ａ经验100以上＋淫乱ルート
	ELSEIF EXP:1 > 100 && KANRAKU(TARGET) == 2
		PRINTFORM 抬高屁股，尤嫌不够
	;Ａ经验70以上
	ELSEIF EXP:1 > 70
		PRINTFORM 有点恋恋不舍地摇动屁股
	;Ａ经验40以上
	ELSEIF EXP:1 > 40
		PRINTFORM 晃了晃屁股，流露出松了口气的表情
	;Ａ经验20以上
	ELSEIF EXP:1 > 20
		PRINTFORM 因强制的排泄感脸颊泛红
	;Ａ经验0以上
	ELSEIF EXP:1 >= 0
		PRINTFORM 神情痛苦，忍受着不适
	ENDIF
	PRINTFORM …
	TFLAG:52 = 1
ELSE
	PRINTFORM 把
	IF TALENT:143
		IF TALENT:143 == 11
			PRINT 皮毛光滑的猫咪
		ELSEIF TALENT:143 == 12
			PRINT 毛茸茸的小狗
		ELSEIF TALENT:143 == 13
			PRINT 软绵绵的兔子
		ELSE
			PRINT 与其兽耳种类相匹配的
		ENDIF
		PRINT 尾巴模样的
	ENDIF
	PRINTFORML 肛塞贴在了%CALLNAME%的肛门口…
	PRINTFORM %CALLNAME%
	;Ａ经验180以上
	IF EXP:1 > 180
		PRINTFORM 一副快要融化了的样子，主动扒开自己的穴口
	;Ａ经验140以上
	ELSEIF EXP:1 > 140
		PRINTFORM 扭动腰部，心急如焚地等待着插入
	;Ａ经验100以上
	ELSEIF EXP:1 > 100
		PRINTFORM 双颊微红地摇晃起屁股
	;Ａ经验70以上
	ELSEIF EXP:1 > 70
		PRINTFORM 的身体因肛塞感触的刺激颤抖着
	;Ａ经验40以上
	ELSEIF EXP:1 > 40
		PRINTFORM 的脸上混杂着期待与抗拒的神情
	;Ａ经验20以上
	ELSEIF EXP:1 > 20
		PRINTFORM 的脸羞得通红
	;Ａ经验0以上
	ELSEIF EXP:1 >= 0
		PRINTFORM 因被侵犯后面肮脏的肛穴，脸上满是屈辱和羞耻
	ENDIF
	PRINTFORML …
	PRINTFORM %CALLNAME:PLAYER%往%CALLNAME%
	;润滑LV4以上
	SIF PALAM:4 >= PALAMLV:4
		PRINTFORM 沾满粘液
	PRINTFORM 的肛门插入了肛塞…
	TFLAG:52 = 1
ENDIF
TFLAG:51 = 1
TFLAG:52 = 1
;-------------------------------------------------
;46,后庭拉珠
;-------------------------------------------------
@MESSAGE_COM46
IF ASSIPLAY
	PRINTFORM %CALLNAME:ASSI%
ELSE
    PRINTFORM %CALLNAME:PLAYER%
ENDIF
IF TEQUIP:15
	PRINTFORM 把埋在%CALLNAME%屁股里的后庭拉珠一口气全都拉了出来…
ELSE
	PRINTFORM 让%CALLNAME%的肛穴一颗颗地吞入了后庭拉珠…
ENDIF
TFLAG:51 = 1
TFLAG:52 = 1
;-------------------------------------------------
;47,振动乳头夹
;-------------------------------------------------
@MESSAGE_COM47
IF TEQUIP:16
	PRINTFORM 从%CALLNAME%的乳头上取下了振动乳头夹
ELSE
	PRINTFORM 把振动乳头夹夹在%CALLNAME%的乳头上了
ENDIF
TFLAG:51 = 1
TFLAG:52 = 1
;-------------------------------------------------
;48,乳头跳蛋
;-------------------------------------------------
@MESSAGE_COM48
IF ASSIPLAY
	PRINTFORM %CALLNAME:ASSI%
ELSE
    PRINTFORM %CALLNAME:PLAYER%
ENDIF
PRINTFORM 把跳蛋压在了%CALLNAME%
;母乳体质
SIF TALENT:149
	PRINTFORM 淌着乳汁
;情欲のPLAMLV3以上
SIF PALAM:6 >= PALAMLV:3
	PRINTFORM 因期待而挺立起来
PRINTFORM 的乳头上…
;-------------------------------------------------
;49,挤奶器
;-------------------------------------------------
@MESSAGE_COM49
;解除時
IF TEQUIP:17
	PRINTFORM 从%CALLNAME%胸上取下了挤奶器
ELSE
	PRINTFORM 把挤奶器装备在%CALLNAME%的胸上了
ENDIF
TFLAG:51 = 1
TFLAG:52 = 1
;-------------------------------------------------
;50,导尿管
;-------------------------------------------------
@MESSAGE_COM50
IF TEQUIP:38 == 1
    IF TALENT:人偶 ||  TALENT:机器人
    	IF TEQUIP:25
    		PRINT 触手
    	ELSE
    		PRINT 把导尿管
    	ENDIF
    	PRINTFORM 从%CALLNAME%的尿道里拔了出来
    ELSE
        IF TEQUIP:25
            PRINT 触手
        ELSE
            PRINT 把导尿管
        ENDIF
        PRINTFORM 从%CALLNAME%尿道里拔出去的同时，
        PRINTL 温热的尿液喷射出来，在空中描绘出抛物线。
        IF ABL:9 >= 5 && TALENT:57
            PRINTFORML %CALLNAME%深深沉浸在小便的快感中。
            PRINTFORM %CALLNAME%的表情因排泄感而松缓，
            PRINTFORM 尿液
            IF TALENT:140
                PRINT 和口水
            ELSE
                PRINT 和爱液
            ENDIF
            PRINTL 一起流淌着。
        ELSEIF ABL:9 >= 4
            PRINTFORML %CALLNAME%的脸上交替着陶醉感和解放感
            PRINTFORML 满怀喜悦地看着自己潮吹似的喷洒着小便的尿道口。
        ELSEIF ABL:9 == 3
            PRINTFORML %CALLNAME%的脸上呈现出一种解放后的清爽感
            PRINTFORML 似乎是终于能够排泄的心情战胜了羞耻心。
        ELSEIF ABL:9 >= 1
            PRINTFORML %CALLNAME%的身体弯曲成く形，下腹留下的残尿感让%CALLNAME%发出苦闷的呻吟。
        ELSE
            PRINTFORML %CALLNAME%因太过羞耻，拼命地别过头不想看自己滴着尿液的性器
        ENDIF
        PRINTFORM %EXPNAME:31%＋２
        EXP:31 += 2
    ENDIF
	TFLAG:52 = 1
ELSE
	IF TEQUIP:25
		PRINTFORM 极细的触手钻入了%CALLNAME%的尿道
	ELSE
		IF ASSIPLAY
        	PRINTFORM %CALLNAME:ASSI%
        ELSE
            PRINTFORM %CALLNAME:PLAYER%
        ENDIF
		PRINTFORML 在%CALLNAME%的尿道里插入了尿道开发用的导尿管
		PRINTFORM %CALLNAME%因尿道里的异物感发出痛苦的呜咽…
		TFLAG:52 = 1
	ENDIF
ENDIF
TFLAG:51 = 1
TFLAG:52 = 1
;-------------------------------------------------
;60,正常位
;-------------------------------------------------
@MESSAGE_COM60
;前回と今回の调教者が同じで、前回の调教が正常位か下着ずらし做爱であり、抜かないフラグがある場合
IF TFLAG:60 && (PREVCOM == 60 || PREVCOM == 72 || PREVCOM == 517 || (TFLAG:709 == 1 && PREVCOM == 593)) && ((ASSIPLAY && TFLAG:50) || (ASSIPLAY == 0 && TFLAG:50 == 0))
	;调教者が男性か双性か扶她である
	IF TALENT:PLAYER:137 || TALENT:PLAYER:139 || TALENT:PLAYER:140
		PRINTFORM 一边品尝着%CALLNAME%小穴内壁的触感一边摆动起腰部…
	;捆绑式假阴茎を使用している場合
	ELSE
		PRINTFORML 戴着捆绑式假阳具翻搅玩弄%CALLNAME%的小穴
		PRINTFORM 一次又一次地摆动着腰部…
	ENDIF
;同様に前回の调教が后背位か骑乘位、对面座位、背面座位、对面立位、背面立位、乱れ牡丹の場合
ELSEIF TFLAG:60 && (PREVCOM == 61 || PREVCOM == 66 || PREVCOM == 68 || PREVCOM == 69 || PREVCOM == 74 || PREVCOM == 76 || PREVCOM == 515 || (TFLAG:709 == 1 && (PREVCOM == 504 || PREVCOM == 505 || PREVCOM == 514 || PREVCOM == 592))) && ((ASSIPLAY && TFLAG:50) || (ASSIPLAY == 0 && TFLAG:50 == 0))
	PRINTFORML 把\@ PENIS(PLAYER) ? 阴茎 # 假阳具 \@从%CALLNAME%体内拔出来
	PRINTFORM 让%CALLNAME%摆成仰面向上的姿势，再次活动起腰部…
ELSE
	IF ASSIPLAY
    	PRINTFORM %CALLNAME:ASSI%
    ELSE
        PRINTFORM %CALLNAME:PLAYER%
    ENDIF
	;着衣プ雷オン
	IF TALENT:PLAYER:58 && (TEQUIP:2 || TEQUIP:4 || TEQUIP:6)
		IF TEQUIP:6 == 7 || TEQUIP:6 == 9
			A = 6
			PRINTFORM 拉下%CALLNAME%
			CALL CLOTHES_NAME
			PRINT 的拉链，
		ELSEIF TEQUIP:6
			A = 6
			PRINTFORM 卷起%CALLNAME%
			CALL CLOTHES_NAME
			PRINT 的下摆，
		ENDIF
		IF TEQUIP:4 == 1
			A = 4
			PRINTFORM 卷起%CALLNAME%的
			CALL CLOTHES_NAME
			PRINT ，
		ELSEIF TEQUIP:4
			A = 4
			PRINTFORM 把%CALLNAME%的
			CALL CLOTHES_NAME
			PRINT 脱至膝弯，
		ELSEIF TEQUIP:2
			A = 2
			PRINTFORM 把%CALLNAME%的
			CALL CLOTHES_NAME
            IF TALENT:鱼尾 || TALENT:蛇尾
                PRINT 脱下来，
            ELSE
                PRINT 脱下来挂到一只腿上，
            ENDIF
		ELSE
			IF TEQUIP:2 == 1
        		PRINTFORM 拨开内裤，
        	ELSEIF TEQUIP:2 == 2
        		PRINTFORM 拨开内衣，
        	ELSEIF TEQUIP:2 == 3
        		PRINTFORM 错开兜裆布，
        	ELSEIF TEQUIP:2 == 6
        		PRINTFORM 拉开灯笼裤，
        	ELSEIF TEQUIP:4 == 6
        		PRINTFORM 错开运动短裤，
        	ENDIF
		ENDIF
	ENDIF
	;丝袜系
	IF TEQUIP:1 == 5 || TEQUIP:1 == 6
		IF TFLAG:55
			PRINT 撕开
		ELSE
			PRINT 撕烂
		ENDIF
	SIF TEQUIP:1 == 5
		PRINT 丝袜，
	SIF TEQUIP:1 == 6
		PRINT 网袜，
	ENDIF
	;自慰棒入れっぱ无
	SIF TEQUIP:13
		PRINTFORM 强行挤开还含着自慰棒的穴口，
	;尾巴插入
	SIF TEQUIP:51
		PRINTFORM 强行挤开还含着尾巴的穴口，
	PRINTFORM 用自己
	;调教者が男性か双性か扶她である
	IF TALENT:PLAYER:137 || TALENT:PLAYER:139 || TALENT:PLAYER:140
		IF TALENT:PLAYER:孩童尺寸
			SIF TALENT:PLAYER:矮小体型 == 0 || !TALENT:PLAYER:扶她
				PRINT 与体型不符
			PRINT 的小小
		ELSEIF TALENT:PLAYER:短小
			SIF TALENT:PLAYER:高大体型 || !TALENT:PLAYER:扶她
				PRINT 与体型不符
			PRINT 的短小
		ELSEIF TALENT:PLAYER:巨根
			SIF TALENT:PLAYER:矮小体型
				PRINT 与体型不符
			PRINT 的粗大
		ELSEIF TALENT:PLAYER:尺寸傲人
			SIF TALENT:PLAYER:矮小体型
				PRINT 与体型不符，
			PRINT 尺寸傲人的
		ELSE
		    PRINT 的
		ENDIF
		PRINT 阴茎
	ELSE
		PRINT 的假阳具
	ENDIF
	PRINTFORM 贯穿了%CALLNAME%
	;处女
	IF TALENT:0
		PRINT 未经人事
		;润滑LV4以上
    	IF PALAM:4 >= PALAMLV:4
    		PRINT ，湿淋淋的
    		;矮小体型
    		IF TALENT:100
        		PRINT 娇嫩小穴…
        	ELSE
        		PRINT 秘裂…
        	ENDIF
        ELSE
            PRINT 的
    		;矮小体型
    		IF TALENT:100
        		PRINT 娇嫩小穴…
        	ELSE
        		PRINT 秘裂…
        	ENDIF
        ENDIF
	ELSE
		;润滑LV4以上
    	IF PALAM:4 >= PALAMLV:4
    		PRINT 湿淋淋的
    		;矮小体型
    		IF TALENT:100
        		PRINT 娇嫩小穴…
        	ELSE
        		PRINT 秘裂…
        	ENDIF
        ELSE
            PRINT 的
    		;矮小体型
    		IF TALENT:100
        		PRINT 娇嫩小穴…
        	ELSE
        		PRINT 秘裂…
        	ENDIF
        ENDIF
	ENDIF
ENDIF
;顺从および欲望LV3以上+(助手でない or 淫乱ルート)
IF ABL:0 >= 3 && ABL:1 >= 3 && !RAND:2 && (!ASSIPLAY || KANRAKU(TARGET) == 2)
	PRINTFORML
    IF TALENT:鱼尾
	    PRINTFORM %CALLNAME%的鱼尾快活地摆动着…
    ELSEIF TALENT:蛇尾
	    PRINTFORM %CALLNAME%蛇尾游走缠住了%CALLNAME:PLAYER%的腰…
    ELSE
	    PRINTFORM %CALLNAME%双腿交叉环住了%CALLNAME:PLAYER%的腰…
    ENDIF
	TFLAG:52 = 1
ENDIF
;Ｖ扩张经验２０以上
IF EXP:52 >= 20 && TALENT:PLAYER:孩童尺寸&& !RAND:3
	PRINTL
	PRINTFORM 在%CALLNAME%已经充分扩张过的阴道面前，%CALLNAME:PLAYER%小孩子尺寸的阴茎完全不能满足%CALLNAME%…
	TFLAG:52 = 1
ELSEIF EXP:52 >= 20 && TALENT:PLAYER:短小 && !RAND:3
	PRINTL
	PRINTFORM 在%CALLNAME%已经充分扩张过的阴道面前，%CALLNAME:PLAYER%短小的阴茎完全派不上用场…
	TFLAG:52 = 1
ENDIF
;-------------------------------------------------
;61,后背位
;-------------------------------------------------
@MESSAGE_COM61
;前回と今回の调教者が同じで、前回の调教が后背位､打屁股做爱､插入中阴茎爱抚であり、抜かないフラグがある場
IF TFLAG:60 && (PREVCOM == 61 || PREVCOM == 515 || (TFLAG:709 == 1 && PREVCOM == 593)) && ((ASSIPLAY && TFLAG:50) || (ASSIPLAY == 0 && TFLAG:50 == 0))
	;调教者が男性か双性か扶她である
	IF TALENT:PLAYER:137 || TALENT:PLAYER:139 || TALENT:PLAYER:140
		PRINTFORM 一边品尝着%CALLNAME%小穴内壁的触感一边摆动起腰部…
	;捆绑式假阴茎を使用している場合
	ELSE
		PRINTFORML 戴着捆绑式假阳具翻搅玩弄%CALLNAME%的小穴
		PRINTFORM 一次又一次地摆动着腰部…
	ENDIF
;同様に、前回の调教が正常位､错开内裤插入である場合
ELSEIF TFLAG:60 && (PREVCOM == 60 || PREVCOM == 72) && ((ASSIPLAY && TFLAG:50) || (ASSIPLAY == 0 && TFLAG:50 == 0))
	PRINTFORML 把\@ PENIS(PLAYER) ? 阴茎 # 假阳具 \@从%CALLNAME%体内拔出来
	PRINTFORM 让%CALLNAME%背过去趴下，再次活动起腰部…
;同様に、前回の调教が对面座位、背面座位、骑乘位、对面立位、背面立位､乱れ牡丹､駅弁､首絞め做爱､騎乗阴茎自慰である場合
ELSEIF TFLAG:60 && (PREVCOM == 66 || PREVCOM == 68 || PREVCOM == 69 || PREVCOM == 74 || PREVCOM == 76 || PREVCOM == 517 || (TFLAG:709 == 1 && (PREVCOM == 504 || PREVCOM == 505 || PREVCOM == 514 || PREVCOM == 592))) && ((ASSIPLAY && TFLAG:50) || (ASSIPLAY == 0 && TFLAG:50 == 0))
	PRINTFORM 保持着插入的姿势将%CALLNAME%摆弄成四肢着地的模样，再次活动起腰部…
ELSE
	IF ASSIPLAY
    	PRINTFORM %CALLNAME:ASSI%
    ELSE
        PRINTFORM %CALLNAME:PLAYER%
    ENDIF
	;着衣プ雷オン
	IF TALENT:PLAYER:58 && (TEQUIP:2 || TEQUIP:4 || TEQUIP:6)
		IF TEQUIP:6 == 7 || TEQUIP:6 == 9
			A = 6
			PRINTFORM 拉下%CALLNAME%
			CALL CLOTHES_NAME
			PRINT 的拉链，
		ELSEIF TEQUIP:6
			A = 6
			PRINTFORM 卷起%CALLNAME%
			CALL CLOTHES_NAME
			PRINT 的下摆，
		ENDIF
		IF TEQUIP:4 == 1
			A = 4
			PRINTFORM 卷起%CALLNAME%的
			CALL CLOTHES_NAME
			PRINT ，
		ELSEIF TEQUIP:4
			A = 4
			PRINTFORM 把%CALLNAME%的
			CALL CLOTHES_NAME
			PRINT 脱至膝弯，
		ELSEIF TEQUIP:2
			A = 2
			PRINTFORM 把%CALLNAME%的
			CALL CLOTHES_NAME
            IF TALENT:鱼尾 || TALENT:蛇尾
                PRINT 脱下来，
            ELSE
                PRINT 脱下来挂到一只腿上，
            ENDIF
		ELSE
			IF TEQUIP:2 == 1
        		PRINTFORM 拨开内裤，
        	ELSEIF TEQUIP:2 == 2
        		PRINTFORM 拨开内衣，
        	ELSEIF TEQUIP:2 == 3
        		PRINTFORM 错开兜裆布，
        	ELSEIF TEQUIP:2 == 6
        		PRINTFORM 拉开灯笼裤，
        	ELSEIF TEQUIP:4 == 6
        		PRINTFORM 错开运动短裤，
        	ENDIF
		ENDIF
	ENDIF
	;丝袜系
	IF TEQUIP:1 == 5 || TEQUIP:1 == 6
		IF TFLAG:55
			PRINT 撕开
		ELSE
			PRINT 撕烂
		ENDIF
	SIF TEQUIP:1 == 5
		PRINT 丝袜，
	SIF TEQUIP:1 == 6
		PRINT 网袜，
	ENDIF
	;自慰棒入れっぱ无
	SIF TEQUIP:13
		PRINTFORM 强行挤开还含着自慰棒的穴口，
	;尾巴插入
	SIF TEQUIP:51
		PRINTFORM 强行挤开还含着尾巴的穴口，
	PRINTFORM 用自己
	;调教者が男性か双性か扶她である
	IF TALENT:PLAYER:137 || TALENT:PLAYER:139 || TALENT:PLAYER:140
		IF TALENT:PLAYER:孩童尺寸
			SIF TALENT:PLAYER:矮小体型 == 0 || !TALENT:PLAYER:扶她
				PRINT 与体型不符
			PRINT 的小小
		ELSEIF TALENT:PLAYER:短小
			SIF TALENT:PLAYER:高大体型 || !TALENT:PLAYER:扶她
				PRINT 与体型不符
			PRINT 的短小
		ELSEIF TALENT:PLAYER:巨根
			SIF TALENT:PLAYER:矮小体型
				PRINT 与体型不符
			PRINT 的粗大
		ELSEIF TALENT:PLAYER:尺寸傲人
			SIF TALENT:PLAYER:矮小体型
				PRINT 与体型不符，
			PRINT 尺寸傲人的
		ELSE
		    PRINT 的
		ENDIF
		PRINT 阴茎
	ELSE
		PRINT 的假阳具
	ENDIF
	PRINTFORM 从后面贯穿了%CALLNAME%
	;处女
	IF TALENT:0
		PRINT 未经人事
		;润滑LV4以上
    	IF PALAM:4 >= PALAMLV:4
    		PRINT ，湿淋淋的
    		;矮小体型
    		IF TALENT:100
        		PRINT 娇嫩小穴…
        	ELSE
        		PRINT 秘裂…
        	ENDIF
        ELSE
            PRINT 的
    		;矮小体型
    		IF TALENT:100
        		PRINT 娇嫩小穴…
        	ELSE
        		PRINT 秘裂…
        	ENDIF
        ENDIF
	ELSE
		;润滑LV4以上
    	IF PALAM:4 >= PALAMLV:4
    		PRINT 湿淋淋的
    		;矮小体型
    		IF TALENT:100
        		PRINT 娇嫩小穴…
        	ELSE
        		PRINT 秘裂…
        	ENDIF
        ELSE
            PRINT 的
    		;矮小体型
    		IF TALENT:100
        		PRINT 娇嫩小穴…
        	ELSE
        		PRINT 秘裂…
        	ENDIF
        ENDIF
	ENDIF
ENDIF
;Ｖ扩张经验２０以上
IF EXP:52 >= 20 && TALENT:PLAYER:孩童尺寸
	PRINTL
	PRINTFORM 在%CALLNAME%已经充分扩张过的阴道面前，%CALLNAME:PLAYER%小孩子尺寸的阴茎完全不能满足%CALLNAME%…
	TFLAG:52 = 1
ELSEIF EXP:52 >= 20 && TALENT:PLAYER:短小
	PRINTL
	PRINTFORM 在%CALLNAME%已经充分扩张过的阴道面前，%CALLNAME:PLAYER%短小的阴茎完全派不上用场…
	TFLAG:52 = 1
ENDIF
;-------------------------------------------------
;62,正常位肛交
;-------------------------------------------------
@MESSAGE_COM62
;前回と今回の调教者が同じで、前回の调教が正常位肛交､错开内裤插入肛门であり、抜かないフラグがある場合
IF TFLAG:60 && (PREVCOM == 62 || PREVCOM == 73 || PREVCOM == 518 || (TFLAG:709 == 2 && PREVCOM == 593)) && ((ASSIPLAY && TFLAG:50) || (ASSIPLAY == 0 && TFLAG:50 == 0))
	;调教者が男性か双性か扶她である
	IF TALENT:PLAYER:137 || TALENT:PLAYER:139 || TALENT:PLAYER:140
		PRINTFORM 一边品尝着%CALLNAME%后穴肠道的触感一边摆动起腰部…
	;捆绑式假阴茎を使用している場合
	ELSE
		PRINTFORML 戴着捆绑式假阳具翻搅玩弄%CALLNAME%的后穴
		PRINTFORM 一次又一次地摆动着腰部…
	ENDIF
;同様に前回の调教が后背位肛交か对面座位肛交か背面座位肛交か骑乘位肛交か对面立位肛か背面立位肛の場合
ELSEIF TFLAG:60 && (PREVCOM == 63 || PREVCOM == 70 || PREVCOM == 71 || PREVCOM == 67 || PREVCOM == 75 || PREVCOM == 77 || PREVCOM == 516 || PREVCOM == 591 || (TFLAG:709 == 2 && (PREVCOM == 504 || PREVCOM == 505 || PREVCOM == 514 || PREVCOM == 592))) && ((ASSIPLAY && TFLAG:50) || (ASSIPLAY == 0 && TFLAG:50 == 0))
	PRINTFORML 把\@ PENIS(PLAYER) ? 阴茎 # 假阳具 \@从%CALLNAME%体内拔出来
	PRINTFORM 让%CALLNAME%摆成仰面向上的姿势，再次活动起腰部…
ELSE
	IF ASSIPLAY
    	PRINTFORM %CALLNAME:ASSI%
    ELSE
        PRINTFORM %CALLNAME:PLAYER%
    ENDIF
	;着衣プ雷オン
	IF TALENT:PLAYER:58 && (TEQUIP:2 || TEQUIP:4 || TEQUIP:6)
		IF TEQUIP:6 == 7 || TEQUIP:6 == 9
			A = 6
			PRINTFORM 拉下%CALLNAME%
			CALL CLOTHES_NAME
			PRINT 的拉链，
		ELSEIF TEQUIP:6
			A = 6
			PRINTFORM 卷起%CALLNAME%
			CALL CLOTHES_NAME
			PRINT 的下摆，
		ENDIF
		IF TEQUIP:4 == 1
			A = 4
			PRINTFORM 卷起%CALLNAME%的
			CALL CLOTHES_NAME
			PRINT ，
		ELSEIF TEQUIP:4
			A = 4
			PRINTFORM 把%CALLNAME%的
			CALL CLOTHES_NAME
			PRINT 脱至膝弯，
		ELSEIF TEQUIP:2
			A = 2
			PRINTFORM 把%CALLNAME%的
			CALL CLOTHES_NAME
            IF TALENT:鱼尾 || TALENT:蛇尾
                PRINT 脱下来，
            ELSE
                PRINT 脱下来挂到一只腿上，
            ENDIF
		ELSE
			IF TEQUIP:2 == 1
        		PRINTFORM 拨开内裤，
        	ELSEIF TEQUIP:2 == 2
        		PRINTFORM 拨开内衣，
        	ELSEIF TEQUIP:2 == 3
        		PRINTFORM 错开兜裆布，
        	ELSEIF TEQUIP:2 == 6
        		PRINTFORM 拉开灯笼裤，
        	ELSEIF TEQUIP:4 == 6
        		PRINTFORM 错开运动短裤，
        	ENDIF
		ENDIF
	ENDIF
	;丝袜系
	IF TEQUIP:1 == 5 || TEQUIP:1 == 6
		IF TFLAG:55
			PRINT 撕开
		ELSE
			PRINT 撕烂
		ENDIF
	SIF TEQUIP:1 == 5
		PRINT 丝袜，
	SIF TEQUIP:1 == 6
		PRINT 网袜，
	ENDIF
	;肛塞后庭拉珠
	IF TEQUIP:14 || TEQUIP:15
		PRINTFORM 强行挤开还含着
		IF TEQUIP:14 && TALENT:143 == 11
			PRINT 猫尾肛塞的穴口，
		ELSEIF TEQUIP:14 && TALENT:143 == 12
			PRINT 狗尾肛塞的穴口，
		ELSEIF TEQUIP:14 && TALENT:143 == 13
			PRINT 兔尾肛塞的穴口，
		ELSEIF TEQUIP:14
			PRINT 肛塞的穴口，
		ELSE
			PRINT 后庭拉珠的穴口，
		ENDIF
	ENDIF
	;尾巴插入
	SIF TEQUIP:52
		PRINTFORM 强行挤开还含着尾巴的穴口，
	PRINT 用自己
	;调教者が男性か双性か扶她である
	IF TALENT:PLAYER:137 || TALENT:PLAYER:139 || TALENT:PLAYER:140
		IF TALENT:PLAYER:孩童尺寸
			SIF TALENT:PLAYER:矮小体型 == 0 || !TALENT:PLAYER:扶她
				PRINT 与体型不符
			PRINT 的小小
		ELSEIF TALENT:PLAYER:短小
			SIF TALENT:PLAYER:高大体型 || !TALENT:PLAYER:扶她
				PRINT 与体型不符
			PRINT 的短小
		ELSEIF TALENT:PLAYER:巨根
			SIF TALENT:PLAYER:矮小体型
				PRINT 与体型不符
			PRINT 的粗大
		ELSEIF TALENT:PLAYER:尺寸傲人
			SIF TALENT:PLAYER:矮小体型
				PRINT 与体型不符，
			PRINT 尺寸傲人的
		ELSE
		    PRINT 的
		ENDIF
		PRINT 阴茎
	ELSE
		PRINT 的假阳具
	ENDIF
	PRINTFORM 贯穿了%CALLNAME%
	;润滑LV4以上
	SIF PALAM:4 >= PALAMLV:4
		PRINT 沾满粘液
	PRINT 的后穴…
ENDIF
;顺从および欲望LV3以上+(助手でない or 淫乱ルート)
IF ABL:0 >= 3 && ABL:1 >= 3 && (!ASSIPLAY || KANRAKU(TARGET) == 2) && !RAND:2
	PRINTFORML 
	PRINTFORM %CALLNAME%双腿交叉环住了%CALLNAME:PLAYER%的腰…
	TFLAG:52 = 1
ENDIF
;Ａ扩张经验２０以上
IF EXP:53 >= 20 && TALENT:PLAYER:孩童尺寸&& !RAND:3
	PRINTL
	PRINTFORM 在%CALLNAME%已经充分扩张过的肛穴面前，%CALLNAME:PLAYER%小孩子尺寸的阴茎完全不能满足%CALLNAME%…
	TFLAG:52 = 1
ELSEIF EXP:53 >= 20 && TALENT:PLAYER:短小 && !RAND:3
	PRINTL
	PRINTFORM 在%CALLNAME%已经充分扩张过的肛穴面前，%CALLNAME:PLAYER%短小的阴茎完全派不上用场…
	TFLAG:52 = 1
ENDIF
;-------------------------------------------------
;63,后背位肛交
;-------------------------------------------------
@MESSAGE_COM63
;前回と今回の调教者が同じで、前回の调教が后背位肛交であり、抜かないフラグがある場合
IF TFLAG:60 && (PREVCOM == 63 || PREVCOM == 516 || PREVCOM == 591 || (TFLAG:709 == 2 && PREVCOM == 593)) && ((ASSIPLAY && TFLAG:50) || (ASSIPLAY == 0 && TFLAG:50 == 0))
	;调教者が男性か双性か扶她である
	IF TALENT:PLAYER:137 || TALENT:PLAYER:139 || TALENT:PLAYER:140
		PRINTFORM 一边品尝着%CALLNAME%后穴肠道的触感一边摆动起腰部…
	;捆绑式假阴茎を使用している場合
	ELSE
		PRINTFORML 戴着捆绑式假阳具翻搅玩弄%CALLNAME%的后穴
		PRINTFORM 一次又一次地摆动着腰部…
	ENDIF
;同様に、前回の调教が正常位肛交である場合
ELSEIF TFLAG:60 && (PREVCOM == 62 || PREVCOM == 518) && ((ASSIPLAY && TFLAG:50) || (ASSIPLAY == 0 && TFLAG:50 == 0))
	PRINTFORML 把\@ PENIS(PLAYER) ? 阴茎 # 假阳具 \@从%CALLNAME%体内拔出来
	PRINTFORM 让%CALLNAME%背过身趴下，再次活动起腰部…
;同様に、前回の调教が对面座位肛交、背面座位肛交、骑乘位肛交か对面立位肛か背面立位肛である場合
ELSEIF TFLAG:60 && (PREVCOM == 67 || PREVCOM == 70 || PREVCOM == 71 || PREVCOM == 75 || PREVCOM == 77 || (TFLAG:709 == 2 && (PREVCOM == 504 || PREVCOM == 505 || PREVCOM == 514 || PREVCOM == 592))) && ((ASSIPLAY && TFLAG:50) || (ASSIPLAY == 0 && TFLAG:50 == 0))
	PRINTFORM 保持着插入的姿势将%CALLNAME%摆弄成四肢着地的模样，再次活动起腰部…
ELSE
	IF ASSIPLAY
    	PRINTFORM %CALLNAME:ASSI%
    ELSE
        PRINTFORM %CALLNAME:PLAYER%
    ENDIF
	;着衣プ雷オン
	IF TALENT:PLAYER:58 && (TEQUIP:2 || TEQUIP:4 || TEQUIP:6)
		IF TEQUIP:6 == 7 || TEQUIP:6 == 9
			A = 6
			PRINTFORM 拉下%CALLNAME%
			CALL CLOTHES_NAME
			PRINT 的拉链，
		ELSEIF TEQUIP:6
			A = 6
			PRINTFORM 卷起%CALLNAME%
			CALL CLOTHES_NAME
			PRINT 的下摆，
		ENDIF
		IF TEQUIP:4 == 1
			A = 4
			PRINTFORM 卷起%CALLNAME%的
			CALL CLOTHES_NAME
			PRINT ，
		ELSEIF TEQUIP:4
			A = 4
			PRINTFORM 把%CALLNAME%的
			CALL CLOTHES_NAME
			PRINT 脱至膝弯，
		ELSEIF TEQUIP:2
			A = 2
			PRINTFORM 把%CALLNAME%的
			CALL CLOTHES_NAME
            IF TALENT:鱼尾 || TALENT:蛇尾
                PRINT 脱下来，
            ELSE
                PRINT 脱下来挂到一只腿上，
            ENDIF
		ELSE
			IF TEQUIP:2 == 1
        		PRINTFORM 拨开内裤，
        	ELSEIF TEQUIP:2 == 2
        		PRINTFORM 拨开内衣，
        	ELSEIF TEQUIP:2 == 3
        		PRINTFORM 错开兜裆布，
        	ELSEIF TEQUIP:2 == 6
        		PRINTFORM 拉开灯笼裤，
        	ELSEIF TEQUIP:4 == 6
        		PRINTFORM 错开运动短裤，
        	ENDIF
		ENDIF
	ENDIF
	;丝袜系
	IF TEQUIP:1 == 5 || TEQUIP:1 == 6
		IF TFLAG:55
			PRINT 撕开
		ELSE
			PRINT 撕烂
		ENDIF
	SIF TEQUIP:1 == 5
		PRINT 丝袜，
	SIF TEQUIP:1 == 6
		PRINT 网袜，
	ENDIF
	;肛塞后庭拉珠
	IF TEQUIP:14 || TEQUIP:15
		PRINTFORM 强行挤开含着
		IF TEQUIP:14 && TALENT:143 == 11
			PRINT 猫尾肛塞
		ELSEIF TEQUIP:14 && TALENT:143 == 12
			PRINT 狗尾肛塞
		ELSEIF TEQUIP:14 && TALENT:143 == 13
			PRINT 兔尾肛塞
		ELSEIF TEQUIP:14
			PRINT 肛塞
		ELSE
			PRINT 后庭拉珠
		ENDIF
		PRINT 的后穴，
	ENDIF
	;尾巴插入
	SIF TEQUIP:52
		PRINTFORM 强行挤开还含着尾巴的穴口，
	PRINT 用自己
	;调教者が男性か双性か扶她である
	IF TALENT:PLAYER:137 || TALENT:PLAYER:139 || TALENT:PLAYER:140
		IF TALENT:PLAYER:孩童尺寸
			SIF TALENT:PLAYER:矮小体型 == 0 || !TALENT:PLAYER:扶她
				PRINT 与体型不符
			PRINT 的小小
		ELSEIF TALENT:PLAYER:短小
			SIF TALENT:PLAYER:高大体型 || !TALENT:PLAYER:扶她
				PRINT 与体型不符
			PRINT 的短小
		ELSEIF TALENT:PLAYER:巨根
			SIF TALENT:PLAYER:矮小体型
				PRINT 与体型不符
			PRINT 的粗大
		ELSEIF TALENT:PLAYER:尺寸傲人
			SIF TALENT:PLAYER:矮小体型
				PRINT 与体型不符，
			PRINT 尺寸傲人的
		ELSE
		    PRINT 的
		ENDIF
		PRINT 阴茎
	ELSE
		PRINT 的假阳具
	ENDIF
	PRINTFORM 从后面贯穿了%CALLNAME%
	;润滑LV4以上
	SIF PALAM:4 >= PALAMLV:4
		PRINT 沾满粘液
	PRINT 的后穴…
ENDIF
;Ａ扩张经验２０以上
IF EXP:53 >= 20 && TALENT:PLAYER:孩童尺寸&& !RAND:3
	PRINTL
	PRINTFORM 在%CALLNAME%已经充分扩张过的肛穴面前，%CALLNAME:PLAYER%小孩子尺寸的阴茎完全不能满足%CALLNAME%…
	TFLAG:52 = 1
ELSEIF EXP:53 >= 20 && TALENT:PLAYER:短小 && !RAND:3
	PRINTL
	PRINTFORM 在%CALLNAME%已经充分扩张过的肛穴面前，%CALLNAME:PLAYER%短小的阴茎完全派不上用场…
	TFLAG:52 = 1
ENDIF
;-------------------------------------------------
;64,逆强奸　この指令は今使われていない、
;逆调教系の211,逆强奸と212,逆强奸肛になった。
;-------------------------------------------------
@MESSAGE_COM64
;-------------------------------------------------
;65,乳头奸
;-------------------------------------------------
@MESSAGE_COM65
PRINTFORM %CALLNAME:PLAYER%用自己的阴茎顶着%CALLNAME%
;贫乳
IF TALENT:109
	PRINTFORM 那薄薄胸脯上的乳头来回摩擦
;绝壁
ELSEIF TALENT:110
	PRINTFORM 那平坦胸口上的乳头来回摩擦
;巨乳
ELSEIF TALENT:111
	IF TALENT:140
	PRINTFORM 那结实胸肌上的乳头，当阴茎顶到乳房变形时来回摩擦
	ELSE
	PRINTFORM 那丰满乳房上的乳头，当阴茎顶到乳房变形时来回摩擦
	ENDIF
;爆乳
ELSEIF TALENT:112
	IF TALENT:140
	PRINTFORM 那异常发达胸肌上的乳头，用深陷其中阴茎抽插进出
	ELSE
	PRINTFORM 那乳量惊人的豪乳上的乳头，用深陷其中阴茎抽插进出
	ENDIF
ELSE
	PRINTFORM 的乳头来回摩擦
ENDIF
PRINTFORM ，以此来发泄欲望…
;-------------------------------------------------
;66,骑乘位
;-------------------------------------------------
@MESSAGE_COM66
;前回と今回の调教者が同じで、前回の调教が骑乘位であり、抜かないフラグがある場合
IF TFLAG:60 && (PREVCOM == 66 || (TFLAG:709 == 1 && (PREVCOM == 592 || PREVCOM == 505))) && ((ASSIPLAY && TFLAG:50) || (ASSIPLAY == 0 && TFLAG:50 == 0))
	PRINTFORM %CALLNAME%绞紧了埋在体内的\@ PENIS(PLAYER) ? 阴茎 # 假阳具 \@，再一次活动起腰部…
;同様に、前回の调教が正常位か后背位である場合
ELSEIF TFLAG:60 && (PREVCOM == 60 || PREVCOM == 61 || PREVCOM == 72 || (TFLAG:709 == 1 && PREVCOM == 593)) && ((ASSIPLAY && TFLAG:50) || (ASSIPLAY == 0 && TFLAG:50 == 0))
	PRINTFORM 保持着\@ PENIS(PLAYER) ? 阴茎 # 假阳具 \@埋在%CALLNAME%体内的姿势
	SIF TALENT:PLAYER:男性 || TALENT:PLAYER:扶她 || TALENT:PLAYER:双性 || TALENT:矮小体型
		PRINTFORM 将其抱起，
	PRINTFORM 从下往上大力顶弄…
;同様に、前回の调教が对面座位か背面座位か对面立位か背面立位である場合
ELSEIF TFLAG:60 && (PREVCOM == 68 || PREVCOM == 69 || PREVCOM == 74 || PREVCOM == 76 || PREVCOM == 515 || PREVCOM == 517 || (TFLAG:709 == 1 && (PREVCOM == 504 || PREVCOM == 514))) && ((ASSIPLAY && TFLAG:50) || (ASSIPLAY == 0 && TFLAG:50 == 0))
	PRINTFORM %CALLNAME:PLAYER%在插入%CALLNAME%后躺了下来，从下往上大力顶弄…
ELSE
	;丝袜系
	IF TEQUIP:1 == 5 || TEQUIP:1 == 6
		PRINTFORM 插入的同时，%CALLNAME%的
		SIF TEQUIP:1 == 5
			PRINT 丝袜
		SIF TEQUIP:1 == 6
			PRINT 网袜
		IF TFLAG:55
			PRINTL 的开口被拉得更大了
		ELSE
			PRINTL 被指甲抓破了
		ENDIF
	ENDIF
	PRINTFORM %CALLNAME:PLAYER%
	IF TEQUIP:2 == 1
		PRINTFORM 拨开%CALLNAME%的内裤，
	ELSEIF TEQUIP:2 == 2
		PRINTFORM 拨开%CALLNAME%的内衣，
	ELSEIF TEQUIP:2 == 3
		PRINTFORM 掀开%CALLNAME%的兜裆布，
	ELSEIF TEQUIP:2 == 6
		PRINTFORM 错开%CALLNAME%的灯笼裤，
	ELSEIF TEQUIP:4 == 6
		PRINTFORM 错开%CALLNAME%的运动短裤，
	ENDIF
	PRINTFORM 仰面横躺，
	;自慰棒入れっぱ无
	SIF TEQUIP:13
		PRINTFORM 强行挤开还含着自慰棒的穴口，
	;尾巴插入
	SIF TEQUIP:51
		PRINTFORM 强行挤开还含着尾巴的穴口，
	PRINTFORM %CALLNAME%
	;调教者が尺寸傲人
	SIF TALENT:PLAYER:117
		PRINTFORM 一边畏惧着%CALLNAME:PLAYER%过于粗大的阴茎，一边
	PRINTFORM 缓缓地把腰沉了下来
	;处女＋矮小体型
	IF TALENT:100 && TALENT:0
		;竿の有無で分岐
		IF TALENT:PLAYER:140 || TALENT:PLAYER:139 || TALENT:PLAYER:137
			PRINTFORM ，用未经人事的娇嫩小穴勉强地将阴茎吞了下去…
		ELSE
			PRINTFORM ，用未经人事的娇嫩小穴勉强地将假阳具吞了下去…
		ENDIF
	;处女
	ELSEIF TALENT:0
		;竿の有無で分岐
		IF TALENT:PLAYER:140 || TALENT:PLAYER:139 || TALENT:PLAYER:137
			PRINTFORM ，用未经人事的秘裂勉强地将阴茎吞了下去…
		ELSE
			PRINTFORM ，用未经人事的秘裂勉强地将假阳具吞了下去…
		ENDIF
	ELSE
		PRINTFORM …
	ENDIF
	;Ｖ经验75以上
	IF EXP:0 >= 75 && !RAND:2
		PRINTFORML  
		PRINTFORM %CALLNAME%
		;矮小体型
		SIF TALENT:100
			PRINTFORM 小小的身体拼命地
		;巨乳
		IF TALENT:111
			IF TALENT:140
				PRINTFORM 结实的胸肌摇晃着，拼命地
			ELSE
				PRINTFORM 丰满的胸部摇晃着，拼命地
			ENDIF
		ENDIF
		;爆乳
		SIF TALENT:112
			PRINTFORM 硕大的胸部连同身体一起摇晃着，拼命地
		PRINTFORM 扭摆着腰肢…
		TFLAG:52 = 1
	ENDIF
ENDIF
;Ｖ扩张经验２０以上
IF EXP:52 >= 20 && TALENT:PLAYER:孩童尺寸&& !RAND:3
	PRINTL
	PRINTFORM 在%CALLNAME%已经充分扩张过的阴道面前，%CALLNAME:PLAYER%小孩子尺寸的阴茎完全不能满足%CALLNAME%…
	TFLAG:52 = 1
ELSEIF EXP:52 >= 20 && TALENT:PLAYER:短小 && !RAND:3
	PRINTL
	PRINTFORM 在%CALLNAME%已经充分扩张过的阴道面前，%CALLNAME:PLAYER%短小的阴茎完全派不上用场…
	TFLAG:52 = 1
ENDIF
;-------------------------------------------------
;67,骑乘位肛交
;-------------------------------------------------
@MESSAGE_COM67
;前回と今回の调教者が同じで、前回の调教が骑乘位であり、抜かないフラグがある場合
IF TFLAG:60 && (PREVCOM == 67  || (TFLAG:709 == 2 && (PREVCOM == 592 || PREVCOM == 505))) && ((ASSIPLAY && TFLAG:50) || (ASSIPLAY == 0 && TFLAG:50 == 0))
	PRINTFORM %CALLNAME%绞紧了埋在体内的\@ PENIS(PLAYER) ? 阴茎 # 假阳具 \@，再一次活动起腰部…
;同様に、前回の调教が正常位肛交か后背位肛交である場合
ELSEIF TFLAG:60 && (PREVCOM == 62 || PREVCOM == 63 || PREVCOM == 73 || PREVCOM == 591 || (TFLAG:709 == 2 && PREVCOM == 593)) && ((ASSIPLAY && TFLAG:50) || (ASSIPLAY == 0 && TFLAG:50 == 0))
	PRINTFORML 保持着\@ PENIS(PLAYER) ? 阴茎 # 假阳具 \@埋在%CALLNAME%后穴的姿势
	SIF TALENT:PLAYER:男性 || TALENT:PLAYER:扶她 || TALENT:PLAYER:双性 || TALENT:矮小体型
		PRINTFORM 将其抱起，
	PRINTFORM 从下往上大力顶弄…
;同様に、前回の调教が对面座位肛交か背面座位肛交か对面立位肛か背面立位肛である場合
ELSEIF TFLAG:60 && (PREVCOM == 70 || PREVCOM == 71 || PREVCOM == 75 || PREVCOM == 77 || PREVCOM == 518 || (TFLAG:709 == 2 && (PREVCOM == 504 || PREVCOM == 514))) && ((ASSIPLAY && TFLAG:50) || (ASSIPLAY == 0 && TFLAG:50 == 0))
	PRINTFORM %CALLNAME:PLAYER%在插入%CALLNAME%后躺了下来，从下往上大力顶弄…
ELSE
	;丝袜系
	IF TEQUIP:1 == 5 || TEQUIP:1 == 6
		PRINTFORM 插入的同时，%CALLNAME%的
		SIF TEQUIP:1 == 5
			PRINT 丝袜
		SIF TEQUIP:1 == 6
			PRINT 网袜
		IF TFLAG:55
			PRINTL 的开口被拉得更大了
		ELSE
			PRINTL 被指甲抓破了
		ENDIF
	ENDIF
	PRINTFORM %CALLNAME:PLAYER%
	IF TEQUIP:2 == 1
		PRINTFORM 拨开%CALLNAME%的内裤，
	ELSEIF TEQUIP:2 == 2
		PRINTFORM 拨开%CALLNAME%的内衣，
	ELSEIF TEQUIP:2 == 3
		PRINTFORM 掀开%CALLNAME%的兜裆布，
	ELSEIF TEQUIP:2 == 6
		PRINTFORM 错开%CALLNAME%的灯笼裤，
	ELSEIF TEQUIP:4 == 6
		PRINTFORM 错开%CALLNAME%的运动短裤，
	ENDIF
	PRINTFORM 仰面横躺，
	;调教者が尺寸傲人
	IF TALENT:PLAYER:117
		PRINTFORML %CALLNAME%一边畏惧着%CALLNAME:PLAYER%过于粗大的阴茎，一边缓缓把腰沉下
		PRINTFORM %CALLNAME:PLAYER%
	ENDIF
	;肛塞后庭拉珠
	IF TEQUIP:14 || TEQUIP:15
	    PRINTFORM 强行挤开含着
		IF TEQUIP:14 && TALENT:143 == 11
			PRINTFORM 猫尾肛塞
		ELSEIF TEQUIP:14 && TALENT:143 == 12
			PRINTFORM 狗尾肛塞
		ELSEIF TEQUIP:14 && TALENT:143 == 13
			PRINTFORM 兔尾肛塞
		ELSEIF TEQUIP:14
			PRINTFORM 肛塞
		ELSE
			PRINT 后庭拉珠
		ENDIF
		PRINTFORM 的后穴，
	ENDIF
	;尾巴插入
	SIF TEQUIP:52
		PRINTFORM 强行挤开还含着尾巴的穴口，
	;后庭处子
	IF TALENT:后庭处子
		;竿の有無で分岐
		IF TALENT:PLAYER:140 || TALENT:PLAYER:139 || TALENT:PLAYER:137
			PRINTFORM 用阴茎贯穿了%CALLNAME%还未接纳过其他人的肛穴…
		ELSE
			PRINTFORM 用假阳具贯穿了%CALLNAME%还未接纳过其他人的肛穴…
		ENDIF
	ELSE
	    PRINT 用自己
		;竿の有無で分岐
		IF TALENT:PLAYER:140 || TALENT:PLAYER:139 || TALENT:PLAYER:137
			IF TALENT:PLAYER:包茎
				PRINT 裹着包皮
			    IF TALENT:PLAYER:孩童尺寸
				    SIF TALENT:PLAYER:矮小体型 == 0 || !TALENT:PLAYER:扶她
					    PRINT ，与体型不符
				    PRINT 的小小
		    	ELSEIF TALENT:PLAYER:短小
			    	SIF TALENT:PLAYER:高大体型 || !TALENT:PLAYER:扶她
			    		PRINT ，与体型不符
			    	PRINT 的短小
			    ELSEIF TALENT:PLAYER:巨根
			    	SIF TALENT:PLAYER:矮小体型
					    PRINT ，与体型不符
			    	PRINT 的粗大
		    	ELSEIF TALENT:PLAYER:尺寸傲人
				    SIF TALENT:PLAYER:矮小体型
					    PRINT ，与体型不符，
			    	PRINT 尺寸傲人的
		    	ELSE
			        PRINT 的
			    ENDIF
			ELSE
			    IF TALENT:PLAYER:孩童尺寸
				    SIF TALENT:PLAYER:矮小体型 == 0 || !TALENT:PLAYER:扶她
					    PRINT 与体型不符
				    PRINT 的小小
		    	ELSEIF TALENT:PLAYER:短小
			    	SIF TALENT:PLAYER:高大体型 || !TALENT:PLAYER:扶她
			    		PRINT 与体型不符
			    	PRINT 的短小
			    ELSEIF TALENT:PLAYER:巨根
			    	SIF TALENT:PLAYER:矮小体型
					    PRINT 与体型不符
			    	PRINT 的粗大
		    	ELSEIF TALENT:PLAYER:尺寸傲人
				    SIF TALENT:PLAYER:矮小体型
					    PRINT 与体型不符，
			    	PRINT 尺寸傲人的
		    	ELSE
			        PRINT 的
			    ENDIF
			ENDIF
			PRINT 阴茎
		ELSE
		;调教者が女の場合
			PRINT 的假阳具
		ENDIF
		PRINTFORM 贯穿了%CALLNAME%
		;润滑LV4以上
		SIF PALAM:4 >= PALAMLV:4
			PRINTFORM 沾满粘液
		PRINTFORM 的肛穴…
	ENDIF
ENDIF
;Ａ经验40以上、顺从LV3以上、欲望LV4以上
IF EXP:1 >= 40 && ABL:顺从 >= 3 && ABL:欲望 >= 4 && PALAM:快Ａ >= 6000 && !RAND:2
	PRINTFORML  
	PRINTFORM %CALLNAME%
	;淫乱か娼妇＋情欲のPALAMLV5以上
	IF (TALENT:淫乱 || TALENT:娼妇) && PALAM:6 >= PALAMLV:5
		PRINTFORM 浮现出沉溺在快乐中的表情，
	;恋慕か相亲相爱＋情欲のPALAMLV5以上＋助手ではない
	ELSEIF (TALENT:恋慕 || TALENT:相亲相爱) && PALAM:6 >= PALAMLV:5 && !ASSIPLAY
		PRINTFORM 用快要融化似的目光把%CALLNAME:PLAYER%牢牢锁住，
	;情欲のPALAMLV4以上
	ELSEIF PALAM:6 >= PALAMLV:4
		PRINTFORM 的嘴边滴落着口涎，
	;恋慕か相亲相爱＋助手ではない
	ELSEIF (TALENT:恋慕 || TALENT:相亲相爱) && !ASSIPLAY
		PRINTFORM 凝视着%CALLNAME:PLAYER%，
	;淫乱か娼妇
	ELSEIF (TALENT:淫乱 || TALENT:娼妇)
		PRINTFORM 双眼流露出淫荡的目光挑衅地看了过来，
	ELSEIF TALENT:反抗 || TALENT:自大
		PRINTFORM 极不甘心地移开视线，
	ELSEIF TALENT:缺乏感情
		PRINTFORM 神色淡漠地
	ELSEIF TALENT:不知羞耻
		PRINTFORM 大胆地迎合着，
	ELSEIF TALENT:害羞 || TALENT:性保守 || TALENT:胆怯
		PRINTFORM 极为害羞地
	ENDIF
	PRINTFORM 自己扭动着腰部
	SIF TALENT:111
		PRINTFORM ，硕大的胸部上下摇晃着
	PRINT …
ELSEIF !TALENT:后庭处子
	PRINTL
	CALL HANYOU_JYOKYO_KIJYO
	TFLAG:52 = 1
ENDIF

;Ａ扩张经验２０以上
IF EXP:53 >= 20 && TALENT:PLAYER:孩童尺寸&& !RAND:3
	PRINTL
	PRINTFORM 在%CALLNAME%已经充分扩张过的肛穴面前，%CALLNAME:PLAYER%小孩子尺寸的阴茎完全不能满足%CALLNAME%…
	TFLAG:52 = 1
ELSEIF EXP:53 >= 20 && TALENT:PLAYER:短小 && !RAND:3
	PRINTL
	PRINTFORM 在%CALLNAME%已经充分扩张过的肛穴面前，%CALLNAME:PLAYER%短小的阴茎完全派不上用场…
	TFLAG:52 = 1
ENDIF
;-------------------------------------------------
;68,对面座位
;-------------------------------------------------
@MESSAGE_COM68
;前回と今回の调教者が同じで、前回の调教が对面座位であり、抜かないフラグがある場合
IF TFLAG:60 && (PREVCOM == 68 || (TFLAG:709 == 1 && PREVCOM == 593)) && ((ASSIPLAY && TFLAG:50) || (ASSIPLAY == 0 && TFLAG:50 == 0))
	PRINTFORM %CALLNAME:PLAYER%
	SIF KANRAKU(TARGET) > 0 && (STAIN:0 < 2 || STAIN:0 == 16 || STAIN:0 == 17 || TALENT:MASTER:64) && TEQUIP:20 == 0 && (!ASSIPLAY || ABL:PLAYER:同性倾向 > 1 || !MALE(TARGET))
		PRINTFORM 与%CALLNAME%唇齿相接，舌尖缠绕，随即
	PRINTFORM 再一次活动起腰部…
	RETURN 0
;同様に前回の调教が正常位、骑乘位、对面立位の場合
ELSEIF TFLAG:60 && (PREVCOM == 60 || PREVCOM == 66 || PREVCOM == 72 || PREVCOM == 74 || PREVCOM == 517 || (TFLAG:709 == 1 && (PREVCOM == 514 || PREVCOM == 592 || PREVCOM == 505))) && ((ASSIPLAY && TFLAG:50) || (ASSIPLAY == 0 && TFLAG:50 == 0))
	PRINTFORM 把\@ PENIS(PLAYER) ? 阴茎 # 假阳具 \@从%CALLNAME%体内拔出来
	;前回の调教が正常位＋调教者が女でない
	IF PREVCOM == 60 && (TALENT:PLAYER:男性 || TALENT:PLAYER:扶她 || TALENT:PLAYER:双性 || TALENT:矮小体型)
		PRINTFORM 将%CALLNAME%抱起，
	;前回の调教正常位か对面立位
	ELSEIF PREVCOM == 60 || PREVCOM == 74 || PREVCOM == 514 || PREVCOM == 517
		PRINTFORM 将%CALLNAME%抱着坐下，
	ELSE
		PRINTFORM 紧紧拥抱着%CALLNAME%，
	ENDIF
;同様に前回の调教が后背位、背面座位、背面立位の場合
ELSEIF TFLAG:60 && (PREVCOM == 61 || PREVCOM == 66 || PREVCOM == 76 || PREVCOM == 515 || (TFLAG:709 == 1 && PREVCOM == 504)) && ((ASSIPLAY && TFLAG:50) || (ASSIPLAY == 0 && TFLAG:50 == 0))
	PRINTFORM 保持着插入的姿势身体相对，
	;前回の调教が后背位
	IF PREVCOM == 61
		IF TALENT:PLAYER:140 || TALENT:PLAYER:139 || TALENT:PLAYER:137 || TALENT:矮小体型
			PRINTFORM 将%CALLNAME%抱起，
		ELSE
			PRINTFORM 将%CALLNAME%抱住，
		ENDIF
	;前回の调教が背面立位
	ELSEIF PREVCOM == 76
		PRINTFORM 一起坐下，
	ELSE
		PRINTFORM 紧紧拥抱着%CALLNAME%，
	ENDIF
ELSE
	;丝袜系
	IF TEQUIP:1 == 5 || TEQUIP:1 == 6
		PRINTFORM 插入的同时，%CALLNAME%的
		SIF TEQUIP:1 == 5
			PRINT 丝袜
		SIF TEQUIP:1 == 6
			PRINT 网袜
		IF TFLAG:55
			PRINTL 的开口被拉得更大了
		ELSE
			PRINTL 被指甲抓破了
		ENDIF
	ENDIF
	IF ASSIPLAY
    	PRINTFORM %CALLNAME:ASSI%
    ELSE
        PRINTFORM %CALLNAME:PLAYER%
    ENDIF
	IF TEQUIP:2 == 1
		PRINTFORM 拨开内裤，
	ELSEIF TEQUIP:2 == 2
		PRINTFORM 拨开内衣，
	ELSEIF TEQUIP:2 == 3
		PRINTFORM 掀开兜裆布，
	ELSEIF TEQUIP:2 == 6
		PRINTFORM 错开灯笼裤，
	ELSEIF TEQUIP:4 == 6
		PRINTFORM 错开运动短裤，
	ENDIF
	IF TALENT:PLAYER:140 || TALENT:PLAYER:139 || TALENT:PLAYER:137 || TALENT:矮小体型
		PRINTFORM 将%CALLNAME%抱起，
	ELSE
		PRINTFORM 将%CALLNAME%抱住，
	ENDIF
	;自慰棒入れっぱ无
	SIF TEQUIP:13
		PRINTFORM 强行挤开还含着自慰棒的穴口，
	;尾巴插入
	SIF TEQUIP:51
		PRINTFORM 强行挤开还含着尾巴的穴口，
	PRINTFORM 慢慢地让其下沉，插入了%CALLNAME%
	;润滑LV4以上
	IF PALAM:4 >= PALAMLV:4
		PRINTFORM 湿黏的
	ELSE
	    PRINTFORM 的
	ENDIF
	;矮小体型
	IF TALENT:100
		PRINTFORML 娇嫩小穴…
	ELSE
		PRINTFORML 秘裂…
	ENDIF
ENDIF
;助手プ雷の場合、男奴隶で、助手に同性倾向が2と接吻しない。
SIF KANRAKU(TARGET) > 0 && (STAIN:0 < 2 || STAIN:0 == 16 || STAIN:0 == 17 || TALENT:MASTER:64) && TEQUIP:20 == 0 && (!ASSIPLAY || ABL:PLAYER:同性倾向 > 1 || !MALE(TARGET))
	PRINTFORM 与%CALLNAME%两唇相接的同时，
;绝壁
IF TALENT:110
	PRINTFORM 一边玩弄着对方的乳头
;贫乳
ELSEIF TALENT:109
	PRINTFORM 一边揉弄着对方薄薄的胸脯
;巨乳か爆乳
ELSEIF TALENT:111 || TALENT:112
	IF TALENT:140
		PRINTFORM 一边抓揉着对方壮观的胸肌
	ELSE
	PRINTFORM 一边抓揉着对方丰满的乳肉
	ENDIF
;普通乳
ELSE
	IF TALENT:140 || TALENT:137
		PRINTFORM 一边玩弄对方的乳头
	ELSE
		PRINTFORM 一边揉弄对方的胸部
	ENDIF
ENDIF
PRINTFORM 一边摆动着腰…
;顺从および欲望LV3以上+(助手でない or 淫乱ルート)
IF ABL:0 >= 3 && ABL:1 >= 3 && (!ASSIPLAY || KANRAKU(TARGET) == 2) && !RAND:2
	PRINTFORML 
	PRINTFORM %CALLNAME%双手挽住%CALLNAME:PLAYER%的脖子紧紧抱过来…
	TFLAG:52 = 1
ENDIF
;Ｖ扩张经验２０以上
;IF EXP:52 >= 20 && TALENT:PLAYER:114
;	PRINTFORM 在%CALLNAME%已经充分扩张过的阴道面前，%CALLNAME:PLAYER%小孩子尺寸的阴茎完全不能满足%CALLNAME%…
;ENDIF


;-------------------------------------------------
;69,背面座位
;-------------------------------------------------
@MESSAGE_COM69
;前回と今回の调教者が同じで、前回の调教が背面座位であり、抜かないフラグがある場合
IF TFLAG:60 && (PREVCOM == 69 || (TFLAG:709 == 1 && (PREVCOM == 504 || PREVCOM == 593))) && ((ASSIPLAY && TFLAG:50) || (ASSIPLAY == 0 && TFLAG:50 == 0))
	PRINTFORM 从背后抱着%CALLNAME%，
	PRINTFORML 再次摇动起%CALLNAME%的身体…
;同様に、前回の调教が后背位か骑乘位である場合
ELSEIF TFLAG:60 && (PREVCOM == 61 || PREVCOM == 66 || (TFLAG:709 == 1 && (PREVCOM == 592 || PREVCOM == 505))) && ((ASSIPLAY && TFLAG:50) || (ASSIPLAY == 0 && TFLAG:50 == 0))
	PRINTFORML 把\@ PENIS(PLAYER) ? 阴茎 # 假阳具 \@从%CALLNAME%体内拔出来
	PRINTFORM 从背后抱紧%CALLNAME%的身体开始活动…
;同様に、前回の调教が对面立位か背面立位である場合
ELSEIF TFLAG:60 && (PREVCOM == 74 || PREVCOM == 76 || PREVCOM == 515 || PREVCOM == 517 || (TFLAG:709 == 1 && PREVCOM == 514)) && ((ASSIPLAY && TFLAG:50) || (ASSIPLAY == 0 && TFLAG:50 == 0))
	PRINTFORML 把\@ PENIS(PLAYER) ? 阴茎 # 假阳具 \@从%CALLNAME%体内拔出来并坐下
	IF PREVCOM == 74
		PRINTFORM 反向抱住%CALLNAME%的身体，激烈地活动起腰部…
	ELSE
		PRINTFORM 从背后抱紧%CALLNAME%的身体，开始活动腰部…
	ENDIF
;同様に、前回の调教が正常位か对面座位である場合
ELSEIF TFLAG:60 && (PREVCOM == 60 || PREVCOM == 68 || PREVCOM == 72) && ((ASSIPLAY && TFLAG:50) || (ASSIPLAY == 0 && TFLAG:50 == 0))	
	PRINTFORM 保持着插入的姿势将%CALLNAME%的身体翻转过去
	;前回の调教が正常位
	IF PREVCOM == 60 && (TALENT:PLAYER:140 || TALENT:PLAYER:139 || TALENT:PLAYER:137 || TALENT:矮小体型)
		PRINTFORM 抱了起来，
	ELSE
		PRINTFORM 紧紧抱住，
	ENDIF
	PRINTFORM 激烈地活动起腰部…
ELSE
	;丝袜系
	IF TEQUIP:1 == 5 || TEQUIP:1 == 6
		PRINTFORM 插入的同时，%CALLNAME%的
		SIF TEQUIP:1 == 5
			PRINT 丝袜
		SIF TEQUIP:1 == 6
			PRINT 网袜
		IF TFLAG:55
			PRINTL 的开口被拉得更大了
		ELSE
			PRINTL 被指甲抓破了
		ENDIF
	ENDIF
	IF ASSIPLAY
    	PRINTFORM %CALLNAME:ASSI%
    ELSE
        PRINTFORM %CALLNAME:PLAYER%
    ENDIF
	IF TEQUIP:2 == 1
		PRINTFORM 拨开内裤，
	ELSEIF TEQUIP:2 == 2
		PRINTFORM 拨开内衣，
	ELSEIF TEQUIP:2 == 3
		PRINTFORM 掀开兜裆布，
	ELSEIF TEQUIP:2 == 6
		PRINTFORM 错开灯笼裤，
	ELSEIF TEQUIP:4 == 6
		PRINTFORM 错开运动短裤，
	ENDIF
	IF TALENT:PLAYER:140 || TALENT:PLAYER:139 || TALENT:PLAYER:137 || TALENT:矮小体型
		PRINTFORM 将%CALLNAME%抱起，
	ELSE
		PRINTFORM 将%CALLNAME%抱在怀里，
	ENDIF
	;自慰棒入れっぱ无
	SIF TEQUIP:13 
		PRINTFORM 强行挤开还含着自慰棒的穴口，
	;尾巴插入
	SIF TEQUIP:51
		PRINTFORM 强行挤开还含着尾巴的穴口，
	;竿の有無で分岐
	IF TALENT:PLAYER:140 || TALENT:PLAYER:139 || TALENT:PLAYER:137
		PRINTFORM 慢慢地用阴茎贯穿了%CALLNAME%
	ELSE
		PRINTFORM 慢慢地用假阳具贯穿了%CALLNAME%
	ENDIF
	;润滑LV4以上
	IF PALAM:4 >= PALAMLV:4
		PRINTFORM 湿黏的
	ELSE
	    PRINTFORM 的
	ENDIF
	;矮小体型
	IF TALENT:100
		PRINTFORM 娇嫩小穴…
	ELSE
		PRINTFORM 秘裂…
	ENDIF
ENDIF
;顺从および欲望LV3以上+(助手でない or 淫乱ルート)
IF ABL:0 >= 3 && ABL:1 >= 3 && (!ASSIPLAY || KANRAKU(TARGET) == 2) && !RAND:2
	PRINTFORML 
	PRINTFORM %CALLNAME%情动地向后仰去，背部紧贴在%CALLNAME:PLAYER%的胸膛上…
	TFLAG:52 = 1
ENDIF


;Ｖ扩张经验２０以上
;IF EXP:52 >= 20 && TALENT:PLAYER:114
;	PRINTFORM 在%CALLNAME%已经充分扩张过的阴道面前，%CALLNAME:PLAYER%小孩子尺寸的阴茎完全不能满足%CALLNAME%…
;ENDIF
;-------------------------------------------------
;70,对面座位肛交
;-------------------------------------------------
@MESSAGE_COM70
;前回と今回の调教者が同じで、前回の调教が对面座位肛交であり、抜かないフラグがある場合
IF TFLAG:60 && (PREVCOM == 70 || (TFLAG:709 == 2 && PREVCOM == 593)) && ((ASSIPLAY && TFLAG:50) || (ASSIPLAY == 0 && TFLAG:50 == 0))
	PRINTFORM %CALLNAME:PLAYER%
	SIF KANRAKU(TARGET) > 0 && (STAIN:0 < 2 || STAIN:0 == 16 || STAIN:0 == 17 || TALENT:MASTER:64) && TEQUIP:20 == 0 && (!ASSIPLAY || ABL:PLAYER:同性倾向 > 1 || !MALE(TARGET))
		PRINTFORM 与%CALLNAME%唇齿相接，舌尖缠绕，随即
	PRINTFORM 再一次活动起腰部…
	RETURN 0
;同様に前回の调教が正常位肛交、骑乘位肛交の場合
ELSEIF TFLAG:60 && (PREVCOM == 62 || PREVCOM == 67 || PREVCOM == 73 || (TFLAG:709 == 2 && (PREVCOM == 592 || PREVCOM == 505))) && ((ASSIPLAY && TFLAG:50) || (ASSIPLAY == 0 && TFLAG:50 == 0))
	PRINTFORM 把\@ PENIS(PLAYER) ? 阴茎 # 假阳具 \@从%CALLNAME%体内拔出来，紧紧抱住对方的身体，
;同様に前回の调教が后背位肛交、背面座位肛交の場合
ELSEIF TFLAG:60 && (PREVCOM == 63 || PREVCOM == 71 || PREVCOM == 591 || (TFLAG:709 == 2 && PREVCOM == 504)) && ((ASSIPLAY && TFLAG:50) || (ASSIPLAY == 0 && TFLAG:50 == 0))
	PRINTFORM 保持着插入的姿势身体相对，
	;前回の调教が后背位肛交
	IF PREVCOM == 63 && (TALENT:PLAYER:男性 || TALENT:PLAYER:扶她 || TALENT:PLAYER:双性 || TALENT:矮小体型)
		PRINTFORM 将%CALLNAME%抱起，
	ELSE
		PRINTFORM 将%CALLNAME%抱住，
	ENDIF
;同様に前回の调教が对面立位肛、背面立位肛の場合
ELSEIF TFLAG:60 && (PREVCOM == 75 || PREVCOM == 77 || PREVCOM == 516 || PREVCOM == 518 || (TFLAG:709 == 2 && PREVCOM == 514)) && ((ASSIPLAY && TFLAG:50) || (ASSIPLAY == 0 && TFLAG:50 == 0))
	PRINTFORM 保持着插入的姿势一起坐下，
	SIF PREVCOM == 77 || PREVCOM == 516 || PREVCOM == 518
		PRINTFORM 让%CALLNAME%面向自己，
	PRINTFORM 两人紧紧相拥，
ELSE
	;丝袜系
	IF TEQUIP:1 == 5 || TEQUIP:1 == 6
		PRINTFORM 插入的同时，%CALLNAME%的
		SIF TEQUIP:1 == 5
			PRINT 丝袜
		SIF TEQUIP:1 == 6
			PRINT 网袜
		IF TFLAG:55
			PRINTL 的开口被拉得更大了
		ELSE
			PRINTL 被指甲抓破了
		ENDIF
	ENDIF
	IF ASSIPLAY
    	PRINTFORM %CALLNAME:ASSI%
    ELSE
        PRINTFORM %CALLNAME:PLAYER%
    ENDIF
	IF TEQUIP:2 == 1
		PRINTFORM 拨开内裤，
	ELSEIF TEQUIP:2 == 2
		PRINTFORM 拨开内衣，
	ELSEIF TEQUIP:2 == 3
		PRINTFORM 掀开兜裆布，
	ELSEIF TEQUIP:2 == 6
		PRINTFORM 错开灯笼裤，
	ELSEIF TEQUIP:4 == 6
		PRINTFORM 错开运动短裤，
	ENDIF
	IF TALENT:PLAYER:140 || TALENT:PLAYER:139 || TALENT:PLAYER:137 || TALENT:矮小体型
		PRINTFORM 将%CALLNAME%抱起，
	ELSE
		PRINTFORM 将%CALLNAME%抱住，
	ENDIF
	;肛塞后庭拉珠
	IF TEQUIP:14 || TEQUIP:15 || TEQUIP:52
	    PRINTFORM 强行挤开还含着
		IF TEQUIP:14 && TALENT:143 == 11
			PRINTFORM 猫尾肛塞
		ELSEIF TEQUIP:14 && TALENT:143 == 12
			PRINTFORM 狗尾肛塞
		ELSEIF TEQUIP:14 && TALENT:143 == 13
			PRINTFORM 兔尾肛塞
		ELSEIF TEQUIP:14
			PRINTFORM 肛塞
	    ELSEIF TEQUIP:52
	        PRINT 尾巴
		ELSE
			PRINT 后庭拉珠
		ENDIF
		PRINTFORM 的肛穴，用自己
		;竿の有無で分岐
		IF TALENT:PLAYER:140 || TALENT:PLAYER:139 || TALENT:PLAYER:137
			IF TALENT:PLAYER:包茎
				PRINT 裹着包皮
				IF TALENT:PLAYER:孩童尺寸
					PRINT 的小小
				ELSEIF TALENT:PLAYER:短小
					PRINT 的短小
				ELSEIF TALENT:PLAYER:巨根 || TALENT:PLAYER:尺寸傲人
					PRINT 的粗大
				ELSE
			   		PRINT 的
				ENDIF
			ELSE
				IF TALENT:PLAYER:孩童尺寸
					PRINT 小小的
				ELSEIF TALENT:PLAYER:短小
					PRINT 短小的
				ELSEIF TALENT:PLAYER:巨根 || TALENT:PLAYER:尺寸傲人
					PRINT 粗大的
				ELSE
			   		PRINT 的
				ENDIF
			ENDIF
			PRINT 阴茎将其贯穿…
		ELSE
			PRINT 硕大的假阳具将其贯穿…
		ENDIF
	ELSE
	    PRINTFORM 用自己
		;竿の有無で分岐
		IF TALENT:PLAYER:140 || TALENT:PLAYER:139 || TALENT:PLAYER:137
			IF TALENT:PLAYER:包茎
				PRINT 裹着包皮
				IF TALENT:PLAYER:孩童尺寸
					PRINT 的小小
				ELSEIF TALENT:PLAYER:短小
					PRINT 的短小
				ELSEIF TALENT:PLAYER:巨根 || TALENT:PLAYER:尺寸傲人
					PRINT 的粗大
				ELSE
			   		PRINT 的
				ENDIF
			ELSE
				IF TALENT:PLAYER:孩童尺寸
					PRINT 小小的
				ELSEIF TALENT:PLAYER:短小
					PRINT 短小的
				ELSEIF TALENT:PLAYER:巨根 || TALENT:PLAYER:尺寸傲人
					PRINT 粗大的
				ELSE
			   		PRINT 的
				ENDIF
			ENDIF
			PRINT 阴茎
		ELSE
			PRINT 的假阳具
		ENDIF
		PRINTFORM 插入了%CALLNAME%
		;润滑LV4以上
		SIF PALAM:4 >= PALAMLV:4
			PRINTFORM 湿黏
		PRINTFORML 的肛穴…
	ENDIF
ENDIF
;助手プ雷の場合、男奴隶で、助手に同性倾向が2だと接吻しない。
SIF KANRAKU(TARGET) > 0 && (STAIN:0 < 2 || STAIN:0 == 16 || STAIN:0 == 17 || TALENT:MASTER:64) && TEQUIP:20 == 0 && (!ASSIPLAY || ABL:PLAYER:同性倾向 > 1 || !MALE(TARGET))
	PRINTFORM 与%CALLNAME%两唇相接的同时，
;绝壁
IF TALENT:110
	PRINTFORM 一边玩弄着对方的乳头
;贫乳
ELSEIF TALENT:109
	PRINTFORM 一边揉弄着对方薄薄的胸脯
;巨乳か爆乳
ELSEIF TALENT:111 || TALENT:112
	IF TALENT:140
		PRINTFORM 一边抓揉着对方壮观的胸肌
	ELSE
	PRINTFORM 一边抓揉着对方丰满的乳肉
	ENDIF
;普通乳
ELSE
	IF TALENT:140 || TALENT:137
		PRINTFORM 一边玩弄对方的乳头
	ELSE
		PRINTFORM 一边揉弄对方的胸部
	ENDIF
ENDIF
PRINTFORM 一边摆动着腰…
;顺从および欲望LV3以上+(助手でない or 淫乱ルート)
IF ABL:0 >= 3 && ABL:1 >= 3 && (!ASSIPLAY || KANRAKU(TARGET) == 2) && !RAND:2
	PRINTFORML 
	PRINTFORM %CALLNAME%双手挽住%CALLNAME:PLAYER%的脖子紧紧抱过来…
	TFLAG:52 = 1
ENDIF

;Ａ扩张经验２０以上
;IF EXP:53 >= 20 && TALENT:PLAYER:114
;	PRINTFORM 在%CALLNAME%已经充分扩张过的阴道面前，%CALLNAME:PLAYER%小孩子尺寸的阴茎完全不能满足%CALLNAME%…
;ENDIF


;-------------------------------------------------
;71,背面座位肛交
;-------------------------------------------------
@MESSAGE_COM71
;前回と今回の调教者が同じで、前回の调教が背面座位肛交であり、抜かないフラグがある場合
IF TFLAG:60 && (PREVCOM == 71 || (TFLAG:709 == 2 && (PREVCOM == 504 || PREVCOM == 593))) && ((ASSIPLAY && TFLAG:50) || (ASSIPLAY == 0 && TFLAG:50 == 0))
	PRINTFORM 从背后抱着%CALLNAME%，再次摇动起%CALLNAME%的身体…
;同様に、前回の调教が后背位肛交か骑乘位肛交である場合
ELSEIF TFLAG:60 && (PREVCOM == 63 || PREVCOM == 67 || PREVCOM == 591 || (TFLAG:709 == 2 && (PREVCOM == 592 || PREVCOM == 505))) && ((ASSIPLAY && TFLAG:50) || (ASSIPLAY == 0 && TFLAG:50 == 0))
	PRINTFORML 把\@ PENIS(PLAYER) ? 阴茎 # 假阳具 \@从%CALLNAME%体内拔出来
	PRINTFORM 从背后抱紧%CALLNAME%的身体开始活动…
;同様に、前回の调教が正常位肛交か对面座位肛交である場合
ELSEIF TFLAG:60 && (PREVCOM == 62 || PREVCOM == 70 || PREVCOM == 73) && ((ASSIPLAY && TFLAG:50) || (ASSIPLAY == 0 && TFLAG:50 == 0))
	PRINTFORM 保持着插入的姿势将%CALLNAME%的身体翻转过去
	;前回の调教が正常位肛交
	IF PREVCOM == 62
		PRINTFORML 抱了起来，
	ELSE
		PRINTFORML 紧紧抱在怀中，
	ENDIF
	PRINTFORM 激烈地活动起腰部…
;同様に前回の调教が对面立位肛、背面立位肛の場合
ELSEIF TFLAG:60 && (PREVCOM == 75 || PREVCOM == 77 || PREVCOM == 516 || PREVCOM == 518 || (TFLAG:709 == 2 && PREVCOM == 514)) && ((ASSIPLAY && TFLAG:50) || (ASSIPLAY == 0 && TFLAG:50 == 0))
	PRINTFORM 保持着插入的姿势坐了下来，将%CALLNAME%的身体
	SIF PREVCOM == 75 || PREVCOM == 514 || PREVCOM == 518
		PRINTFORM 翻转过去
	PRINTFORM 紧紧搂住，激烈地活动起腰部…
ELSE
	;竿の有無で分岐
	IF TALENT:PLAYER:140 || TALENT:PLAYER:139 || TALENT:PLAYER:137
    	;丝袜系
	    IF TEQUIP:1 == 5 || TEQUIP:1 == 6
		    PRINTFORM 插入的同时，%CALLNAME%的
	    	SIF TEQUIP:1 == 5
	    		PRINT 丝袜
	    	SIF TEQUIP:1 == 6
	    		PRINT 网袜
	    	IF TFLAG:55
	    		PRINTL 的开口被拉得更大了
	    	ELSE
	    		PRINTL 被指甲抓破了
	    	ENDIF
	    ENDIF
		SIF ASSIPLAY
    		PRINTFORM %CALLNAME:PLAYER%
	    IF TEQUIP:2 == 1
	    	PRINTFORM 拨开内裤，
	    ELSEIF TEQUIP:2 == 2
	    	PRINTFORM 拨开内衣，
	    ELSEIF TEQUIP:2 == 3
	    	PRINTFORM 掀开兜裆布，
	    ELSEIF TEQUIP:2 == 6
	    	PRINTFORM 错开灯笼裤，
	    ELSEIF TEQUIP:4 == 6
	    	PRINTFORM 错开运动短裤，
	    ENDIF
		PRINTFORM 抱起%CALLNAME%的身体，
		;肛塞后庭拉珠
		IF TEQUIP:14 || TEQUIP:15 || TEQUIP:52
		    PRINT 强行挤开还含着
			IF TEQUIP:14 && TALENT:143 == 11
				PRINTFORM 猫尾肛塞
			ELSEIF TEQUIP:14 && TALENT:143 == 12
				PRINTFORM 狗尾肛塞
			ELSEIF TEQUIP:14 && TALENT:143 == 13
				PRINTFORM 兔尾肛塞
			ELSEIF TEQUIP:14
				PRINTFORM 肛塞
			ELSEIF TEQUIP:52
			    PRINT 尾巴
			ELSE
				PRINT 后庭拉珠
			ENDIF
			PRINTFORM 的肛穴，用自己
			IF TALENT:PLAYER:包茎
				PRINT 裹着包皮
				IF TALENT:PLAYER:孩童尺寸
					PRINT 的小小
				ELSEIF TALENT:PLAYER:短小
					PRINT 的短小
				ELSEIF TALENT:PLAYER:巨根 || TALENT:PLAYER:尺寸傲人
					PRINT 的粗大
				ELSE
			   		PRINT 的
				ENDIF
			ELSE
				IF TALENT:PLAYER:孩童尺寸
					PRINT 小小的
				ELSEIF TALENT:PLAYER:短小
					PRINT 短小的
				ELSEIF TALENT:PLAYER:巨根 || TALENT:PLAYER:尺寸傲人
					PRINT 粗大的
				ELSE
			   		PRINT 的
				ENDIF
			ENDIF
			PRINTL 阴茎将其贯穿…
		ELSE
		    IF ASSIPLAY
            	PRINTFORM %CALLNAME:ASSI%
            ELSE
                PRINTFORM %CALLNAME:PLAYER%
            ENDIF
            PRINT 用自己
			IF TALENT:PLAYER:孩童尺寸
				SIF TALENT:PLAYER:矮小体型 == 0 || !TALENT:PLAYER:扶她
					PRINT 与体型不符
				PRINT 的小小
			ELSEIF TALENT:PLAYER:短小
				SIF TALENT:PLAYER:高大体型 || !TALENT:PLAYER:扶她
					PRINT 与体型不符
				PRINT 的短小
			ELSEIF TALENT:PLAYER:巨根
				SIF TALENT:PLAYER:矮小体型
					PRINT 与体型不符
				PRINT 的粗大
			ELSEIF TALENT:PLAYER:尺寸傲人
				SIF TALENT:PLAYER:矮小体型
					PRINT 与体型不符，
				PRINT 尺寸傲人的
			ELSE
			    PRINT 的
			ENDIF
			PRINTFORM 阴茎贯穿了%CALLNAME%
			;润滑LV4以上
			SIF PALAM:4 >= PALAMLV:4
				PRINTFORM 湿黏
			PRINTFORM 的后穴…
		ENDIF
	ELSE
    	;丝袜系
	    IF TEQUIP:1 == 5 || TEQUIP:1 == 6
		    PRINTFORM 插入的同时，%CALLNAME%的
	    	SIF TEQUIP:1 == 5
	    		PRINT 丝袜
	    	SIF TEQUIP:1 == 6
	    		PRINT 网袜
	    	IF TFLAG:55
	    		PRINTL 的开口被拉得更大了
	    	ELSE
	    		PRINTL 被指甲抓破了
	    	ENDIF
	    ENDIF
		IF ASSIPLAY
        	PRINTFORM %CALLNAME:ASSI%
        ELSE
            PRINTFORM %CALLNAME:PLAYER%
        ENDIF
		IF TALENT:矮小体型
			PRINTFORM 将%CALLNAME%抱起，
		ELSE
			PRINTFORM 将%CALLNAME%紧搂在怀，
		ENDIF
		;肛塞后庭拉珠
		IF TEQUIP:14 || TEQUIP:15 || TEQUIP:52
		    PRINT 强行挤开还含着
			IF TEQUIP:14 && TALENT:143 == 11
				PRINTFORM 猫尾肛塞
			ELSEIF TEQUIP:14 && TALENT:143 == 12
				PRINTFORM 狗尾肛塞
			ELSEIF TEQUIP:14 && TALENT:143 == 13
				PRINTFORM 兔尾肛塞
			ELSEIF TEQUIP:14
				PRINTFORM 肛塞
			ELSEIF TEQUIP:52
			    PRINT 尾巴
			ELSE
				PRINT 后庭拉珠
			ENDIF
			PRINTFORM 的后穴，把假阳具插了进去…
		ELSE
		    PRINTFORM 贯穿了%CALLNAME%
			;润滑LV4以上
			SIF PALAM:4 >= PALAMLV:4
				PRINTFORM 湿黏
			PRINTFORM 的后穴…
		ENDIF
	ENDIF
ENDIF
;顺从および欲望LV3以上+(助手でない or 淫乱ルート)
IF ABL:0 >= 3 && ABL:1 >= 3 && (!ASSIPLAY || KANRAKU(TARGET) == 2) && !RAND:2
	PRINTFORML 
	PRINTFORM %CALLNAME%情动地向后仰去，背部紧贴在%CALLNAME:PLAYER%的胸膛上…
	TFLAG:52 = 1
ENDIF


;Ａ扩张经验２０以上
;IF EXP:53 >= 20 && TALENT:PLAYER:114
;	PRINTFORM 在%CALLNAME%已经充分扩张过的肛穴面前，%CALLNAME:PLAYER%小孩子尺寸的阴茎完全不能满足%CALLNAME%…
;ENDIF

;-------------------------------------------------
;72,错开内裤插入
;-------------------------------------------------
@MESSAGE_COM72

PRINTFORM %CALLNAME:PLAYER%
;丝袜系
IF TEQUIP:1 == 5 || TEQUIP:1 == 6
	IF TFLAG:55
		PRINT 撕开
	ELSE
		PRINT 扯破
	ENDIF
	SIF TEQUIP:1 == 5
		PRINTFORM %CALLNAME%的丝袜，
	SIF TEQUIP:1 == 6
		PRINTFORM %CALLNAME%的网袜，
ENDIF
IF TEQUIP:2 == 1
	PRINTFORM 拨开内裤，
ELSEIF TEQUIP:2 == 2
	PRINTFORM 拨开内衣，
ELSEIF TEQUIP:2 == 3
	PRINTFORM 掀开兜裆布，
ELSEIF TEQUIP:2 == 6
	PRINTFORM 错开灯笼裤，
ELSEIF TEQUIP:4 == 6
	PRINTFORM 错开运动短裤，
ENDIF
PRINTFORM 贯穿了那暴露在外
;处女
IF TALENT:0
	PRINTFORM ，未经人事的
;润滑LV4以上
ELSEIF PALAM:4 >= PALAMLV:4
	PRINTFORM ，滴落爱液的
ELSE
    PRINT 的
ENDIF
;矮小体型
IF TALENT:100
	PRINTFORM 娇嫩小穴
ELSE
	PRINTFORM 秘裂
ENDIF
;顺从および欲望LV3以上+(助手でない or 淫乱ルート)
IF ABL:0 >= 3 && ABL:1 >= 3 && (!ASSIPLAY || KANRAKU(TARGET) == 2)
	PRINTFORML
	PRINTFORM %CALLNAME%双腿交叉环住了%CALLNAME:PLAYER%的腰…
	TFLAG:52 = 1
ELSE
	PRINTFORM …
ENDIF

;-------------------------------------------------
;73,错开内裤插入肛门
;-------------------------------------------------
@MESSAGE_COM73

PRINTFORM %CALLNAME:PLAYER%
;丝袜系
IF TEQUIP:1 == 5 || TEQUIP:1 == 6
	IF TFLAG:55
		PRINT 撕开
	ELSE
		PRINT 扯破
	ENDIF
	SIF TEQUIP:1 == 5
		PRINTFORM %CALLNAME%的丝袜，
	SIF TEQUIP:1 == 6
		PRINTFORM %CALLNAME%的网袜，
ENDIF
IF TEQUIP:2 == 1
	PRINTFORM 拨开内裤，
ELSEIF TEQUIP:2 == 2
	PRINTFORM 拨开内衣，
ELSEIF TEQUIP:2 == 3
	PRINTFORM 掀开兜裆布，
ELSEIF TEQUIP:2 == 6
	PRINTFORM 错开灯笼裤，
ELSEIF TEQUIP:4 == 6
	PRINTFORM 错开运动短裤，
ENDIF
PRINTFORM 贯穿了那暴露在外
;润滑LV4以上
SIF PALAM:4 >= PALAMLV:4
	PRINTFORM ，滴落着粘液
;顺从および欲望LV3以上+(助手でない or 淫乱ルート)
IF ABL:0 >= 3 && ABL:1 >= 3 && (!ASSIPLAY || KANRAKU(TARGET) == 2)
	PRINTFORML 的肛穴
	PRINTFORM %CALLNAME%双腿交叉环住了%CALLNAME:PLAYER%的腰…
	TFLAG:52 = 1
ELSE
	PRINTFORM 的肛穴…
ENDIF
;-------------------------------------------------
;74,对面立位
;-------------------------------------------------
@MESSAGE_COM74
;前回と今回の调教者が同じで、前回の调教が对面立位であり、抜かないフラグがある場合
IF TFLAG:60 && (PREVCOM == 74 || PREVCOM == 517 || (TFLAG:709 == 1 && (PREVCOM == 514 || PREVCOM == 593))) && ((ASSIPLAY && TFLAG:50) || (ASSIPLAY == 0 && TFLAG:50 == 0))
	PRINTFORM %CALLNAME:PLAYER%
	SIF KANRAKU(TARGET) > 0 && (STAIN:0 < 2 || STAIN:0 == 16 || STAIN:0 == 17 || TALENT:MASTER:64) && TEQUIP:20 == 0 && (!ASSIPLAY || ABL:PLAYER:同性倾向 > 1 || !MALE(TARGET))
		PRINTFORM 与%CALLNAME%唇齿相接，舌尖缠绕，随即
	PRINTFORM 再一次活动起腰部…
	RETURN 0
;同様に前回の调教が正常位、骑乘位、对面座位の場合
ELSEIF TFLAG:60 && (PREVCOM == 60 || PREVCOM == 66 || PREVCOM == 68 || PREVCOM == 72 || (TFLAG:709 == 1 && (PREVCOM == 592 || PREVCOM == 505))) && ((ASSIPLAY && TFLAG:50) || (ASSIPLAY == 0 && TFLAG:50 == 0))
	PRINTFORM 把\@ PENIS(PLAYER) ? 阴茎 # 假阳具 \@从%CALLNAME%体内拔出来，让其站立，
;同様に前回の调教が后背位、背面座位、背面立位の場合
ELSEIF TFLAG:60 && (PREVCOM == 61 || PREVCOM == 66 || PREVCOM == 76 || PREVCOM == 515 || (TFLAG:709 == 1 && PREVCOM == 504)) && ((ASSIPLAY && TFLAG:50) || (ASSIPLAY == 0 && TFLAG:50 == 0))
	PRINTFORM 保持着插入的姿势把%CALLNAME%的身体翻转过来，
	SIF PREVCOM == 61 || PREVCOM == 66
		PRINTFORM 让其站立，
ELSE
	;丝袜系
	IF TEQUIP:1 == 5 || TEQUIP:1 == 6
		PRINTFORM 插入的同时，%CALLNAME%的
		SIF TEQUIP:1 == 5
			PRINT 丝袜
		SIF TEQUIP:1 == 6
			PRINT 网袜
		IF TFLAG:55
			PRINTL 的开口被拉得更大了
		ELSE
			PRINTL 被指甲抓破了
		ENDIF
	ENDIF
	IF ASSIPLAY
    	PRINTFORM %CALLNAME:ASSI%
    ELSE
        PRINTFORM %CALLNAME:PLAYER%
    ENDIF
	IF TEQUIP:2 == 1
		PRINTFORM 拨开内裤，
	ELSEIF TEQUIP:2 == 2
		PRINTFORM 拨开内衣，
	ELSEIF TEQUIP:2 == 3
		PRINTFORM 掀开兜裆布，
	ELSEIF TEQUIP:2 == 6
		PRINTFORM 错开灯笼裤，
	ELSEIF TEQUIP:4 == 6
		PRINTFORM 错开运动短裤，
	ENDIF
	PRINTFORM 让%CALLNAME%站立着，
	;自慰棒入れっぱ无
	SIF TEQUIP:13 
		PRINTFORM 强行挤开还含着自慰棒的穴口，
	;尾巴插入
	SIF TEQUIP:51
		PRINTFORM 强行挤开还含着尾巴的穴口，
	PRINTFORM 插入了%CALLNAME%
	;润滑LV4以上
	SIF PALAM:4 >= PALAMLV:4
		PRINTFORM 湿黏
	;矮小体型
	IF TALENT:100
		PRINTFORM 的娇嫩小穴
	ELSE
		PRINTFORM 的秘裂
	ENDIF
	PRINTFORML …
ENDIF
SIF KANRAKU(TARGET) > 0 && (STAIN:0 < 2 || STAIN:0 == 16 || STAIN:0 == 17 || TALENT:MASTER:64) && TEQUIP:20 == 0 && (!ASSIPLAY || ABL:PLAYER:同性倾向 > 1 || !MALE(TARGET))
	PRINTFORM 与%CALLNAME%两唇相接的同时，
;绝壁
IF TALENT:110
	PRINTFORM 一边玩弄着对方的乳头
;贫乳
ELSEIF TALENT:109
	PRINTFORM 一边揉弄着对方薄薄的胸脯
;巨乳か爆乳
ELSEIF TALENT:111 || TALENT:112
	IF TALENT:140
		PRINTFORM 一边抓揉着对方壮观的胸肌
	ELSE
	PRINTFORM 一边抓揉着对方丰满的乳肉
	ENDIF
;普通乳
ELSE
	IF TALENT:140 || TALENT:137
		PRINTFORM 一边玩弄对方的乳头
	ELSE
		PRINTFORM 一边揉弄对方的胸部
	ENDIF
ENDIF
PRINTFORM 一边摆动着腰…

IF ABL:Ｖ感觉 >= 3 && PALAM:快Ｖ >= 6000 && !RAND:2
	PRINTL 
	PRINTFORM %CALLNAME%被剧烈顶撞着，快乐地呻吟出声的同时又因站立困难左右摇摆…
	TFLAG:52 = 1
ENDIF


;Ｖ扩张经验２０以上
;IF EXP:52 >= 20 && TALENT:PLAYER:114
;	PRINTFORM 在%CALLNAME%已经充分扩张过的阴道面前，%CALLNAME:PLAYER%小孩子尺寸的阴茎完全不能满足%CALLNAME%…
;ENDIF
;-------------------------------------------------
;75,对面立位肛
;-------------------------------------------------
@MESSAGE_COM75
;前回と今回の调教者が同じで、前回の调教が对面立位肛であり、抜かないフラグがある場合
IF TFLAG:60 && (PREVCOM == 75 || PREVCOM == 518 || (TFLAG:709 == 2 && (PREVCOM == 514 || PREVCOM == 593))) && ((ASSIPLAY && TFLAG:50) || (ASSIPLAY == 0 && TFLAG:50 == 0))
	PRINTFORM %CALLNAME:PLAYER%
	SIF KANRAKU(TARGET) > 0 && (STAIN:0 < 2 || STAIN:0 == 16 || STAIN:0 == 17 || TALENT:MASTER:64) && TEQUIP:20 == 0 && TEQUIP:25 == 0 && (!ASSIPLAY || ABL:PLAYER:同性倾向 > 1 || !MALE(TARGET))
		PRINTFORM 与%CALLNAME%唇齿相接，舌尖缠绕，随即
	PRINTFORM 再一次活动起腰部…
	RETURN 0
;同様に前回の调教が正常位肛交、骑乘位肛交、对面座位肛交の場合
ELSEIF TFLAG:60 && (PREVCOM == 62 || PREVCOM == 67 || PREVCOM == 70 || PREVCOM == 73 || (TFLAG:709 == 2 && (PREVCOM == 592 || PREVCOM == 505))) && ((ASSIPLAY && TFLAG:50) || (ASSIPLAY == 0 && TFLAG:50 == 0))
	PRINTFORM 把\@ PENIS(PLAYER) ? 阴茎 # 假阳具 \@从%CALLNAME%体内拔出来，让其站立，
;同様に前回の调教が后背位肛交、背面座位肛交、背面立位肛の場合
ELSEIF TFLAG:60 && (PREVCOM == 63 || PREVCOM == 71 || PREVCOM == 77 || PREVCOM == 516 || PREVCOM == 591 || (TFLAG:709 == 2 && PREVCOM == 504)) && ((ASSIPLAY && TFLAG:50) || (ASSIPLAY == 0 && TFLAG:50 == 0))
	PRINTFORM 保持着插入的姿势把%CALLNAME%的身体翻转过来，
	;前回の调教が后背位肛交、背面座位肛交
	SIF PREVCOM == 63 || PREVCOM == 71 || PREVCOM == 504 || PREVCOM == 591
		PRINTFORM 让其站立，
ELSE
	;丝袜系
	IF TEQUIP:1 == 5 || TEQUIP:1 == 6
		PRINTFORM 插入的同时，%CALLNAME%的
		SIF TEQUIP:1 == 5
			PRINT 丝袜
		SIF TEQUIP:1 == 6
			PRINT 网袜
		IF TFLAG:55
			PRINTL 的开口被拉得更大了
		ELSE
			PRINTL 被指甲抓破了
		ENDIF
	ENDIF
	IF ASSIPLAY
    	PRINTFORM %CALLNAME:ASSI%
    ELSE
        PRINTFORM %CALLNAME:PLAYER%
    ENDIF
	IF TEQUIP:2 == 1
		PRINTFORM 拨开内裤，
	ELSEIF TEQUIP:2 == 2
		PRINTFORM 拨开内衣，
	ELSEIF TEQUIP:2 == 3
		PRINTFORM 掀开兜裆布，
	ELSEIF TEQUIP:2 == 6
		PRINTFORM 错开灯笼裤，
	ELSEIF TEQUIP:4 == 6
		PRINTFORM 错开运动短裤，
	ENDIF
	PRINTFORM 让%CALLNAME%站立着，
	;肛塞后庭拉珠
	IF TEQUIP:14 || TEQUIP:15 || TEQUIP:52
	    PRINT 强行挤开还含着
		IF TEQUIP:14 && TALENT:143 == 11
			PRINTFORM 猫尾肛塞
		ELSEIF TEQUIP:14 && TALENT:143 == 12
			PRINTFORM 狗尾肛塞
		ELSEIF TEQUIP:14 && TALENT:143 == 13
			PRINTFORM 兔尾肛塞
		ELSEIF TEQUIP:14
			PRINTFORM 肛塞
		ELSEIF TEQUIP:52
		    PRINT 尾巴
		ELSE
			PRINT 后庭拉珠
		ENDIF
		PRINTFORM 的肛穴，用自己
		;竿の有無で分岐
		IF TALENT:PLAYER:140 || TALENT:PLAYER:139 || TALENT:PLAYER:137
			IF TALENT:PLAYER:孩童尺寸
				PRINT 小小的
			ELSEIF TALENT:PLAYER:短小
				PRINT 短小的
			ELSEIF TALENT:PLAYER:巨根 || TALENT:PLAYER:尺寸傲人
				PRINT 粗大的
			ELSE
			    PRINT 的
			ENDIF
			PRINTL 阴茎
		ELSE
			PRINTL 的假阳具
		ENDIF
		PRINT 将其贯穿…
	ELSE
		IF ASSIPLAY
        	PRINTFORM %CALLNAME:ASSI%
        ELSE
            PRINTFORM %CALLNAME:PLAYER%
        ENDIF
    	PRINTFORM 用
		;竿の有無で分岐
		IF TALENT:PLAYER:140 || TALENT:PLAYER:139 || TALENT:PLAYER:137
			IF TALENT:PLAYER:包茎
				PRINT 裹着包皮
				IF TALENT:PLAYER:孩童尺寸
		    	    SIF TALENT:PLAYER:矮小体型 == 0 || !TALENT:PLAYER:扶她
				        PRINT ，与体型不符
					PRINT 的小小
				ELSEIF TALENT:PLAYER:短小
		    	    SIF TALENT:PLAYER:高大体型 || !TALENT:PLAYER:扶她
				        PRINT ，与体型不符
					PRINT 的短小
				ELSEIF TALENT:PLAYER:巨根 || TALENT:PLAYER:尺寸傲人
		    	    SIF TALENT:PLAYER:矮小体型
				        PRINT ，与体型不符
					PRINT 的粗大
				ELSE
			   		PRINT 的
				ENDIF
			ELSE
				IF TALENT:PLAYER:孩童尺寸
		    	    SIF TALENT:PLAYER:矮小体型 == 0 || !TALENT:PLAYER:扶她
				        PRINT 与体型不符
					PRINT 的小小
				ELSEIF TALENT:PLAYER:短小
		    	    SIF TALENT:PLAYER:高大体型 || !TALENT:PLAYER:扶她
				        PRINT 与体型不符
					PRINT 的短小
				ELSEIF TALENT:PLAYER:巨根 || TALENT:PLAYER:尺寸傲人
		    	    SIF TALENT:PLAYER:矮小体型
				        PRINT 与体型不符
					PRINT 的粗大
				ELSE
			   		PRINT 的
				ENDIF
			ENDIF
			PRINT 阴茎
		ELSE
			PRINT 硕大的假阳具
		ENDIF
		PRINTFORM 贯穿了%CALLNAME%
		;润滑LV4以上
		SIF PALAM:4 >= PALAMLV:4
			PRINTFORM 湿黏
		PRINTFORML 的肛穴…
	ENDIF
ENDIF
SIF KANRAKU(TARGET) > 0 && (STAIN:0 < 2 || STAIN:0 == 16 || STAIN:0 == 17 || TALENT:MASTER:64) && TEQUIP:20 == 0 && TEQUIP:25 == 0 && (!ASSIPLAY || ABL:PLAYER:同性倾向 > 1 || !MALE(TARGET))
	PRINTFORM 与%CALLNAME%两唇相接的同时，
;绝壁
IF TALENT:110
	PRINTFORM 一边玩弄着对方的乳头
;贫乳
ELSEIF TALENT:109
	PRINTFORM 一边揉弄着对方薄薄的胸脯
;巨乳か爆乳
ELSEIF TALENT:111 || TALENT:112
	IF TALENT:140
		PRINTFORM 一边抓揉着对方壮观的胸肌
	ELSE
	PRINTFORM 一边抓揉着对方丰满的乳肉
	ENDIF
;普通乳
ELSE
	IF TALENT:140 || TALENT:137
		PRINTFORM 一边玩弄对方的乳头
	ELSE
		PRINTFORM 一边揉弄对方的胸部
	ENDIF
ENDIF
PRINTFORM 一边摆动着腰…

IF ABL:Ａ感觉 >= 3 && PALAM:快Ａ >= 6000 && !RAND:2
	PRINTFORML 
	PRINTFORM %CALLNAME%被剧烈顶撞着，快乐地呻吟出声的同时又因站立困难左右摇摆…
	TFLAG:52 = 1
ENDIF

;Ａ扩张经验２０以上
;IF EXP:53 >= 20 && TALENT:PLAYER:114
;	PRINTFORM 在%CALLNAME%已经充分扩张过的肛穴面前，%CALLNAME:PLAYER%小孩子尺寸的阴茎完全不能满足%CALLNAME%…
;ENDIF


;-------------------------------------------------
;76,背面立位
;-------------------------------------------------
@MESSAGE_COM76
;前回と今回の调教者が同じで、前回の调教が背面立位であり、抜かないフラグがある場合
IF TFLAG:60 && (PREVCOM == 76 || PREVCOM == 515 || (TFLAG:709 == 1 && PREVCOM == 593)) && ((ASSIPLAY && TFLAG:50) || (ASSIPLAY == 0 && TFLAG:50 == 0))
	PRINTFORML 从背后抱紧%CALLNAME%，再次摇动起%CALLNAME%的身体…
;同様に、前回の调教が后背位か骑乘位か背面座位である場合
ELSEIF TFLAG:60 && (PREVCOM == 61 || PREVCOM == 66 || PREVCOM == 69 || (TFLAG:709 == 1 && (PREVCOM == 504 || PREVCOM == 505 || PREVCOM == 592))) && ((ASSIPLAY && TFLAG:50) || (ASSIPLAY == 0 && TFLAG:50 == 0))
	PRINTFORML 把\@ PENIS(PLAYER) ? 阴茎 # 假阳具 \@从%CALLNAME%体内拔出来，让其站立
	PRINTFORM 从背面抱紧%CALLNAME%再次摆动起腰部…
;同様に、前回の调教が正常位か对面座位か对面立位である場合
ELSEIF TFLAG:60 && (PREVCOM == 60 || PREVCOM == 68 || PREVCOM == 72 || PREVCOM == 74 || PREVCOM == 517 || (TFLAG:709 == 1 && PREVCOM == 514)) && ((ASSIPLAY && TFLAG:50) || (ASSIPLAY == 0 && TFLAG:50 == 0))	
	;前回の调教が正常位か对面座位
	IF PREVCOM == 60 || PREVCOM == 68 || PREVCOM == 72
		PRINTFORM 保持着插入的姿势翻转%CALLNAME%的身体让其站立，继续激烈地活动起腰部…
	ELSE
		PRINTFORM 保持着插入的姿势翻转%CALLNAME%的身体，继续激烈地活动起腰部…
	ENDIF
ELSE
    ;丝袜系
	IF TEQUIP:1 == 5 || TEQUIP:1 == 6
	    PRINTFORM 插入的同时，%CALLNAME%的
		SIF TEQUIP:1 == 5
			PRINT 丝袜
		SIF TEQUIP:1 == 6
			PRINT 网袜
		IF TFLAG:55
			PRINTL 的开口被拉得更大了
		ELSE
			PRINTL 被指甲抓破了
		ENDIF
    ENDIF
	IF ASSIPLAY
    	PRINTFORM %CALLNAME:ASSI%
    ELSE
        PRINTFORM %CALLNAME:PLAYER%
    ENDIF
    IF TEQUIP:2 == 1
    	PRINTFORM 拨开内裤，
    ELSEIF TEQUIP:2 == 2
    	PRINTFORM 拨开内衣，
    ELSEIF TEQUIP:2 == 3
    	PRINTFORM 掀开兜裆布，
    ELSEIF TEQUIP:2 == 6
    	PRINTFORM 错开灯笼裤，
    ELSEIF TEQUIP:4 == 6
    	PRINTFORM 错开运动短裤，
    ENDIF
	PRINTFORM 让%CALLNAME%站立，
	;自慰棒入れっぱ无
	SIF TEQUIP:13 
		PRINTFORM 强行挤开还含着自慰棒的穴口，
	;尾巴插入
	SIF TEQUIP:51
		PRINTFORM 强行挤开还含着尾巴的穴口，
	;竿の有無で分岐
	IF TALENT:PLAYER:140 || TALENT:PLAYER:139 || TALENT:PLAYER:137
		PRINTFORM 慢慢地用阴茎贯穿了%CALLNAME%
	ELSE
		PRINTFORM 慢慢地用假阳具贯穿了%CALLNAME%
	ENDIF
	;润滑LV4以上
	SIF PALAM:4 >= PALAMLV:4
		PRINTFORM 湿黏
	;矮小体型
	IF TALENT:100
		PRINTFORM 的娇嫩小穴…
	ELSE
		PRINTFORM 的秘裂…
	ENDIF
ENDIF

IF ABL:Ｖ感觉 >= 3 && PALAM:快Ｖ >= 6000 && !RAND:2
	PRINTL 
	PRINTFORM %CALLNAME%被剧烈顶撞着，快乐地呻吟出声的同时又因站立困难左右摇摆…
	TFLAG:52 = 1
ENDIF

;Ｖ扩张经验２０以上
;IF EXP:52 >= 20 && TALENT:PLAYER:114
;	PRINTFORML 
;	PRINTFORM 在%CALLNAME%已经充分扩张过的阴道面前，%CALLNAME:PLAYER%小孩子尺寸的阴茎完全不能满足%CALLNAME%…
;ENDIF


;-------------------------------------------------
;77,背面立位肛
;-------------------------------------------------
@MESSAGE_COM77
;前回と今回の调教者が同じで、前回の调教が背面立位肛であり、抜かないフラグがある場合
IF TFLAG:60 && (PREVCOM == 77 || PREVCOM == 516 || (TFLAG:709 == 2 && PREVCOM == 593)) && ((ASSIPLAY && TFLAG:50) || (ASSIPLAY == 0 && TFLAG:50 == 0))
	PRINTFORM 从背后抱紧%CALLNAME%，再次摇动起%CALLNAME%的身体…
;同様に、前回の调教が后背位肛交か骑乘位肛交か背面座位肛交である場合
ELSEIF TFLAG:60 && (PREVCOM == 63 || PREVCOM == 67 || PREVCOM == 71 || PREVCOM == 591 || (TFLAG:709 == 2 && (PREVCOM == 504 || PREVCOM == 505 || PREVCOM == 592))) && ((ASSIPLAY && TFLAG:50) || (ASSIPLAY == 0 && TFLAG:50 == 0))
	PRINTFORML 把\@ PENIS(PLAYER) ? 阴茎 # 假阳具 \@从%CALLNAME%体内拔出来，让其站立
	PRINTFORM 从背面抱紧%CALLNAME%再次摆动起腰部…
;同様に、前回の调教が正常位肛交か对面座位肛交か对面立位肛である場合
ELSEIF TFLAG:60 && (PREVCOM == 62 || PREVCOM == 70 || PREVCOM == 73 || PREVCOM == 75 || PREVCOM == 518 || (TFLAG:709 == 2 && PREVCOM == 514)) && ((ASSIPLAY && TFLAG:50) || (ASSIPLAY == 0 && TFLAG:50 == 0))
	;前回の调教が正常位か对面座位
	IF PREVCOM == 62 || PREVCOM == 70 || PREVCOM == 73
		PRINTFORM 保持着插入的姿势翻转%CALLNAME%的身体让其站立，继续激烈地活动起腰部…
	ELSE
		PRINTFORM 保持着插入的姿势翻转%CALLNAME%的身体，继续激烈地活动起腰部…
	ENDIF
ELSE
    ;丝袜系
	IF TEQUIP:1 == 5 || TEQUIP:1 == 6
	    PRINTFORM 插入的同时，%CALLNAME%的
		SIF TEQUIP:1 == 5
			PRINT 丝袜
		SIF TEQUIP:1 == 6
			PRINT 网袜
		IF TFLAG:55
			PRINTL 的开口被拉得更大了
		ELSE
			PRINTL 被指甲抓破了
		ENDIF
    ENDIF
	IF ASSIPLAY
    	PRINTFORM %CALLNAME:ASSI%
    ELSE
        PRINTFORM %CALLNAME:PLAYER%
    ENDIF
    IF TEQUIP:2 == 1
    	PRINTFORM 拨开内裤，
    ELSEIF TEQUIP:2 == 2
    	PRINTFORM 拨开内衣，
    ELSEIF TEQUIP:2 == 3
    	PRINTFORM 掀开兜裆布，
    ELSEIF TEQUIP:2 == 6
    	PRINTFORM 错开灯笼裤，
    ELSEIF TEQUIP:4 == 6
    	PRINTFORM 错开运动短裤，
    ENDIF
	PRINTFORM 让%CALLNAME%站立，
	;肛塞后庭拉珠
	IF TEQUIP:14 || TEQUIP:15 || TEQUIP:52
	    PRINT 强行挤开还含着
		IF TEQUIP:14 && TALENT:143 == 11
			PRINTFORM 猫尾肛塞
		ELSEIF TEQUIP:14 && TALENT:143 == 12
			PRINTFORM 狗尾肛塞
		ELSEIF TEQUIP:14 && TALENT:143 == 13
			PRINTFORM 兔尾肛塞
		ELSEIF TEQUIP:14
			PRINTFORM 肛塞
		ELSEIF TEQUIP:52
		    PRINT 尾巴
		ELSE
			PRINT 后庭拉珠
		ENDIF
		PRINTFORM 的肛穴，用
		;调教者が男性か双性か扶她である
		IF TALENT:PLAYER:137 || TALENT:PLAYER:139 || TALENT:PLAYER:140
		    PRINT 自己
			IF TALENT:PLAYER:孩童尺寸
				PRINT 小小的
			ELSEIF TALENT:PLAYER:短小
				PRINT 短小的
			ELSEIF TALENT:PLAYER:巨根 || TALENT:PLAYER:尺寸傲人
				PRINT 粗大的
			ELSE
			    PRINT 的
			ENDIF
			PRINT 阴茎
		ELSE
			PRINTFORM 硕大的假阳具
		ENDIF
		PRINT 将其贯穿…
	ELSE
	    PRINTFORM %CALLNAME:PLAYER%用
		;调教者が男性か双性か扶她である
		IF TALENT:PLAYER:137 || TALENT:PLAYER:139 || TALENT:PLAYER:140
		    PRINT 自己
			IF TALENT:PLAYER:包茎
				PRINT 裹着包皮
				IF TALENT:PLAYER:孩童尺寸
		    	    SIF TALENT:PLAYER:矮小体型 == 0 || !TALENT:PLAYER:扶她
				        PRINT ，与体型不符
					PRINT 的小小
				ELSEIF TALENT:PLAYER:短小
		    	    SIF TALENT:PLAYER:高大体型 || !TALENT:PLAYER:扶她
				        PRINT ，与体型不符
					PRINT 的短小
				ELSEIF TALENT:PLAYER:巨根 || TALENT:PLAYER:尺寸傲人
		    	    SIF TALENT:PLAYER:矮小体型
				        PRINT ，与体型不符
					PRINT 的粗大
				ELSE
			   		PRINT 的
				ENDIF
			ELSE
				IF TALENT:PLAYER:孩童尺寸
		    	    SIF TALENT:PLAYER:矮小体型 == 0 || !TALENT:PLAYER:扶她
				        PRINT 与体型不符
					PRINT 的小小
				ELSEIF TALENT:PLAYER:短小
		    	    SIF TALENT:PLAYER:高大体型 || !TALENT:PLAYER:扶她
				        PRINT 与体型不符
					PRINT 的短小
				ELSEIF TALENT:PLAYER:巨根 || TALENT:PLAYER:尺寸傲人
		    	    SIF TALENT:PLAYER:矮小体型
				        PRINT 与体型不符
					PRINT 的粗大
				ELSE
			   		PRINT 的
				ENDIF
			ENDIF
			PRINTFORM 阴茎贯穿了%CALLNAME%
			;润滑LV4以上
			SIF PALAM:4 >= PALAMLV:4
				PRINTFORM 湿黏
			PRINTFORM 的肛穴…
		ELSE
			PRINTFORM 硕大的假阳具贯穿了%CALLNAME%
			SIF PALAM:4 >= PALAMLV:4
				PRINTFORM 满是肠液
			PRINTFORM 的肛穴…
		ENDIF
	ENDIF
ENDIF

IF ABL:Ａ感觉 >= 3 && PALAM:快Ａ >= 6000 && !RAND:2
	PRINTFORML 
	PRINTFORM %CALLNAME%被剧烈顶撞着，快乐地呻吟出声的同时又因站立困难左右摇摆…
	TFLAG:52 = 1
ENDIF


;Ａ扩张经验２０以上
;IF EXP:53 >= 20 && TALENT:PLAYER:114
;	PRINTFORM 在%CALLNAME%已经充分扩张过的肛穴面前，%CALLNAME:PLAYER%小孩子尺寸的阴茎完全不能满足%CALLNAME%…
;ENDIF

;-------------------------------------------------
;80,手交命令
;-------------------------------------------------
@MESSAGE_COM80
PRINTFORM %CALLNAME%
;恋慕か相亲相爱
SIF (TALENT:3 || TALENT:6) && !ASSIPLAY
	PRINTFORM 满怀爱意地
;淫乱か娼妇
SIF (TALENT:4 || TALENT:7)
	PRINTFORM 主动积极地

PRINTFORM 用手指摩擦抚弄着%CALLNAME:PLAYER%
IF TALENT:PLAYER:包茎
	PRINT 裹着包皮
	IF TALENT:PLAYER:孩童尺寸
		SIF TALENT:PLAYER:矮小体型 == 0 || !TALENT:PLAYER:扶她
			PRINT ，与体型不符
		PRINT 的小小
	ELSEIF TALENT:PLAYER:短小
		SIF TALENT:PLAYER:高大体型 || !TALENT:PLAYER:扶她
			PRINT ，与体型不符
		PRINT 的短小
	ELSEIF TALENT:PLAYER:巨根
		SIF TALENT:PLAYER:矮小体型
			PRINT ，与体型不符
		PRINT 的粗大
	ELSEIF TALENT:PLAYER:尺寸傲人
		SIF TALENT:PLAYER:矮小体型
			PRINT ，与体型不符，
		PRINT 尺寸傲人的
	ELSE
   		PRINT 的
	ENDIF
ELSE
	IF TALENT:PLAYER:孩童尺寸
		SIF TALENT:PLAYER:矮小体型 == 0 || !TALENT:PLAYER:扶她
			PRINT 与体型不符
		PRINT 的小小
	ELSEIF TALENT:PLAYER:短小
		SIF TALENT:PLAYER:高大体型 || !TALENT:PLAYER:扶她
			PRINT 与体型不符
		PRINT 的短小
	ELSEIF TALENT:PLAYER:巨根
		SIF TALENT:PLAYER:矮小体型
			PRINT 与体型不符
		PRINT 的粗大
	ELSEIF TALENT:PLAYER:尺寸傲人
		SIF TALENT:PLAYER:矮小体型
			PRINT 与体型不符，
		PRINT 尺寸傲人的
	ELSE
   		PRINT 的
	ENDIF
ENDIF
PRINTFORML 阴茎…
;-------------------------------------------------
;81,口交命令
;-------------------------------------------------
@MESSAGE_COM81
PRINTFORM %CALLNAME%
;恋慕か相亲相爱
SIF (TALENT:3 || TALENT:6) && !ASSIPLAY
	PRINTFORM 眼含爱意地
;淫乱か娼妇
SIF (TALENT:4 || TALENT:7)
	PRINTFORM 贪婪地
PRINTFORM 舔舐吞吐着%CALLNAME:PLAYER%
IF TALENT:PLAYER:包茎
	PRINT 裹着包皮
	IF TALENT:PLAYER:孩童尺寸
		SIF TALENT:PLAYER:矮小体型 == 0 || !TALENT:PLAYER:扶她
			PRINT ，与体型不符
		PRINT 的小小
	ELSEIF TALENT:PLAYER:短小
		SIF TALENT:PLAYER:高大体型 || !TALENT:PLAYER:扶她
			PRINT ，与体型不符
		PRINT 的短小
	ELSEIF TALENT:PLAYER:巨根
		SIF TALENT:PLAYER:矮小体型
			PRINT ，与体型不符
		PRINT 的粗大
	ELSEIF TALENT:PLAYER:尺寸傲人
		SIF TALENT:PLAYER:矮小体型
			PRINT ，与体型不符，
		PRINT 尺寸傲人的
	ELSE
   		PRINT 的
	ENDIF
ELSE
	IF TALENT:PLAYER:孩童尺寸
		SIF TALENT:PLAYER:矮小体型 == 0 || !TALENT:PLAYER:扶她
			PRINT 与体型不符
		PRINT 的小小
	ELSEIF TALENT:PLAYER:短小
		SIF TALENT:PLAYER:高大体型 || !TALENT:PLAYER:扶她
			PRINT 与体型不符
		PRINT 的短小
	ELSEIF TALENT:PLAYER:巨根
		SIF TALENT:PLAYER:矮小体型
			PRINT 与体型不符
		PRINT 的粗大
	ELSEIF TALENT:PLAYER:尺寸傲人
		SIF TALENT:PLAYER:矮小体型
			PRINT 与体型不符，
		PRINT 尺寸傲人的
	ELSE
   		PRINT 的
	ENDIF
ENDIF
PRINTFORML 阴茎…
;-------------------------------------------------
;82,乳交
;-------------------------------------------------
@MESSAGE_COM82
PRINTFORM %CALLNAME%
;恋慕か相亲相爱
SIF (TALENT:3 || TALENT:6) && !ASSIPLAY
	PRINTFORM 一脸认真，
;贫乳
IF TALENT:109
	PRINTFORM 用自己薄薄的胸脯摩擦着
;巨乳
ELSEIF TALENT:111
	IF TALENT:140
	PRINTFORM 用自己结实的胸肌包裹着
	ELSE
	PRINTFORM 用自己丰满的胸部包裹着
	ENDIF
;爆乳
ELSEIF TALENT:112
	IF TALENT:140
	PRINTFORM 用自己令人赞叹的发达胸肌包裹摩擦着
	ELSE
	PRINTFORM 用自己令人目眩的豪乳包裹摩擦着
	ENDIF
ELSE
	PRINTFORM 用自己的胸部摩擦着
ENDIF
PRINTFORM %CALLNAME:PLAYER%
IF TALENT:PLAYER:包茎
	PRINT 裹着包皮
	IF TALENT:PLAYER:孩童尺寸
		SIF TALENT:PLAYER:矮小体型 == 0 || !TALENT:PLAYER:扶她
			PRINT ，与体型不符
		PRINT 的小小
	ELSEIF TALENT:PLAYER:短小
		SIF TALENT:PLAYER:高大体型 || !TALENT:PLAYER:扶她
			PRINT ，与体型不符
		PRINT 的短小
	ELSEIF TALENT:PLAYER:巨根
		SIF TALENT:PLAYER:矮小体型
			PRINT ，与体型不符
		PRINT 的粗大
	ELSEIF TALENT:PLAYER:尺寸傲人
		SIF TALENT:PLAYER:矮小体型
			PRINT ，与体型不符，
		PRINT 尺寸傲人的
	ELSE
   		PRINT 的
	ENDIF
ELSE
	IF TALENT:PLAYER:孩童尺寸
		SIF TALENT:PLAYER:矮小体型 == 0 || !TALENT:PLAYER:扶她
			PRINT 与体型不符
		PRINT 的小小
	ELSEIF TALENT:PLAYER:短小
		SIF TALENT:PLAYER:高大体型 || !TALENT:PLAYER:扶她
			PRINT 与体型不符
		PRINT 的短小
	ELSEIF TALENT:PLAYER:巨根
		SIF TALENT:PLAYER:矮小体型
			PRINT 与体型不符
		PRINT 的粗大
	ELSEIF TALENT:PLAYER:尺寸傲人
		SIF TALENT:PLAYER:矮小体型
			PRINT 与体型不符，
		PRINT 尺寸傲人的
	ELSE
   		PRINT 的
	ENDIF
ENDIF
PRINTFORML 阴茎…
;-------------------------------------------------
;83,素股させる
;-------------------------------------------------
@MESSAGE_COM83
PRINTFORM %CALLNAME%
IF PENIS(PLAYER)
	PRINTFORM 双腿夹住%CALLNAME:PLAYER%
	IF TALENT:PLAYER:包茎
    	PRINT 裹着包皮
    	IF TALENT:PLAYER:孩童尺寸
    		SIF TALENT:PLAYER:矮小体型 == 0 || !TALENT:PLAYER:扶她
    			PRINT ，与体型不符
    		PRINT 的小小
    	ELSEIF TALENT:PLAYER:短小
    		SIF TALENT:PLAYER:高大体型 || !TALENT:PLAYER:扶她
    			PRINT ，与体型不符
    		PRINT 的短小
    	ELSEIF TALENT:PLAYER:巨根
    		SIF TALENT:PLAYER:矮小体型
    			PRINT ，与体型不符
    		PRINT 的粗大
    	ELSEIF TALENT:PLAYER:尺寸傲人
    		SIF TALENT:PLAYER:矮小体型
    			PRINT ，与体型不符，
    		PRINT 尺寸傲人的
    	ELSE
       		PRINT 的
    	ENDIF
    ELSE
    	IF TALENT:PLAYER:孩童尺寸
    		SIF TALENT:PLAYER:矮小体型 == 0 || !TALENT:PLAYER:扶她
    			PRINT 与体型不符
    		PRINT 的小小
    	ELSEIF TALENT:PLAYER:短小
    		SIF TALENT:PLAYER:高大体型 || !TALENT:PLAYER:扶她
    			PRINT 与体型不符
    		PRINT 的短小
    	ELSEIF TALENT:PLAYER:巨根
    		SIF TALENT:PLAYER:矮小体型
    			PRINT 与体型不符
    		PRINT 的粗大
    	ELSEIF TALENT:PLAYER:尺寸傲人
    		SIF TALENT:PLAYER:矮小体型
    			PRINT 与体型不符，
    		PRINT 尺寸傲人的
    	ELSE
       		PRINT 的
    	ENDIF
    ENDIF
	PRINTFORM 阴茎压向自己股间，
ENDIF
;淫乱か娼妇＋情欲のPALAMLV5以上
IF (TALENT:淫乱 || TALENT:娼妇) && PALAM:6 >= PALAMLV:5
	PRINTFORM 脸上浮现出沉溺在快乐中的神情，
;恋慕か相亲相爱＋情欲のPALAMLV5以上＋助手ではない
ELSEIF (TALENT:恋慕 || TALENT:相亲相爱) && PALAM:6 >= PALAMLV:6 && !ASSIPLAY
	PRINTFORM 用快要融化似的目光把%CALLNAME:PLAYER%牢牢锁住，
;情欲のPALAMLV6以上
ELSEIF PALAM:6 >= PALAMLV:6
	PRINTFORM 嘴边滴落着口涎，
;恋慕か相亲相爱＋助手ではない
ELSEIF (TALENT:恋慕 || TALENT:相亲相爱) && !ASSIPLAY
	PRINTFORM 深情凝视着%CALLNAME:PLAYER%，
;淫乱か娼妇
ELSEIF (TALENT:淫乱 || TALENT:娼妇)
	PRINTFORM 双眼流露出淫猥的视线挑衅地看了过来，
ELSEIF TALENT:反抗 || TALENT:自大
	PRINTFORM 极不甘心地移开视线，
ELSEIF TALENT:缺乏感情
	PRINTFORM 神色淡漠地
ELSEIF TALENT:不知羞耻
	PRINTFORM 大胆地迎合着，
ELSEIF TALENT:害羞 || TALENT:性保守 || TALENT:胆怯
	PRINTFORM 极为害羞地
ENDIF
IF PENIS(PLAYER)
	;矮小体型
	IF TALENT:100
		PRINTFORM 用纤细的大腿
	ELSE
		PRINTFORM 用私密的场所
	ENDIF
	PRINTFORML 摩擦着阴茎…
ELSE
    PRINT 用自己
	;情欲のPALAMLV3以上
	IF PALAM:6 >= PALAMLV:3
		PRINTFORM 已完全挺立
	    IF TALENT:包茎
			PRINT ，裹着包皮
    		IF TALENT:孩童尺寸
    		    SIF TALENT:矮小体型 == 0
    		        PRINT ，与其体型不符
		    	PRINT 的小小
		    ELSEIF TALENT:短小
		        SIF TALENT:高大体型
		            PRINT ，与其高大体型不符
		        PRINT 的短小
		    ELSEIF TALENT:巨根
		        SIF TALENT:矮小体型
		            PRINT ，与其体型不符
		    	PRINT 的粗大
		    ELSEIF TALENT:尺寸傲人
		        SIF TALENT:矮小体型
		            PRINT ，与其体型不符
		    	PRINT ，尺寸傲人的
		    ELSE
		    	PRINT 的
		    ENDIF
	    ELSE
    		IF TALENT:孩童尺寸
    		    SIF TALENT:矮小体型 == 0
    		        PRINT ，与其体型不符
		    	PRINT 的小小
		    ELSEIF TALENT:短小
		        SIF TALENT:高大体型
		            PRINT ，与其高大体型不符
		        PRINT 的短小
		    ELSEIF TALENT:巨根
		        SIF TALENT:矮小体型
		            PRINT ，与其体型不符
		    	PRINT 的粗大
		    ELSEIF TALENT:尺寸傲人
		        SIF TALENT:矮小体型
		            PRINT ，与其体型不符
		    	PRINT ，尺寸傲人的
		    ELSE
		    	PRINT 的
		    ENDIF
		ENDIF
	ELSE
	    IF TALENT:包茎
		    PRINT 裹着包皮
    	    IF TALENT:孩童尺寸
    		    SIF TALENT:矮小体型 == 0
    		        PRINT ，与其体型不符
		    	PRINT 的小小
		    ELSEIF TALENT:短小
		        SIF TALENT:高大体型
		            PRINT ，与其高大体型不符
		        PRINT 的短小
		    ELSEIF TALENT:巨根
		        SIF TALENT:矮小体型
		            PRINT ，与其体型不符
		    	PRINT 的粗大
		    ELSEIF TALENT:尺寸傲人
		        SIF TALENT:矮小体型
		            PRINT ，与其体型不符
		        PRINT ，尺寸傲人的
		    ELSE
		    	PRINT 的
		    ENDIF
		ELSE
    		IF TALENT:孩童尺寸
    		    SIF TALENT:矮小体型 == 0
    		       PRINT 与其体型不符
		    	PRINT 的小小
		    ELSEIF TALENT:短小
		        SIF TALENT:高大体型
		            PRINT 与其高大体型不符
		        PRINT 的短小
		    ELSEIF TALENT:巨根
		        SIF TALENT:矮小体型
		            PRINT 与其体型不符
		    	PRINT 的粗大
		    ELSEIF TALENT:尺寸傲人
		        SIF TALENT:矮小体型
		            PRINT 与其体型不符，
		    	PRINT 尺寸傲人的
		    ELSE
		        PRINT 的
		    ENDIF
		ENDIF
    ENDIF
	PRINTFORML 阴茎在%CALLNAME:PLAYER%的秘唇上来回摩擦着…
ENDIF
;丝袜系
IF TEQUIP:1 == 5 || TEQUIP:1 == 6
	SIF TEQUIP:1 == 5
		PRINT 丝袜
	SIF TEQUIP:1 == 6
		PRINT 网袜
	IF TFLAG:55
		PRINT 的裂缝撕扯得更大了…
	ELSE
		PRINT 被蹭破了…
	ENDIF
ENDIF
;-------------------------------------------------
;84,泡泡浴
;-------------------------------------------------
@MESSAGE_COM84
PRINTFORML %CALLNAME%把香波涂在自己身上搓出泡沫，
;恋慕か相亲相爱
SIF (TALENT:3 || TALENT:6) && !ASSIPLAY
	PRINTFORM 露出温柔的表情，
;男か
IF TALENT:140 || TALENT:137
	PRINTFORM 用身体擦洗%CALLNAME:PLAYER%挺立的阴茎乃至全身…
ELSE
	;矮小体型
	IF TALENT:100
		PRINTFORM 用小小的身体擦洗%CALLNAME:PLAYER%的全身
	;巨乳か爆乳＋矮小体型でない
	ELSEIF TALENT:111 || TALENT:112
		IF TALENT:140
		PRINTFORM 壮观的胸肌紧贴%CALLNAME:PLAYER%的肌肤，擦洗%CALLNAME:PLAYER%的全身
		ELSE
		PRINTFORM 丰满的胸部紧贴%CALLNAME:PLAYER%的肌肤，擦洗%CALLNAME:PLAYER%的全身
		ENDIF
	ELSE
		PRINTFORM 用身体擦洗%CALLNAME:PLAYER%的全身
	ENDIF
	PRINTFORM …
ENDIF
;-------------------------------------------------
;85,足交
;-------------------------------------------------
@MESSAGE_COM85
PRINTFORM %CALLNAME%把%CALLNAME:PLAYER%
IF TALENT:PLAYER:包茎
	PRINT 裹着包皮
	IF TALENT:PLAYER:孩童尺寸
		SIF TALENT:PLAYER:矮小体型 == 0 || !TALENT:PLAYER:扶她
			PRINT ，与体型不符
		PRINT 的小小
	ELSEIF TALENT:PLAYER:短小
		SIF TALENT:PLAYER:高大体型 || !TALENT:PLAYER:扶她
			PRINT ，与体型不符
		PRINT 的短小
	ELSEIF TALENT:PLAYER:巨根
		SIF TALENT:PLAYER:矮小体型
			PRINT ，与体型不符
		PRINT 的粗大
	ELSEIF TALENT:PLAYER:尺寸傲人
		SIF TALENT:PLAYER:矮小体型
			PRINT ，与体型不符，
		PRINT 尺寸傲人的
	ELSE
   		PRINT 的
	ENDIF
ELSE
	IF TALENT:PLAYER:孩童尺寸
		SIF TALENT:PLAYER:矮小体型 == 0 || !TALENT:PLAYER:扶她
			PRINT 与体型不符
		PRINT 的小小
	ELSEIF TALENT:PLAYER:短小
		SIF TALENT:PLAYER:高大体型 || !TALENT:PLAYER:扶她
			PRINT 与体型不符
		PRINT 的短小
	ELSEIF TALENT:PLAYER:巨根
		SIF TALENT:PLAYER:矮小体型
			PRINT 与体型不符
		PRINT 的粗大
	ELSEIF TALENT:PLAYER:尺寸傲人
		SIF TALENT:PLAYER:矮小体型
			PRINT 与体型不符，
		PRINT 尺寸傲人的
	ELSE
   		PRINT 的
	ENDIF
ENDIF
PRINT 阴茎
;靴を履いている時
IF TEQUIP:9 > 0 || TEQUIP:1 > 0
	IF TEQUIP:9
        PRINT 踩在穿着
		A = 9
		CALL CLOTHES_NAME
		PRINT 的脚下，
	ELSEIF TEQUIP:1
	    PRINT 踩在被
		A = 1
		CALL CLOTHES_NAME
		PRINT 包裹的脚下，
	ELSE
	    PRINT 夹在足心之间
	ENDIF
	;施虐狂
	IF TALENT:83
		PRINT 好像很开心似的碾磨着
	ELSE
		PRINT 缓慢地摩擦玩弄着
	ENDIF
	;施虐狂じゃない＋男性
	SIF TALENT:83 == 0 && (TALENT:137 || TALENT:140)
		PRINT ，脸上露出有点微妙的表情
	PRINT …
ELSE
	PRINT 夹在足心之间
	;施虐狂
	SIF TALENT:83
		PRINTFORM 重重地
	;恋慕か相亲相爱
	SIF (TALENT:3 || TALENT:6) && !ASSIPLAY
		PRINTFORM 细致地
	PRINTFORM 摩擦玩弄着…
ENDIF
;-------------------------------------------------
;86,舔足命令
;-------------------------------------------------
@MESSAGE_COM86
;野外PLAY中
IF TEQUIP:29
	;昼
	IF TIME == 0
		PRINTFORM 在阳光耀眼的晴天下，
	;夜
	ELSE
		PRINTFORM 在月光柔和的深夜，
	ENDIF
	PRINTFORM %CALLNAME%
	;胆怯、高傲、害羞のいずれか
	IF TALENT:10 || TALENT:15 || TALENT:35
		PRINTFORML 一边担忧着这副痴态是否会被人看见，
	ELSE
		PRINTFORML 一边在微风拂过肌肤的刺激下颤抖着身体，
	ENDIF
	PRINT 一边
;媚薬有効
ELSEIF TEQUIP:26
	PRINTFORML %CALLNAME%双目浑浊，视线漂浮在空中，
ELSE
    PRINTFORM %CALLNAME%
ENDIF
PRINTFORM 匍匐在地
SIF ABL:0 < 3
	PRINT 生涩地
PRINTFORM 舔着%CALLNAME:PLAYER%伸出来的脚…
;-------------------------------------------------
;87,縦乳交
;-------------------------------------------------
@MESSAGE_COM87
PRINTFORM %CALLNAME%
;恋慕か相亲相爱
SIF (TALENT:3 || TALENT:6) && !ASSIPLAY
	PRINTFORM 一脸认真，
;巨乳
IF TALENT:111
	IF TALENT:140
	PRINTFORML 用自己结实的胸肌将阴茎包住
	ELSE
	PRINTFORML 用自己丰满的乳肉将阴茎包住
	ENDIF
	PRINTFORM 淫秽地摇晃胸脯
;爆乳
ELSEIF TALENT:112
	PRINTFORML 像是要揉进自己的胸脯一般，
	IF TALENT:140
	PRINTFORM 用发达的胸肌包裹住阴茎淫秽地摇晃着
	ELSE
	PRINTFORM 用丰满的乳肉包裹住阴茎淫秽地摇晃着
	ENDIF
ENDIF
PRINTFORM 施以刺激…
;-------------------------------------------------
;88,乳夹口交
;-------------------------------------------------
@MESSAGE_COM88
PRINTFORM %CALLNAME%
;恋慕か相亲相爱
SIF (TALENT:3 || TALENT:6) && !ASSIPLAY
	PRINTFORM 一脸认真，
;贫乳
IF TALENT:109
	PRINTFORM 用自己薄薄的胸脯和舌头一起努力地侍奉着整个阴茎…
;巨乳
ELSEIF TALENT:111
	IF TALENT:140
	PRINTFORM 用自己结实的胸肌夹住阴茎，舌尖舔弄刺激着露出的龟头…
	ELSE
	PRINTFORM 用自己丰满的乳肉夹住阴茎，舌尖舔弄刺激着露出的龟头…
	ENDIF
;爆乳
ELSEIF TALENT:112
	IF TALENT:140
	PRINTFORM 用自己硕大发达的胸肌将阴茎完全包裹住，舌尖努力地舔弄摩擦间暴露出来的阴茎头部…
	ELSE
	PRINTFORM 用自己硕大丰满的乳肉将阴茎完全包裹住，舌尖努力地舔弄摩擦间暴露出来的阴茎头部…
	ENDIF
ELSE
	PRINTFORM 用自己的胸部夹着阴茎，舌尖舔弄着露出来的部分…
ENDIF
;-------------------------------------------------
;89,饮用母乳
;-------------------------------------------------
@MESSAGE_COM89
PRINTFORM 将%CALLNAME%
;贫乳＋矮小体型
IF TALENT:100 && TALENT:109
	PRINTFORM 仅只是膨胀程度的
;贫乳
ELSEIF TALENT:109
	PRINTFORM 纤薄的
;巨乳
ELSEIF TALENT:111
	IF TALENT:140
	PRINTFORM 胸肌从手中溢出的
	ELSE
	PRINTFORM 乳肉从手中溢出的
	ENDIF
;爆乳
ELSEIF TALENT:112
	PRINTFORM 一只手根本包不住的
ENDIF
;恋慕か相亲相爱＋情欲のPALAMLV3以上
IF (TALENT:3 || TALENT:6) && PALAM:6 >= PALAMLV:3 && !ASSIPLAY
	PRINTFORML 胸部抓在手里挤弄，分别吮吸凸出的尖端，将分泌出的乳汁喝了下去
	PRINTFORM %CALLNAME%发出荡漾的声音，轻轻抱着%CALLNAME:PLAYER%的头压向自己
	;贫乳
	SIF TALENT:109
		PRINTFORM 小小
	;巨乳
	IF TALENT:111
		IF TALENT:140
		PRINTFORM 结实
		ELSE
		PRINTFORM 丰满
		ENDIF
	ENDIF
	;爆乳
	SIF TALENT:112
		PRINTFORM 硕大
	PRINTFORM 的胸部…
	TFLAG:52 = 1
;淫乱か娼妇＋情欲のPALAMLV3以上
ELSEIF (TALENT:4 || TALENT:7) && PALAM:6 >= PALAMLV:3
	PRINTFORML 胸部抓在手里挤弄，分别吮吸凸出的尖端，将分泌出的乳汁喝了下去
	PRINTFORM %CALLNAME%发出荡漾的声音，急切地抱着%CALLNAME:PLAYER%的头压向自己
	;贫乳
	SIF TALENT:109
		PRINTFORM 小小
	;巨乳
	IF TALENT:111
		IF TALENT:140
		PRINTFORM 结实
		ELSE
		PRINTFORM 丰满
		ENDIF
	ENDIF
	;爆乳
	SIF TALENT:112
		PRINTFORM 硕大
	PRINTFORM 的胸部…
	TFLAG:52 = 1
;恋慕か相亲相爱
ELSEIF (TALENT:3 || TALENT:6) && !ASSIPLAY
	PRINTFORML 胸部抓在手里挤弄，分别吮吸凸出的尖端，将分泌出的乳汁喝了下去
	PRINTFORM %CALLNAME%微微笑着，轻轻抱着%CALLNAME:PLAYER%的头压向自己
	;贫乳
	SIF TALENT:109
		PRINTFORM 小小
	;巨乳
	IF TALENT:111
		IF TALENT:140
		PRINTFORM 结实
		ELSE
		PRINTFORM 丰满
		ENDIF
	ENDIF
	;爆乳
	SIF TALENT:112
		PRINTFORM 硕大
	PRINTFORM 的胸部…
	TFLAG:52 = 1
ELSE
	PRINTFORM 胸部抓在手里挤弄，分别吮吸凸出的尖端，将分泌出的乳汁喝了下去
ENDIF
;-------------------------------------------------
;90,髪扱き
;-------------------------------------------------
@MESSAGE_COM90
IF TALENT:长发
	PRINTFORML %CALLNAME:PLAYER%拉近%CALLNAME%，将%CALLNAME%的头发缠绕在阴茎上
ELSE
	PRINTFORML %CALLNAME:PLAYER%拉近%CALLNAME%，用%CALLNAME%的头发摩擦著阴茎
ENDIF
PRINTFORM %CALLNAME%见此行为
;淫乱か娼妇で精液成瘾3以上
IF (TALENT:4 || TALENT:7) && ABL:16 >= 3
	PRINTFORM 迫不及待地微张着口喘息着，就那样看着%CALLNAME:PLAYER%用自己的头发自慰…
;服从か隶属
ELSEIF (TALENT:5 || TALENT:8)
	PRINTFORM 顺服地充当%CALLNAME:PLAYER%的飞机杯…
;恋慕相亲相爱无で精液成瘾0、もしくは侍奉精神1以下
ELSEIF TALENT:3 == 0 && TALENT:6 == 0 && (ABL:16 == 0 || ABL:7 <= 1)
	PRINTFORM 移开视线装作没看见…
;精液成瘾4以上
ELSEIF ABL:16 >= 4
	PRINTFORM 像是期待着射精的那刻一般看着%CALLNAME:PLAYER%用自己的头发自慰…
;それ以外
ELSE
	PRINTFORM 微微仰头看着%CALLNAME:PLAYER%用自己的头发自慰…
ENDIF
;-------------------------------------------------
;91,指チュパ
;-------------------------------------------------
@MESSAGE_COM91
PRINTFORM %CALLNAME%
;恋慕か相亲相爱
SIF (TALENT:3 || TALENT:6) && !ASSIPLAY
	PRINTFORM 脸上浮现出温柔的表情，
;淫乱か娼妇
SIF (TALENT:4 || TALENT:7)
	PRINTFORM 主动
;顺从LV3未満
SIF ABL:0 < 3
	PRINT 生涩地
PRINTFORM 含着%CALLNAME:PLAYER%的手指吮吸舔舐…
;-------------------------------------------------
;92,腋扱き
;-------------------------------------------------
@MESSAGE_COM92
PRINTFORM %CALLNAME:PLAYER%把阴茎插进%CALLNAME%腋下，命其夹紧后开始活动…
;-------------------------------------------------
;93,乳を飲ませる
;-------------------------------------------------
@MESSAGE_COM93
IF ASSIPLAY
	PRINTFORM %CALLNAME:ASSI%
ELSE
    PRINTFORM %CALLNAME:PLAYER%
ENDIF
SIF TALENT:幼稚 || TALENT:幼儿／幼儿退行
	PRINT 像哺乳婴儿似的
PRINTFORM 抱着%CALLNAME%的头压向自己
SIF TALENT:贫乳
	PRINT 薄薄的
IF TALENT:巨乳
	IF TALENT:140
		PRINT 结实的
	ELSE
		PRINT 丰满的
	ENDIF
ENDIF
SIF TALENT:爆乳
	PRINT 硕大的
PRINT 胸部给%CALLNAME%喂奶…

PRINTFORM %CALLNAME%
;淫乱か娼妇＋情欲のPALAMLV5以上
IF (TALENT:淫乱 || TALENT:娼妇) && PALAM:6 >= PALAMLV:5
	PRINTL 满脸兴奋，故意吮吸得滋滋有声
;恋慕か相亲相爱＋情欲のPALAMLV5以上＋助手ではない
ELSEIF (TALENT:恋慕 || TALENT:相亲相爱) && PALAM:6 >= PALAMLV:5 && !ASSIPLAY
	PRINTL 通红着脸，口涎从嘴边滑落
;情欲のPALAMLV4以上
ELSEIF PALAM:6 >= PALAMLV:4
	PRINTL 的口涎从嘴边滑落
ELSEIF KANRAKU(TARGET)
	IF (TALENT:淫乱 || TALENT:娼妇) && (ABL:技巧 > 3 || TALENT:善用舌头)
		PRINT 灵活地转动舌头，
	ELSEIF (TALENT:恋慕 || TALENT:相亲相爱) && !RAND:2
		PRINT 满怀爱意地
	ELSEIF (TALENT:服从 || TALENT:隶属) && !RAND:2
		PRINT 一脸敬畏虔诚的表情，
	ELSEIF TALENT:缺乏感情 && !RAND:2
		PRINT 神情淡漠，
	ELSEIF TALENT:害羞 || TALENT:性保守
		PRINT 羞得耳根通红，
	ELSEIF CFLAG:600 == 6 && !TALENT:隶属
		PRINT 虽然对这个姿势有些抵触，还是
	ELSE
		PRINT 虽然因这个姿势有些害羞，还是
	ENDIF
ELSEIF !TALENT:幼稚 && !TALENT:幼儿／幼儿退行
	IF !KANRAKU(TARGET) && (TALENT:反抗 || TALENT:自大)
		PRINT 带着后悔
	ELSEIF TALENT:刚强
		PRINT 屈辱地咬紧嘴唇，
	ELSEIF TALENT:无动于衷 || TALENT:缺乏感情
		PRINT 面无表情地
	ELSEIF TALENT:胆怯 || TALENT:老实
		PRINT 颤抖着身体，
	ELSEIF TALENT:害羞 || TALENT:性保守
		PRINTL 羞得耳根通红，
	ELSE
		PRINT 动作生涩地
	ENDIF
ENDIF
IF TALENT:幼稚 || TALENT:幼儿／幼儿退行
	PRINT 努力吮吸着奶头…
ELSEIF KANRAKU(TARGET)
	;情欲のPALAMLV4以上
	IF PALAM:6 >= PALAMLV:4 && !RAND:2
		PRINT 咬住尖端，努力吮吸着乳汁…
	ELSEIF PALAM:6 >= PALAMLV:4
		PRINT 用力吸住尖端，全神贯注地汲取乳汁…
	ELSEIF ABL:欲望 > ABL:顺从
		PRINT 咬住尖端，贪婪地吮吸着乳汁…
	ELSE
		PRINT 吮吸着乳汁…
	ENDIF
ELSE
	IF ABL:顺从 > 3
		PRINT 顺从地饮用母乳…
	ELSE
		PRINT 舔得津津有味…
	ENDIF
ENDIF

;情欲のPALAMLV4以上､相亲相爱か、淫乱か、娼夫の場合
IF PALAM:6 >= PALAMLV:4 && (TALENT:4 || TALENT:6 || TALENT:7) && SOUCHAKU(1)
	IF TALENT:137 || TALENT:139 || TALENT:140
		PRINTL
		PRINTFORM %CALLNAME%被哺乳的同时，下身
	    IF TALENT:包茎
		    PRINT 裹着包皮
    	    IF TALENT:孩童尺寸
    		    SIF TALENT:矮小体型 == 0
    		        PRINT ，与其体型不符
		    	PRINT 的小小
		    ELSEIF TALENT:短小
		        SIF TALENT:高大体型
		            PRINT ，与其高大体型不符
		        PRINT 的短小
		    ELSEIF TALENT:巨根
		        SIF TALENT:矮小体型
		            PRINT ，与其体型不符
		    	PRINT 的粗大
		    ELSEIF TALENT:尺寸傲人
		        SIF TALENT:矮小体型
		            PRINT ，与其体型不符
		        PRINT ，尺寸傲人的
		    ELSE
		    	PRINT 的
		    ENDIF
		ELSE
    		IF TALENT:孩童尺寸
    		    SIF TALENT:矮小体型 == 0
    		       PRINT 与其体型不符
		    	PRINT 的小小
		    ELSEIF TALENT:短小
		        SIF TALENT:高大体型
		            PRINT 与其高大体型不符
		        PRINT 的短小
		    ELSEIF TALENT:巨根
		        SIF TALENT:矮小体型
		            PRINT 与其体型不符
		    	PRINT 的粗大
		    ELSEIF TALENT:尺寸傲人
		        SIF TALENT:矮小体型
		            PRINT 与其体型不符，
		    	PRINT 尺寸傲人的
		    ELSE
		        PRINT 的
		    ENDIF
		ENDIF
		PRINT 阴茎硬邦邦地勃起了…
	ENDIF
ENDIF
TFLAG:52 = 1
;-------------------------------------------------
;94,尾交
;-------------------------------------------------
@MESSAGE_COM94
PRINTFORM %CALLNAME:PLAYER%用%CALLNAME%的尾巴包裹住阴茎，来回摩擦起来…
;-------------------------------------------------
;95,耳交
;-------------------------------------------------
@MESSAGE_COM95
PRINTFORM %CALLNAME:PLAYER%在%CALLNAME%的兽耳上摩擦着阴茎，摆动起腰部…
;-------------------------------------------------
;100,打屁股
;-------------------------------------------------
@MESSAGE_COM100
IF ASSIPLAY
	PRINTFORM %CALLNAME:ASSI%
ELSE
    PRINTFORM %CALLNAME:PLAYER%
ENDIF
PRINTFORML 用手掌拍打着%CALLNAME%的屁股，一次又一次地发出“啪啪”的声响
PRINTFORM %CALLNAME%
;受虐属性LV2以上
IF ABL:9 >= 2
	PRINTFORM 的胯下被自己的
	SIF TALENT:140 || TALENT:139 || TALENT:137
		PRINT 腺液
	SIF TALENT:139 || TALENT:137
		PRINT 和
	SIF TALENT:140 == 0
		PRINT 爱液
	PRINTL 濡湿了…
ELSE
	PRINTFORM 的屁股肿得通红…
ENDIF
;-------------------------------------------------
;101,打胸部
;-------------------------------------------------
@MESSAGE_COM101
IF ASSIPLAY
	PRINTFORM %CALLNAME:ASSI%
ELSE
    PRINTFORM %CALLNAME:PLAYER%
ENDIF
;贫乳か绝壁
IF (TALENT:109 || !BREAST(TARGET))
	;贫乳
	IF TALENT:109
		PRINTFORM 拍打着%CALLNAME%薄薄的胸脯
	;胸无
	ELSEIF !BREAST(TARGET)
		PRINTFORM 拍打着%CALLNAME%几乎没有膨胀的平坦胸脯
	ENDIF
	PRINTFORM ，抓弹凌虐%CALLNAME%的乳头和乳肉…
ELSE
	;巨乳
	IF TALENT:111
		IF TALENT:140
			PRINTFORM 拉拽摇晃着%CALLNAME%结实的胸肌…
		ELSE
			PRINTFORM 拉拽摇晃着%CALLNAME%丰满的乳房
		ENDIF
	;爆乳
	ELSEIF TALENT:112
		PRINTFORM 拍打玩弄着%CALLNAME%硕大饱满的奶子，让其蹦来跳去
	ELSE
		PRINTFORM 拍打蹂躏着%CALLNAME%的胸部
	ENDIF
	PRINTFORM …
ENDIF
;-------------------------------------------------
;102,鞭子
;-------------------------------------------------
@MESSAGE_COM102
IF ASSIPLAY
	PRINTFORM %CALLNAME:ASSI%
ELSE
    PRINTFORM %CALLNAME:PLAYER%
ENDIF
PRINTFORML 高扬起手臂，将鞭子狠狠地挥下去
PRINTFORM %CALLNAME%的肌肤上印现出鲜明的红痕…
;-------------------------------------------------
;103,蜡烛
;-------------------------------------------------
@MESSAGE_COM103
IF ASSIPLAY
	PRINTFORM %CALLNAME:ASSI%
ELSE
    PRINTFORM %CALLNAME:PLAYER%
ENDIF
PRINTFORM 点燃了蜡烛，让烛液垂落在
;绳子などで紧缚中
SIF TEQUIP:19
	PRINTFORM 被绳子束缚，无法动弹的
PRINTFORM %CALLNAME%
;调教者が施虐狂
IF TALENT:PLAYER:83
	PRINTFORM 身上敏感的部位
ELSE
	;(eraElona用素質[アルビノ]の代わり)、白肤
	IF TALENT:白肤
		PRINTFORM 如同珍珠一般洁白艳丽的肌肤上
	ELSEIF TALENT:高大体型 && TALENT:喜欢运动 && MALE(TARGET)
		PRINTFORM 高大健壮的身体上
	ELSE
		PRINTFORM 的身体上
	ENDIF
ENDIF
PRINTFORM …
;-------------------------------------------------
;104,针
;-------------------------------------------------
@MESSAGE_COM104
IF ASSIPLAY
	PRINTFORM %CALLNAME:ASSI%
ELSE
    PRINTFORM %CALLNAME:PLAYER%
ENDIF
;调教者が擅长用针か施虐狂
IF TALENT:PLAYER:53 || TALENT:PLAYER:83
	PRINTFORM 毫不留情地将针扎在
ELSE
	PRINTFORM 将针扎在
ENDIF
PRINTFORM %CALLNAME%的乳头和性器上…
;-------------------------------------------------
;105,眼罩
;-------------------------------------------------
@MESSAGE_COM105
;解除時
IF TEQUIP:18
	PRINTFORM 摘掉了%CALLNAME%脸上的眼罩
ELSE
	PRINTFORM 给%CALLNAME%戴上了眼罩
ENDIF
TFLAG:51 = 1
TFLAG:52 = 1
;-------------------------------------------------
;106,绳子
;-------------------------------------------------
@MESSAGE_COM106
;解除時
IF TEQUIP:19
	PRINTFORM 解开了%CALLNAME%身上的绳子
ELSE
	IF ABL:MASTER:技巧 >= 4
		;受虐属性3以上で、紧缚经验が50以上で、反抗刻印がないとき
		IF ABL:受虐属性 > 2 && EXP:紧缚经验 >= 50 && MARK:反抗刻印 < 1
			PRINTFORML 将%CALLNAME%捆成了龟甲缚的模样
			PRINTFORML 身体被绳子牢牢勒紧的状态似乎让%CALLNAME%产生了快感…
		;反抗か抵抗を持ち陷落してない場合
		ELSEIF (TALENT:反抗 || TALENT:抵抗) && !KANRAKU(TARGET)
			PRINTFORML 将激烈反抗，一脸嫌恶的%CALLNAME:TARGET%捆成了龟甲缚的模样…
		;胆怯で、刚强と缺乏感情を持たず、陷落していない場合
		ELSEIF  TALENT:胆怯 && !TALENT:刚强 && !TALENT:缺乏感情 && !KANRAKU(TARGET)
			PRINTFORML 将恐惧害怕的%CALLNAME:TARGET%捆成了龟甲缚的模样…
		;高傲、屈服刻印が1以下で、陷落していない場合
		ELSEIF TALENT:高傲 && MARK:屈服刻印 < 2 && !KANRAKU(TARGET)
			PRINTFORML 将%CALLNAME:TARGET%捆成了龟甲缚的模样。
			PRINTFORML 即便被拘束起来，%CALLNAME:TARGET%极高的自尊心也没有受到任何动摇…
		;怕痛がなく、反抗刻印がない、性保守でない、乐观か性好奇持ち、陷落済み、
		ELSEIF !TALENT:怕痛 && !MARK:反抗刻印 && !TALENT:性保守 && (TALENT:乐观 || TALENT:性好奇) && KANRAKU(TARGET)
			PRINTFORML 将不知为何一脸开心的%CALLNAME:TARGET%捆成了龟甲缚的模样…
		ELSE
			PRINTFORML 将%CALLNAME%捆成了龟甲缚的模样…
		ENDIF
	ELSE
		PRINTFORM 用绳子把%CALLNAME%捆了起来
	ENDIF
ENDIF
TFLAG:51 = 1
TFLAG:52 = 1
;-------------------------------------------------
;107,口塞球
;-------------------------------------------------
@MESSAGE_COM107
;解除時
IF TEQUIP:20
	PRINTFORM 卸除了%CALLNAME%口中的口塞球
ELSE
	PRINTFORM 给%CALLNAME%装上了口塞球
ENDIF
TFLAG:51 = 1
TFLAG:52 = 1
;-------------------------------------------------
;108,拷問 09/09/18
;-------------------------------------------------
@MESSAGE_COM108
PRINTFORM %CALLNAME:PLAYER%对%CALLNAME%进行了拷问
;-------------------------------------------------
;109,射精止め
;-------------------------------------------------
@MESSAGE_COM109
IF TEQUIP:37
	;解除時
	PRINTFORM 解开了%CALLNAME%阴茎根部的蝴蝶结…
ELSE
	PRINTFORML 在%CALLNAME%的阴茎根部紧紧系上了带有褶边的可爱蝴蝶结
	PRINT 血液的流通受到阻碍，龟头呈现出黑红色肿胀的模样…
ENDIF
TFLAG:51 = 1
TFLAG:52 = 1
;-------------------------------------------------
;110,家具にする
;-------------------------------------------------
@MESSAGE_COM110
IF ASSIPLAY
	PRINTFORM %CALLNAME:ASSI%
ELSE
    PRINTFORM %CALLNAME:PLAYER%
ENDIF
PRINTFORM 让%CALLNAME%四肢着地趴下
;调教者が矮小体型､奴隶が矮小でない。かつ奴隶に受虐属性无､顺从LV2以上
IF TALENT:PLAYER:矮小体型 && !TALENT:矮小体型 && ABL:9 == 0 && ABL:顺从 >= 2
	PRINTFORML ，然后坐在了%CALLNAME%的背上
	PRINTFORMW 但不知道是哪里弄错了什么，%CALLNAME%开始载着%CALLNAME:PLAYER%爬来爬去…
	PRINTFORML ・
	PRINTFORML ・
	PRINTFORMW ・
	PRINTFORML 玩了骑马马游戏。真开心啊。
	TFLAG:52 = 1
	TFLAG:51 = 1
ELSE
;奴隶が小人体型で调教者が小人体型でないときは足置きに
	IF TALENT:142 && TALENT:PLAYER:142 == 0
		PRINTFORML ，然后将其当作足踏把脚放了上去
	ELSE
		PRINTFORML ，然后将其当作椅子坐了上去
	ENDIF
	PRINTFORM 过了一会儿，
	PRINTFORM %CALLNAME%
;受虐属性３以上、５以上
	IF ABL:9 >= 5
		PRINTFORM 兴奋地流着口水，
		SIF TALENT:140 || TALENT:139 || TALENT:137
			PRINT 腺液
		SIF TALENT:139 || TALENT:137
			PRINT 和
		SIF TALENT:140 == 0
			PRINT 爱液
		PRINTL 也从两腿间滑落下来
	ELSEIF ABL:9 >= 3
		PRINTFORM 满脸潮红地喘着气，
	ENDIF 
;奴隶の顺从5以上
	IF ABL:0 >= 4
		PRINTFORM 几乎只有些微的颤动，尽职地充当着一样出色的家具…
	ELSE
		PRINTFORM 因背上的重量呻吟着…
	ENDIF
	TFLAG:52 = 1
ENDIF
;-------------------------------------------------
;111,剃毛
;-------------------------------------------------
@MESSAGE_COM111
PRINTFORML 把%CALLNAME%的阴毛一根不留剃得干干净净
PRINTFORM %CALLNAME%
IF ABL:8 < 2
	PRINT 满面通红，
	IF TEQUIP:19
		PRINTFORM 在与%CALLNAME:PLAYER%对上视线后不自然地别开了脸…
	ELSE
		PRINT 想要用手遮盖变得光秃秃的性器…
	ENDIF
ELSEIF ABL:8 == 2
	PRINT 害羞地凝视着自己暴露出来的性器…
ELSEIF ABL:8 == 3
	PRINT 微微低下了头…
ELSEIF ABL:8 == 4
	PRINT 虽然因性器全部暴露出来有些羞耻，但似乎也有些享受…
ELSE
	PRINT 大方地享受着空气抚过性器的开放感…
ENDIF
TFLAG:52 = 1
TFLAG:51 = 1
;-------------------------------------------------
;112,首絞め
;-------------------------------------------------
@MESSAGE_COM112
IF ASSIPLAY
	PRINTFORM %CALLNAME:ASSI%
ELSE
    PRINTFORM %CALLNAME:PLAYER%
ENDIF
PRINTFORM 掐紧了%CALLNAME%
;小人体型
IF TALENT:142
	PRINTFORM 柔软纤细
;矮小体型
ELSEIF TALENT:100
	PRINTFORM 树枝般易折断
;高大体型
ELSEIF TALENT:113
	PRINTFORM 结实健壮
ENDIF
PRINTFORM 的脖子…
;-------------------------------------------------
;113,顔騎圧迫
;-------------------------------------------------
@MESSAGE_COM113
IF ASSIPLAY
	PRINTFORM %CALLNAME:ASSI%
ELSE
    PRINTFORM %CALLNAME:PLAYER%
ENDIF
PRINTFORML 跨到%CALLNAME%脑袋上方，将私处强行压在%CALLNAME%的脸上…
PRINTFORM %CALLNAME%
;受虐属性＋侍奉精神
IF ABL:9 + ABL:7 < 3
	PRINTFORM 脸上浮现出苦闷和屈辱，挣扎着发出呻吟…
ELSEIF ABL:9 + ABL:7 < 5
	PRINTFORM 呼吸不畅，痛苦地忍受着压迫感…
ELSEIF ABL:9 + ABL:7 < 7
	PRINTFORM 在呼吸不畅脸色涨红的同时啪嗒啪嗒地舔着%CALLNAME:PLAYER%的秘部…
ELSEIF ABL:9 + ABL:7 < 9
	PRINTFORM 在呼吸不畅脸色涨红的同时努力地用舌头和嘴唇侍奉%CALLNAME:PLAYER%的秘部…
ELSEIF ABL:9 + ABL:7 < 11
	PRINTFORM 面色苦闷，脸颊通红地舔着%CALLNAME:PLAYER%的秘部…
ELSE
	PRINTFORML 因被作为道具对待，兴奋地流下了口水和腺液
	PRINTFORM 像狗一样摆动舌头，啪嗒啪嗒地舔着%CALLNAME:PLAYER%的秘部…
ENDIF
;情欲のPALAMLV4以上､相亲相爱か、淫乱か、娼夫の場合
IF PALAM:6 >= PALAMLV:4 && (TALENT:4 || TALENT:6 || TALENT:7) && SOUCHAKU(1)
	IF TALENT:137 || TALENT:139 || TALENT:140
		PRINTL
		PRINTFORM 在这样的状态下，%CALLNAME%
	    IF TALENT:包茎
		    PRINT 裹着包皮
    	    IF TALENT:孩童尺寸
    		    SIF TALENT:矮小体型 == 0
    		        PRINT ，与其体型不符
		    	PRINT 的小小
		    ELSEIF TALENT:短小
		        SIF TALENT:高大体型
		            PRINT ，与其高大体型不符
		        PRINT 的短小
		    ELSEIF TALENT:巨根
		        SIF TALENT:矮小体型
		            PRINT ，与其体型不符
		    	PRINT 的粗大
		    ELSEIF TALENT:尺寸傲人
		        SIF TALENT:矮小体型
		            PRINT ，与其体型不符
		        PRINT ，尺寸傲人的
		    ELSE
		    	PRINT 的
		    ENDIF
		ELSE
    		IF TALENT:孩童尺寸
    		    SIF TALENT:矮小体型 == 0
    		       PRINT 与其体型不符
		    	PRINT 的小小
		    ELSEIF TALENT:短小
		        SIF TALENT:高大体型
		            PRINT 与其高大体型不符
		        PRINT 的短小
		    ELSEIF TALENT:巨根
		        SIF TALENT:矮小体型
		            PRINT 与其体型不符
		    	PRINT 的粗大
		    ELSEIF TALENT:尺寸傲人
		        SIF TALENT:矮小体型
		            PRINT 与其体型不符，
		    	PRINT 尺寸傲人的
		    ELSE
		        PRINT 的
		    ENDIF
		ENDIF
		PRINT 阴茎
		PRINTFORM 硬挺地勃起了…
	ENDIF
ENDIF
TFLAG:52 = 1
;-------------------------------------------------
;114,金蹴り
;-------------------------------------------------
@MESSAGE_COM114
IF ASSIPLAY
	PRINTFORM %CALLNAME:ASSI%
ELSE
    PRINTFORM %CALLNAME:PLAYER%
ENDIF
PRINTFORML 瞄准%CALLNAME%的胯下，毫不留情地踹了上去
PRINTFORM %CALLNAME%
;受虐属性
IF ABL:受虐属性 < 4
	PRINTFORM 承受了无法想象的剧痛，在地上哀嚎着拼命打滚…
ELSEIF ABL:受虐属性 < 6
	PRINTFORM 冷汗直流，当场蹲下，捂住胯部痛苦不已…
ELSEIF ABL:受虐属性 < 9
	PRINTFORM 因过于剧痛，眼角含泪…
ELSE
	PRINTFORM 因过于剧痛，眼角含泪，但下身好像有所反应…
ENDIF
TFLAG:51 = 1
TFLAG:52 = 1
;-------------------------------------------------
;115,電気アンマ
;-------------------------------------------------
@MESSAGE_COM115
IF ASSIPLAY
	PRINTFORM %CALLNAME:ASSI%
ELSE
    PRINTFORM %CALLNAME:PLAYER%
ENDIF
PRINTFORML 抱起%CALLNAME%的两条腿，一只脚踩上%CALLNAME%的胯部，来回擦弄着阴茎和睾丸
IF PALAM:快Ｃ < 500
	PRINTL
	IF ABL:受虐属性 == 0
		PRINTFORM %CALLNAME%投来了怨恨的眼神
	ELSEIF ABL:受虐属性 <= 5
		PRINTFORM %CALLNAME%连连喊痛
	ELSE
		PRINTFORM %CALLNAME%翻着白眼，又痛又爽
	ENDIF
	TFLAG:52 = 1
ELSEIF PALAM:快Ｃ < 3000
	PRINTL
	IF ABL:受虐属性 == 0
		PRINTFORM %CALLNAME%痛得动弹不得
	ELSEIF ABL:受虐属性 <= 3
		PRINTFORM %CALLNAME%虽然很痛，却不由自主勃起了
	ELSE
		PRINTFORM %CALLNAME%虽然很痛，却不由自主挺起腰追逐更多
	ENDIF
	TFLAG:52 = 1
ELSEIF PALAM:快Ｃ < 6000
	PRINTL
	IF ABL:受虐属性 <= 3
			PRINTFORM %CALLNAME%痛呼出声，下身却勃起了
	ELSEIF ABL:受虐属性 <= 6
			PRINTFORM %CALLNAME%眼含泪水，一副快乐享受的模样
	ELSE
			PRINTFORM %CALLNAME%虽然很痛，又好像很开心
	ENDIF
	TFLAG:52 = 1
ENDIF
;-------------------------------------------------
;120,強制舔阴
;-------------------------------------------------
@MESSAGE_COM120
IF ASSIPLAY
	PRINTFORM %CALLNAME:ASSI%
ELSE
    PRINTFORM %CALLNAME:PLAYER%
ENDIF
PRINTFORML 强硬地命令%CALLNAME%舔弄自己的私处…
PRINTFORM %CALLNAME%
IF ABL:7 < 5
	;技巧が1以下で「善用舌头」が無い
	IF ABL:2 < 2 && TALENT:52 == 0
		PRINTFORM 笨拙地转动舌头，
	;技巧が4以上か「善用舌头」
	ELSEIF  ABL:2 > 3 || TALENT:52
		PRINTFORM 灵活地转动舌头，
	ENDIF
ENDIF
;侍奉精神を見る
IF ABL:7 < 2
	PRINTFORM 舔得滋溜有声…
ELSEIF ABL:7 < 3
	PRINTFORM 顺从地舔舐着…
ELSEIF ABL:7 < 4
	PRINTFORM 仔细舔过每一寸穴肉…
ELSEIF KANRAKU(TARGET) == 1
	PRINTFORM 来回舔舐，面红耳赤地咬住了阴核…
ELSE
	IF TALENT:140
		PRINTFORM 气息有一丝紊乱…
		PRINTFORM 像狗一样啪嗒啪嗒地舔得津津有味…
	ELSE
		PRINTFORM 作为一名合格的性奴隶，极为积极地侍奉着…
	ENDIF
ENDIF
;情欲のPALAMLV4以上､相亲相爱か、淫乱か、娼夫の場合
IF PALAM:6 >= PALAMLV:4 && (TALENT:4 || TALENT:6 || TALENT:7) && SOUCHAKU(1)
	IF TALENT:137 || TALENT:139 || TALENT:140
		PRINTL
		PRINTFORM 与此同时，%CALLNAME%
	    IF TALENT:包茎
		    PRINT 裹着包皮
    	    IF TALENT:孩童尺寸
    		    SIF TALENT:矮小体型 == 0
    		        PRINT ，与其体型不符
		    	PRINT 的小小
		    ELSEIF TALENT:短小
		        SIF TALENT:高大体型
		            PRINT ，与其高大体型不符
		        PRINT 的短小
		    ELSEIF TALENT:巨根
		        SIF TALENT:矮小体型
		            PRINT ，与其体型不符
		    	PRINT 的粗大
		    ELSEIF TALENT:尺寸傲人
		        SIF TALENT:矮小体型
		            PRINT ，与其体型不符
		        PRINT ，尺寸傲人的
		    ELSE
		    	PRINT 的
		    ENDIF
		ELSE
    		IF TALENT:孩童尺寸
    		    SIF TALENT:矮小体型 == 0
    		       PRINT 与其体型不符
		    	PRINT 的小小
		    ELSEIF TALENT:短小
		        SIF TALENT:高大体型
		            PRINT 与其高大体型不符
		        PRINT 的短小
		    ELSEIF TALENT:巨根
		        SIF TALENT:矮小体型
		            PRINT 与其体型不符
		    	PRINT 的粗大
		    ELSEIF TALENT:尺寸傲人
		        SIF TALENT:矮小体型
		            PRINT 与其体型不符，
		    	PRINT 尺寸傲人的
		    ELSE
		        PRINT 的
		    ENDIF
		ENDIF
		PRINT 阴茎
		PRINTFORM 硬挺地抬起头，完全勃起了…
	ENDIF
ENDIF
TFLAG:52 = 1
;-------------------------------------------------
;121,磨镜
;-------------------------------------------------
@MESSAGE_COM121
PRINTFORM %CALLNAME:PLAYER%和%CALLNAME%相互摩擦彼此的私处…
;-------------------------------------------------
;122,使之侵犯助手
;-------------------------------------------------
@MESSAGE_COM122
PRINTFORM 让%CALLNAME%侵犯%CALLNAME:ASSI%…
;-------------------------------------------------
;123,足交
;-------------------------------------------------
@MESSAGE_COM123
PRINTFORM %CALLNAME:PLAYER%
;调教者の技巧がLV3以上
SIF ABL:PLAYER:技巧 >= 3
	PRINTFORM 有技巧地
;调教者が施虐狂
IF TALENT:PLAYER:83
	PRINTFORM 践踏着
ELSE
    PRINTFORM 踩弄着
ENDIF
PRINTFORM %CALLNAME%
IF TALENT:包茎
	PRINT 裹着包皮
    IF TALENT:孩童尺寸
    	SIF TALENT:矮小体型 == 0
    		PRINT ，与其体型不符
		PRINT 的小小
	ELSEIF TALENT:短小
		SIF TALENT:高大体型
		    PRINT ，与其高大体型不符
		PRINT 的短小
	ELSEIF TALENT:巨根
		SIF TALENT:矮小体型
		    PRINT ，与其体型不符
		PRINT 的粗大
	ELSEIF TALENT:尺寸傲人
		SIF TALENT:矮小体型
		    PRINT ，与其体型不符
		PRINT ，尺寸傲人的
	ELSE
		PRINT 的
	ENDIF
ELSE
    IF TALENT:孩童尺寸
        SIF TALENT:矮小体型 == 0
    	    PRINT 与其体型不符
		PRINT 的小小
	ELSEIF TALENT:短小
		SIF TALENT:高大体型
		    PRINT 与其高大体型不符
		PRINT 的短小
	ELSEIF TALENT:巨根
		SIF TALENT:矮小体型
		    PRINT 与其体型不符
		PRINT 的粗大
	ELSEIF TALENT:尺寸傲人
		SIF TALENT:矮小体型
		    PRINT 与其体型不符，
		PRINT 尺寸傲人的
	ELSE
		PRINT 的
	ENDIF
ENDIF
PRINTL 阴茎
IF PALAM:快Ｃ < 100 && !TEQUIP:60 
	PRINTL
	IF ABL:受虐属性 == 0
		PRINTFORM %CALLNAME%投来怨恨的目光
	ELSEIF ABL:受虐属性 <= 2
		PRINTFORM %CALLNAME%连连喊痛
	ELSE
		PRINTFORM %CALLNAME%痛呼出声，却也有些沉醉
	ENDIF
	TFLAG:52 = 1
ELSEIF PALAM:快Ｃ < 500
	PRINTL
	IF ABL:受虐属性 == 0
		PRINTFORM %CALLNAME%痛得无法动弹
	ELSEIF ABL:受虐属性 <= 2
		PRINTFORM %CALLNAME%虽然很痛，下面却勃起了
	ELSE
		PRINTFORM %CALLNAME%虽然很痛，却仍挺着腰追求更多
	ENDIF
	TFLAG:52 = 1
ELSEIF PALAM:快Ｃ < 1500
	PRINTL
	IF ABL:受虐属性 <= 2
			PRINTFORM %CALLNAME%痛呼出声，下身却勃起了
	ELSEIF ABL:受虐属性 <= 4
			PRINTFORM %CALLNAME%眼含泪水，一副快乐享受的模样
	ELSE
			PRINTFORM %CALLNAME%虽然很痛，又好像很开心
	ENDIF
	TFLAG:52 = 1
ENDIF
;-------------------------------------------------
;124,Ｗ口交
;-------------------------------------------------
@MESSAGE_COM124
PRINTFORM %CALLNAME%和%CALLNAME:ASSI%
;奴隶と助手が恋慕か相亲相爱
IF (TALENT:3 || TALENT:6) && (TALENT:ASSI:3 || TALENT:ASSI:6)
	PRINTFORM 眼神陶醉地仰视着%CALLNAME:PLAYER%，
;奴隶と助手が淫乱ルート
ELSEIF (TALENT:4 || TALENT:7) && (TALENT:ASSI:4 || TALENT:ASSI:7)
	PRINTFORM 眼神饥渴地仰视着%CALLNAME:PLAYER%，
ENDIF
PRINTFORM 一同用唇舌侍奉着%CALLNAME:PLAYER%
IF TALENT:PLAYER:孩童尺寸
	PRINT 小小的
ELSEIF TALENT:PLAYER:短小
	PRINT 短小的
ELSEIF TALENT:PLAYER:巨根
	SIF TALENT:PLAYER:矮小体型
		PRINT 与体型不符
	PRINT 的粗大
ELSEIF TALENT:PLAYER:尺寸傲人
	SIF TALENT:PLAYER:矮小体型
		PRINT 与体型不符，
	PRINT 尺寸傲人的
ELSE
    PRINT 的
ENDIF
PRINTFORM 阴茎…
;-------------------------------------------------
;125,Ｗ乳交
;-------------------------------------------------
@MESSAGE_COM125
PRINTFORM %CALLNAME%和%CALLNAME:ASSI%
;奴隶と助手が恋慕か相亲相爱
IF (TALENT:3 || TALENT:6) && (TALENT:ASSI:3 || TALENT:ASSI:6)
	PRINTFORM 眼神陶醉地仰视着%CALLNAME:PLAYER%，
;奴隶と助手が淫乱ルート
ELSEIF (TALENT:4 || TALENT:7) && (TALENT:ASSI:4 || TALENT:ASSI:7)
	PRINTFORM 眼神饥渴地仰视着%CALLNAME:PLAYER%，
ENDIF
PRINTFORM 分别用各自
;奴隶と助手が巨乳か爆乳
IF (TALENT:110 || TALENT:111) && (TALENT:ASSI:110 || TALENT:ASSI:111)
	IF TALENT:140
	PRINTFORM 壮观
	ELSE
	PRINTFORM 丰满
	ENDIF
;奴隶と助手が贫乳
ELSEIF TALENT:109 && TALENT:ASSI:109
	PRINTFORM 贫乏
ENDIF
PRINTFORM 的胸部轮流侍奉%CALLNAME:PLAYER%
IF TALENT:PLAYER:孩童尺寸
	PRINT 小小的
ELSEIF TALENT:PLAYER:短小
	PRINT 短小的
ELSEIF TALENT:PLAYER:巨根
	SIF TALENT:PLAYER:矮小体型
		PRINT 与体型不符
	PRINT 的粗大
ELSEIF TALENT:PLAYER:尺寸傲人
	SIF TALENT:PLAYER:矮小体型
		PRINT 与体型不符，
	PRINT 尺寸傲人的
ELSE
    PRINT 的
ENDIF
PRINTFORM 阴茎…
;-------------------------------------------------
;126,互相推搡胸部
;-------------------------------------------------
@MESSAGE_COM126
PRINTFORM %CALLNAME:PLAYER%和%CALLNAME%彼此磨蹭挤压着胸部…
;-------------------------------------------------
;127,阴茎相互摩擦
;-------------------------------------------------
@MESSAGE_COM127
PRINTFORM %CALLNAME:PLAYER%把阴茎和%CALLNAME%的贴到一起相互摩擦……
;-------------------------------------------------
;140,强制口交
;-------------------------------------------------
@MESSAGE_COM140
PRINTFORM %CALLNAME:PLAYER%粗暴地把自己
IF TALENT:PLAYER:巨根
	SIF TALENT:PLAYER:矮小体型
		PRINT 与体型不符的
	PRINT 粗大
ELSEIF TALENT:PLAYER:尺寸傲人
	SIF TALENT:PLAYER:矮小体型
		PRINT 与体型不符，
	PRINT 尺寸傲人的
ELSE
    PRINT 的
ENDIF
PRINTFORM 阴茎塞进%CALLNAME%嘴里快速抽插…
;-------------------------------------------------
;141,拳交
;-------------------------------------------------
@MESSAGE_COM141
PRINTFORM %CALLNAME:PLAYER%
;丝袜系
IF TEQUIP:1 == 5 || TEQUIP:1 == 6
    PRINT 弄破
	SIF TEQUIP:1 == 5
		PRINT 丝袜，
	SIF TEQUIP:1 == 6
		PRINT 网袜，
ENDIF
PRINTFORML 用拳头搅动起%CALLNAME%的阴道…
;Ｖ感觉
IF ABL:4 <= 3
	PRINTFORM %CALLNAME%的阴道被撑出一个大口
ELSEIF ABL:4 == 4
	PRINTFORM %CALLNAME%的阴道被强行扩充，无法恢复原状了
ELSEIF ABL:4 == 5
	PRINTFORM %CALLNAME%阴道里的媚肉露了出来，从入口可以看到里面抽动着的子宫口
ELSEIF ABL:4 >= 6
	PRINTFORM %CALLNAME%用自己的手指拉着阴唇，神情恍惚
ENDIF
TFLAG:52 = 1
;-------------------------------------------------
;142,肛门拳交
;-------------------------------------------------
@MESSAGE_COM142
PRINTFORM %CALLNAME:PLAYER%
;丝袜系
IF TEQUIP:1 == 5 || TEQUIP:1 == 6
    PRINT 弄破
	SIF TEQUIP:1 == 5
		PRINT 丝袜，
	SIF TEQUIP:1 == 6
		PRINT 网袜，
ENDIF
PRINTFORML 用拳头搅动起%CALLNAME%的肛门…
;Ａ感觉
IF ABL:5 <= 3
	PRINTFORM %CALLNAME%的肛门被撑出一个大口
ELSEIF ABL:5 == 4
	PRINTFORM %CALLNAME%的肛门被强行扩充，无法恢复原状了
ELSEIF ABL:5 == 5
	PRINTFORM %CALLNAME%肛门里的媚肉露了出来，从入口可以看到里面蠕动着的肠道
ELSEIF ABL:5 >= 6
	PRINTFORM %CALLNAME%用自己的手指拉着肛门边缘的褶皱，神情恍惚
ENDIF
TFLAG:52 = 1
;-------------------------------------------------
;143,两穴拳交
;-------------------------------------------------
@MESSAGE_COM143
PRINTFORM %CALLNAME:PLAYER%
;丝袜系
IF TEQUIP:1 == 5 || TEQUIP:1 == 6
    PRINT 弄破
	SIF TEQUIP:1 == 5
		PRINT 丝袜，
	SIF TEQUIP:1 == 6
		PRINT 网袜，
ENDIF
PRINTFORML 用拳头搅动起%CALLNAME%的小穴和肛门…
;Ｖ感觉かＡ感觉がLV4以下
IF ABL:4 <= 4 || ABL:5 <= 4
    IF TALENT:蛇尾 || TALENT:鱼尾
        PRINTFORML %CALLNAME%被扩张了的小穴和肛门里流出黏答答的爱液
    ELSE
	    PRINTFORML %CALLNAME%双腿大开，被扩张了的小穴和肛门里流出黏答答的爱液
	ENDIF
;Ｖ感觉かＡ感觉がLV5以上
ELSEIF ABL:4 >= 5 && ABL:5 >= 5
	PRINTFORML 像是在品味着余韵一般，%CALLNAME%亲自扒开小穴和肛门暴露出里面的媚肉，手指在边缘来回抚弄
ENDIF
TFLAG:52 = 1
;-------------------------------------------------
;144,放尿
;-------------------------------------------------
@MESSAGE_COM144
;触手プ雷中
IF TEQUIP:触手
	PRINTFORML 被触手缠绕着的%CALLNAME%失禁了，尿液像喷泉一样喷向空中…
;绳子使用中に放尿
ELSEIF TEQUIP:绳子
	PRINTFORML 被绑缚着的%CALLNAME%在身体无法动弹的情况下淅淅沥沥地尿了出来…
;野外PLAY中、男の場合はしゃがまない
ELSEIF TEQUIP:29
	IF TALENT:137 || TALENT:140
		PRINTFORML %CALLNAME%因羞耻和屈辱感眼中含泪
		PRINTFORM 一边在意着周围的目光一边淅淅沥沥地尿了出来…
	ELSE
		PRINTFORM %CALLNAME%怀着屈辱和羞耻，泪光盈盈地在草丛里撒尿了，尿液溅到了草叶上…
	ENDIF
;羞耻PLAY中、男の場合はしゃがまない
ELSEIF TEQUIP:30
    IF TALENT:鱼尾 || TALENT:蛇尾 || TALENT:马身
        PRINTFORM %CALLNAME%避开镜中自己的视线，淅淅沥沥地尿了出来…
    ELSE
        IF TALENT:137 || TALENT:140
            PRINTFORM %CALLNAME%避开镜中自己的视线，张开腿淅淅沥沥地尿了出来…
        ELSE
            PRINTFORM %CALLNAME%避开镜中自己的视线，蹲下身淅淅沥沥地尿了出来…
        ENDIF
	ENDIF
;矮小体型、男の場合はしゃがまない
ELSEIF TALENT:矮小体型
    IF TALENT:鱼尾 || TALENT:蛇尾 || TALENT:马身
        PRINTFORM %CALLNAME%被%CALLNAME:PLAYER%像给小儿把尿一样抱在怀里，身体悬空，淅淅沥沥地尿了出来…
    ELSE
        IF TALENT:137 || TALENT:140
            IF TALENT:PLAYER:高大体型
                PRINTFORM %CALLNAME%被%CALLNAME:PLAYER%像给小儿把尿一样抱在怀里，两脚叉开淅淅沥沥地尿了出来…
            ELSE
                PRINTFORM %CALLNAME%\@ !KANRAKU(TARGET) ? 带着羞耻和屈辱 # 满面通红地 \@开始撒尿，耳边响起了淅淅沥沥的声音…
            ENDIF
        ELSE
            PRINTFORM %CALLNAME%被%CALLNAME:PLAYER%像给小儿把尿一样抱起，身体悬空，两脚叉开，淅淅沥沥地尿了出来…
        ENDIF
	ENDIF
ELSE
	IF TALENT:137 || TALENT:140
		PRINTFORM %CALLNAME%\@ !KANRAKU(TARGET) ? 带着羞耻和屈辱 # 满面通红地 \@开始撒尿，耳边响起了淅淅沥沥的声音…
	ELSE
	    IF TALENT:鱼尾 || TALENT:蛇尾 || TALENT:马身
		    PRINTFORM %CALLNAME%满面通红地开始撒尿，耳边响起了淅淅沥沥的声音…
	    ELSE
		    PRINTFORM %CALLNAME%羞得满面通红，蹲下来开始撒尿，耳边响起了淅淅沥沥的声音…
	    ENDIF
	ENDIF
ENDIF
IF ABL:9 == 3
	PRINTL
	PRINTFORM %CALLNAME%发出了煽情的喘息声
ELSEIF ABL:9 == 4
	PRINTL
	PRINTFORM %CALLNAME%神情恍惚地享受着小便的快意
ELSEIF ABL:9 >= 5
	PRINTL
	PRINTFORM %CALLNAME%尽情地体会着放尿的羞耻感
ENDIF
TFLAG:52 = 1
;-------------------------------------------------
;145,肛门侍奉強制
;-------------------------------------------------
@MESSAGE_COM145
PRINTFORML %CALLNAME:PLAYER%命令%CALLNAME%舔舐自己的肛门
PRINTFORM %CALLNAME%
;顺从LV4未満 未陷落
IF ABL:顺从 < 4 && !KANRAKU(TARGET)
	SIF ASSIPLAY && MALE(ASSI) && ABL:同性倾向 <= 1
		PRINT 因面对男人的屁股一脸恶心，动作僵硬地
	IF TALENT:反抗
		PRINT 的眼中写满反抗，动作僵硬地
	ELSEIF TALENT:无动于衷 || TALENT:缺乏感情
		PRINT 面无表情地
	ELSEIF TALENT:胆怯
		PRINT 颤颤巍巍地
	ELSE
		PRINT 生涩地
	ENDIF
	PRINT 伸出舌头舔了起来…
;受虐属性＋侍奉精神
ELSEIF ABL:9 + ABL:7 >= 7
	PRINT 脸色潮红，主动舔舐肛门周边的每一寸褶皱…
ELSEIF ABL:9 + ABL:7 >= 5
	PRINT 努力地活动舌头侍奉着…
ELSE
	SIF ASSIPLAY && MALE(ASSI) && ABL:同性倾向 <= 1
		PRINT 因面对男人的屁股一脸恶心，但还是
	PRINTFORM 顺从地张开嘴伸出了舌头…
ENDIF
;-------------------------------------------------
;146,灌肠器＋塞头
;-------------------------------------------------
@MESSAGE_COM146
;解除時
IF TEQUIP:21
	PRINTFORML 肛门栓被拔掉的一刻，%CALLNAME%肠内的污物尽数排出…
	;受虐属性
	IF ABL:9 == 0
		PRINTFORM %CALLNAME%脸上浮现出苦闷的表情
	ELSEIF ABL:9 == 1
		PRINTFORM %CALLNAME%的额头上布满汗珠，拼命忍受着腹下的排泄感
	ELSEIF ABL:9 == 2
		PRINTFORM %CALLNAME%虽然无比羞耻，却也因开放感流露出放松的神色
	ELSEIF ABL:9 == 3
		PRINTFORM %CALLNAME%在排泄物经过肛门时发出了娇艳的喘息
	ELSEIF ABL:9 == 4
		PRINTFORM %CALLNAME%露出陶醉的神色，享受着排泄的快感
	ELSEIF ABL:9 >= 5
		PRINTFORML %CALLNAME%在排泄的过程中饱尝了耻辱
		IF TALENT:140
			PRINTFORM 然而在污物经过肛门时，%CALLNAME%紧绷的脸色因快感而有所缓和
		ELSE
			PRINTFORM 然而在污物经过肛门时，%CALLNAME%的阴道里分泌出了爱液
		ENDIF
	ENDIF
ELSE
	IF ASSIPLAY
    	PRINTFORM %CALLNAME:ASSI%
    ELSE
        PRINTFORM %CALLNAME:PLAYER%
    ENDIF
	;丝袜系
	IF TEQUIP:1 == 5 || TEQUIP:1 == 6
		IF TFLAG:55
			PRINT 撕开
		ELSE

			PRINT 撕破
		ENDIF
		SIF TEQUIP:1 == 5
			PRINT 丝袜，
		SIF TEQUIP:1 == 6
			PRINT 网袜，
	ENDIF
	PRINTFORML 把灌肠液灌入%CALLNAME%的肛门，堵上了肛门栓
	;受虐属性
	IF ABL:9 == 0
		PRINTFORM %CALLNAME%的脸上浮现出苦闷的表情
	ELSEIF ABL:9 == 1
		PRINTFORM %CALLNAME%满头大汗地忍受着排泄感
	ELSEIF ABL:9 == 2
		PRINTFORM %CALLNAME%面泛潮红，不时扭动着屁股
	ELSEIF ABL:9 == 3
		PRINTFORM %CALLNAME%被排泄感折磨着，但偶尔也会露出恍惚的表情
	ELSEIF ABL:9 == 4
		PRINTFORM %CALLNAME%一脸陶醉地享受着腹下的鼓胀感与排泄感
	ELSEIF ABL:9 >= 5
		PRINTFORML 尽情品味着排泄感的%CALLNAME%因快感松缓了神色
		PRINTFORM 口中
		SIF TALENT:140 == 0
			PRINTFORM 和小穴
		PRINTFORM 流下了亮晶晶的液体
	ENDIF
ENDIF
TFLAG:51 = 1
TFLAG:52 = 1
;-------------------------------------------------
;147,空气灌肠
;-------------------------------------------------
@MESSAGE_COM147
IF ASSIPLAY
	PRINTFORM %CALLNAME:ASSI%
ELSE
    PRINTFORM %CALLNAME:PLAYER%
ENDIF
PRINTFORML 向%CALLNAME%的肛穴输送空气，塞上了肛门栓
PRINTFORM 稍等片刻拔去塞子，便听到了%CALLNAME%响亮的放屁声…
TFLAG:52 = 1
;-------------------------------------------------
;148,扩张气球
;-------------------------------------------------
@MESSAGE_COM148
;解除時
IF ASSIPLAY
	PRINTFORM %CALLNAME:ASSI%
ELSE
    PRINTFORM %CALLNAME:PLAYER%
ENDIF
IF TEQUIP:22
	PRINTFORML 拔掉了%CALLNAME%肛穴里的扩张气球…
	;Ａ感觉
	IF ABL:5 == 0
		PRINTFORM %CALLNAME%的肛穴缓缓闭合
	ELSEIF ABL:5 == 1
		PRINTFORM %CALLNAME%的肛穴张开了好一阵才缓缓闭合
	ELSEIF ABL:5 == 2
		PRINTFORM %CALLNAME%的肛穴保持着张开的模样，暴露出内里的嫩肉
	ELSEIF ABL:5 == 3
		PRINTFORM %CALLNAME%的肛穴大大地张着，好像很难恢复原状
	ELSEIF ABL:5 == 4
		PRINTFORM %CALLNAME%的肛穴大大地张着，能看到里面的肠肉
	ELSEIF ABL:5 >= 5
		PRINTFORM %CALLNAME%把手指插入自己的肛穴搅动着，神情恍惚
	ENDIF
	TFLAG:51 = 1
	TFLAG:52 = 1
ELSE	
	;丝袜系
	IF TEQUIP:1 == 5 || TEQUIP:1 == 6
		IF TFLAG:55
			PRINT 撕开
		ELSE

			PRINT 撕破
		ENDIF
		SIF TEQUIP:1 == 5
			PRINT 丝袜，
		SIF TEQUIP:1 == 6
			PRINT 网袜，
	ENDIF
	PRINTFORM 往%CALLNAME%的肛穴插入扩张气球，开始往里面打气…
	TFLAG:51 = 1
	TFLAG:52 = 1
ENDIF

;-------------------------------------------------
;149,肛门电极
;-------------------------------------------------
@MESSAGE_COM149
;解除時
IF TEQUIP:23
	PRINTFORM 拔出了%CALLNAME%肛门里的电极…
ELSE	
	;丝袜系
	IF TEQUIP:1 == 5 || TEQUIP:1 == 6
		IF TFLAG:55
			PRINT 撕开
		ELSE

			PRINT 撕破
		ENDIF
		SIF TEQUIP:1 == 5
			PRINT 丝袜，
		SIF TEQUIP:1 == 6
			PRINT 网袜，
	ENDIF
	PRINTFORM 将肛门电极插入了%CALLNAME%的后穴…
ENDIF
TFLAG:51 = 1
TFLAG:52 = 1
;-------------------------------------------------
;150,乳房电极
;-------------------------------------------------
@MESSAGE_COM150
;解除時
IF TEQUIP:24
	PRINTFORM 取下了%CALLNAME%胸上的电极…
ELSE
	PRINTFORM 把电极贴在%CALLNAME%胸上打开了开关…
ENDIF
TFLAG:51 = 1
TFLAG:52 = 1
;-------------------------------------------------
;151,鳗鱼池
;-------------------------------------------------
@MESSAGE_COM151
PRINTFORML %CALLNAME:PLAYER%将装有无数鳗鱼的水桶扣在了%CALLNAME%身上将其淹没
PRINTFORML 湿黏生物在身体上滑行的异样感使得%CALLNAME%无法忍受地尖叫出声
;Ｖに道具を插入していない
IF TEQUIP:13 || TALENT:140 || TEQUIP:51
ELSE
	PRINTFORML 　
	PRINTFORML 数条鳗鱼发现了%CALLNAME%未经防备的阴道口，先后潜了进去
	;处女
	IF TALENT:0 && !TALENT:人偶 && !TALENT:机器人
		PRINTFORML %CALLNAME%吓得眼泪直流，半癫狂地叫着，拼命抓住鳗鱼的尾巴想要把它们都拖出来
		PRINTFORML 但由于鳗鱼身上分泌的湿滑粘液，%CALLNAME%没有成功
		PRINTFORML 鳗鱼们顶破了%CALLNAME%的处女膜，象征失贞的鲜血缓缓流了出来…
	ELSE
		PRINTFORML %CALLNAME%被鳗鱼们顶弄着阴道跟子宫，不住地翻着白眼…
	ENDIF
ENDIF

;Ａに道具を插入していない
IF TEQUIP:14 || TEQUIP:15 || TEQUIP:21 || TEQUIP:22 || TEQUIP:23 || TEQUIP:52
ELSE
	PRINTFORML 　
	SIF !TEQUIP:13 && !TALENT:140
		PRINT 此外，
	PRINTFORML 数条鳗鱼发现了%CALLNAME%空荡荡的肛穴口，一条接一条钻了进去
	PRINTFORML %CALLNAME%反射性地收紧屁股想要抵抗异物入侵，结果徒劳而终…
ENDIF
PRINTFORML 　
PRINTFORM 从浴室返回后好一阵，%CALLNAME%仍然神情呆滞，浑身发抖…
TFLAG:52 = 1
;-------------------------------------------------
;152,虫子池
;-------------------------------------------------
@MESSAGE_COM152
PRINTFORML 将装有无数蠕动着的虫子的水桶扣在了%CALLNAME%身上将其淹没
PRINTFORML %CALLNAME%张大嘴发出了无声的悲鸣…
PRINTFORM 即使将%CALLNAME%从虫子堆里拉出来，%CALLNAME%脸上仍保持着恐慌到极点的神情，不住颤抖…
TFLAG:52 = 1
;-------------------------------------------------
;153,尿道バルーン
;-------------------------------------------------
@MESSAGE_COM153
;解除時
IF TEQUIP:尿道气球
	PRINTFORM %CALLNAME:PLAYER%抽出气球里的空气後，一口气将%CALLNAME%尿道里的气球拔出…
ELSE
	PRINTFORM %CALLNAME:PLAYER%拿出宛如吸管纤细的尿道气球，塞入%CALLNAME%尿道里後，开始充气……
ENDIF
TFLAG:51 = 1
TFLAG:52 = 1
;-------------------------------------------------
;154,肉便器
;-------------------------------------------------
@MESSAGE_COM154
PRINTFORM %CALLNAME:PLAYER%
;前回の调教が正常位肛交か后背位肛交か对面座位肛交か背面座位肛交か骑乘位肛交で、抜かないフラグがある場合
IF TFLAG:60 && (PREVCOM == 26 || PREVCOM == 27 || PREVCOM == 28 || PREVCOM == 29 || PREVCOM == 36) && ((ASSIPLAY && TFLAG:50) || (ASSIPLAY == 0 && TFLAG:50 == 0))
	PRINTFORM 保持着插入的姿势，
ELSE
	PRINTFORM 在%CALLNAME%
	;润滑LV4以上
	SIF PALAM:4 >= PALAMLV:4
		PRINTFORM 沾满粘液
	PRINTFORM 的肛门里
ENDIF
PRINTFORML 尿了出来
PRINTFORM 大量温热液体拍打冲击肠壁的感觉
;受虐属性
IF ABL:9 == 0
	PRINTFORML 使得%CALLNAME%浮现出苦闷的表情
ELSEIF ABL:9 == 1
	PRINTFORML 让%CALLNAME%出了满头的汗
ELSEIF ABL:9 == 2
	PRINTFORML 使得%CALLNAME%露出了痛苦的表情，面色潮红地扭动着屁股
ELSEIF ABL:9 == 3
	PRINTFORML 和随之伴来的排泄感让%CALLNAME%陷入了双重的折磨之中，但偶尔还是会露出恍惚的表情
ELSEIF ABL:9 == 4
	PRINTFORML 和随之伴来的腹痛与排泄感让%CALLNAME%满脸沉醉，快乐地享受着
ELSEIF ABL:9 >= 5
	PRINTFORML 和随之伴来的排泄感让%CALLNAME%沉醉其中
ENDIF
PRINTFORM 抽出阴茎后，淡黄色的尿液从%CALLNAME%的肛穴里流了出来…
TFLAG:52 = 1
;-------------------------------------------------
;155,兽姦
;-------------------------------------------------
@MESSAGE_COM155
PRINTFORML 笼子里放出的野兽扑上前来袭击了%CALLNAME%
PRINTFORM 胯下直立的兽根
;下半身着衣、破れてない丝袜、又はＡとＶ両方開いていない
IF (TEQUIP:2 || TEQUIP:4 == 2 || ((TEQUIP:1 == 5 || TEQUIP:1 == 6) && TFLAG:60 == 0)) || ((TEQUIP:13 || TALENT:140) && (TEQUIP:14 || TEQUIP:15 || TEQUIP:21 || TEQUIP:22 || TEQUIP:23))
	IF TEQUIP:2 || TEQUIP:4 == 2
		PRINTFORM 隔着衣服摩擦着%CALLNAME%的股间…
	;润滑LV4以上
	ELSEIF PALAM:4 >= PALAMLV:4
	    PRINTFORM 摩擦着%CALLNAME%沾满
		IF TALENT:140
			PRINT 体液
		ELSE
			PRINT 爱液
		ENDIF
		PRINTFORM 的股间…
	ENDIF
;男性、又はＶに道具插入でＡに
ELSEIF TEQUIP:13 || TALENT:140
	IF TEQUIP:肛塞 || TEQUIP:后庭拉珠 || TEQUIP:52
	    PRINT 强行挤开还含着
		IF TEQUIP:肛塞 && TALENT:兽耳 == 11
			PRINTFORM 猫尾肛塞
		ELSEIF TEQUIP:肛塞 && TALENT:兽耳 == 12
			PRINTFORM 狗尾肛塞
		ELSEIF TEQUIP:肛塞 && TALENT:兽耳 == 13
			PRINTFORM 兔尾肛塞
		ELSEIF TEQUIP:肛塞
			PRINTFORM 肛塞
		ELSEIF TEQUIP:后庭拉珠
			PRINT 后庭拉珠
		ELSEIF TEQUIP:52
		    PRINT 尾巴
	    PRINT 的
		ENDIF
		PRINTFORM 肛穴，
	ENDIF
	PRINTFORM 用人类无法想象的姿势和动作压榨着%CALLNAME%，打桩般抽插着…
;Ｖに
ELSE
	PRINTFORM 用人类无法想象的姿势和动作压榨着%CALLNAME%的小穴，打桩般抽插着…
ENDIF
;けもみみ＋肛塞
IF TALENT:143 && TEQUIP:14
	PRINTL 
	PRINTFORM 两只兽互相纠缠的样子荒诞至极
ENDIF
;-------------------------------------------------
;156,烙印
;-------------------------------------------------
@MESSAGE_COM156
;地の文を@COM156に移した
;-------------------------------------------------
;157,饮尿
;-------------------------------------------------
@MESSAGE_COM157
;前回が顔騎圧迫の場合
IF PREVCOM == 113
	IF ABL:9 <= 3
		PRINTFORM %CALLNAME:PLAYER%用大腿压住神情厌恶的%CALLNAME%的脸，强迫%CALLNAME%喝自己的尿…
	ELSEIF ABL:9 <= 4
		PRINTFORML 收到命令后，被%CALLNAME:PLAYER%跨在脸上的%CALLNAME%张开嘴断断续续地把尿喝干了…
	ELSE
		PRINTFORML %CALLNAME%焦急地催促着%CALLNAME:PLAYER%排尿
		PRINTFORM 一边兴奋地扭动身子把尿喝干了…
	ENDIF
ELSE
	IF ABL:9 <= 3
		PRINTFORM %CALLNAME:PLAYER%把神情厌恶的%CALLNAME%的脸压到胯下，强迫%CALLNAME%喝自己的尿…
	ELSEIF ABL:9 <= 4
		PRINTFORML 收到命令后，%CALLNAME%贴近%CALLNAME:PLAYER%的股间断断续续把尿喝干了…
	ELSE
		PRINTFORML %CALLNAME%主动靠近%CALLNAME:PLAYER%胯下
		PRINTFORM 兴奋地扭动身子把尿喝干了…
	ENDIF
ENDIF
TFLAG:52 = 1
;-------------------------------------------------
;158,尾巴插入
;-------------------------------------------------
@MESSAGE_COM158
;解除時
IF TEQUIP:51 == 0
	PRINTFORML 从%CALLNAME%身体里抽出了\@ MALE(TARGET) ? 他 # 她 \@自己的尾巴
	;Ｖ经验200以上＋(恋慕ルートで助手でないor=淫乱ルート)
	IF EXP:0 > 200 && ((KANRAKU(TARGET) == 1 && !ASSIPLAY ) || KANRAKU(TARGET) == 2)
		PRINTFORM %CALLNAME%不太适应阴道里空荡荡的失落感，眼泪跟口涎不自觉地流淌着
	;Ｖ经验150以上＋(恋慕ルートで助手でないor=淫乱ルート)
	ELSEIF EXP:0 > 150 && ((KANRAKU(TARGET) == 1 && !ASSIPLAY ) || KANRAKU(TARGET) == 2)
		PRINTFORM %CALLNAME%阴道里积蓄着的液体被连带着挤出，滴滴答答的在地上形成了一个水坑
	;Ｖ经验120以上＋(恋慕ルートで助手でないor=淫乱ルート)
	ELSEIF EXP:0 > 120 && ((KANRAKU(TARGET) == 1 && !ASSIPLAY ) || KANRAKU(TARGET) == 2)
		PRINTFORM 被拔走自慰棒后，%CALLNAME%的内壁流出了晶莹的爱液
	;Ｖ经验70以上＋恋慕ルート
	ELSEIF EXP:0 > 70 && (KANRAKU(TARGET) == 1 || KANRAKU(TARGET) == 2)
		PRINTFORM %CALLNAME%扭动身子，一边看着%PLAYER_NAME(2)%一边
		IF TALENT:鱼尾 || TALENT:蛇尾
		    PRINT 扭动着腰肢
		ELSEIF TALENT:马身
		    PRINT 扭动着臀部
        ELSE
		    PRINT 摩擦起双腿
		ENDIF
	;Ｖ经验40以上
	ELSEIF EXP:0 > 40
		PRINTFORM %CALLNAME%吐出了热切的呼吸
	;Ｖ经验20以上
	ELSEIF EXP:0 > 20
		PRINTFORM %CALLNAME%拉开了松弛的阴道口
	;Ｖ经验20未満
	ELSE
		PRINTFORM %CALLNAME%露出松了一口气的表情
	ENDIF
	PRINTFORM …
	TFLAG:52 = 1
ELSE
	PRINTFORM %CALLNAME:PLAYER%用%CALLNAME%的尾巴前端摩擦着\@ MALE(TARGET) ? 他 # 她 \@
	;润滑LV5以上でさらに追加
	SIF PALAM:4 >= PALAMLV:5
		PRINTFORM 沾满
	;润滑LV3以上でさらに追加
	SIF PALAM:4 >= PALAMLV:3
		PRINTFORM 粘液的
	;矮小体型
	IF TALENT:100
		PRINTFORML 娇嫩小穴
	ELSE
		PRINTFORML 秘裂
	ENDIF
	PRINTFORM %CALLNAME%
	;处女＋娼妇+今も男
	IF TALENT:0 && TALENT:7 && MALE(TARGET)
		PRINTFORM 想到自己即将结束处女生涯，不由展现出淫靡的笑容，已经把曾是男人的过去抛在脑后了
	;处女＋娼妇+性転換した
	ELSEIF TALENT:0 && TALENT:7 && !MALE(TARGET)
		PRINTFORM 想到自己即将结束处女生涯，不由展现出淫靡的笑容，已经把曾是男人的过去抛在脑后了
	;处女＋恋慕か相亲相爱
	ELSEIF TALENT:0 && (TALENT:3 || TALENT:6)
		PRINTFORM 有点寂寞地看向%PLAYER_NAME(1)%
	;处女＋淫乱
	ELSEIF TALENT:0 && TALENT:4
		PRINTFORM 毫不在乎是否会破处，只一个劲儿地扭摆腰部
	;处女＋难以越过的底线
	ELSEIF TALENT:0 && TALENT:27
		PRINTFORM 像是已经放弃了什么似的，眼含泪水
	;处女
	ELSEIF TALENT:0
		PRINTFORM 脸色青白，浑身战栗，畏惧着失去处女的那一刻
	;Ｖ经验40以上＋恋慕ルートか淫乱ルート
	ELSEIF EXP:0 > 40 && (KANRAKU(TARGET) == 1 || KANRAKU(TARGET) == 2)
		;Ｖ经验200以上
		IF EXP:0 > 200
			PRINTFORM 身心都期待着被插入，嘎吱嘎吱地晃动着腰
		;Ｖ经验150以上
		ELSEIF EXP:0 > 150
			PRINTFORM 用像是要融化了似的眼神看着%CALLNAME:PLAYER%，自己抚弄起小穴边缘
		;Ｖ经验120以上
		ELSEIF EXP:0 > 120
			PRINTFORM 一脸恍惚，迫不及待地晃动着腰
		;Ｖ经验70以上
		ELSEIF EXP:0 > 70
			PRINTFORM 用湿润的眼睛看着%PLAYER_NAME(2)%
		;Ｖ经验40以上
		ELSE
			PRINTFORM 沉浸在敏感小穴被摩擦的快感中，吐露出浅碎的呻吟
		ENDIF
	;Ｖ经验20以上
	ELSEIF EXP:0 > 20
		PRINTFORM 在自己尾巴的刺激下分泌出爱液
	;Ｖ经验0以上
	ELSEIF EXP:0 >= 0
		PRINTFORM 稍微挺起了下身迎接尾巴的进入
	ENDIF
	PRINTFORML …
	PRINTFORM %CALLNAME:PLAYER%往%CALLNAME%
	;润滑LV4以上
	SIF PALAM:4 >= PALAMLV:4
		PRINTFORM 湿漉漉
	PRINTFORM 的性器里插入了\@ MALE(TARGET) ? 他 # 她 \@自己的尾巴…
	TFLAG:52 = 1
ENDIF
TFLAG:51 = 1
TFLAG:52 = 1
;-------------------------------------------------
;159,尾巴肛插入
;-------------------------------------------------
@MESSAGE_COM159
;解除時
IF TEQUIP:52 == 0
	PRINTFORML 从%CALLNAME%的屁股里拔出了\@ MALE(TARGET) ? 他 # 她 \@自己的尾巴
	PRINTFORM %CALLNAME%
	;Ａ经验180以上＋淫乱ルート
	IF EXP:1 > 180 && KANRAKU(TARGET) == 2
		PRINTFORM 的肛穴里流出粘液，穴口寂寞难耐地翕张着
	;Ａ经验140以上＋淫乱ルート
	ELSEIF EXP:1 > 140 && KANRAKU(TARGET) == 2
		PRINTFORM 投来湿润的眼神，摇动着屁股
	;Ａ经验100以上＋淫乱ルート
	ELSEIF EXP:1 > 100 && KANRAKU(TARGET) == 2
		PRINTFORM 抬高屁股，尤嫌不够
	;Ａ经验70以上
	ELSEIF EXP:1 > 70
		PRINTFORM 有点恋恋不舍地摇动屁股
	;Ａ经验40以上
	ELSEIF EXP:1 > 40
		PRINTFORM 晃了晃屁股，流露出松了口气的表情
	;Ａ经验20以上
	ELSEIF EXP:1 > 20
		PRINTFORM 因强制的排泄感脸颊泛红
	;Ａ经验0以上
	ELSEIF EXP:1 >= 0
		PRINTFORM 神情痛苦，忍受着不适
	ENDIF
	PRINTFORM …
	TFLAG:52 = 1
ELSE
	PRINTFORML 把%CALLNAME%的尾巴前端贴在了\@ MALE(TARGET) ? 他 # 她 \@的肛门口…
	PRINTFORM %CALLNAME%
	;Ａ经验180以上
	IF EXP:1 > 180
		PRINTFORM 一副快要融化了的样子，主动扒开自己的穴口
	;Ａ经验140以上
	ELSEIF EXP:1 > 140
		PRINTFORM 扭动腰部，心急如焚地等待着插入
	;Ａ经验100以上
	ELSEIF EXP:1 > 100
		PRINTFORM 双颊微红地摇晃起屁股
	;Ａ经验70以上
	ELSEIF EXP:1 > 70
		PRINTFORM 的身体因肛塞感触的刺激颤抖着
	;Ａ经验40以上
	ELSEIF EXP:1 > 40
		PRINTFORM 的脸上混杂着期待与抗拒的神情
	;Ａ经验20以上
	ELSEIF EXP:1 > 20
		PRINTFORM 的脸羞得通红
	;Ａ经验0以上
	ELSEIF EXP:1 >= 0
		PRINTFORM 因被侵犯后面肮脏的肛穴，脸上满是屈辱和羞耻
	ENDIF
	PRINTFORML …
	PRINTFORM %CALLNAME:PLAYER%往%CALLNAME%
	;润滑LV4以上
	SIF PALAM:4 >= PALAMLV:4
		PRINTFORM 沾满粘液
	PRINTFORM 的肛门插入了\@ MALE(TARGET) ? 他 # 她 \@自己的尾巴…
	TFLAG:52 = 1
ENDIF
TFLAG:51 = 1
TFLAG:52 = 1
;-------------------------------------------------
;160,触手召喚
;-------------------------------------------------
@MESSAGE_COM160
;解除時
IF TEQUIP:25
	PRINTFORM 送还触手，解放了%CALLNAME%的身体…
ELSE
	PRINTFORM %CALLNAME:PLAYER%召唤出触手，缠绕住%CALLNAME%的身体…
ENDIF
TFLAG:51 = 1
TFLAG:52 = 1
;-------------------------------------------------
;161,触手插入
;-------------------------------------------------
@MESSAGE_COM161
;解除時
IF TEQUIP:13
	PRINTFORM 触手从%CALLNAME%的性器里拔了出来
ELSE
	PRINTFORM 遍布细密突起的触手钻入了%CALLNAME%的性器
ENDIF
TFLAG:51 = 1
TFLAG:52 = 1
;-------------------------------------------------
;162,触手插肛
;-------------------------------------------------
@MESSAGE_COM162
;解除時
IF TEQUIP:14
	PRINTFORM 触手从%CALLNAME%的肛门里拔了出来
ELSE
	;丝袜系
	IF TEQUIP:1 == 5 || TEQUIP:1 == 6
		SIF TEQUIP:1 == 5
			PRINT 丝袜
		SIF TEQUIP:1 == 6
			PRINT 网袜
		IF TFLAG:55
			PRINT 的裂纹扩大了，
		ELSE

			PRINT 被粘液所溶解，
		ENDIF
	ENDIF
	PRINTFORM 粗长的触手贯穿了%CALLNAME%的肛门
ENDIF
TFLAG:51 = 1
TFLAG:52 = 1
;-------------------------------------------------
;163,触手揉阴
;-------------------------------------------------
@MESSAGE_COM163
;解除時
IF TEQUIP:11
	PRINTFORM 触手停止揉搓%CALLNAME%的阴核
ELSE
	;丝袜系
	IF TEQUIP:1 == 5 || TEQUIP:1 == 6
		SIF TEQUIP:1 == 5
			PRINT 丝袜
		SIF TEQUIP:1 == 6
			PRINT 网袜
		IF TFLAG:55
			PRINT 的裂纹扩大了，
		ELSE

			PRINT 被粘液所溶解，
		ENDIF
	ENDIF
	PRINTFORM 分支的细小触手开始蹂躏%CALLNAME%的阴蒂
ENDIF
TFLAG:51 = 1
TFLAG:52 = 1
;-------------------------------------------------
;164,触手揉乳
;-------------------------------------------------
@MESSAGE_COM164
;解除時
IF TEQUIP:16
	PRINTFORM 触手从%CALLNAME%的乳头上离开了
ELSE
	PRINTFORM 微微蠢动着的触手开始玩弄%CALLNAME%的乳头
ENDIF
TFLAG:51 = 1
TFLAG:52 = 1
;-------------------------------------------------
;165,触手挤奶
;-------------------------------------------------
@MESSAGE_COM165
;解除時
IF TEQUIP:17
	PRINTFORM 触手松开了%CALLNAME%的乳头
ELSE
	PRINTFORM 吸盘状的触手吸上了%CALLNAME%的乳头
ENDIF
TFLAG:51 = 1
TFLAG:52 = 1
;-------------------------------------------------
;166,触手紧缚
;-------------------------------------------------
@MESSAGE_COM166
;解除時
IF TEQUIP:19
	PRINTFORM 触手解开了对%CALLNAME%的拘束
ELSE
	PRINTFORM 无数触手缠绕住了%CALLNAME%的身体
ENDIF
TFLAG:51 = 1
TFLAG:52 = 1
;-------------------------------------------------
;167,触手灌肠
;-------------------------------------------------
@MESSAGE_COM167
;解除時
IF TEQUIP:21
	PRINTFORML 触手拔掉的那一刻，%CALLNAME%肠内的污物尽数排出…
	;受虐属性
	IF ABL:9 == 0
		PRINTFORM %CALLNAME%脸上浮现出苦闷的表情
	ELSEIF ABL:9 == 1
		PRINTFORM %CALLNAME%的额头上布满汗珠，拼命忍受着腹下的排泄感
	ELSEIF ABL:9 == 2
		PRINTFORM %CALLNAME%虽然无比羞耻，却也因开放感流露出放松的神色
	ELSEIF ABL:9 == 3
		PRINTFORM %CALLNAME%在排泄物经过肛门时发出了娇艳的喘息
	ELSEIF ABL:9 == 4
		PRINTFORM %CALLNAME%露出陶醉的神色，享受着排泄的快感
	ELSEIF ABL:9 >= 5
		PRINTFORML %CALLNAME%在排泄的过程中饱尝了耻辱
		PRINTFORM 然而在污物经过肛门时，%CALLNAME%紧绷的脸色因快感而有所缓和，性器里分泌出了爱液
	ENDIF
	TFLAG:52 = 1
ELSE
	;丝袜系
	IF TEQUIP:1 == 5 || TEQUIP:1 == 6
		SIF TEQUIP:1 == 5
			PRINT 丝袜
		SIF TEQUIP:1 == 6
			PRINT 网袜
		IF TFLAG:55
			PRINT 的裂纹扩大了，
		ELSE

			PRINT 被粘液所溶解，
		ENDIF
	ENDIF
	PRINTFORM 极粗的触手堵住了%CALLNAME%的肛穴，开始往里灌入粘液
ENDIF
TFLAG:51 = 1
TFLAG:52 = 1
;-------------------------------------------------
;168,触手口辱
;-------------------------------------------------
@MESSAGE_COM168
;解除時
IF TEQUIP:36
	PRINTFORM 触手从%CALLNAME%口中拔出
ELSE
	PRINTFORM 湿润泛光的触手侵入到%CALLNAME%口中
ENDIF
TFLAG:51 = 1
TFLAG:52 = 1
;-------------------------------------------------
;169,触手玩弄阴茎
;-------------------------------------------------
@MESSAGE_COM169
;解除時
IF TEQUIP:12
	PRINTFORM 触手解放了%CALLNAME%的阴茎
ELSE
	;丝袜系
	IF TEQUIP:1 == 5 || TEQUIP:1 == 6
		SIF TEQUIP:1 == 5
			PRINT 丝袜
		SIF TEQUIP:1 == 6
			PRINT 网袜
		IF TFLAG:55
			PRINT 的裂纹扩大了，
		ELSE

			PRINT 被粘液所溶解，
		ENDIF
	ENDIF
	PRINTFORM 内含多处肉突的恶心触手包裹缠绕住%CALLNAME%的阴茎
ENDIF
TFLAG:51 = 1
TFLAG:52 = 1
;-------------------------------------------------
;170,触手尿道插入,
;-------------------------------------------------
@MESSAGE_COM170
IF TEQUIP:38 == 1
    IF TALENT:人偶 || TALENT:机器人
        PRINTFORM 触手从%CALLNAME%的尿道里拔了出来
    ELSE
        PRINTFORML 触手从%CALLNAME%尿道里拔出的那一刻，尿液像喷泉一样喷向空中，划过了一道抛物线
        IF ABL:9 >= 5 && TALENT:57
            PRINTFORML %CALLNAME%深深沉浸在失禁的快感当中，表情因排泄感而松缓
            PRINTFORML 放尿的同时
            IF TALENT:140
                PRINT 腺液
            ELSE
                PRINT 爱液
            ENDIF
            PRINTL 也滴滴答答地落了下来
        ELSEIF ABL:9 >= 4
            PRINTFORML %CALLNAME%的脸上流露出些许欢愉，有些忧郁地凝视着自己还在潮吹的尿道口
        ELSEIF ABL:9 == 3
            PRINTFORML %CALLNAME%虽然很害羞，却更加沉浸在排泄的开放感中，神情也为之松缓
        ELSEIF ABL:9 >= 1
            PRINTFORML %CALLNAME%的身体弯曲成く字形，因下腹的排泄感皱着脸
        ELSE
            PRINTFORML %CALLNAME%太过羞耻，拼命别过脸去不愿看到自己的生殖器
        ENDIF
        PRINTFORM %EXPNAME:31%＋２
        EXP:31 += 2
    ENDIF
	TFLAG:52 = 1
ELSE
	PRINTFORM 极细的触手钻入了%CALLNAME%的尿道
ENDIF
TFLAG:51 = 1
TFLAG:52 = 1
;-------------------------------------------------
;180,润滑乳液,
;-------------------------------------------------
@MESSAGE_COM180
PRINTFORM 给%CALLNAME%涂上了润滑乳液，弄得黏黏糊糊…
TFLAG:51 = 1
;-------------------------------------------------
;181,媚薬,
;-------------------------------------------------
@MESSAGE_COM181
;触手召喚中
IF TEQUIP:25
	PRINTFORML 触手上渗出了具有催淫效果的液体
ELSE
	PRINTFORM %CALLNAME:PLAYER%
	;调教者の技巧がLV3以上、顺从LV3以上、助手じゃない
	SIF ABL:PLAYER:2 >= 3 && ABL:顺从 >= 3 && !ASSIPLAY
		PRINTFORM 嘴对嘴
	;顺从LV2以下
	SIF ABL:0 <= 2
		PRINTFORM 强行
	PRINTFORML 给%CALLNAME%灌下了媚药
ENDIF
PRINTFORM %CALLNAME%微弱地喘息着，肌肤泛起潮红…
TFLAG:52 = 1
;-------------------------------------------------
;182,利尿剂,
;-------------------------------------------------
@MESSAGE_COM182
;触手召喚中
IF TEQUIP:25
	PRINTFORML 极细的触手对准%CALLNAME%的尿道口灌入了有利尿效果的液体…
ELSE
	PRINTFORM %CALLNAME:PLAYER%
;调教者の技巧がLV3以上、顺从LV3以上、助手じゃない
SIF ABL:PLAYER:2 >= 3 && ABL:顺从 >= 3 && !ASSIPLAY
	PRINTFORM 嘴对嘴
;顺从LV2以下
SIF ABL:0 <= 2
	PRINTFORM 强行
PRINTFORML 给%CALLNAME%灌下了利尿剂
ENDIF
PRINTFORM %CALLNAME%微弱地喘息着，咬着唇忍耐着尿意…
TFLAG:52 = 1
;-------------------------------------------------
;183,摄像机,
;-------------------------------------------------
@MESSAGE_COM183
;解除時
IF TEQUIP:摄像机
	PRINTFORM 停止对%CALLNAME%的拍摄…
ELSE
	IF FLAG:22 == 1
		PRINTFORM %PLAYER_NAME(1)%开始用摄像机拍摄
		IF ASSIPLAY
			PRINTFORMW 两人的痴态…
		ELSE
			PRINTFORMW %CALLNAME%…
		ENDIF
	ELSE
		PRINTFORMW 决定让%CALLNAME:ASSI%用摄像机拍摄%CALLNAME%的视频…
	ENDIF
	PRINTFORML 催促%CALLNAME%进行自我介绍后，
	SELECTCASE KANRAKU(TARGET)
		;恋慕陷落
		CASE 1
			PRINTFORML %CALLNAME%在镜头前介绍起自己…
		;淫乱陷落
		CASE 2
			;露出癖６以上
			IF ABL:露出癖 >= 6
				PRINTFORM %CALLNAME%开心地述说起自己的本名和至今为止的性体验经历
				SIF ABL:自慰成瘾 >= 3
					PRINTFORM ，甚至连自慰时的妄想都尽数倒出
				PRINTFORML …
				PRINTFORML 幻想着自己为主角的色情影片会在外面流传开来，%CALLNAME%的股间就变得\@ PENIS(TARGET) ? 硬邦邦 # 湿哒哒 \@的了…
			;露出癖３以上
			ELSEIF ABL:露出癖 >= 3
				PRINTFORML %CALLNAME%对着镜头说起了不堪入耳的下流话…
			;それ以下
			ELSE
				PRINTFORML %CALLNAME%说着说着就开始挑衅起%CALLNAME:PLAYER%来…
			ENDIF
		;服从陷落
		CASE 3
			;露出癖4以上
			SIF ABL:露出癖 >= 4
				PRINTFORM 抱着这副痴态被拍摄下来的期待，股间变得\@ PENIS(TARGET) ? 硬邦邦 # 湿漉漉 \@的
			PRINTFORM %CALLNAME%郑重其事地说起开场白…
		;其他
		CASEELSE
			;屈服３かつ、露出癖３以上もしくは顺从４以上
			IF MARK:屈服刻印 == 3 && (ABL:露出癖 >= 3 || ABL:顺从 >= 4)
				PRINTFORML %CALLNAME%在镜头前介绍起自己…
			ELSE
				PRINTFORML %CALLNAME%扭过脸紧咬住了牙关…
			ENDIF
	ENDSELECT
ENDIF
TFLAG:52 = 1
TFLAG:51 = 1
;-------------------------------------------------
;184,野外PLAY,
;-------------------------------------------------
@MESSAGE_COM184
;解除時
IF TEQUIP:29
	PRINTFORMW 返回了房间…
ELSE
	PRINTFORMW 给%CALLNAME%套上项圈牵出去了…
ENDIF
TFLAG:52 = 1
TFLAG:51 = 1
;-------------------------------------------------
;185,侵犯助手,
;-------------------------------------------------
@MESSAGE_COM185
;前回の调教が侵犯助手であり、抜かないフラグがある場合
IF TFLAG:60 && PREVCOM == 185
	PRINTFORM 当着%CALLNAME%的面持续侵犯%CALLNAME:ASSI%…
ELSE
	PRINTFORM 当着%CALLNAME%的面
	IF TFLAG:705
		IF EXP:ASSI:1 == 0 && TALENT:PLAYER:137 == 0 && TALENT:PLAYER:139 == 0 && TALENT:PLAYER:140 == 0
			PRINTFORM 夺走了%CALLNAME:ASSI%的后庭处子…
		ELSEIF EXP:ASSI:1 == 0
			PRINTFORM 贯穿了%CALLNAME:ASSI%的未经人事的肛门…
		ELSE
			PRINTFORM 贯穿了%CALLNAME:ASSI%的肛门…
		ENDIF
	ELSE
	    PRINTFORM 贯穿了%CALLNAME:ASSI%
		SIF EXP:ASSI:0 == 0
			PRINT 未经人事
		PRINT 的阴道…
	ENDIF
ENDIF
;-------------------------------------------------
;186,羞耻PLAY,
;-------------------------------------------------
@MESSAGE_COM186
;解除時
IF TEQUIP:30
	PRINTFORMW 带着%CALLNAME%远离了镜子…
ELSE
	PRINTFORMW 把%CALLNAME%带到了镜子前…
ENDIF
TFLAG:52 = 1
TFLAG:51 = 1
;-------------------------------------------------
;187,浴室PLAY,
;-------------------------------------------------
@MESSAGE_COM187
;解除時
IF TEQUIP:31
	PRINTFORMW 返回了房间…
ELSE
	PRINTFORMW 带着%CALLNAME%去了浴室…
ENDIF
TFLAG:52 = 1
TFLAG:51 = 1
;-------------------------------------------------
;188,淋浴,
;-------------------------------------------------
@MESSAGE_COM188
IF TEQUIP:32
	PRINTFORMW 停止让%CALLNAME%淋浴…
ELSE
	PRINTFORMW 开始给%CALLNAME%淋浴…
ENDIF
TFLAG:52 = 1
TFLAG:51 = 1
;-------------------------------------------------
;189,新妻PLAY,
;-------------------------------------------------
@MESSAGE_COM189
IF TEQUIP:33
	PRINTFORM 把%CALLNAME%的
	IF (TEQUIP:1 == 2 && TEQUIP:4 != 3) || TEQUIP:6 == 1
		PRINTFORM 烹饪服
	ELSE
		PRINTFORM 围裙
	ENDIF
	PRINTFORM 脱掉了…
ELSE
	PRINTFORM %CALLNAME%穿上了
	IF (TEQUIP:1 == 2 && TEQUIP:4 != 3) || TEQUIP:6 == 1
		PRINTFORM 烹饪服
	ELSE
		PRINTFORM 围裙
	ENDIF
	PRINTFORM …
ENDIF
TFLAG:52 = 1
TFLAG:51 = 1
;-------------------------------------------------
;190,写真摄影,
;-------------------------------------------------
@MESSAGE_COM190
PRINTFORML %CALLNAME:PLAYER%把%CALLNAME%的痴态记录在了胶卷上
;缺乏感情が無い＋淫乱か娼妇でない＋露出癖LV3未満
IF TALENT:22 == 0 && (TALENT:4 || TALENT:7) == 0 && ABL:8 < 3
	PRINTFORML 大概是发现自己不堪入目的样子被拍了下来，%CALLNAME%的脸僵住了…
;缺乏感情か淫乱か娼妇
ELSEIF TALENT:22 || TALENT:4 || TALENT:7
	PRINTFORM %CALLNAME%全然不在意自己的痴态被拍下来…
ENDIF
TFLAG:52 = 1
TFLAG:51 = 1
;-------------------------------------------------
;191,强精神药,
;-------------------------------------------------
@MESSAGE_COM191
PRINTFORM %CALLNAME:PLAYER%
;调教者の技巧がLV3以上、顺从LV3以上、助手じゃない
SIF ABL:PLAYER:2 >= 3 && ABL:顺从 >= 3 && !ASSIPLAY
	PRINTFORM 嘴对嘴
;顺从LV2以下
SIF ABL:0 <= 2
	PRINTFORM 强行
PRINTFORML 给%CALLNAME%灌下了强精神药
PRINTFORM %CALLNAME%
;顺从LV3以上かつ反抗刻印LV3未満
IF ABL:0 >= 3 && MARK:3 < 3
	PRINTFORM 一脸困惑地看着诡异笑着的%CALLNAME:PLAYER%…
;反抗刻印LV1以上
ELSEIF MARK:3 >= 1
	PRINTFORM 警戒地盯着诡异笑着的%CALLNAME:PLAYER%…
ELSE
	PRINTFORM 一瞬间露出惊讶的表情，像是想要从诡异笑着的%CALLNAME:PLAYER%脸上找出什么似的紧盯着%CALLNAME:PLAYER%…
ENDIF
TFLAG:52 = 1
TFLAG:51 = 1
;-------------------------------------------------
;192,避孕套精飲（奴隶）
;-------------------------------------------------
@MESSAGE_COM192
PRINTFORM 将用后微暖的避孕套贴在了%CALLNAME%的脸颊上…
;精液成瘾5以上
IF ABL:16 >= 5
	PRINTFORM %CALLNAME%将避孕套翻过来，恋恋不舍地吸吮着残精…
;精液成瘾3以上
ELSEIF ABL:16 >= 3
	PRINTFORM %CALLNAME%神情恍惚地品尝着流入口中的精液…
;精液成瘾1以上
ELSEIF ABL:16 >= 1
	PRINTFORM %CALLNAME%微皱着眉头，有些厌恶和抵触，但还是伸出舌头接受了精液…
;精液成瘾无
ELSE
	PRINTFORM 强行撬开%CALLNAME%紧闭的嘴，注入了腥臭的精液…
ENDIF
TFLAG:52 = 1
;-------------------------------------------------
;193,避孕套精飲（助手）
;-------------------------------------------------
@MESSAGE_COM193
PRINTFORM 把用过的避孕套递到%CALLNAME:ASSI%手上…
;助手が施虐狂
IF TALENT:ASSI:83
	PRINTFORM %CALLNAME:ASSI%在%CALLNAME%伸出舌头就能够到的距离
ELSE
	PRINTFORM %CALLNAME:ASSI%当着%CALLNAME%的面
ENDIF
;助手の精液成瘾がLV5以上
IF ABL:ASSI:16 >= 5
	PRINTFORML 带着恍惚的表情品尝精液…
ELSE
	PRINTFORML 津津有味地品尝着精液…
ENDIF
PRINTFORM %CALLNAME%
IF ABL:16 >= 5
	PRINTFORM 大概是忍不住了吧，流着口水看着那副光景…
ELSEIF ABL:16 >= 3
	PRINTFORM 眼神饥渴，目不准将地看着那副光景…
ELSEIF ABL:16 >= 1
	PRINTFORM 拼命地想要转移视线，眼睛却违背意志牢牢地盯着那副光景…
ELSE
	PRINTFORM 呆呆地看着那副光景…
ENDIF
TFLAG:52 = 1
TFLAG:51 = 1
;-------------------------------------------------
;194,含精接吻
;-------------------------------------------------
@MESSAGE_COM194
IF ASSIPLAY
	PRINTFORM %CALLNAME:ASSI%
ELSE
    PRINTFORM %CALLNAME:PLAYER%
ENDIF
PRINTFORML 含着精液与唾液的混合物，就那样吻上了%CALLNAME%的嘴唇…
;精液成瘾5以上
IF ABL:16 >= 5
	PRINTFORML %CALLNAME%贪婪地舔吸%CALLNAME:PLAYER%的唾液
	PRINTFORM 扫遍%CALLNAME:PLAYER%口腔的每一个角落享受着精液残余的味道…
;精液成瘾3以上
ELSEIF ABL:16 >= 3
	PRINTFORM %CALLNAME%神情恍惚地与%CALLNAME:PLAYER%交缠唇舌，分享品尝
	;直前が口交の場合
	SIF PREVCOM == 2
		PRINTFORM 自己的
	PRINTFORM 精液…
;精液成瘾1以上
ELSEIF ABL:16 >= 1
	PRINTFORM %CALLNAME%因饮下
	SIF PREVCOM == 2
		PRINTFORM 自己的
	PRINTFORM 精液，满面屈辱…
;精液成瘾无
ELSE
	PRINTFORML %CALLNAME%泪眼盈盈地想要逃离…
	PRINTFORM 然而%CALLNAME:PLAYER%并不允许，两手固定住%CALLNAME%的头，用舌头撬开了面前紧抿的嘴唇…
ENDIF
TFLAG:52 = 1
;-------------------------------------------------
;195,约会に行く
;行先とか中身とかは口上任せにした方がいいのかも
;-------------------------------------------------
@MESSAGE_COM195
PRINTFORM %CALLNAME:PLAYER%和打扮齐整的%CALLNAME%一起去约会了…
TFLAG:52 = 1
TFLAG:51 = 1
;-------------------------------------------------
;196,剥いてやる
;-------------------------------------------------
@MESSAGE_COM196
PRINTFORM %CALLNAME:PLAYER%剥下了%CALLNAME%的包皮…
;-------------------------------------------------
;197,水下PLAY
;-------------------------------------------------
@MESSAGE_COM197
;解除時
IF TEQUIP:53
	PRINTFORMW 返回了房间…
ELSE
	PRINTFORMW 带着%CALLNAME%来到温水泳池，一起泡进水中…
ENDIF
TFLAG:52 = 1
TFLAG:51 = 1
;-------------------------------------------------
;200,当面手淫
;-------------------------------------------------
@MESSAGE_COM200
IF TEQUIP:60
PRINTFORM %CALLNAME:PLAYER%像是故意挑衅那样，当着%CALLNAME%的面开始自慰了起来……
ELSE
PRINTFORM %CALLNAME:PLAYER%当着%CALLNAME%的面自慰
ENDIF
;-------------------------------------------------
;201,胸を揉ませる
;-------------------------------------------------
@MESSAGE_COM201
PRINTFORML %CALLNAME:PLAYER%要求%CALLNAME%揉自己的胸
PRINTFORM %CALLNAME%
;顺从3以上､欲望LV3以上
SIF ABL:顺从 >= 3  && ABL:欲望 >= 3 && (!ASSIPLAY  || KANRAKU(TARGET) == 2)
	PRINTFORM 积极
;恋慕ルート、
IF ABL:7 >= 3 && KANRAKU(TARGET) == 1 && !ASSIPLAY
	PRINTFORM 温柔地
;侍奉精神３以上
ELSEIF ABL:7 >= 3 && (!ASSIPLAY || ABL:同性倾向 >= 1 || !MALE(PLAYER))
	PRINTFORM 仔细地
;技巧ゼロ
ELSEIF ABL:2 == 0
	PRINTFORM 胡乱地
ENDIF
;男性か绝壁
IF MALE(PLAYER) || TALENT:PLAYER:110
	PRINTFORM 捏弄着
ELSE
	PRINTFORM 揉搓着
ENDIF
PRINTFORM %CALLNAME:PLAYER%
;男性か绝壁
IF MALE(PLAYER) || TALENT:PLAYER:110
	PRINT 的乳头
;矮小体型＋贫乳
ELSEIF TALENT:PLAYER:100 && TALENT:PLAYER:109
	PRINT 仅只是膨胀程度的胸脯
;贫乳
ELSEIF TALENT:PLAYER:109
	PRINT 微微膨胀的胸脯
;巨乳
ELSEIF TALENT:PLAYER:111
	IF TALENT:140
		PRINT 乳肉从手中溢出的胸肌
	ELSE
		PRINT 乳肉从手中溢出的丰乳
	ENDIF
;爆乳
ELSEIF TALENT:PLAYER:112
	IF TALENT:140
		PRINT 一只手无法包住的发达胸肌
	ELSE
		PRINT 一只手无法包住的豪乳
	ENDIF
ELSE
	PRINT 的胸部
ENDIF
PRINT …
;-------------------------------------------------
;202,使之爱抚
;-------------------------------------------------
@MESSAGE_COM202
PRINTFORML %CALLNAME:PLAYER%让%CALLNAME%爱抚自己的身体
PRINTFORM %CALLNAME%
;顺从3以上､欲望LV3以上
SIF ABL:顺从 >= 3  && ABL:欲望 >= 3 && (!ASSIPLAY || KANRAKU(TARGET) == 2)
	PRINTFORM 积极地
;接吻をする場合
SIF (STAIN:PLAYER:0 < 2 || STAIN:PLAYER:0 == 16 || STAIN:PLAYER:0 == 17 || TALENT:脏污无视 || ASSIPLAY) && TEQUIP:20 == 0 && TEQUIP:25 == 0 && ((KANRAKU(TARGET) == 1 && !ASSIPLAY ) || KANRAKU(TARGET) == 2)
	PRINTFORM 一边接吻一边
;侍奉精神３以上
IF ABL:7 >= 3 && (!ASSIPLAY || ABL:同性倾向 >= 1 || !MALE(PLAYER))
	PRINTFORM 仔细
;技巧ゼロ
ELSEIF ABL:2 == 0
	PRINTFORM 胡乱
ENDIF
PRINTFORM 爱抚起%CALLNAME:PLAYER%
;矮小体型
SIF TALENT:PLAYER:100
	PRINTFORM 纤细的
PRINTFORM 身体…
;-------------------------------------------------
;203,痛骂
;-------------------------------------------------
@MESSAGE_COM203
PRINTFORML %CALLNAME:PLAYER%让%CALLNAME%辱骂自己
PRINTFORM %CALLNAME%
;嗜虐属性
IF ABL:10 == 0
	PRINTFORML 绞尽脑汁地思考着骂人的词…
ELSEIF ABL:10 < 3
	PRINTFORML 口中一个接一个蹦出侮辱性词汇…
ELSEIF ABL:10 < 5
	PRINTFORML 破口大骂，似乎在辱骂的过程中产生了快感…
ELSE
	PRINTFORML 面色潮红，鼻息紊乱，似乎在辱骂的过程中产生了快感
	PRINTFORM 在用言语攻击%CALLNAME:PLAYER%的同时，下身渗出了
	SIF TALENT:140 || TALENT:139 || TALENT:137
		PRINT 腺液
	SIF TALENT:139 || TALENT:137
		PRINT 和
	SIF TALENT:140 == 0
		PRINT 爱液
	PRINTL …
	IF TALENT:PLAYER:受虐狂
		PRINTFORML 看到对辱骂有所反应的%CALLNAME:PLAYER%
		PRINTFORM %CALLNAME%眼底闪耀着晦暗不明的光，骂得更加起劲了…
	ENDIF
ENDIF
TFLAG:52 = 1
TFLAG:51 = 1
;-------------------------------------------------
;204,索要礼物
;-------------------------------------------------
@MESSAGE_COM204
PRINTFORMW %CALLNAME:PLAYER%向%CALLNAME%索要礼物
IF TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8
	;恋慕、相亲相爱
	IF TALENT:3 || TALENT:6
		PRINTFORM %CALLNAME%微微吃惊，随即为%CALLNAME:PLAYER%献上了
		K:1 = RAND:3
			IF K:1 == 0
				PRINTFORML 一桌丰盛的饭菜
			ELSEIF K:1 == 1
				PRINTFORML 一套新的衣服
			ELSEIF K:1 == 2
				PRINTFORML 自己作为礼物
			ENDIF
		PRINTFORML 快乐的时间转瞬即逝…
	;淫乱、娼妇
	ELSEIF TALENT:4 || TALENT:7
		PRINTFORM %CALLNAME%为%CALLNAME:PLAYER%献上了
		;受虐狂か施虐狂持ちで、鞭子を持っている
		IF (TALENT:83 || TALENT:84) && ITEM:13 == 1 && NOITEM == 0
			PRINTFORML 崭新的鞭子作为礼物
		;A感覚３以上で、男性かA感覚＞V感覚で、肛塞を持っている
		ELSEIF ABL:5 >= 3 && (TALENT:140 || ABL:5 > ABL:4) && ITEM:5 == 1
			PRINTFORML 崭新的肛塞作为礼物
		;V感覚３以上で、处女でなく、自慰棒を持っている
		ELSEIF ABL:4 >= 3 && TALENT:0 == 0 && ITEM:4 == 1
			PRINTFORML 崭新的自慰棒作为礼物
		;B感覚３以上で、乳头夹を持っている
		ELSEIF ABL:6 >= 3 && ITEM:10 == 1
			PRINTFORML 崭新的乳头夹作为礼物
		;跳蛋を持っている
		ELSEIF ITEM:0 == 1
			PRINTFORML 崭新的跳蛋作为礼物
		ELSE 
			PRINTFORML 自己作为礼物
		ENDIF
		PRINTFORM 总觉得，送礼的%CALLNAME%自己更加高兴…
	;服从、隶属
	ELSEIF TALENT:5 || TALENT:8
		PRINTFORM %CALLNAME%困惑地表示自己能拿得出手的礼物只有对主人的忠诚侍奉…
	ENDIF
;反抗刻印Lv3
ELSEIF MARK:3 == 3
	PRINTFORM %CALLNAME:PLAYER%收到了足以杀死人的视线了…
;それ以外
ELSE
	PRINTFORM %CALLNAME%一副“哈，这家伙在说什么呢”的表情看着这边…
ENDIF
TFLAG:52 = 1
TFLAG:51 = 1
;-------------------------------------------------
;205,逆正常位
;-------------------------------------------------
@MESSAGE_COM205
;前回と今回の调教者が同じで、前回の调教が逆正常位である場合
IF PREVCOM == 205 && ((ASSIPLAY && TFLAG:50) || (ASSIPLAY == 0 && TFLAG:50 == 0))
	;男性か双性か扶她である
	IF TALENT:137 || TALENT:139 || TALENT:140
		IF PALAM:快Ｃ < 1000 && (ABL:顺从 +  ABL:欲望 >= 3) && !RAND:2
			PRINTFORM %CALLNAME%一边品尝着%CALLNAME:PLAYER%小穴内壁的触感一边活动起腰部…
		ELSE 
			CALL HANYOU_JYOKYO_CSEX
		ENDIF
	;捆绑式假阴茎を使用している場合
	ELSE
		PRINTFORM %CALLNAME%戴着捆绑式假阳具翻搅玩弄%CALLNAME:PLAYER%的小穴…
	ENDIF
;同様に前回の调教が逆后背位か逆强奸の場合
ELSEIF (PREVCOM == 206 || PREVCOM == 211) && ((ASSIPLAY && TFLAG:50) || (ASSIPLAY == 0 && TFLAG:50 == 0))
	PRINTFORML %CALLNAME:PLAYER%拔出体内的\@ PENIS(TARGET) ? 阴茎 # 假阳具 \@，仰面向上扭摆起腰部…
	IF KANRAKU(TARGET) == 1 && !ASSIPLAY && ABL:侍奉精神 >= 4 && PENIS(TARGET) && !RAND:2
		IF BASE:2 >= 8000 && BASE:2 < 10000
			PRINTFORML 或许是临近高潮了，%CALLNAME%一脸忍耐的表情抱住%CALLNAME:PLAYER%加快了活塞运动…
		ELSEIF RAND:2 == 0
			PRINTFORM %CALLNAME%
			IF TEQUIP:14
				PRINT 忍受着肛塞给予的刺激，
			ELSEIF TEQUIP:15
				PRINT 忍受着后庭拉珠给予的刺激，
			ENDIF
			SIF ABL:技巧 < 4
				PRINT 尽管笨手笨脚，却仍
			PRINT 为了使主人满足，
			SIF (STAIN:PLAYER:0 < 2 || STAIN:PLAYER:0 == 16 || STAIN:PLAYER:0 == 17 || TALENT:脏污无视) && TEQUIP:20 == 0 && TEQUIP:25 == 0
				PRINT 一边接吻一边
			PRINT 拼命地活动着腰部…
		ELSE
			PRINTFORML %CALLNAME%活动着腰部，像被浸泡在温水里似的露出满足的表情
			PRINTFORM 一次又一次呼唤了%CALLNAME:PLAYER%的名字…
		ENDIF
	ELSEIF TALENT:137 || TALENT:139 || TALENT:140
		CALL HANYOU_JYOKYO_CSEX
	ENDIF
	TFLAG:52 = 1
ELSE
	PRINTFORM %CALLNAME:PLAYER%仰躺着，向%CALLNAME%展示了自己
	;调教者が处女
	SIF TALENT:PLAYER:0
		PRINTFORM 未经人事
	PRINTFORM 的小穴，命令%CALLNAME%把
	IF TALENT:男性 || TALENT:双性
    	PRINT 他
    ELSE
    	PRINT 她
    ENDIF
	;男性か双性か扶她である
	IF TALENT:137 || TALENT:139 || TALENT:140
    	;情欲のPALAMLV3以上
    	IF PALAM:6 >= PALAMLV:3
    		PRINTFORM 已完全挺立
    	    IF TALENT:包茎
    			PRINT ，裹着包皮
        		IF TALENT:孩童尺寸
        		    SIF TALENT:矮小体型 == 0
        		        PRINT ，与其体型不符
    		    	PRINT 的小小
    		    ELSEIF TALENT:短小
    		        SIF TALENT:高大体型
    		            PRINT ，与其高大体型不符
    		        PRINT 的短小
    		    ELSEIF TALENT:巨根
    		        SIF TALENT:矮小体型
    		            PRINT ，与其体型不符
    		    	PRINT 的粗大
    		    ELSEIF TALENT:尺寸傲人
    		        SIF TALENT:矮小体型
    		            PRINT ，与其体型不符
    		    	PRINT ，尺寸傲人的
    		    ELSE
    		    	PRINT 的
    		    ENDIF
    	    ELSE
        		IF TALENT:孩童尺寸
        		    SIF TALENT:矮小体型 == 0
        		        PRINT ，与其体型不符
    		    	PRINT 的小小
    		    ELSEIF TALENT:短小
    		        SIF TALENT:高大体型
    		            PRINT ，与其高大体型不符
    		        PRINT 的短小
    		    ELSEIF TALENT:巨根
    		        SIF TALENT:矮小体型
    		            PRINT ，与其体型不符
    		    	PRINT 的粗大
    		    ELSEIF TALENT:尺寸傲人
    		        SIF TALENT:矮小体型
    		            PRINT ，与其体型不符
    		    	PRINT ，尺寸傲人的
    		    ELSE
    		    	PRINT 的
    		    ENDIF
    		ENDIF
    	ELSE
    	    IF TALENT:包茎
    		    PRINT 裹着包皮
        	    IF TALENT:孩童尺寸
        		    SIF TALENT:矮小体型 == 0
        		        PRINT ，与其体型不符
    		    	PRINT 的小小
    		    ELSEIF TALENT:短小
    		        SIF TALENT:高大体型
    		            PRINT ，与其高大体型不符
    		        PRINT 的短小
    		    ELSEIF TALENT:巨根
    		        SIF TALENT:矮小体型
    		            PRINT ，与其体型不符
    		    	PRINT 的粗大
    		    ELSEIF TALENT:尺寸傲人
    		        SIF TALENT:矮小体型
    		            PRINT ，与其体型不符
    		        PRINT ，尺寸傲人的
    		    ELSE
    		    	PRINT 的
    		    ENDIF
    		ELSE
        		IF TALENT:孩童尺寸
        		    SIF TALENT:矮小体型 == 0
        		       PRINT 与其体型不符
    		    	PRINT 的小小
    		    ELSEIF TALENT:短小
    		        SIF TALENT:高大体型
    		            PRINT 与其高大体型不符
    		        PRINT 的短小
    		    ELSEIF TALENT:巨根
    		        SIF TALENT:矮小体型
    		            PRINT 与其体型不符
    		    	PRINT 的粗大
    		    ELSEIF TALENT:尺寸傲人
    		        SIF TALENT:矮小体型
    		            PRINT 与其体型不符，
    		    	PRINT 尺寸傲人的
    		    ELSE
    		        PRINT 的
    		    ENDIF
    		ENDIF
        ENDIF
		PRINT 阴茎
	ELSE
		PRINT 的假阳具
	ENDIF
	PRINTFORML 插进来…
	
	;恋慕ルート追加メッセージ。自慰棒などを入れられているとダメ
	IF KANRAKU(TARGET) == 1 && MALE(TARGET) && !MALE(MASTER) && !ASSIPLAY && !TEQUIP:13 && !TEQUIP:14 && !TEQUIP:15 && !TEQUIP:45 && !TEQUIP:46 && !TEQUIP:47 && !TEQUIP:48
		IF TALENT:恋慕
			PRINTFORML 和%CALLNAME:PLAYER%做了普通恋人之间最亲密的事
			PRINTFORM %CALLNAME%非常开心…
		ELSEIF TALENT:相亲相爱
			PRINTFORML 和%CALLNAME:PLAYER%做了普通恋人之间最亲密的事
			PRINTFORM %CALLNAME%非常幸福…
		ENDIF
	ELSEIF TALENT:137 || TALENT:139 || TALENT:140
		CALL HANYOU_JYOKYO_CSEX
	ENDIF
	TFLAG:52 = 1
ENDIF

;-------------------------------------------------
;206,逆后背位
;-------------------------------------------------
@MESSAGE_COM206
;前回と今回の调教者が同じで、前回の调教が逆后背位である場合
IF PREVCOM == 206 && ((ASSIPLAY && TFLAG:50) || (ASSIPLAY == 0 && TFLAG:50 == 0))
	;男性か双性か扶她である
	IF TALENT:137 || TALENT:139 || TALENT:140
		IF PALAM:快Ｃ < 1000 && (ABL:顺从 +  ABL:欲望 >= 3) && !RAND:2
			PRINTFORM %CALLNAME%一边品尝着%CALLNAME:PLAYER%小穴内壁的触感一边活动起腰部…
		ELSE 
			CALL HANYOU_JYOKYO_CSEX
		ENDIF
	;捆绑式假阴茎を使用している場合
	ELSE
		PRINTFORM %CALLNAME%戴着捆绑式假阳具翻搅玩弄%CALLNAME:PLAYER%的小穴…
	ENDIF
;同様に前回の调教が逆正常位か逆强奸の場合
ELSEIF (PREVCOM == 205 || PREVCOM == 211) && ((ASSIPLAY && TFLAG:50) || (ASSIPLAY == 0 && TFLAG:50 == 0))
	PRINTFORML %CALLNAME:PLAYER%拔出体内的\@ PENIS(TARGET) ? 阴茎 # 假阳具 \@，四肢着地趴下将%CALLNAME%吞入…
	IF KANRAKU(TARGET) == 1 && !ASSIPLAY && ABL:侍奉精神 >= 4 && PENIS(TARGET) && !RAND:2
		IF BASE:2 >= 8000 && BASE:2 < 10000
			PRINTFORML 或许是临近高潮了，%CALLNAME%一脸忍耐的表情抱住%CALLNAME:PLAYER%加快了活塞运动…
		ELSEIF RAND:2 == 0
			PRINTFORM %CALLNAME%
			IF TEQUIP:14
				PRINT 忍受着肛塞给予的刺激，
			ELSEIF TEQUIP:15
				PRINT 忍受着后庭拉珠给予的刺激，
			ENDIF
			SIF ABL:技巧 < 4
				PRINT 尽管笨手笨脚，却仍
			PRINTL 为了使主人满足，不断询问着%CALLNAME:PLAYER%的感受
			PRINT 一边积极爱抚着一边拼命地活动着腰部…
		ELSE
			PRINTFORML %CALLNAME%活动着腰部，像被浸泡在温水里似的露出满足的表情
			PRINTFORM 一次又一次呼唤了%CALLNAME:PLAYER%的名字…
		ENDIF
	ELSEIF TALENT:137 || TALENT:139 || TALENT:140
		CALL HANYOU_JYOKYO_CSEX
	ENDIF
	TFLAG:52 = 1
ELSE
	PRINTFORM %CALLNAME:PLAYER%背对%CALLNAME%四肢着地趴下，向%CALLNAME%展示了自己
	;调教者が处女
	SIF TALENT:PLAYER:0
		PRINTFORM 未经人事
	PRINTFORM 的小穴，命令%CALLNAME%把
	IF TALENT:男性 || TALENT:双性
    	PRINT 他
    ELSE
    	PRINT 她
    ENDIF
	;男性か双性か扶她である
	IF TALENT:137 || TALENT:139 || TALENT:140
    	;情欲のPALAMLV3以上
    	IF PALAM:6 >= PALAMLV:3
    		PRINTFORM 已完全挺立
    	    IF TALENT:包茎
    			PRINT ，裹着包皮
        		IF TALENT:孩童尺寸
        		    SIF TALENT:矮小体型 == 0
        		        PRINT ，与其体型不符
    		    	PRINT 的小小
    		    ELSEIF TALENT:短小
    		        SIF TALENT:高大体型
    		            PRINT ，与其高大体型不符
    		        PRINT 的短小
    		    ELSEIF TALENT:巨根
    		        SIF TALENT:矮小体型
    		            PRINT ，与其体型不符
    		    	PRINT 的粗大
    		    ELSEIF TALENT:尺寸傲人
    		        SIF TALENT:矮小体型
    		            PRINT ，与其体型不符
    		    	PRINT ，尺寸傲人的
    		    ELSE
    		    	PRINT 的
    		    ENDIF
    	    ELSE
        		IF TALENT:孩童尺寸
        		    SIF TALENT:矮小体型 == 0
        		        PRINT ，与其体型不符
    		    	PRINT 的小小
    		    ELSEIF TALENT:短小
    		        SIF TALENT:高大体型
    		            PRINT ，与其高大体型不符
    		        PRINT 的短小
    		    ELSEIF TALENT:巨根
    		        SIF TALENT:矮小体型
    		            PRINT ，与其体型不符
    		    	PRINT 的粗大
    		    ELSEIF TALENT:尺寸傲人
    		        SIF TALENT:矮小体型
    		            PRINT ，与其体型不符
    		    	PRINT ，尺寸傲人的
    		    ELSE
    		    	PRINT 的
    		    ENDIF
    		ENDIF
    	ELSE
    	    IF TALENT:包茎
    		    PRINT 裹着包皮
        	    IF TALENT:孩童尺寸
        		    SIF TALENT:矮小体型 == 0
        		        PRINT ，与其体型不符
    		    	PRINT 的小小
    		    ELSEIF TALENT:短小
    		        SIF TALENT:高大体型
    		            PRINT ，与其高大体型不符
    		        PRINT 的短小
    		    ELSEIF TALENT:巨根
    		        SIF TALENT:矮小体型
    		            PRINT ，与其体型不符
    		    	PRINT 的粗大
    		    ELSEIF TALENT:尺寸傲人
    		        SIF TALENT:矮小体型
    		            PRINT ，与其体型不符
    		        PRINT ，尺寸傲人的
    		    ELSE
    		    	PRINT 的
    		    ENDIF
    		ELSE
        		IF TALENT:孩童尺寸
        		    SIF TALENT:矮小体型 == 0
        		       PRINT 与其体型不符
    		    	PRINT 的小小
    		    ELSEIF TALENT:短小
    		        SIF TALENT:高大体型
    		            PRINT 与其高大体型不符
    		        PRINT 的短小
    		    ELSEIF TALENT:巨根
    		        SIF TALENT:矮小体型
    		            PRINT 与其体型不符
    		    	PRINT 的粗大
    		    ELSEIF TALENT:尺寸傲人
    		        SIF TALENT:矮小体型
    		            PRINT 与其体型不符，
    		    	PRINT 尺寸傲人的
    		    ELSE
    		        PRINT 的
    		    ENDIF
    		ENDIF
        ENDIF
		PRINT 阴茎
	ELSE
		PRINT 的假阳具
	ENDIF
	PRINTFORML 插进来…
	;恋慕ルート追加メッセージ。自慰棒などを入れられているとダメ
	IF KANRAKU(TARGET) == 1 && MALE(TARGET) && !MALE(MASTER) && !ASSIPLAY && !TEQUIP:13 && !TEQUIP:14 && !TEQUIP:15 && !TEQUIP:45 && !TEQUIP:46 && !TEQUIP:47 && !TEQUIP:48
		IF TALENT:恋慕
			PRINTFORML 和%CALLNAME:PLAYER%做了普通恋人之间最亲密的事
			PRINTFORM %CALLNAME%非常开心…
		ELSEIF TALENT:相亲相爱
			PRINTFORML 和%CALLNAME:PLAYER%做了普通恋人之间最亲密的事
			PRINTFORM %CALLNAME%非常幸福…
		ENDIF
		TFLAG:52 = 1
	ELSEIF TALENT:137 || TALENT:139 || TALENT:140
		CALL HANYOU_JYOKYO_CSEX
		TFLAG:52 = 1
	ENDIF
ENDIF
;-------------------------------------------------
;207,使之打屁股
;-------------------------------------------------
@MESSAGE_COM207
PRINTFORM %CALLNAME:PLAYER%背对%CALLNAME%趴下，翘起屁股让
IF TALENT:男性 || TALENT:双性
	PRINT 他
ELSE
	PRINT 她
ENDIF
PRINTL 打
;嗜虐属性３以上
IF ABL:10 >= 3
	PRINTFORML %CALLNAME%兴奋地喘息着，将%CALLNAME:PLAYER%的屁股打得啪啪作响
	TFLAG:52 = 1
ENDIF
;-------------------------------------------------
;208,鞭子打たせる
;-------------------------------------------------
@MESSAGE_COM208
;嗜虐属性5以上
IF ABL:10 >= 5
	PRINTFORML %CALLNAME%刚听到命令就一把抢走鞭子，在%CALLNAME:PLAYER%的肌肤上留下了数条鲜红翻卷的条纹
	TFLAG:52 = 1
ELSE
	PRINTFORML %CALLNAME%接过鞭子挥舞起来
ENDIF
;-------------------------------------------------
;209,强制口交する
;-------------------------------------------------
@MESSAGE_COM209
PRINTFORM %CALLNAME:PLAYER%把%CALLNAME%拉过来，强行吞入了%CALLNAME%
;情欲のPALAMLV3以上
IF PALAM:6 >= PALAMLV:3
	PRINTFORM 已完全挺立
	IF TALENT:包茎
		PRINT ，裹着包皮
    	IF TALENT:孩童尺寸
    		SIF TALENT:矮小体型 == 0
    		    PRINT ，与其体型不符
		    PRINT 的小小
		ELSEIF TALENT:短小
		    SIF TALENT:高大体型
		        PRINT ，与其高大体型不符
		    PRINT 的短小
		ELSEIF TALENT:巨根
		    SIF TALENT:矮小体型
		        PRINT ，与其体型不符
		  	PRINT 的粗大
        ELSEIF TALENT:尺寸傲人
		    SIF TALENT:矮小体型
		        PRINT ，与其体型不符
		  	PRINT ，尺寸傲人的
		ELSE
	    	PRINT 的
	    ENDIF
	ELSE
   		IF TALENT:孩童尺寸
   		    SIF TALENT:矮小体型 == 0
   		        PRINT ，与其体型不符
	    	PRINT 的小小
	    ELSEIF TALENT:短小
	        SIF TALENT:高大体型
	            PRINT ，与其高大体型不符
	        PRINT 的短小
	    ELSEIF TALENT:巨根
	        SIF TALENT:矮小体型
	            PRINT ，与其体型不符
	    	PRINT 的粗大
	    ELSEIF TALENT:尺寸傲人
	        SIF TALENT:矮小体型
	            PRINT ，与其体型不符
	    	PRINT ，尺寸傲人的
	    ELSE
	    	PRINT 的
	    ENDIF
	ENDIF
ELSE
    IF TALENT:包茎
	    PRINT 裹着包皮
   	    IF TALENT:孩童尺寸
   		    SIF TALENT:矮小体型 == 0
   		        PRINT ，与其体型不符
	    	PRINT 的小小
	    ELSEIF TALENT:短小
	        SIF TALENT:高大体型
	            PRINT ，与其高大体型不符
	        PRINT 的短小
	    ELSEIF TALENT:巨根
	        SIF TALENT:矮小体型
	            PRINT ，与其体型不符
	    	PRINT 的粗大
	    ELSEIF TALENT:尺寸傲人
	        SIF TALENT:矮小体型
	            PRINT ，与其体型不符
	        PRINT ，尺寸傲人的
	    ELSE
	    	PRINT 的
	    ENDIF
	ELSE
   		IF TALENT:孩童尺寸
   		    SIF TALENT:矮小体型 == 0
   		       PRINT 与其体型不符
	    	PRINT 的小小
	    ELSEIF TALENT:短小
	        SIF TALENT:高大体型
	            PRINT 与其高大体型不符
	        PRINT 的短小
	    ELSEIF TALENT:巨根
	        SIF TALENT:矮小体型
	            PRINT 与其体型不符
	    	PRINT 的粗大
	    ELSEIF TALENT:尺寸傲人
	        SIF TALENT:矮小体型
	            PRINT 与其体型不符，
	    	PRINT 尺寸傲人的
	    ELSE
	        PRINT 的
	    ENDIF
	ENDIF
ENDIF
PRINT 阴茎…
;-------------------------------------------------
;210,舔足
;-------------------------------------------------
@MESSAGE_COM210
;调教者の受虐属性3以上
SIF ABL:PLAYER:9 >=3
	PRINT 像是要好好品尝一样
;恋慕か相亲相爱
SIF (TALENT:3 || TALENT:6) && !ASSIPLAY
	PRINT 仔细地
PRINT 舔舐着
PRINTFORM %CALLNAME%
;靴を履いている
IF TEQUIP:9
	PRINT 穿着
	A = 9
	CALL CLOTHES_NAME
ELSEIF TEQUIP:1
	PRINT 被
	A = 1
	CALL CLOTHES_NAME
	PRINT 包裹
ENDIF
PRINT 的脚…
;-------------------------------------------------
;211,逆强奸
;-------------------------------------------------
@MESSAGE_COM211
;前回と今回の调教者が同じで、前回の调教が逆强奸である場合
IF PREVCOM == 211 && ((ASSIPLAY && TFLAG:50) || (ASSIPLAY == 0 && TFLAG:50 == 0))
	PRINTFORM %CALLNAME:PLAYER%紧紧绞住%CALLNAME%的阴茎扭摆着腰…
;前回と今回の调教者が同じで、前回の调教が逆正常位である場合
ELSEIF PREVCOM == 205 && ((ASSIPLAY && TFLAG:50) || (ASSIPLAY == 0 && TFLAG:50 == 0)) && EXP:PLAYER:Ｖ经验 >= 30
	PRINTFORML %CALLNAME:PLAYER%绞紧体内的阴茎，起身将%CALLNAME%推倒骑乘，尽情地扭动腰肢…
;前回と今回の调教者が同じで、前回の调教が逆后背位である場合
ELSEIF PREVCOM == 206 && ((ASSIPLAY && TFLAG:50) || (ASSIPLAY == 0 && TFLAG:50 == 0)) && EXP:PLAYER:Ｖ经验 >= 30
	PRINTFORML %CALLNAME:PLAYER%绞紧体内的阴茎，起身将%CALLNAME%推倒骑乘，尽情地扭动腰肢…
ELSE
	PRINTFORM %CALLNAME:PLAYER%用自己
	;调教者が处女
	SIF TALENT:PLAYER:0
		PRINTFORM 未经人事
	PRINTFORM 的小穴吮吸吞吐着%CALLNAME%
	;奴隶の竿の有無
	IF TALENT:137 || TALENT:139 || TALENT:140
		IF TALENT:孩童尺寸
			SIF !TALENT:矮小体型
				PRINT 与体型不符
			PRINT 的小小
		ELSEIF TALENT:短小
			SIF TALENT:高大体型
				PRINT 与高大体型不符
			PRINT 的短小
		ELSEIF TALENT:巨根
			SIF TALENT:矮小体型
				PRINT 与体型不符
			PRINT 的粗大
		ELSEIF TALENT:尺寸傲人
			SIF TALENT:矮小体型
				PRINT 与体型不符，
			PRINT 尺寸傲人的
		ELSE
		    PRINT 的
		ENDIF
		PRINT 阴茎
	ELSE
		PRINT 的假阳具
	ENDIF
	PRINTFORM …
ENDIF

IF PALAM:快Ｃ < 100 && !TEQUIP:60 
	PRINTL
	IF ABL:受虐属性 == 0  && !(CFLAG:600 == 68)
		PRINTFORM %CALLNAME%投来了怨恨的目光
	ELSEIF ABL:受虐属性 <= 2
		PRINTFORM %CALLNAME%连连喊痛
	ELSE
		PRINTFORM %CALLNAME%沉醉在苦痛中
	ENDIF
	TFLAG:52 = 1
ENDIF
;-------------------------------------------------
;212,逆强奸肛
;-------------------------------------------------
@MESSAGE_COM212
;前回と今回の调教者が同じで、前回の调教が逆强奸肛である場合
IF PREVCOM == 212 && ((ASSIPLAY && TFLAG:50) || (ASSIPLAY == 0 && TFLAG:50 == 0))
	PRINTFORM %CALLNAME:PLAYER%紧紧绞住%CALLNAME%的阴茎执拗地扭摆着腰…
;前回と今回の调教者が同じで、前回の调教が逆正常位肛交である場合
ELSEIF PREVCOM == 213 && ((ASSIPLAY && TFLAG:50) || (ASSIPLAY == 0 && TFLAG:50 == 0)) && EXP:PLAYER:Ｖ经验 >= 30
	PRINTFORML %CALLNAME:PLAYER%绞紧体内的阴茎，起身将%CALLNAME%推倒骑乘，尽情地扭动腰肢…
;前回と今回の调教者が同じで、前回の调教が逆后背位肛交である場合
ELSEIF PREVCOM == 214 && ((ASSIPLAY && TFLAG:50) || (ASSIPLAY == 0 && TFLAG:50 == 0)) && EXP:PLAYER:Ｖ经验 >= 30
	PRINTFORML %CALLNAME:PLAYER%绞紧体内的阴茎，起身将%CALLNAME%推倒骑乘，尽情地扭动腰肢…
ELSE
	PRINTFORM %CALLNAME:PLAYER%用自己
	;调教者が后庭处子
	SIF TALENT:PLAYER:161
		PRINTFORM 未经人事
	PRINTFORM 的肛穴吮吸吞吐着%CALLNAME%
	;奴隶の竿の有無
	IF TALENT:137 || TALENT:139 || TALENT:140
		IF TALENT:孩童尺寸
			SIF !TALENT:矮小体型
				PRINT 与体型不符
			PRINT 的小小
		ELSEIF TALENT:短小
			SIF TALENT:高大体型
				PRINT 与高大体型不符
			PRINT 的短小
		ELSEIF TALENT:巨根
			SIF TALENT:矮小体型
				PRINT 与体型不符
			PRINT 的粗大
		ELSEIF TALENT:尺寸傲人
			SIF TALENT:矮小体型
				PRINT 与体型不符，
			PRINT 尺寸傲人的
		ELSE
		    PRINT 的
		ENDIF
		PRINT 阴茎
	ELSE
		PRINT 的假阳具
	ENDIF
	PRINTFORM …
ENDIF
IF PALAM:快Ｃ < 100 && !TEQUIP:60 
	PRINTL
	IF ABL:受虐属性 == 0 && !(CFLAG:600 == 68)
		PRINTFORM %CALLNAME%投来了怨恨的目光
	ELSEIF ABL:受虐属性 <= 2
		PRINTFORM %CALLNAME%连连喊痛
	ELSE
		PRINTFORM %CALLNAME%沉醉在苦痛中
	ENDIF
	TFLAG:52 = 1
ENDIF
;-------------------------------------------------
;213,逆正常位肛交
;-------------------------------------------------
@MESSAGE_COM213
;前回と今回の调教者が同じで、前回の调教が逆正常位肛交である場合
IF PREVCOM == 213 && ((ASSIPLAY && TFLAG:50) || (ASSIPLAY == 0 && TFLAG:50 == 0))
	;男性か双性か扶她である
	IF TALENT:137 || TALENT:139 || TALENT:140
		IF PALAM:快Ｃ < 1000 && (ABL:顺从 +  ABL:欲望 >= 3) && !RAND:2
			PRINTFORM %CALLNAME%一边品尝着%CALLNAME:PLAYER%狭窄肠道内的滋味一边活动起腰部…
		ELSE 
			CALL HANYOU_JYOKYO_CSEX
		ENDIF
	;捆绑式假阴茎を使用している場合
	ELSE
		PRINTFORM %CALLNAME%戴着捆绑式假阳具翻搅玩弄%CALLNAME:PLAYER%的肛穴…
	ENDIF
;同様に前回の调教が逆后背位肛交か逆强奸肛の場合
ELSEIF (PREVCOM == 214 || PREVCOM == 212) && ((ASSIPLAY && TFLAG:50) || (ASSIPLAY == 0 && TFLAG:50 == 0))
	PRINTFORML %CALLNAME:PLAYER%拔出体内的\@ PENIS(TARGET) ? 阴茎 # 假阳具 \@，仰面向上扭摆起腰部…
	IF KANRAKU(TARGET) == 1 && !ASSIPLAY && ABL:侍奉精神 >= 4 && PENIS(TARGET) && !RAND:2
		IF RAND:3 == 0
			PRINTFORM %CALLNAME%
			IF TEQUIP:14
				PRINT 忍受着肛塞给予的刺激，
			ELSEIF TEQUIP:15
				PRINT 忍受着后庭拉珠给予的刺激，
			ENDIF
			SIF ABL:技巧 < 4
				PRINT 尽管笨手笨脚，却仍
			PRINT 为了使主人满足，
			SIF (STAIN:PLAYER:0 < 2 || STAIN:PLAYER:0 == 16 || STAIN:PLAYER:0 == 17 || TALENT:脏污无视) && TEQUIP:20 == 0 && TEQUIP:25 == 0
				PRINT 一边接吻一边
			PRINT 拼命地活动着腰部…
		ELSEIF RAND:2 == 0
			PRINTFORML %CALLNAME%活动着腰部，像被浸泡在温水里似的露出满足的表情
			PRINTFORM 一次又一次呼唤了%CALLNAME:PLAYER%的名字…
		ELSEIF BASE:2 > 7000 && BASE:2 < 10000
			PRINTFORML 或许是临近高潮了，%CALLNAME%一脸忍耐的表情抱住%CALLNAME:PLAYER%加快了活塞运动…
		ENDIF
	ELSEIF TALENT:137 || TALENT:139 || TALENT:140
		CALL HANYOU_JYOKYO_CSEX
	ENDIF
	TFLAG:52 = 1
ELSE
	PRINTFORM %CALLNAME:PLAYER%仰躺着，向%CALLNAME%展示了自己
	;调教者が后庭处子
	SIF TALENT:PLAYER:161
		PRINTFORM 未经人事
	PRINTFORM 的肛穴，命令%CALLNAME%把
	;男性か双性か扶她である
	IF TALENT:137 || TALENT:139 || TALENT:140
		IF TALENT:孩童尺寸
			SIF !TALENT:矮小体型
				PRINT 与体型不符
			PRINT 的小小
		ELSEIF TALENT:短小
			SIF TALENT:高大体型
				PRINT 与高大体型不符
			PRINT 的短小
		ELSEIF TALENT:巨根
			SIF TALENT:矮小体型
				PRINT 与体型不符
			PRINT 的粗大
		ELSEIF TALENT:尺寸傲人
			SIF TALENT:矮小体型
				PRINT 与体型不符，
			PRINT 尺寸傲人的
		ELSE
		    PRINT 的
		ENDIF
		PRINT 阴茎
	ELSE
		PRINT 假阳具
	ENDIF
	PRINTFORM 插进来…
ENDIF
;-------------------------------------------------
;214,逆后背位肛交
;-------------------------------------------------
@MESSAGE_COM214
;前回と今回の调教者が同じで、前回の调教が逆后背位肛交である場合
IF PREVCOM == 214 && ((ASSIPLAY && TFLAG:50) || (ASSIPLAY == 0 && TFLAG:50 == 0))
	;男性か双性か扶她である
	IF TALENT:137 || TALENT:139 || TALENT:140
		IF PALAM:快Ｃ < 1000 && (ABL:顺从 +  ABL:欲望 >= 3) && !RAND:2
			PRINTFORM %CALLNAME%一边品尝着%CALLNAME:PLAYER%狭窄肠道内的滋味一边活动起腰部…
		ELSE 
			CALL HANYOU_JYOKYO_CSEX
		ENDIF
	;捆绑式假阴茎を使用している場合
	ELSE
		PRINTFORM %CALLNAME%戴着捆绑式假阳具翻搅玩弄%CALLNAME:PLAYER%的肛穴…
	ENDIF
;同様に前回の调教が逆正常位肛交か逆强奸肛の場合
ELSEIF (PREVCOM == 213 || PREVCOM == 212) && ((ASSIPLAY && TFLAG:50) || (ASSIPLAY == 0 && TFLAG:50 == 0))
	PRINTFORML %CALLNAME:PLAYER%拔出体内的\@ PENIS(TARGET) ? 阴茎 # 假阳具 \@，四肢着地趴下将%CALLNAME%吞入…
	IF KANRAKU(TARGET) == 1 && !ASSIPLAY && ABL:侍奉精神 >= 4 && PENIS(TARGET) && !RAND:2
		PRINTL
		IF RAND:3 == 0
			PRINTFORM %CALLNAME%
			IF TEQUIP:14
				PRINT 忍受着肛塞给予的刺激，
			ELSEIF TEQUIP:15
				PRINT 忍受着后庭拉珠给予的刺激，
			ENDIF
			SIF ABL:技巧 < 4
				PRINT 尽管笨手笨脚，却仍
			PRINTL 为了使主人满足，不断询问着%CALLNAME:PLAYER%的感受
			PRINT 一边积极爱抚着一边拼命地活动着腰部…
		ELSEIF RAND:2 == 0
			PRINTFORML %CALLNAME%活动着腰部，像被浸泡在温水里似的露出满足的表情
			PRINTFORM 一次又一次呼唤了%CALLNAME:PLAYER%的名字…
		ELSEIF BASE:2 > 7000 && BASE:2 < 10000
			PRINTFORML 或许是临近高潮了，%CALLNAME%一脸忍耐的表情抱住%CALLNAME:PLAYER%加快了活塞运动…
		ENDIF
	ELSEIF TALENT:137 || TALENT:139 || TALENT:140
		CALL HANYOU_JYOKYO_CSEX
	ENDIF
	TFLAG:52 = 1
ELSE
	PRINTFORM %CALLNAME:PLAYER%四肢着地趴下，向%CALLNAME%展示了自己
	;调教者が后庭处子
	SIF TALENT:PLAYER:161
		PRINTFORM 未经人事
	PRINTFORM 的肛穴，命令%CALLNAME%把
	;男性か双性か扶她である
	IF TALENT:137 || TALENT:139 || TALENT:140
		IF TALENT:孩童尺寸
			SIF !TALENT:矮小体型
				PRINT 与体型不符
			PRINT 的小小
		ELSEIF TALENT:短小
			SIF TALENT:高大体型
				PRINT 与高大体型不符
			PRINT 的短小
		ELSEIF TALENT:巨根
			SIF TALENT:矮小体型
				PRINT 与体型不符
			PRINT 的粗大
		ELSEIF TALENT:尺寸傲人
			SIF TALENT:矮小体型
				PRINT 与体型不符，
			PRINT 尺寸傲人的
		ELSE
		    PRINT 的
		ENDIF
		PRINT 阴茎
	ELSE
		PRINT 假阳具
	ENDIF
	PRINTFORM 插进来…
ENDIF
;-------------------------------------------------
;215,助手に犯させる
;-------------------------------------------------
@MESSAGE_COM215
;前回の调教が助手に犯させるであり、抜かないフラグがある場合
IF TFLAG:60 && TFLAG:709 > 0 && PREVCOM == 215
	PRINTFORM 当着%CALLNAME%的面继续与%CALLNAME:ASSI%身体交缠…
ELSE
	PRINTFORM 当着%CALLNAME%的面，用自己
	IF TFLAG:705
		SIF TALENT:MASTER:后庭处子
			PRINT 未经人事
		PRINT 的肛穴
	ELSE
		SIF TALENT:MASTER:处女 || TALENT:MASTER:再生处女
			PRINT 未经人事
		PRINT 的小穴
	ENDIF
	PRINTFORM 吞入了%CALLNAME:ASSI%的阴茎…
ENDIF
;-------------------------------------------------
;助手命令指令
;220,阴蒂爱抚命令
;-------------------------------------------------
@MESSAGE_COM220

;-------------------------------------------------
;221,阴茎爱抚命令
;-------------------------------------------------
@MESSAGE_COM221

;-------------------------------------------------
;222,胸爱抚命令
;-------------------------------------------------
@MESSAGE_COM222

;-------------------------------------------------
;223,手指插入命令
;-------------------------------------------------
@MESSAGE_COM223

;-------------------------------------------------
;224,肛门爱抚命令
;-------------------------------------------------
@MESSAGE_COM224

;-------------------------------------------------
;225,插入命令
;-------------------------------------------------
@MESSAGE_COM225

;-------------------------------------------------
;226,肛插入命令
;-------------------------------------------------
@MESSAGE_COM226

;-------------------------------------------------
;拡張特殊指令
;320,人を呼ぶ
;-------------------------------------------------
@MESSAGE_COM320
TFLAG:52 = 1
TFLAG:51 = 1
;誰もいない
IF TFLAG:151 == 0
	PRINTFORML 环视四周，感觉周围好像没有其他人
	PRINTFORM %CALLNAME:PLAYER%带着%CALLNAME:TARGET%走向人更多的地方
;誰もいない→気配を感じる
ELSEIF TFLAG:151 == 1
	PRINTFORM %CALLNAME:PLAYER%为吸引过路人的注意，故意发出了极大的声响
;気配を感じる→視線を感じる
ELSEIF TFLAG:151 == 2
	IF TFLAG:500
		PRINTFORML 感觉到了视线……是%OUTDOOR_GALLERY_NAME()%
	ELSE
		PRINTL 虽然不知道来者何人，但毫无疑问有人注意到了这边
	ENDIF
	PRINTFORM %CALLNAME:PLAYER%以淫猥的言辞呼唤着周围的人
;視線を感じる→演出费リーが姿を表す（集団視姦モードへ）
ELSEIF TFLAG:151 == 3
	PRINTFORML \@ TFLAG:500 ? %OUTDOOR_GALLERY_NAME()% # 视线的主人 \@们并未现身……
	PRINTFORM %CALLNAME:PLAYER%
	;顺从5以上or露出癖3以上
	IF ABL:0 >= 5 || ABL:8 >= 3
		PRINTFORML 命令%CALLNAME:TARGET%诉说自己是暴露狂，主动发出想要被视奸的恳求
	ELSE
		PRINT 挑衅周围，说着“反正都看到了不如再靠近一点怎样”之类的话
	ENDIF
;集団視姦モード
ELSEIF TFLAG:151 == 4
	PRINTFORML 用言辞煽动了以饥渴目光视奸着%CALLNAME:TARGET%的%OUTDOOR_GALLERY_NAME()%们
	PRINTFORM 充分地展现了%CALLNAME:TARGET%的痴态
;テンションMAX（輪姦実行可）
ELSEIF TFLAG:151 >= 5
	PRINTFORML %CALLNAME:PLAYER%不断地煽动着“观众席”的各位
	PRINTFORM %OUTDOOR_GALLERY_NAME()%们的兴奋度已达到无可比拟的高度
	PRINTL
	PRINTFORML 得到了%CALLNAME:PLAYER%的许可后，%OUTDOOR_GALLERY_NAME()%们一拥而上
	PRINTFORM 复数膨胀的兽欲包围住%CALLNAME:TARGET%，准备将其一遍遍蹂躏凌辱……
ENDIF

;-------------------------------------------------
;321,輪姦
;-------------------------------------------------
@MESSAGE_COM321
TFLAG:52 = 1
TFLAG:51 = 1
PRINTFORMW 兴奋度达到顶点的%OUTDOOR_GALLERY_NAME()%们用充血的眼睛注视着%CALLNAME%
IF TEQUIP:13 || ACHECK(TARGET)
    PRINTFORM %CALLNAME:PLAYER%展示般地将%CALLNAME%体内的
	SIF TEQUIP:13
		PRINT 自慰棒
	IF ACHECK(TARGET)
		SIF TEQUIP:13
			PRINT 和
		SIF TEQUIP:14
			PRINT 肛塞
		SIF TEQUIP:15
			PRINT 后庭拉珠
		SIF TEQUIP:21
			PRINT 肛门栓
		SIF TEQUIP:22
			PRINT 扩张气球
		SIF TEQUIP:23
			PRINT 肛门电极
	ENDIF
	PRINTL 缓缓拉出，粘液拉长成丝根根垂落
	TEQUIP:13 = 0
	TEQUIP:14 = 0
	TEQUIP:15 = 0
	TEQUIP:21 = 0
	TEQUIP:22 = 0
	TEQUIP:23 = 0
	PRINT 随即
ENDIF
PRINTFORM %CALLNAME:PLAYER%一把将
IF PALAM:6 >= PALAMLV:3
	PRINT 沉沦在情欲中的
ELSEIF PALAM:11 >= PALAMLV:3
	PRINT 因恐惧感颤抖着身体的
ELSEIF PALAM:9 >= PALAMLV:3
	PRINT 屈辱地颤抖着身体的
ENDIF
PRINTFORMW %CALLNAME%推到了%OUTDOOR_GALLERY_NAME()%们之中
PRINTL 
PRINTFORMW %OUTDOOR_GALLERY_NAME()%们的视线像线一般缠绕着%CALLNAME%的身体，紧紧将其注视着。
;[处女][再生处女]でＶを犯す
IF (TALENT:0 || TALENT:1) && !TALENT:158 && !TALENT:140
	;子供、狗
	IF TFLAG:500 == 3 || TFLAG:500 == 4
		PRINTFORML %OUTDOOR_GALLERY_NAME()%们好奇地环绕着%CALLNAME%
		PRINTFORMW 用膨胀起来的阴茎尝试性地贯穿了%CALLNAME%未经人事的处女小穴
	ELSE
		PRINTFORML 发现%CALLNAME%还是处女的%OUTDOOR_GALLERY_NAME()%们欢呼着，争先恐后地扑上%CALLNAME%的身体
		PRINTFORMW 用自己粗长的阳具狠狠贯穿了%CALLNAME%的处女小穴
	ENDIF
	PRINTFORM %OUTDOOR_GALLERY_NAME()%们将%CALLNAME%的
ELSE
	PRINTFORM %OUTDOOR_GALLERY_NAME()%们侵犯着%CALLNAME%的身体，将%CALLNAME%的
ENDIF
IF !TALENT:158
	;Ｖを犯す　Ｖがあって[攻]でない
	SIF !TALENT:140
		PRINT 小穴、
	;Ａを犯す　[攻]でない
	PRINT 肛门、
ENDIF
;逆强奸　[受]でなく、阴茎があり、道具が付いておらず、演出费リーが通行人/カップル/人外のどれか
SIF !TALENT:157 && PENIS(TARGET) && !CCHECK(TARGET) && (TFLAG:500 == 1 || TFLAG:500 == 2 || TFLAG:500 == 8)
	PRINT 阴茎乃至
;逆强奸无で攻だと口交とぶっかけくらいしかすることなくて困る
PRINTL 全身都射满了黏糊糊又暖融融的精液
PRINTFORMW %CALLNAME%身上所有可以用来使用的地方都被无数次侵犯了个彻底…
PRINTL 
PRINTW ・
PRINTW ・
PRINTW ・
PRINTFORM 尽情发泄过欲望后，%OUTDOOR_GALLERY_NAME()%们离去了

;状況と演出费リーを重置
TFLAG:151 = 0
TFLAG:500 = 0
;-------------------------------------------------
;拡張触手指令
;300,触手乳腺責め
;-------------------------------------------------
@MESSAGE_COM300
IF TEQUIP:40
	PRINTFORML 极细的触手从%CALLNAME%的乳腺中拔了出来
	SIF TALENT:149
		PRINTFORML %CALLNAME%得到解放的一瞬间，乳汁呈放射状喷射了出来
ELSE
	PRINTFORM 极细的触手靠近了%CALLNAME%
	IF TALENT:110
		PRINT 薄薄的胸部
	ELSEIF TALENT:109
		PRINT 小小的胸脯
	ELSEIF TALENT:111
		IF TALENT:140
			PRINT 结实的胸肌
		ELSE
			PRINT 丰满的乳房
		ENDIF
	ELSEIF TALENT:112
		IF TALENT:140
			PRINT 过分发达的胸肌
		ELSE
			PRINT 过分丰满的豪乳
		ENDIF
	ELSE
		PRINT 形状良好的胸部
	ENDIF
	PRINTW ，侵入了双丘前端的乳腺…
	PRINTL 
	IF TALENT:149 == 0
		IF PALAM:13 > PALAMLV:5
			PRINTFORM 是触手所分泌的液体的作用吗，%CALLNAME%的乳孔里开始流出白色的母乳
		ELSE
			PRINTFORM 双乳里的异物感令%CALLNAME%的额头浮现出汗珠
		ENDIF
	ELSE
		PRINTFORM 乳汁的润滑让侵入变得更加容易了，%CALLNAME%的乳腺开始习惯触手的蹂躏…
	ENDIF
ENDIF
TFLAG:52 = 1
TFLAG:51 = 1
;-------------------------------------------------
;301,触手Ｇスポ責め
;-------------------------------------------------
@MESSAGE_COM301
IF TEQUIP:13 == 2
	PRINTFORM 触手继续对%CALLNAME%阴道内最敏感脆弱的部分展开折磨凌辱
ELSEIF TEQUIP:13 == 3
	PRINTFORML 潜入%CALLNAME%子宫内的触手们突然回流
	PRINTFORM 下一瞬间，触手们开始折磨起%CALLNAME%的阴道内壁，轮流攻击%CALLNAME%最敏感脆弱的部分
ELSE
	PRINTFORML 潜入%CALLNAME%体内的触手们在%CALLNAME%的阴道里撑出一个鼓起的小包
	PRINTFORM 从外面就能看出它们在里面是如何淫猥地活动
ENDIF
TFLAG:52 = 1
TFLAG:51 = 1
;-------------------------------------------------
;302,触手子宮責め
;-------------------------------------------------
@MESSAGE_COM302
IF TEQUIP:13 == 2
	PRINTFORML 在%CALLNAME%阴道壁横行无忌的触手们转移了目标，这次看上的是%CALLNAME%的子宫
	PRINTFORM 无数的触手把%CALLNAME%的肚子塞得满满当当，就像是怀孕了一样…
ELSEIF TEQUIP:13 == 3
	PRINTFORM 在%CALLNAME%子宫里蠕动的触手像是要抚平子宫膜的每一处皱褶一样来回挤压抚弄着
ELSE
	PRINTFORML %CALLNAME%体内的触手们往更深更隐秘的地方钻了进去
	PRINTFORML 发现%CALLNAME%敞开无防备的子宫口后，便成群结队一拥而入
	PRINTFORM 无数的触手把%CALLNAME%的肚子塞得满满当当，就像是怀孕了一样…
ENDIF
TFLAG:52 = 1
TFLAG:51 = 1
;-------------------------------------------------
;303,触手前立腺責め　
;-------------------------------------------------
@MESSAGE_COM303
IF TEQUIP:14 == 2
	PRINTFORM 触手继续对%CALLNAME%肛门内最敏感脆弱的部分展开折磨凌辱
ELSEIF TEQUIP:14 == 3
	PRINTFORML 潜入%CALLNAME%直肠内的触手们突然回流
	PRINTFORM 下一瞬间，触手们开始折磨起%CALLNAME%的肛穴内壁，轮流攻击%CALLNAME%最敏感脆弱的部分
ELSE
	PRINTFORML 潜入%CALLNAME%屁股的触手们在%CALLNAME%的肚子上撑出一个鼓起的小包
	PRINTFORM 从外面就能看出它们在里面是如何淫猥地活动
ENDIF
TFLAG:52 = 1
TFLAG:51 = 1
;-------------------------------------------------
;304,触手ヴァギナ拡張
;-------------------------------------------------
@MESSAGE_COM304
PRINTFORML %CALLNAME%的小穴被黄瓜一样粗大的触手玩弄翻搅着
IF ABL:4 <= 1
	PRINTFORM 触手拔出后，%CALLNAME%的小穴痉挛一般颤抖抽动
ELSEIF ABL:4 == 2
	PRINTFORM 触手拔出后，%CALLNAME%的小穴夸张地张着口，好一会儿才恢复
ELSEIF ABL:4 == 3
	PRINTFORM 触手拔出后，%CALLNAME%的小穴维持着被扩张时的形状，难以恢复原状
ELSEIF ABL:4 == 4
	PRINTFORML 触手拔出后，%CALLNAME%小穴里的媚肉都翻滚出来抽动着
	PRINTFORM 从大大敞开的阴道口里还能看到里面嫩红的子宫口
ELSE
	PRINTFORM %CALLNAME%大睁着眼注视着自己被扩张的小穴，神情恍惚
ENDIF
TFLAG:52 = 1
;-------------------------------------------------
;305,触手肛拡張
;-------------------------------------------------
@MESSAGE_COM305
PRINTFORML %CALLNAME%的肛穴被黄瓜一样粗大的触手玩弄翻搅着
PRINTL 
IF ABL:5 <= 1
	PRINTFORM 触手拔出后，%CALLNAME%的肛穴痉挛一般颤抖抽动
ELSEIF ABL:5 == 2
	PRINTFORM 触手拔出后，%CALLNAME%的肛穴夸张地张着口，好一会儿才恢复
ELSEIF ABL:5 == 3
	PRINTFORM 触手拔出后，%CALLNAME%的肛穴维持着被扩张时的形状，难以恢复原状
ELSEIF ABL:5 == 4
	PRINTFORML 触手拔出后，%CALLNAME%肛穴里的媚肉都翻滚出来了
	PRINTFORM 从大大敞开的肛门口里还能看到里面蠕动着的肠道
ELSE
	PRINTFORM %CALLNAME%大睁着眼注视着自己被扩张的肛门，神情恍惚
ENDIF
TFLAG:52 = 1
;-------------------------------------------------
;306,触手両穴拡張
;-------------------------------------------------
@MESSAGE_COM306
PRINTFORML %CALLNAME%的小穴和肛门被黄瓜一样粗大的触手玩弄翻搅着
PRINTL 
IF ABL:4 >= 5 && ABL:5 >= 5
	PRINTFORML %CALLNAME%回味着两穴被同时扩张的余韵
	PRINTFORML %CALLNAME%的阴道口和肛门口都大大地敞开着
	PRINTW 即便在触手抽离后也没有恢复原状
ELSEIF ABL:4 > 4 || ABL:5 > 4
	PRINTFORML %CALLNAME%浑身瘫软，时不时抽搐一下
	PRINTW 被触手狠狠侵犯扩张过的小穴和肛门保持着大敞的模样，晶莹的粘液从穴口里流了出来
ENDIF
TFLAG:52 = 1
;-------------------------------------------------
;307,触手产卵
;-------------------------------------------------
@MESSAGE_COM307
IF TEQUIP:41
	PRINTFORML 触手在%CALLNAME%的子宫里追加产卵了
ELSEIF TEQUIP:13 == 2
	PRINTFORML %CALLNAME%的小穴被触手侵犯蹂躏着，浑身痉挛似的颤抖
	PRINTL 最终触手们强行撑开子宫口，在里面产下了一颗又一颗的卵
ELSEIF TEQUIP:13 == 1
	PRINTFORML 钻入%CALLNAME%性器的触手突然向着更深处突进
	PRINTL %CALLNAME%的子宫口里被触手产下了一颗又一颗的卵
ELSE
	PRINTFORML 极粗的触手钻入%CALLNAME%的阴道，在其子宫里产下了一颗又一颗的卵
ENDIF
TFLAG:52 = 1
TFLAG:51 = 1
;-------------------------------------------------
;308,触手肛産卵
;-------------------------------------------------
@MESSAGE_COM308
IF TEQUIP:42
	PRINTFORML 触手在%CALLNAME%的直肠里追加产卵了
ELSEIF TEQUIP:14
	PRINTFORML %CALLNAME%的肛门被触手侵犯蹂躏着，浑身痉挛似的颤抖
	PRINTL 最终被触手在直肠深处产下了一颗又一颗的卵
ELSE
	PRINTFORML 极粗的触手钻入%CALLNAME%的阴道，在其直肠里产下了一颗又一颗的卵
ENDIF
TFLAG:52 = 1
TFLAG:51 = 1
;-------------------------------------------------
;派生指令郡
;500,３Ｐ
;-------------------------------------------------
@MESSAGE_COM500
;助手が调教中
IF ASSIPLAY
	;主人がヴァギナ插入
	IF TFLAG:40 == 1
		PRINTFORM 插入了%CALLNAME%的小穴，
	;主人が肛插入
	ELSEIF TFLAG:40 == 2
		;竿の有無で分岐
		IF TALENT:MASTER:140 || TALENT:MASTER:139 || TALENT:MASTER:137
			PRINTFORM 把阴茎插入了%CALLNAME%的肛门，
		ELSE
			PRINTFORM 戴着假阳具插入了%CALLNAME%的肛门，
		ENDIF
	;主人が口辱
	ELSEIF TFLAG:40 == 3
		PRINTFORM 让%CALLNAME%含着%CALLNAME:PLAYER%的阴茎，
	;主人が逆
	ELSEIF TFLAG:40 == 9
		;竿の有無で分岐
		IF TALENT:TARGET:140 || TALENT:TARGET:139 || TALENT:TARGET:137
			PRINTFORM %CALLNAME:PLAYER%用下身含住%CALLNAME%的阴茎後，
		ELSE
			PRINTFORM %CALLNAME:PLAYER%用下身含住%CALLNAME%的假阳具後，
		ENDIF
	ENDIF
	;助手がヴァギナ插入
	IF TFLAG:41 == 1
		PRINTFORM 同时命令%CALLNAME:ASSI%贯穿了%CALLNAME%的小穴…
	;助手が肛插入
	ELSEIF TFLAG:41 == 2
		PRINTFORM 同时命令%CALLNAME:ASSI%贯穿了%CALLNAME%的肛门…
	;助手が口辱
	ELSEIF TFLAG:41 == 3
		PRINTFORM 同时命令%CALLNAME:ASSI%侵犯了%CALLNAME%的口腔…
	;助手が逆
	ELSEIF TFLAG:41 == 9
		;竿の有無で分岐
		IF TALENT:TARGET:140 || TALENT:TARGET:139 || TALENT:TARGET:137
			PRINTFORM 同时命令%CALLNAME:ASSI%用下身含住%CALLNAME%的阴茎…
		ELSE
			PRINTFORM 同时命令%CALLNAME:ASSI%用下身含住%CALLNAME%的假阳具…
		ENDIF
	ENDIF
ELSE
	;助手がヴァギナ插入
	IF TFLAG:41 == 1
		PRINTFORM 让%CALLNAME:ASSI%贯穿%CALLNAME%小穴的同时，
	;助手が肛插入
	ELSEIF TFLAG:41 == 2
		PRINTFORM 让%CALLNAME:ASSI%贯穿%CALLNAME%肛门的同时，
	;助手が口辱
	ELSEIF TFLAG:41 == 3
		PRINTFORM 让%CALLNAME:ASSI%侵犯%CALLNAME%口腔的同时，
	;助手が逆
	ELSEIF TFLAG:41 == 9
		;竿の有無で分岐
		IF TALENT:ASSI:140 || TALENT:ASSI:139 || TALENT:ASSI:137
			PRINTFORM %CALLNAME:ASSI%的下身被%CALLNAME%阴茎侵犯的同时，
		ELSE
			PRINTFORM %CALLNAME:ASSI%的下身被%CALLNAME%假阳具侵犯的同时，
		ENDIF
	ENDIF
	;主人がヴァギナ插入
	IF TFLAG:40 == 1
		PRINTFORM %CALLNAME:PLAYER%插入了%CALLNAME%的小穴…
	;主人が肛插入
	ELSEIF TFLAG:40 == 2
		;竿の有無で分岐
		IF TALENT:MASTER:140 || TALENT:MASTER:139 || TALENT:MASTER:137
			PRINTFORM %CALLNAME:PLAYER%把阴茎插入了%CALLNAME%的肛门…
		ELSE
			PRINTFORM %CALLNAME:PLAYER%戴着假阳具插入了%CALLNAME%的肛门…
		ENDIF
	;主人が口辱
	ELSEIF TFLAG:40 == 3
		PRINTFORM %CALLNAME:PLAYER%蹂躏起%CALLNAME%的口腔…
	;主人が逆
	ELSEIF TFLAG:40 == 9
		;竿の有無で分岐
		IF TALENT:140 || TALENT:139 || TALENT:137
			PRINTFORM %CALLNAME:PLAYER%的下身也吞吐著%CALLNAME%的阴茎…
		ELSE
			PRINTFORM %CALLNAME:PLAYER%的下身也吞吐著%CALLNAME%的假阳具…
		ENDIF
	ENDIF
ENDIF
;-------------------------------------------------
;501,６９式
;-------------------------------------------------
@MESSAGE_COM501
PRINTFORM %CALLNAME%
;助手が调教中
IF ASSIPLAY
	PRINTFORM 和%CALLNAME:ASSI%
ELSE
    PRINTFORM 和%CALLNAME:PLAYER%
ENDIF
PRINTFORM 首尾相对舔舐着彼此的性器…
;-------------------------------------------------
;502,岩清水
;-------------------------------------------------
@MESSAGE_COM502
PRINTFORM %CALLNAME:PLAYER%让%CALLNAME%坐到自己脸上，噗滋噗滋地舔舐着%CALLNAME%的小穴…
;-------------------------------------------------
;503,Gスポット刺激
;-------------------------------------------------
@MESSAGE_COM503
;自慰棒插入中
IF TEQUIP:13
	PRINTFORML %CALLNAME:PLAYER%有技巧地操纵着自慰棒，在%CALLNAME%敏感的Ｇ点上来回顶弄
	;情欲のPALAMLV3以上＋Ｖ感觉LV3以上
	IF PALAM:6 >= PALAMLV:3 && ABL:4 >= 3
		PRINTFORM %CALLNAME%的蜜穴里分泌出大量爱液，将自慰棒弄得黏黏糊糊…
	;情欲のPALAMLV3未満＋Ｖ感觉LV3以上
	ELSEIF PALAM:6 < PALAMLV:3 && ABL:4 >= 3
		PRINTFORM %CALLNAME%在自慰棒的刺激下皱着眉头…
	;情欲のPALAMLV3以上＋Ｖ感觉LV3未満
	ELSEIF PALAM:6 >= PALAMLV:3 && ABL:4 < 3
		PRINTFORM %CALLNAME%脸颊绯红，发出细微的呻吟…
	;情欲のPALAMLV3未満＋Ｖ感觉LV3未満
	ELSE
		PRINTFORM %CALLNAME%露出了些许苦闷的表情…
	ENDIF
ELSE
	PRINTFORML %CALLNAME:PLAYER%弯曲手指，玩弄刺激着%CALLNAME%小穴里的Ｇ点
	;Ｖ经验が150より多い
	IF EXP:0 > 150
		PRINTFORM %CALLNAME%流着口水绞紧了内部，主动扭摆腰肢追逐快乐…
	;Ｖ经验が120より多い
	ELSEIF EXP:0 > 120
		PRINTFORM %CALLNAME%最敏感的地方被玩弄侵犯，不由得皱紧眉头，微红着脸，口中泄出炽热的吐息…
	;Ｖ经验が80より多い
	ELSEIF EXP:0 > 80
		PRINTFORM %CALLNAME%有些沉醉于手指在自己体内抽插戳弄的感觉…
	;Ｖ经验が50より多い
	ELSEIF EXP:0 > 50
		PRINTFORM %CALLNAME%享受着被手指玩弄小穴的快感…
	;Ｖ经验が30より多い
	ELSEIF EXP:0 > 30
		PRINTFORM %CALLNAME%忍受着体内的异物感，艰难地吐息着…
	ELSE
		PRINTFORM %CALLNAME%因体内的异物感微微发抖…
	ENDIF
ENDIF
TFLAG:52 = 1
;-------------------------------------------------
;504,乱れ牡丹
;-------------------------------------------------
@MESSAGE_COM504
IF ASSIPLAY
	PRINTFORM %CALLNAME:ASSI%
ELSE
    PRINTFORM %CALLNAME:PLAYER%
ENDIF
;-男の場合限定
IF TALENT:140
	;前回と今回の调教者が同じで、前回の调教が乱れ牡丹であり、抜かないフラグがある場合
	IF TFLAG:60 && PREVCOM == 504 && ((ASSIPLAY && TFLAG:50) || (ASSIPLAY == 0 && TFLAG:50 == 0))
		PRINTFORML 将手伸向%CALLNAME%抖动摇晃的阴茎以及跟自己结合相连的地方
		PRINTFORML 为让%CALLNAME%看得更清楚，愈加大力地顶弄着怀里人的腰…
	;同様に、前回の调教が后背位肛交か骑乘位肛交である場合
	ELSEIF TFLAG:60 && (PREVCOM == 63 || PREVCOM == 67) && ((ASSIPLAY && TFLAG:50) || (ASSIPLAY == 0 && TFLAG:50 == 0))
		;竿の有無で分岐
		IF TALENT:PLAYER:140 || TALENT:PLAYER:139 || TALENT:PLAYER:137
			PRINTFORML 并未抽离埋在%CALLNAME%体内的阴茎，只是将其从背后抱紧，拉开了%CALLNAME%的双腿…
		ELSE
			PRINTFORML 并未抽离埋在%CALLNAME%体内的假阳具，只是将其从背后抱紧，拉开了%CALLNAME%的双腿…
		ENDIF
	;同様に、前回の调教が正常位肛交か对面座位肛交である場合
	ELSEIF TFLAG:60 && (PREVCOM == 62 || PREVCOM == 70) && ((ASSIPLAY && TFLAG:50) || (ASSIPLAY == 0 && TFLAG:50 == 0))
		PRINTFORM 保持着插入的状态将%CALLNAME%的身体翻了个转
		;前回の调教が正常位肛交
		IF PREVCOM == 62
			PRINTFORM 抱在怀里
		ELSE
			PRINTFORM 紧紧搂住
		ENDIF
		PRINTFORML ，拉开了%CALLNAME%的双腿…
	ELSE
		PRINTFORM 抱住%CALLNAME%的身体，慢慢插入%CALLNAME%
		;润滑LV4以上
		SIF PALAM:4 >= PALAMLV:4
			PRINTFORM 被肠液濡湿
		PRINTFORML 的肛门，拉开了%CALLNAME%的双腿…
	ENDIF
	;露出癖
	IF ABL:8 >= 5
		PRINTFORM %CALLNAME%双目痴迷地看着镜中自己淫荡的身姿，发出了欢喜的呻吟…
	ELSEIF ABL:8 >= 4
		PRINTFORM %CALLNAME%凝视着镜中自己臣服于快感，既羞耻又快乐的痴态…
	ELSEIF ABL:8 >= 2
		PRINTFORM %CALLNAME%满脸羞红，间或泄露出动听的呻吟…
	ELSEIF ABL:8 >= 1
		IF MALE(TARGET)
			PRINTFORM %CALLNAME%别过脸去，不想看镜中不堪入目的自己，身体也因屈辱和羞耻微微颤抖…
		ELSE
			PRINTFORM %CALLNAME%双手捂面，因羞耻浑身颤抖，不愿接受镜中那个不堪入目的人就是自己…
		ENDIF
	ENDIF
ELSE
	;前回と今回の调教者が同じで、前回の调教が乱れ牡丹であり、抜かないフラグがある場合
	IF TFLAG:60 && PREVCOM == 504 && ((ASSIPLAY && TFLAG:50) || (ASSIPLAY == 0 && TFLAG:50 == 0))
		PRINTFORML 再次伸手在%CALLNAME%的結合部与胸部游走抚摸，像是为了让他更清楚似地摆动起胯部…
	;同様に、前回の调教が后背位か骑乘位である場合
	ELSEIF TFLAG:60 && (PREVCOM == 61 || PREVCOM == 66) && ((ASSIPLAY && TFLAG:50) || (ASSIPLAY == 0 && TFLAG:50 == 0))
		;竿の有無で分岐
		IF TALENT:PLAYER:140 || TALENT:PLAYER:139 || TALENT:PLAYER:137
			PRINTFORML 并未抽离埋在%CALLNAME%体内的阴茎，只是将其从背后抱紧，拉开了%CALLNAME%的双腿…
		ELSE
			PRINTFORML 并未抽离埋在%CALLNAME%体内的假阳具，只是将其从背后抱紧，拉开了%CALLNAME%的双腿…
		ENDIF
	;同様に、前回の调教が正常位か对面座位である場合
	ELSEIF TFLAG:60 && (PREVCOM == 60 || PREVCOM == 68) && ((ASSIPLAY && TFLAG:50) || (ASSIPLAY == 0 && TFLAG:50 == 0))
		PRINTFORM 保持着插入的状态将%CALLNAME%的身体翻了个转
		;前回の调教が正常位
		IF PREVCOM == 60
			PRINTFORM 抱在怀里
		ELSE
			PRINTFORM 紧紧搂住
		ENDIF
		PRINTFORML ，拉开了%CALLNAME%的双腿…
	ELSE	
		PRINTFORM 抱住%CALLNAME%的身体，慢慢插入%CALLNAME%
		;润滑LV4以上
		SIF PALAM:4 >= PALAMLV:4
			PRINTFORM 湿漉漉
		;矮小体型
		IF TALENT:100
			PRINTFORM 的狭窄小穴
		ELSE
			PRINTFORM 的小穴
		ENDIF
		PRINTFORML ，拉开了%CALLNAME%的双腿…
	ENDIF
	;露出癖
	IF ABL:8 >= 5
		PRINTFORM %CALLNAME%双目痴迷地看着镜中自己淫荡的身姿，发出了欢喜的呻吟…
	ELSEIF ABL:8 >= 4
		PRINTFORM %CALLNAME%凝视着镜中自己臣服于快感，既羞耻又快乐的痴态…
	ELSEIF ABL:8 >= 2
		PRINTFORM %CALLNAME%满脸羞红，间或泄露出动听的呻吟…
	ELSEIF ABL:8 >= 1
		IF MALE(TARGET)
			PRINTFORM %CALLNAME%别过脸去，不想看镜中不堪入目的自己，身体也因屈辱和羞耻微微颤抖…
		ELSE
			PRINTFORM %CALLNAME%双手捂面，因羞耻浑身颤抖，不愿接受镜中那个不堪入目的人就是自己…
		ENDIF
	ENDIF
ENDIF
TFLAG:52 = 1
;-------------------------------------------------
;505,骑乘位揉乳自慰
;-------------------------------------------------
@MESSAGE_COM505
;前回と今回の调教者が同じで、前回の调教が骑乘位であり、抜かないフラグがある場合
IF TFLAG:60 && PREVCOM == 66 && ((ASSIPLAY && TFLAG:50) || (ASSIPLAY == 0 && TFLAG:50 == 0))
	PRINTFORML 保持着插入的状态抱起%CALLNAME%，腰部往上顶弄着…
	PRINTFORM %CALLNAME%
;前回と今回の调教者が同じで、前回の调教が骑乘位肛交であり、抜かないフラグがある場合
ELSEIF TFLAG:60 && PREVCOM == 67 && ((ASSIPLAY && TFLAG:50) || (ASSIPLAY == 0 && TFLAG:50 == 0))
	;竿の有無で分岐
	IF TALENT:PLAYER:140 || TALENT:PLAYER:139 || TALENT:PLAYER:137
		PRINTFORM %CALLNAME%绞紧了插在体内的阴茎，一边
	ELSE
		PRINTFORM %CALLNAME%绞紧了插在体内的假阳具，一边
	ENDIF
ELSE
	PRINTFORM %CALLNAME%用
	;前回の调教が骑乘位
	IF PREVCOM == 66
		PRINTFORM 小穴
	;前回の调教が骑乘位肛交
	ELSEIF PREVCOM == 67
		PRINTFORM 肛门
	ENDIF
	PRINTFORM 紧咬着%CALLNAME:PLAYER%的
	;竿の有無で分岐
	IF TALENT:PLAYER:140 || TALENT:PLAYER:139 || TALENT:PLAYER:137
		PRINTFORM 阴茎
	ELSE
		PRINTFORM 假阳具
	ENDIF
	PRINTFORM ，一边
ENDIF
;挤奶器装着中
IF TEQUIP:17
	PRINTFORM 用挤奶器刺激着自己
ELSE
	PRINTFORM 用手指玩弄着自己
ENDIF
IF TALENT:137 || TALENT:140
	PRINTFORM 的乳头
ELSE
	;贫乳
	SIF TALENT:109
		PRINTFORM 薄薄的
	;巨乳
	IF TALENT:111
		IF TALENT:140
			PRINTFORM 结实的
		ELSE
		PRINTFORM 丰满的
		ENDIF
	ENDIF
	;爆乳
	SIF TALENT:112
		PRINTFORM 硕大的
	;母乳体质
	SIF TALENT:149
		PRINTFORM 垂落着乳汁的
	PRINTFORM 胸部
ENDIF
PRINTFORM ，腰部迎合般扭动着…
;-------------------------------------------------
;506,插入Ｇ点玩弄
;-------------------------------------------------
@MESSAGE_COM506
PRINTFORM 抓着%CALLNAME%的腰在其敏感点来回戳弄…
;-------------------------------------------------
;507,插入子宫口玩弄
;-------------------------------------------------
@MESSAGE_COM507
PRINTFORM 紧紧地抓着%CALLNAME%的腰侵入其最深处，狠狠蹂躏%CALLNAME%的子宫…
;-------------------------------------------------
;508,６９乳交
;-------------------------------------------------
@MESSAGE_COM508
PRINTFORM %CALLNAME%的下身被%CALLNAME:PLAYER%的舌头玩弄着，一边努力用自己
;贫乳
IF TALENT:109
	PRINTFORML 薄薄的胸部摩擦着%CALLNAME:PLAYER%的阴茎…
;巨乳
ELSEIF TALENT:111
	IF TALENT:140
		PRINTFORML 结实的胸部包着%CALLNAME:PLAYER%的阴茎上下摩擦…
	ELSE
		PRINTFORML 丰满的胸部包着%CALLNAME:PLAYER%的阴茎上下摩擦…
	ENDIF
;爆乳
ELSEIF TALENT:112
	IF TALENT:140
		PRINTFORML 硕大发达的胸膛包着%CALLNAME:PLAYER%的阴茎揉搓摩擦…
	ELSE
		PRINTFORML 硕大丰满的豪乳包着%CALLNAME:PLAYER%的阴茎揉搓摩擦…
	ENDIF
ELSE
	PRINTFORML 的胸部摩擦%CALLNAME:PLAYER%的阴茎…
ENDIF
TFLAG:52 = 1
;-------------------------------------------------
;509,搾乳
;-------------------------------------------------
@MESSAGE_COM509
PRINTFORM %CALLNAME%
;情欲のPALAMLV3以上
SIF PALAM:6 >= PALAMLV:3
	PRINTFORM 的身子微微颤抖，
;贫乳＋矮小体型
IF TALENT:100 && TALENT:109
	PRINTFORM 仅只是膨胀程度
;贫乳
ELSEIF TALENT:109
	PRINTFORM 薄薄
;巨乳
ELSEIF TALENT:111
	IF TALENT:140
		PRINTFORM 胸肌从手中溢出
	ELSE
		PRINTFORM 乳肉从手中溢出
	ENDIF	
;爆乳
ELSEIF TALENT:112
	PRINTFORM 一只手无法掌控
ENDIF
;情欲のPALAMLV3以上＋恋慕か相亲相爱
IF PALAM:6 >= PALAMLV:3 && (TALENT:3 || TALENT:6)
	PRINTFORML 的胸部被大力地挤弄着
	PRINTFORM %CALLNAME%发出呻吟，强调似的挺起自己
	;母乳体质
	SIF TALENT:149
		PRINTFORM 滴着乳汁
	PRINTFORML 的胸部索求着更加强烈的刺激…
;情欲のPALAMLV3以上
ELSEIF PALAM:6 > PALAMLV:3
	PRINTFORM 的胸部被大力地挤弄着…
ELSE
	PRINTFORM 的胸部被挤弄着…
ENDIF
;-------------------------------------------------
;510,Ｗ素股
;-------------------------------------------------
@MESSAGE_COM510
;淫乱か娼妇＋情欲のPALAMLV5以上
IF (TALENT:淫乱 || TALENT:娼妇) && PALAM:6 >= PALAMLV:5
	PRINTFORM 脸上浮现出沉溺在快乐中的表情，
;恋慕か相亲相爱＋情欲のPALAMLV5以上＋助手ではない
ELSEIF (TALENT:恋慕 || TALENT:相亲相爱) && PALAM:6 >= PALAMLV:6 && !ASSIPLAY
	PRINTFORM 带着一副快要融化了的表情，
;情欲のPALAMLV6以上
ELSEIF PALAM:6 >= PALAMLV:6
	PRINTFORM 带着一副沉溺于情欲之中的表情，
;淫乱か娼妇
ELSEIF (TALENT:淫乱 || TALENT:娼妇)
	PRINTFORM 眼神淫乱的
ELSEIF TALENT:反抗 || TALENT:自大
	PRINTFORM 眼里噙着悔恨的
ELSEIF TALENT:缺乏感情
	PRINTFORM 有些无精打采的
ELSEIF TALENT:害羞 || TALENT:性保守 || TALENT:胆怯
	PRINTFORM 有点害羞的
ENDIF
;阴茎相互摩擦->素股
IF PREVCOM == 127
	PRINTFORML %CALLNAME%和%CALLNAME:ASSI%
	PRINTFORM 先后在%CALLNAME:PLAYER%的股间摩擦着自己的阴茎，彼此的阴茎也时有碰撞…
;磨镜->素股
ELSE
	PRINTFORM %CALLNAME%和%CALLNAME:ASSI%一起用私处夹住阴茎摩擦着…
ENDIF
;-------------------------------------------------
;511,双枪口交
;-------------------------------------------------
@MESSAGE_COM511
PRINTFORM %CALLNAME%同时侍奉着%PLAYER_NAME(1)%和%CALLNAME:ASSI%的阴茎…

;-------------------------------------------------
;512,二輪刺し
;-------------------------------------------------
@MESSAGE_COM512
;助手が调教中
IF ASSIPLAY
	;主人の竿の有無
	IF TALENT:MASTER:140 || TALENT:MASTER:139 || TALENT:MASTER:137
		;助手の竿の有無
		IF TALENT:ASSI:140 || TALENT:ASSI:139 || TALENT:ASSI:137
			PRINTFORML %CALLNAME:PLAYER%保持插入状态强行扩张%CALLNAME%的小穴
			PRINTFORML 让其除自己的阴茎外又多接纳了一根%CALLNAME:ASSI%的阴茎
		ELSE
			PRINTFORML %CALLNAME:PLAYER%保持插入状态强行扩张%CALLNAME%的小穴
			PRINTFORML 让其除自己的阴茎外又接纳了%CALLNAME:ASSI%的假阳具
		ENDIF
	ELSE
		;助手の竿の有無
		IF TALENT:ASSI:140 || TALENT:ASSI:139 || TALENT:ASSI:137
			PRINTFORML %CALLNAME:PLAYER%保持插入状态强行扩张%CALLNAME%的小穴
			PRINTFORML 让其除自己的假阳具外又接纳了%CALLNAME:ASSI%的阴茎
		ELSE
			PRINTFORML %CALLNAME:PLAYER%保持插入状态强行扩张%CALLNAME%的小穴
			PRINTFORML 让其除自己的假阳具外又多接纳了一根%CALLNAME:ASSI%的假阳具
		ENDIF
	ENDIF
ELSE
	;主人の竿の有無で分岐
	IF TALENT:MASTER:140 || TALENT:MASTER:139 || TALENT:MASTER:137
		;助手の竿の有無
		IF TALENT:ASSI:140 || TALENT:ASSI:139 || TALENT:ASSI:137
			PRINTFORML 在%CALLNAME:ASSI%插入%CALLNAME%的同时强行扩张%CALLNAME%的小穴
			PRINTFORML 让其除%CALLNAME:ASSI%的阴茎外又多接纳了一根自己的阴茎
		ELSE
			PRINTFORML 在%CALLNAME:ASSI%插入%CALLNAME%的同时强行扩张%CALLNAME%的小穴
			PRINTFORML 让其除%CALLNAME:ASSI%的假阳具外又接纳了自己的阴茎
		ENDIF
	ELSE
		;助手の竿の有無
		IF TALENT:ASSI:140 || TALENT:ASSI:139 || TALENT:ASSI:137
			PRINTFORML 在%CALLNAME:ASSI%插入%CALLNAME%的同时强行扩张%CALLNAME%的小穴
			PRINTFORML 让其除%CALLNAME:ASSI%的阴茎外又接纳了自己的假阳具
		ELSE
			PRINTFORML 在%CALLNAME:ASSI%插入%CALLNAME%的同时强行扩张%CALLNAME%的小穴
			PRINTFORML 让其除%CALLNAME:ASSI%的假阳具外又多接纳了一根自己的假阳具
		ENDIF
	ENDIF
ENDIF
;Ｖ扩张经验
IF EXP:52 >= 30
	;竿の有無で分岐
	IF TALENT:MASTER:140 || TALENT:MASTER:139 || TALENT:MASTER:137
		IF TALENT:ASSI:140 || TALENT:ASSI:139 || TALENT:ASSI:137
			PRINTFORM %CALLNAME%神情恍惚，沉浸在被两根阴茎一起抽插蹂躏的快乐之中…
		ELSE
			PRINTFORM %CALLNAME%神情恍惚，沉浸在被两个人同时抽插蹂躏的快乐之中…
		ENDIF
	ELSE
		PRINTFORM %CALLNAME%神情恍惚，沉浸在被两个人同时抽插蹂躏的快乐之中…
	ENDIF
	TFLAG:52 = 1
ENDIF

;-------------------------------------------------
;513,二輪刺し肛
;-------------------------------------------------
@MESSAGE_COM513
;助手が调教中
IF ASSIPLAY
	;主人の竿の有無
	IF TALENT:MASTER:140 || TALENT:MASTER:139 || TALENT:MASTER:137
		PRINTFORML %CALLNAME:PLAYER%保持插入状态强行扩张%CALLNAME%的肛穴
		;助手の竿の有無
		IF TALENT:ASSI:140 || TALENT:ASSI:139 || TALENT:ASSI:137
			PRINTFORM 让其除自己的阴茎外又多接纳了一根%CALLNAME:ASSI%的阴茎
		ELSE
			PRINTFORM 让其除自己的阴茎外又接纳了%CALLNAME:ASSI%的假阳具
		ENDIF
	ELSE
		PRINTFORML %CALLNAME:PLAYER%保持插入状态强行扩张%CALLNAME%的肛穴
		;助手の竿の有無
		IF TALENT:ASSI:140 || TALENT:ASSI:139 || TALENT:ASSI:137
			PRINTFORM 让其除自己的假阳具外又接纳了%CALLNAME:ASSI%的阴茎
		ELSE
			PRINTFORM 让其除自己的假阳具外又多接纳了一根%CALLNAME:ASSI%的假阳具
		ENDIF
	ENDIF
ELSE
	;主人の竿の有無で分岐
	IF TALENT:MASTER:140 || TALENT:MASTER:139 || TALENT:MASTER:137
		;助手の竿の有無
		IF TALENT:ASSI:140 || TALENT:ASSI:139 || TALENT:ASSI:137
			PRINTFORML 在%CALLNAME:ASSI%插入%CALLNAME%的同时强行扩张%CALLNAME%的肛穴
			PRINTFORML 让其除%CALLNAME:ASSI%的阴茎外又多接纳了一根自己的阴茎
		ELSE
			PRINTFORML 在%CALLNAME:ASSI%插入%CALLNAME%的同时强行扩张%CALLNAME%的肛穴
			PRINTFORML 让其除%CALLNAME:ASSI%的假阳具外又接纳了自己的阴茎
		ENDIF
	ELSE
		;助手の竿の有無
		IF TALENT:ASSI:140 || TALENT:ASSI:139 || TALENT:ASSI:137
			PRINTFORML 在%CALLNAME:ASSI%插入%CALLNAME%的同时强行扩张%CALLNAME%的肛穴
			PRINTFORM 让其除%CALLNAME:ASSI%的阴茎外又接纳了自己的假阳具
		ELSE
			PRINTFORML 在%CALLNAME:ASSI%插入%CALLNAME%的同时强行扩张%CALLNAME%的肛穴
			PRINTFORM 让其除%CALLNAME:ASSI%的假阳具外又多接纳了一根自己的假阳具
		ENDIF
	ENDIF
ENDIF
;Ａ扩张经验
IF EXP:53 >= 30
	;竿の有無で分岐
	IF TALENT:MASTER:140 || TALENT:MASTER:139 || TALENT:MASTER:137
		IF TALENT:ASSI:140 || TALENT:ASSI:139 || TALENT:ASSI:137
			PRINTFORM %CALLNAME%神情恍惚，沉浸在被两根阴茎一起抽插蹂躏肛门的快乐之中…
		ELSE
			PRINTFORM %CALLNAME%神情恍惚，沉浸在被两个人同时插蹂蹂躏肛门的快乐之中…
		ENDIF
	ELSE
		PRINTFORM %CALLNAME%神情恍惚，沉浸在被两个人同时插蹂蹂躏肛门的快乐之中…
	ENDIF
	TFLAG:52 = 1
ENDIF

;-------------------------------------------------
;514,駅弁
;-------------------------------------------------
@MESSAGE_COM514
;前回と今回の调教者が同じで、抜かないフラグがある場合
IF TFLAG:60 && ((ASSIPLAY && TFLAG:50) || (ASSIPLAY == 0 && TFLAG:50 == 0))
	;前回の调教が对面立位
	IF PREVCOM == 74
		PRINTFORM 插入小穴的同时
	;前回の调教が对面立位肛
	ELSE
		PRINTFORM 插入肛门的同时
	ENDIF
	PRINTFORM 抱起了%CALLNAME%的身子，一边抽插
ELSE
	PRINTFORM 抱起%CALLNAME%的身子
	;前回の调教が对面立位
	IF PREVCOM == 74
		PRINTFORM 一边抽插小穴
	;前回の调教が对面立位肛
	ELSE
		PRINTFORM 一边抽插肛门
	ENDIF
ENDIF
PRINTFORM 一边摇摇晃晃地走动…
;-------------------------------------------------
;515,打屁股做爱
;-------------------------------------------------
@MESSAGE_COM515
PRINTFORM 一边抽插%CALLNAME%的小穴一边拍打%CALLNAME%的屁股，直到那两瓣肉圆又红又肿…
;-------------------------------------------------
;516,打屁股肛
;-------------------------------------------------
@MESSAGE_COM516
PRINTFORM 一边抽插%CALLNAME%的肛穴一边拍打%CALLNAME%的屁股，直到那两瓣肉圆又红又肿…
;-------------------------------------------------
;517,首絞め做爱
;-------------------------------------------------
@MESSAGE_COM517
PRINTFORM 插入%CALLNAME%小穴的同时掐住了%CALLNAME%的脖子…
;-------------------------------------------------
;518,首絞め肛
;-------------------------------------------------
@MESSAGE_COM518
PRINTFORM 插入%CALLNAME%肛门的同时掐住了%CALLNAME%的脖子…
;-------------------------------------------------
;590,前立腺刺激
;-------------------------------------------------
@MESSAGE_COM590
;肛塞插入中
IF TEQUIP:14
	PRINTFORML %CALLNAME:PLAYER%有技巧地操纵着肛塞，在%CALLNAME%敏感的前列腺上来回顶弄
	;情欲のPALAMLV3以上＋Ａ感觉LV3以上
	IF PALAM:6 >= PALAMLV:3 && ABL:5 >= 3
		PRINTFORM %CALLNAME%的后穴里分泌出大量腺液，将肛塞弄得黏黏糊糊…
	;情欲のPALAMLV3未満＋Ａ感觉LV3以上
	ELSEIF PALAM:6 < PALAMLV:3 && ABL:5 >= 3
		PRINTFORM %CALLNAME%在肛塞的刺激下皱着眉头…
	;情欲のPALAMLV3以上＋Ａ感觉LV3未満
	ELSEIF PALAM:6 >= PALAMLV:3 && ABL:5 < 3
		PRINTFORM %CALLNAME%脸颊绯红，发出细微的呻吟…
	;情欲のPALAMLV3未満＋Ａ感觉LV3未満
	ELSE
		PRINTFORM %CALLNAME%露出了些许苦闷的表情…
	ENDIF
ELSE
	PRINTFORML %CALLNAME:PLAYER%弯曲手指，玩弄刺激着%CALLNAME%的前列腺
	;Ａ感觉LV7以上
	IF ABL:5 >= 6
		PRINTFORM %CALLNAME%流着口水收紧了屁股，主动扭摆腰肢追逐快乐…
	;Ａ感觉LV5
	ELSEIF ABL:5 == 5
		PRINTFORM %CALLNAME%最敏感的地方被玩弄折磨，不由皱紧眉头微红着脸，口中泄出炽热的吐息…
	;Ａ感觉LV4
	ELSEIF ABL:5 == 4
		PRINTFORM %CALLNAME%有些沉醉于手指在自己屁股里抽插戳弄的感觉…
	;Ａ感觉LV3
	ELSEIF ABL:5 == 3
		PRINTFORM %CALLNAME%享受着被手指玩弄后穴的快感…
	;Ａ感觉LV2
	ELSEIF ABL:5 == 2
		PRINTFORM %CALLNAME%忍受着体内的异物感，艰难地吐息着…
	;Ａ感觉LV2未満
	ELSE
		PRINTFORM %CALLNAME%因体内的异物感微微发抖…
	ENDIF
ENDIF
TFLAG:52 = 1
;-------------------------------------------------
;591,插入前立腺責め
;-------------------------------------------------
@MESSAGE_COM591
IF ASSIPLAY
	PRINTFORM %CALLNAME:ASSI%
ELSE
    PRINTFORM %CALLNAME:PLAYER%
ENDIF
PRINTFORML 抓着%CALLNAME%的腰在其肠道内的前列腺上来回戳弄…
;Ａ感觉LV7以上
IF ABL:5 >= 7
	PRINTFORM %CALLNAME%流着口水收紧了屁股，主动扭摆腰肢追逐快乐…
;Ａ感觉LV5
ELSEIF ABL:5 == 5
	PRINTFORM %CALLNAME%最敏感的地方被玩弄折磨，不由皱紧眉头微红着脸，口中泄出炽热的吐息…
;Ａ感觉LV4
ELSEIF ABL:5 == 4
	PRINTFORM %CALLNAME%有些沉醉于前列腺被抽插戳弄的感觉…
;Ａ感觉LV3
ELSEIF ABL:5 == 3
	PRINTFORM %CALLNAME%享受着被前列腺被玩弄的快感…
;Ａ感觉LV2
ELSEIF ABL:5 == 2
	PRINTFORM %CALLNAME%在强烈的刺激里艰难地吐息…
ELSE
	PRINTFORM %CALLNAME%因强烈的刺激微微发抖…
ENDIF
TFLAG:52 = 1
;-------------------------------------------------
;592,騎乗阴茎自慰
;-------------------------------------------------
@MESSAGE_COM592
;前回と今回の调教者が同じで、前回の调教が骑乘位であり、抜かないフラグがある場合
IF TFLAG:60 && PREVCOM == 66 && ((ASSIPLAY && TFLAG:50) || (ASSIPLAY == 0 && TFLAG:50 == 0))
	PRINTFORML 保持插入小穴的状态抱起%CALLNAME%，腰部摆动不断往上顶撞着…
	;顺从LV3未満
	IF ABL:0 < 3
		PRINTFORM 被命令自慰的%CALLNAME%
		;高傲
		SIF TALENT:15
			PRINTFORM 表现出极高的自尊心，
		;刚强
		IF TALENT:刚强
			PRINT 屈辱地咬着唇，
		;反抗
		ELSEIF TALENT:反抗
			PRINT 投来了抗拒的视线，于是再次下达了同样的命令，
		;无动于衷、缺乏感情
		ELSEIF TALENT:无动于衷 || TALENT:缺乏感情
			PRINT 面无表情地沉默着，
		;胆怯、おと无い
		ELSEIF TALENT:胆怯 || TALENT:老实
			PRINT 指尖颤抖着
		;害羞
		ELSEIF TALENT:害羞
			PRINT 双耳通红地
		ELSE
			PRINT 生涩地
		ENDIF
	;陷落(恋慕以外)
	ELSEIF KANRAKU(TARGET) == 2 || KANRAKU(TARGET) == 3
		PRINTFORML %CALLNAME%对自慰的命令没有表现出任何的反抗，完全就是一副牝犬的样子
	ELSE
		PRINTFORM 调教的成果出来了，%CALLNAME%遵从命令，
	ENDIF
;前回と今回の调教者が同じで、前回の调教が骑乘位肛交であり、抜かないフラグがある場合
ELSEIF TFLAG:60 && PREVCOM == 67 && ((ASSIPLAY && TFLAG:50) || (ASSIPLAY == 0 && TFLAG:50 == 0))
	;顺从LV3未満
	IF ABL:0 < 3
		PRINTFORM 被命令自慰的%CALLNAME%
		;高傲
		SIF TALENT:15
			PRINTFORM 表现出极高的自尊心，
		;刚强
		IF TALENT:刚强
			PRINT 屈辱地咬着唇，
		;反抗
		ELSEIF TALENT:反抗
			PRINT 投来了抗拒的视线，于是再次下达了同样的命令，
		;无动于衷、缺乏感情
		ELSEIF TALENT:无动于衷 || TALENT:缺乏感情
			PRINT 面无表情地沉默着，
		;胆怯、おと无い
		ELSEIF TALENT:胆怯 || TALENT:老实
			PRINT 指尖颤抖着
		;害羞
		ELSEIF TALENT:害羞
			PRINT 双耳通红地
		ELSE
			PRINT 生涩地
		ENDIF
	;陷落(恋慕以外)
	ELSEIF KANRAKU(TARGET) == 2 || KANRAKU(TARGET) == 3
		PRINTFORML %CALLNAME%对自慰的命令没有表现出任何的反抗，完全就是一副牝犬的样子
	ELSE
		PRINTFORM 调教的成果出来了，%CALLNAME%遵从命令，
	ENDIF
	PRINTFORM 一边夹紧屁股里的\@ PENIS(PLAYER) ? 阴茎 # 假阳具 \@，一边
ELSE
	;顺从LV3未満
	IF ABL:0 < 3
		PRINTFORM 被命令自慰的%CALLNAME%
		;高傲
		SIF TALENT:15
			PRINTFORM 表现出极高的自尊心，
		;刚强
		IF TALENT:刚强
			PRINT 屈辱地咬着唇，
		;反抗
		ELSEIF TALENT:反抗
			PRINT 投来了抗拒的视线，于是再次下达了同样的命令，
		;无动于衷、缺乏感情
		ELSEIF TALENT:无动于衷 || TALENT:缺乏感情
			PRINT 面无表情地沉默着，
		;胆怯、おと无い
		ELSEIF TALENT:胆怯 || TALENT:老实
			PRINT 指尖颤抖着
		;害羞
		ELSEIF TALENT:害羞
			PRINT 双耳通红地
		ELSE
			PRINT 生涩地
		ENDIF
	;陷落(恋慕以外)
	ELSEIF KANRAKU(TARGET) == 2 || KANRAKU(TARGET) == 3
		PRINTFORML %CALLNAME%对自慰的命令没有表现出任何的反抗，完全就是一副牝犬的样子
	ELSE
		PRINTFORM 调教的成果出来了，%CALLNAME%遵从命令，
	ENDIF
	PRINTFORM 一边夹紧
	;前回の调教が骑乘位
	IF PREVCOM == 66
		PRINTFORM 小穴
	;前回の调教が骑乘位肛交
	ELSEIF PREVCOM == 67
		PRINTFORM 屁股
	ENDIF
	PRINTFORM 里%CALLNAME:PLAYER%的\@ PENIS(PLAYER) ? 阴茎 # 假阳具 \@，一边
ENDIF
;●ここから阴茎自慰関連のテ接吻ト
;视频拍摄中
SIF TEQUIP:28
	PRINTFORM 在镜头前
;露出癖LV3以上
SIF ABL:8 >= 3
	PRINTFORML 高调地展示着身体，
;自慰棒＋肛塞
IF TEQUIP:13 && KANRAKU(TARGET) == 2
	PRINTFORML 亲自抽插起埋在小穴里的自慰棒
;肛塞
ELSEIF TEQUIP:14 && KANRAKU(TARGET) == 2
	PRINTFORML 亲自拔插起吞在肛穴里的肛塞
ENDIF
PRINTFORM %CALLNAME%在%CALLNAME:PLAYER%身上大开双腿，
IF TALENT:137 || TALENT:140 || TALENT:139
	PRINTFORM 双手搓动着自己
    ;情欲のPALAMLV3以上
    IF PALAM:6 >= PALAMLV:3
    	PRINTFORM 已完全挺立
    	IF TALENT:包茎
    		PRINT ，裹着包皮
        	IF TALENT:孩童尺寸
        		SIF TALENT:矮小体型 == 0
        		    PRINT ，与其体型不符
    		    PRINT 的小小
    		ELSEIF TALENT:短小
    		    SIF TALENT:高大体型
    		        PRINT ，与其高大体型不符
    		    PRINT 的短小
    		ELSEIF TALENT:巨根
    		    SIF TALENT:矮小体型
    		        PRINT ，与其体型不符
    		  	PRINT 的粗大
            ELSEIF TALENT:尺寸傲人
    		    SIF TALENT:矮小体型
    		        PRINT ，与其体型不符
    		  	PRINT ，尺寸傲人的
    		ELSE
    	    	PRINT 的
    	    ENDIF
    	ELSE
       		IF TALENT:孩童尺寸
       		    SIF TALENT:矮小体型 == 0
       		        PRINT ，与其体型不符
    	    	PRINT 的小小
    	    ELSEIF TALENT:短小
    	        SIF TALENT:高大体型
    	            PRINT ，与其高大体型不符
    	        PRINT 的短小
    	    ELSEIF TALENT:巨根
    	        SIF TALENT:矮小体型
    	            PRINT ，与其体型不符
    	    	PRINT 的粗大
    	    ELSEIF TALENT:尺寸傲人
    	        SIF TALENT:矮小体型
    	            PRINT ，与其体型不符
    	    	PRINT ，尺寸傲人的
    	    ELSE
    	    	PRINT 的
    	    ENDIF
    	ENDIF
    ELSE
        IF TALENT:包茎
    	    PRINT 裹着包皮
       	    IF TALENT:孩童尺寸
       		    SIF TALENT:矮小体型 == 0
       		        PRINT ，与其体型不符
    	    	PRINT 的小小
    	    ELSEIF TALENT:短小
    	        SIF TALENT:高大体型
    	            PRINT ，与其高大体型不符
    	        PRINT 的短小
    	    ELSEIF TALENT:巨根
    	        SIF TALENT:矮小体型
    	            PRINT ，与其体型不符
    	    	PRINT 的粗大
    	    ELSEIF TALENT:尺寸傲人
    	        SIF TALENT:矮小体型
    	            PRINT ，与其体型不符
    	        PRINT ，尺寸傲人的
    	    ELSE
    	    	PRINT 的
    	    ENDIF
    	ELSE
       		IF TALENT:孩童尺寸
       		    SIF TALENT:矮小体型 == 0
       		       PRINT 与其体型不符
    	    	PRINT 的小小
    	    ELSEIF TALENT:短小
    	        SIF TALENT:高大体型
    	            PRINT 与其高大体型不符
    	        PRINT 的短小
    	    ELSEIF TALENT:巨根
    	        SIF TALENT:矮小体型
    	            PRINT 与其体型不符
    	    	PRINT 的粗大
    	    ELSEIF TALENT:尺寸傲人
    	        SIF TALENT:矮小体型
    	            PRINT 与其体型不符，
    	    	PRINT 尺寸傲人的
    	    ELSE
    	        PRINT 的
    	    ENDIF
    	ENDIF
    ENDIF
	PRINTFORM 阴茎…
ELSE
	PRINTFORM 手指玩弄着自己的小穴…
ENDIF
;自慰棒＋Ｖ感觉LV5以上＋Ｃ感觉LV5以上＋Ｂ感觉LV5以上＋情欲のPALAMLV4以上
IF TEQUIP:13 && ABL:4 >= 5 && ABL:3 >= 5 && ABL:6 >= 5 && PALAM:6 >= PALAMLV:4
	PRINTL 
	PRINTFORML 爱液四散飞溅，但%CALLNAME%已经一点都顾不上了
	PRINTFORM 现在的%CALLNAME%只知道拼命抽插自慰棒来慰藉自己了…
;肛塞＋Ａ感觉LV5以上＋Ｃ感觉LV5以上＋Ｂ感觉LV5以上＋情欲のPALAMLV4以上
ELSEIF TEQUIP:14 && ABL:5 >= 5 && ABL:3 >= 5 && ABL:6 >= 5 && PALAM:6 >= PALAMLV:4
	PRINTL 
	PRINTFORML %CALLNAME%颤抖着身体不停用自慰棒突击自己的肛穴
	PRINTFORM 每一次挖搅自己的后面都仿佛要高潮一样高调呻吟着…
;自慰成瘾LV3以上＋Ｃ感觉LV5以上＋Ｂ感觉LV5以上＋情欲のPALAMLV4以上
ELSEIF ABL:15 >= 3 && ABL:3 >= 5 && ABL:6 >= 5 && PALAM:6 >= PALAMLV:4 && !RAND:2
	PRINTL 
	PRINTFORML %CALLNAME%一边娇喘着一边专心致志地玩弄自己的\@ PENIS(TARGET) ? 阴茎 # 小穴 \@
	PRINTFORM 就算想要停下来，%CALLNAME%的手也不会允许…
ELSE
	PRINTL 
	CALL HANYOU_JYOKYO_C_JII
ENDIF
TFLAG:52 = 1
;-------------------------------------------------
;593,插入中阴茎爱抚
;-------------------------------------------------
@MESSAGE_COM593
IF (TFLAG:706 == 1 && TFLAG:707 == 1) || (TFLAG:706 == 0 && TFLAG:708 == 1) 
	PRINTFORM 一边操干着%CALLNAME%的小穴，一边
ELSEIF (TFLAG:706 == 1 && TFLAG:707 == 2) || (TFLAG:706 == 0 && TFLAG:708 == 2) 
	PRINTFORM 一边操干着%CALLNAME%的后穴，一边
ELSE
	PRINTFORM 一边在%CALLNAME%体内抽插，一边
ENDIF
;前回调教が后背位など背面の場合
SIF PREVCOM == 61 || PREVCOM == 63 || PREVCOM == 69 || PREVCOM == 71 || PREVCOM == 76 || PREVCOM == 77 || PREVCOM == 504 || PREVCOM == 515 || PREVCOM == 516 || PREVCOM == 591
	PRINTFORM 收回了紧握其腰胯的手，
;着衣プ雷
IF TALENT:PLAYER:58 && (TEQUIP:2 || TEQUIP:4 > 1 || TEQUIP:6 == 7)
    PRINTFORM 揉弄起
	;下身外衣か布偶装を着ている
	IF TEQUIP:4 > 1 || TEQUIP:6 == 7
		IF TEQUIP:4
			A = 4
		ELSE
			A = 6
		ENDIF
		CALL CLOTHES_NAME
	;下身内衣を着ている
	ELSEIF TEQUIP:2
		A = 2
		CALL CLOTHES_NAME
	ENDIF
	PRINTFORM 下%CALLNAME%的阴茎…
;飞机杯使用中
ELSEIF TEQUIP:12
	;奴隶が男で、调教者の技巧が3以上のテ接吻ト
	SIF TALENT:140 && ABL:PLAYER:2 >= 3
		PRINTFORM 肆意揉弄%CALLNAME%的睾丸，并
	PRINTFORM  摆弄起套在阴茎上的飞机杯给予刺激…
ELSE
    PRINTFORM 用指腹
	;丝袜系
	IF TEQUIP:1 == 5 || TEQUIP:1 == 6
	    PRINTFORM 隔着
		SIF TEQUIP:1 == 5
			PRINT 丝袜
		SIF TEQUIP:1 == 6
			PRINT 网袜
	PRINTFORM 摩擦玩弄起%CALLNAME%
	ELSE
		IF RAND:3 == 0
			PRINTFORM 淫靡地摇晃起
		ELSEIF RAND:2 == 0
			PRINTFORM 上下撸动起
		ELSE
			PRINTFORM 把玩起
	 	ENDIF
	 	PRINTFORM %CALLNAME%
	ENDIF
	IF TALENT:孩童尺寸
		PRINT 小小
	ELSEIF TALENT:短小
		PRINT 短小
	ELSEIF TALENT:巨根 || TALENT:尺寸傲人
		PRINT 粗大
	ENDIF
	PRINTFORM 的阴茎
	;奴隶が男で、调教者の技巧が3以上のテ接吻ト
	SIF TALENT:140 && ABL:PLAYER:2 >= 3
		PRINTFORM ，同时肆意揉弄%CALLNAME%的睾丸
	PRINTL …
ENDIF
;乱れ牡丹の場合
IF PREVCOM == 504
	;露出癖
	IF ABL:8 >= 5
		PRINTL 
		PRINTFORM %CALLNAME%双目痴迷地看着镜中自己淫荡的身姿，发出了欢喜的呻吟…
	ELSEIF ABL:8 >= 4
		PRINTL 
		PRINTFORM %CALLNAME%凝视着镜中自己臣服于快感，既羞耻又快乐的痴态…
	ELSEIF ABL:8 >= 2
		PRINTL 
		PRINTFORM %CALLNAME%满脸羞红，间或泄露出动听的呻吟…
	ENDIF
	TFLAG:52 = 1
ELSEIF TALENT:男性
	IF PALAM:快Ｃ < 1500
	ELSE
		TFLAG:52 = 1
		PRINTL 
		PRINTFORM %CALLNAME%
		IF PALAM:快Ｃ < 3000
			IF PALAM:快Ａ < 500
				IF EXP:Ａ经验 >= 75
					PRINT 被玩弄的前面渐渐有了感觉
				ELSEIF EXP:Ａ经验 >= 30
					PRINT 虽然感到疼痛，但被玩弄的前面渐渐地起了反应
				ELSEIF EXP:Ａ经验 >= 10
					PRINT 虽然感到疼痛，但被玩弄的前面传来了阵阵酥麻
				ELSE
					PRINT 虽然十分疼痛，但前面被玩弄所带来的些微快感稍微减轻了这种痛苦
				ENDIF
			ELSEIF PALAM:快Ａ < 1500
				IF EXP:Ａ经验 >= 75
					PRINT 除疼痛外还有了其他的感觉
				ELSEIF EXP:Ａ经验 >= 30
					PRINT 努力捕捉着疼痛以外的感觉
				ELSEIF EXP:Ａ经验 >= 10
					PRINT 虽然感到疼痛，但被玩弄的前面渐渐有了快感
				ELSE
					PRINT 承受着痛苦，唯有前面被玩弄所带来的些许刺激能稍稍将之减轻
				ENDIF
			ELSEIF PALAM:快Ａ < 3000
				IF EXP:Ａ经验 >= 75
					PRINT 隐约有了微妙的快感
				ELSEIF EXP:Ａ经验 >= 30
					PRINT 除疼痛外还感到了下身传来的丝丝酥麻
				ELSEIF EXP:Ａ经验 >= 10
					PRINT 在强烈的疼痛中捕捉到了其他的感觉
				ELSE
					PRINT 承受着痛苦的同时，前方所受到的刺激带来了疼痛外的别的感觉
				ENDIF
			ELSEIF PALAM:快Ａ < 6000
					PRINT 在后穴被抽插的过程里产生了明显的快感
			ELSEIF PALAM:快Ａ < 8000
					PRINT 在倒错的快感中颤抖痉挛
			ELSEIF PALAM:快Ａ < 10000
					PRINT 在被抽插后穴的过程中濒临绝顶了
			ENDIF
		ELSEIF PALAM:快Ｃ < 6000
			IF PALAM:快Ａ < 500
				IF EXP:Ａ经验 >= 75
					PRINT 被玩弄的前面有了明显的感觉
				ELSEIF EXP:Ａ经验 >= 30
					PRINT 虽然感到疼痛，但被玩弄的前面有了明显的感觉
				ELSEIF EXP:Ａ经验 >= 10
					PRINT 虽然十分痛苦，好在前面被玩弄的酥麻快感能够稍稍减轻这种疼痛
				ELSE
					PRINT 承受着剧痛，唯有被玩弄的前面所带来的酥麻快感能聊以慰藉
				ENDIF
			ELSEIF PALAM:快Ａ < 1500
				IF EXP:Ａ经验 >= 75
					PRINT 被玩弄的前面有了明显的感觉，后面除疼痛外也有了微妙的快感
				ELSEIF EXP:Ａ经验 >= 30
					PRINT 被玩弄的前面有了明显的感觉，后面在强烈的疼痛中也捕捉到了其他的感受
				ELSEIF EXP:Ａ经验 >= 10
					PRINT 虽然感到疼痛，但被玩弄的前面有了明显的感觉
				ELSE
					PRINT 虽然十分痛苦，好在前面被玩弄的酥麻快感能够稍稍减轻这种疼痛
				ENDIF
			ELSEIF PALAM:快Ａ < 3000
				IF EXP:Ａ经验 >= 75
					PRINT 被玩弄的前面有了明显的感觉，后面也隐约有了微妙的快感
				ELSEIF EXP:Ａ经验 >= 30
					PRINT 被玩弄的前面有了明显的感觉，后面除疼痛外还感到了丝丝酥麻
				ELSEIF EXP:Ａ经验 >= 10
					PRINT 在强烈的疼痛中捕捉到了其他的感觉，前面也起了反应
				ELSE
					PRINT 承受着痛苦的同时，前方所受到的刺激也带来了明显的感觉
				ENDIF
			ELSEIF PALAM:快Ａ < 6000
					PRINT 前后同时被蹂躏，产生了明显的感觉
			ELSEIF PALAM:快Ａ < 8000
					PRINT 被玩弄的前面有了明显的感觉，后面则在倒错的快感中颤抖痉挛
			ELSEIF PALAM:快Ａ < 10000
					PRINT 被玩弄的前面有了明显的感觉，后面则快乐得濒临绝顶了
			ENDIF
		ELSEIF PALAM:快Ｃ < 8000
			IF PALAM:快Ａ < 500
				IF EXP:Ａ经验 >= 75
					PRINT 因前方被玩弄而浑身战栗，仿佛下一秒就会高潮
				ELSEIF EXP:Ａ经验 >= 30
					PRINT 后方虽然十分疼痛，前面却太过有感觉以至浑身战栗
				ELSEIF EXP:Ａ经验 >= 10
					PRINT 后面承受着疼痛，但前面太过有感觉以至浑身战栗
				ELSE
					PRINT 浑身痉挛抽搐着，也不知是因为后面的剧痛，还是前面所受到的刺激
				ENDIF
			ELSEIF PALAM:快Ａ < 1500
				IF EXP:Ａ经验 >= 75
					PRINT 后面除疼痛外还有了其他的感觉，前方则因快感抽搐战栗
				ELSEIF EXP:Ａ经验 >= 30
					PRINT 努力捕捉着疼痛以外的感觉，前方则因快感抽搐战栗
				ELSEIF EXP:Ａ经验 >= 10
					PRINT 虽然感到疼痛，但前面太过有感觉以至浑身战栗
				ELSE
					PRINT 浑身痉挛抽搐着，也不知是因为后面的剧痛，还是前面所受到的刺激
				ENDIF
			ELSEIF PALAM:快Ａ < 3000
				IF EXP:Ａ经验 >= 75
					PRINT 因前方的快感浑身战栗，后面也渐渐有了感觉
				ELSEIF EXP:Ａ经验 >= 30
					PRINT 因前方的快感浑身战栗，后面除疼痛外也升起了其他的感觉
				ELSEIF EXP:Ａ经验 >= 10
					PRINT 在强烈的疼痛中捕捉到了什么，前面则因快感抽搐战栗
				ELSE
					PRINT 因后面的痛苦眉头紧皱，又不时因前方的刺激舒展容颜
				ENDIF
			ELSEIF PALAM:快Ａ < 6000
                IF TALENT:鱼尾 || TALENT:蛇尾 || TALENT:马身
                    PRINT 前后同时被蹂躏，在快感中翻着白眼，浑身痉挛
                ELSE
                    PRINT 前后同时被蹂躏，在快感中手脚张合，浑身痉挛
                ENDIF
			ELSEIF PALAM:快Ａ < 8000
					PRINT 前面濒临绝顶，后面也沉浸在倒错的快感中
			ELSEIF PALAM:快Ａ < 10000
					PRINT 前后都临近高潮，整个人都在快感的漩涡里飘摇沉浮
			ENDIF
		ELSEIF PALAM:快Ｃ < 10000
			IF PALAM:快Ａ < 500
				IF EXP:Ａ经验 >= 75
					PRINT 阴茎又硬又烫，里面的东西好像下一秒就要喷薄而出
				ELSEIF EXP:Ａ经验 >= 30
					PRINT 尽管后面很痛，阴茎却又硬又烫，里面的东西好像下一秒就要喷薄而出
				ELSEIF EXP:Ａ经验 >= 10
					PRINT 承受着后方的疼痛，但阴茎又硬又烫，里面的东西好像下一秒就要喷薄而出
				ELSE
					PRINT 承受着后面的剧痛，但阴茎又硬又烫，里面的东西好像下一秒就要喷薄而出
				ENDIF
			ELSEIF PALAM:快Ａ < 1500
				IF EXP:Ａ经验 >= 75
					PRINT 后面除疼痛外还有了别的感觉，阴茎也又硬又烫，里面的东西好像下一秒就要喷薄而出
				ELSEIF EXP:Ａ经验 >= 30
					PRINT 在疼痛中捕捉到了什么，阴茎则又硬又烫，里面的东西好像下一秒就要喷薄而出
				ELSEIF EXP:Ａ经验 >= 10
					PRINT 承受着疼痛，但阴茎又硬又烫，里面的东西好像下一秒就要喷薄而出
				ELSE
					PRINT 承受着剧痛，但阴茎又硬又烫，里面的东西好像下一秒就要喷薄而出
				ENDIF
			ELSEIF PALAM:快Ａ < 3000
				IF EXP:Ａ经验 >= 75
					PRINT 渐渐好像不那么痛了，同时阴茎又硬又烫，里面的东西好像下一秒就要喷薄而出
				ELSEIF EXP:Ａ经验 >= 30
					PRINT 尽管疼痛，却又很有感觉
				ELSEIF EXP:Ａ经验 >= 10
					PRINT 疼痛中有了别的感觉，阴茎也又硬又烫，里面的东西好像下一秒就要喷薄而出
				ELSE
					PRINT 后面有些疼痛，但阴茎又硬又烫，里面的东西好像下一秒就要喷薄而出
				ENDIF
			ELSEIF PALAM:快Ａ < 6000
					PRINT 后面传来的快感十分强烈，同时阴茎又硬又烫，里面的东西好像下一秒就要喷薄而出
			ELSEIF PALAM:快Ａ < 8000
					PRINT 沉沦在倒错的快感中，同时阴茎又硬又烫，里面的东西好像下一秒就要喷薄而出
			ELSEIF PALAM:快Ａ < 10000
					PRINT 前面和后面都超有感觉，仿佛随时都能一起高潮似的
			ENDIF
		ENDIF
	ENDIF
ENDIF
;-------------------------------------------------
;594,插入中胸爱抚系
;-------------------------------------------------
@MESSAGE_COM594
IF (TFLAG:706 == 1 && TFLAG:707 == 1) || (TFLAG:706 == 0 && TFLAG:708 == 1) 
	PRINTFORM 一边操干着%CALLNAME%的小穴，一边
ELSEIF (TFLAG:706 == 1 && TFLAG:707 == 2) || (TFLAG:706 == 0 && TFLAG:708 == 2) 
	PRINTFORM 一边操干着%CALLNAME%的后穴，一边
ELSE
	PRINTFORM 一边在%CALLNAME%体内抽插，一边
ENDIF
;前回调教が后背位など背面の場合
SIF PREVCOM == 61 || PREVCOM == 63 || PREVCOM == 69 || PREVCOM == 71 || PREVCOM == 76 || PREVCOM == 77 || PREVCOM == 504 || PREVCOM == 515 || PREVCOM == 516 || PREVCOM == 591
	PRINTFORM 收回了紧握其腰胯的手，
;着衣プ雷
IF TALENT:PLAYER:58 && (TEQUIP:3 || TEQUIP:5 || TEQUIP:6)
    PRINTFORM 隔着
	IF TEQUIP:6
		A = 6
	ELSEIF TEQUIP:5
		A = 5
	ELSEIF TEQUIP:3
		A = 3
	ENDIF
	CALL CLOTHES_NAME
	IF TFLAG:57 == 1
		PRINTFORML 揉弄起%CALLNAME%的胸…
	ELSEIF TFLAG:57 == 2
		PRINTFORML 拨弄起%CALLNAME%的乳头…
	ELSE
		PRINTFORML 抚弄起%CALLNAME%的胸…
	ENDIF
ELSE
	IF TFLAG:57 == 1
		;母乳体质
		IF TALENT:149
			;情欲のPALAMLV3以上
			IF PALAM:6 > PALAMLV:3
				PRINTFORM 强有力地揉捏起%CALLNAME%泌着母乳的
			ELSE
				PRINTFORM 揉搓着%CALLNAME%
			ENDIF
		ELSE
			IF PALAM:6 > PALAMLV:3
				PRINTFORM 强有力地揉捏起%CALLNAME%
			ELSE
				PRINTFORM 揉搓着%CALLNAME%
			ENDIF
		ENDIF
	ELSEIF TFLAG:57 == 2
		;情欲のPALAMLV3以上
		IF PALAM:6 >= PALAMLV:3
			PRINTFORM 数次拨弄%CALLNAME%胸上硬挺的乳头，顺便揉捏其
		ELSE
			PRINTFORM 数次拨弄%CALLNAME%胸上柔软的乳头，顺便揉捏其
		ENDIF
	ELSE
		IF PALAM:6 >= PALAMLV:3 && TEQUIP:17 == 0 && TEQUIP:16 == 0
			PRINTFORM 拨弄着%CALLNAME%胸上硬挺的乳头，顺便揉捏其
		ELSE
			PRINTFORM 温柔地抚弄着%CALLNAME%
		ENDIF
	ENDIF
	;矮小体型＋贫乳
	IF TALENT:100 && TALENT:109
		PRINTFORM 仅只是膨胀程度
	;贫乳
	ELSEIF TALENT:109
		PRINTFORM 小小
	;巨乳
	ELSEIF TALENT:111
		IF TALENT:140
		PRINTFORM 胸肌从手中溢出
		ELSE
		PRINTFORM 乳肉从手中溢出
		ENDIF		
	;爆乳
	ELSEIF TALENT:112
		PRINTFORM 一只手无法掌控
	ELSEIF TALENT:110 || MALE(TARGET)
	    PRINTFORM 平坦
	ENDIF
	PRINT 的胸部…
ENDIF
;乱れ牡丹の場合
IF PREVCOM == 504
	;露出癖
	IF ABL:8 >= 5
		PRINTL 
		PRINTFORM %CALLNAME%双目痴迷地看着镜中自己淫荡的身姿，发出了欢喜的呻吟…
	ELSEIF ABL:8 >= 4
		PRINTL 
		PRINTFORM %CALLNAME%凝视着镜中自己臣服于快感，既羞耻又快乐的痴态…
	ELSEIF ABL:8 >= 2
		PRINTL 
		PRINTFORM %CALLNAME%满脸羞红，间或泄露出动听的呻吟…
	ENDIF
	TFLAG:52 = 1
ELSEIF TALENT:男性
	IF PALAM:快Ｂ < 1500
	ELSE
		TFLAG:52 = 1
		PRINTL 
		PRINTFORM %CALLNAME%
		IF PALAM:快Ｂ < 3000
			IF PALAM:快Ａ < 500
				IF EXP:Ａ经验 >= 75
					PRINT 被玩弄的胸部渐渐有了感觉
				ELSEIF EXP:Ａ经验 >= 30
					PRINT 虽然感到疼痛，但被玩弄的胸部渐渐地起了反应
				ELSEIF EXP:Ａ经验 >= 10
					PRINT 虽然感到疼痛，但被玩弄的胸部传来了阵阵酥麻
				ELSE
					PRINT 虽然十分疼痛，但胸部被玩弄所带来的些微快感稍微减轻了这种痛苦
				ENDIF
			ELSEIF PALAM:快Ａ < 1500
				IF EXP:Ａ经验 >= 75
					PRINT 除疼痛外还有了其他的感觉
				ELSEIF EXP:Ａ经验 >= 30
					PRINT 努力捕捉着疼痛以外的感觉
				ELSEIF EXP:Ａ经验 >= 10
					PRINT 虽然感到疼痛，但被玩弄的胸部渐渐有了快感
				ELSE
					PRINT 承受着痛苦，唯有胸部被玩弄所带来的些许刺激能稍稍将之减轻
				ENDIF
			ELSEIF PALAM:快Ａ < 3000
				IF EXP:Ａ经验 >= 75
					PRINT 隐约有了微妙的快感
				ELSEIF EXP:Ａ经验 >= 30
					PRINT 除疼痛外还感到了被玩弄部位传来的丝丝酥麻
				ELSEIF EXP:Ａ经验 >= 10
					PRINT 在强烈的疼痛中捕捉到了其他的感觉
				ELSE
					PRINT 承受着痛苦的同时，胸部所受到的刺激带来了疼痛外的别的感觉
				ENDIF
			ELSEIF PALAM:快Ａ < 6000
					PRINT 在后穴被抽插的过程里产生了明显的快感
			ELSEIF PALAM:快Ａ < 8000
					PRINT 在倒错的快感中颤抖痉挛
			ELSEIF PALAM:快Ａ < 10000
					PRINT 在被抽插后穴的过程中濒临绝顶了
			ENDIF
		ELSEIF PALAM:快Ｂ < 6000
			IF PALAM:快Ａ < 500
				IF EXP:Ａ经验 >= 75
					PRINT 被玩弄的胸部有了明显的感觉
				ELSEIF EXP:Ａ经验 >= 30
					PRINT 虽然感到疼痛，但被玩弄的胸部有了明显的感觉
				ELSEIF EXP:Ａ经验 >= 10
					PRINT 虽然十分痛苦，好在胸部被玩弄的酥麻快感能够稍稍减轻这种疼痛
				ELSE
					PRINT 承受着剧痛，唯有被玩弄的胸部所带来的酥麻快感能聊以慰藉
				ENDIF
			ELSEIF PALAM:快Ａ < 1500
				IF EXP:Ａ经验 >= 75
					PRINT 被玩弄的胸部有了明显的感觉，后面除疼痛外也有了微妙的快感
				ELSEIF EXP:Ａ经验 >= 30
					PRINT 被玩弄的胸部有了明显的感觉，后面在强烈的疼痛中也捕捉到了其他的感受
				ELSEIF EXP:Ａ经验 >= 10
					PRINT 虽然感到疼痛，但被玩弄的胸部有了明显的感觉
				ELSE
					PRINT 虽然十分痛苦，好在胸部被玩弄的酥麻快感能够稍稍减轻这种疼痛
				ENDIF
			ELSEIF PALAM:快Ａ < 3000
				IF EXP:Ａ经验 >= 75
					PRINT 被玩弄的胸部有了明显的感觉，后面也隐约有了微妙的快感
				ELSEIF EXP:Ａ经验 >= 30
					PRINT 被玩弄的胸部有了明显的感觉，后面除疼痛外还感到了丝丝酥麻
				ELSEIF EXP:Ａ经验 >= 10
					PRINT 在强烈的疼痛中捕捉到了其他的感觉，胸部也起了反应
				ELSE
					PRINT 承受着痛苦的同时，胸部所受到的刺激也带来了明显的感觉
				ENDIF
			ELSEIF PALAM:快Ａ < 6000
					PRINT 后穴和胸部同时被蹂躏，产生了明显的感觉
			ELSEIF PALAM:快Ａ < 8000
					PRINT 被玩弄的胸部有了明显的感觉，后面则在倒错的快感中颤抖痉挛
			ELSEIF PALAM:快Ａ < 10000
					PRINT 被玩弄的胸部有了明显的感觉，后面则快乐得濒临绝顶了
			ENDIF
		ELSEIF PALAM:快Ｂ < 8000
			IF PALAM:快Ａ < 500
				IF EXP:Ａ经验 >= 75
					PRINT 因胸部被玩弄而浑身战栗，仿佛下一秒就会高潮
				ELSEIF EXP:Ａ经验 >= 30
					PRINT 后方虽然十分疼痛，胸部却太过有感觉以至浑身战栗
				ELSEIF EXP:Ａ经验 >= 10
					PRINT 后面承受着疼痛，但胸部太过有感觉以至浑身战栗
				ELSE
					PRINT 浑身痉挛抽搐着，也不知是因为后面的剧痛，还是胸部所受到的刺激
				ENDIF
			ELSEIF PALAM:快Ａ < 1500
				IF EXP:Ａ经验 >= 75
					PRINT 后面除疼痛外还有了其他的感觉，胸部则沉沦在强烈的快感中
				ELSEIF EXP:Ａ经验 >= 30
					PRINT 努力捕捉着疼痛以外的感觉，胸部则沉沦在强烈的快感中
				ELSEIF EXP:Ａ经验 >= 10
					PRINT 虽然感到疼痛，但胸部太过有感觉以至浑身战栗
				ELSE
					PRINT 浑身痉挛抽搐着，也不知是因为后面的剧痛，还是胸部所受到的刺激
				ENDIF
			ELSEIF PALAM:快Ａ < 3000
				IF EXP:Ａ经验 >= 75
					PRINT 因胸部的快感浑身战栗，后面也渐渐有了感觉
				ELSEIF EXP:Ａ经验 >= 30
					PRINT 因胸部的快感浑身战栗，后面除疼痛外也升起了其他的感觉
				ELSEIF EXP:Ａ经验 >= 10
					PRINT 在强烈的疼痛中捕捉到了什么，胸部则因快感抽搐战栗
				ELSE
					PRINT 因后面的痛苦眉头紧皱，又不时因胸部的刺激舒展容颜
				ENDIF
			ELSEIF PALAM:快Ａ < 6000
			    IF TALENT:鱼尾 || TALENT:蛇尾 || TALENT:马身
			        PRINT 后穴和胸部同时被蹂躏，在快感中翻着白眼，浑身痉挛
			    ELSE
				    PRINT 后穴和胸部同时被蹂躏，在快感中手脚张合，浑身痉挛
                ENDIF
			ELSEIF PALAM:快Ａ < 8000
					PRINT 胸部在强烈的刺激中沉沦，后面也沉浸在倒错的快感中
			ELSEIF PALAM:快Ａ < 10000
					PRINT 因胸部和后穴的刺激浑身战栗，整个人都在快感的漩涡里飘摇沉浮
			ENDIF
		ELSEIF PALAM:快Ｂ < 10000
			IF PALAM:快Ａ < 500
				IF EXP:Ａ经验 >= 75
					PRINT 胸部被玩弄得太过舒服，整个人飘飘欲仙
				ELSEIF EXP:Ａ经验 >= 30
					PRINT 尽管后面很痛，但因胸部被玩弄得太过舒服，整个人飘飘欲仙
				ELSEIF EXP:Ａ经验 >= 10
					PRINT 承受着后方的疼痛，但因胸部被玩弄得太过舒服，整个人飘飘欲仙
				ELSE
					PRINT 承受着后面的剧痛，但因胸部被玩弄得太过舒服，整个人飘飘欲仙
				ENDIF
			ELSEIF PALAM:快Ａ < 1500
				IF EXP:Ａ经验 >= 75
					PRINT 后面除疼痛外还有了别的感觉，胸部也因被玩弄得太过舒服，整个人飘飘欲仙
				ELSEIF EXP:Ａ经验 >= 30
					PRINT 在疼痛中捕捉到了什么，胸部也因被玩弄得太过舒服，整个人飘飘欲仙
				ELSEIF EXP:Ａ经验 >= 10
					PRINT 承受着疼痛，但因胸部被玩弄得太过舒服，整个人飘飘欲仙
				ELSE
					PRINT 承受着剧痛，但因胸部被玩弄得太过舒服，整个人飘飘欲仙
				ENDIF
			ELSEIF PALAM:快Ａ < 3000
				IF EXP:Ａ经验 >= 75
					PRINT 渐渐好像不那么痛了，同时胸部被玩弄得太过舒服，整个人飘飘欲仙
				ELSEIF EXP:Ａ经验 >= 30
					PRINT 尽管疼痛，却又很有感觉
				ELSEIF EXP:Ａ经验 >= 10
					PRINT 疼痛中有了别的感觉，胸部也因被玩弄得太过舒服，整个人飘飘欲仙
				ELSE
					PRINT 后面有些疼痛，但因胸部被玩弄得太过舒服，整个人飘飘欲仙
				ENDIF
			ELSEIF PALAM:快Ａ < 6000
					PRINT 后面传来的快感十分强烈，同时胸部也因被玩弄得太过舒服，整个人飘飘欲仙
			ELSEIF PALAM:快Ａ < 8000
					PRINT 沉沦在倒错的快感中，同时胸部也因被玩弄得太过舒服，整个人飘飘欲仙
			ELSEIF PALAM:快Ａ < 10000
					PRINT 胸部和后面都超有感觉，仿佛随时都能一起高潮似的
			ENDIF
		ENDIF
	ENDIF
ENDIF
;-------------------------------------------------
;595,三点攻（男奴隶限定）
;-------------------------------------------------
@MESSAGE_COM595
PRINTFORM %CALLNAME:PLAYER%
IF (TFLAG:706 == 1 && TFLAG:707 == 1) || (TFLAG:706 == 0 && TFLAG:708 == 1) 
	PRINTFORM 一边操干着%CALLNAME%的小穴，一边
ELSEIF (TFLAG:706 == 1 && TFLAG:707 == 2) || (TFLAG:706 == 0 && TFLAG:708 == 2) 
	PRINTFORM 一边操干着%CALLNAME%的后穴，一边
ELSE
	PRINTFORM 一边在%CALLNAME%体内抽插，一边
ENDIF
PRINTFORM 有技巧地
;着衣プ雷
IF TALENT:PLAYER:58 && ((TEQUIP:3 || TEQUIP:5 || TEQUIP:6) || (TEQUIP:2 || TEQUIP:4 > 1 || TEQUIP:6 == 7))
	IF TEQUIP:3 || TEQUIP:5 || TEQUIP:6
	    PRINT 隔着
		IF TEQUIP:6
			A = 6
		ELSEIF TEQUIP:5
			A = 5
		ELSEIF TEQUIP:3
			A = 3
		ENDIF
		CALL CLOTHES_NAME
	ENDIF
	PRINTFORM 玩弄%CALLNAME%的乳头，
	IF TEQUIP:2 || TEQUIP:4 > 1
	    PRINT 把玩揉搓
		;下身外衣か布偶装を着ている
		IF TEQUIP:4 > 1
			IF TEQUIP:4
				A = 4
			ELSE
				A = 6
			ENDIF
			CALL CLOTHES_NAME
		;下身内衣を着ている
		ELSEIF TEQUIP:2
			A = 2
			CALL CLOTHES_NAME
		ENDIF
		PRINTFORM 下
	ENDIF
	PRINTFORM %CALLNAME%的阴茎…
ELSE
    PRINTFORM 拨弄拧转着%CALLNAME%
	;情欲のPALAMLV3以上
	IF PALAM:6 >= PALAMLV:3
		PRINTFORML 完全变硬了的乳头
	ELSE
		PRINTFORML 尚且柔软的乳头
	ENDIF
	PRINT 同时也不忘
	;飞机杯使用中
	IF TEQUIP:12
		PRINTFORM 用飞机杯上下套弄%CALLNAME%的阴茎…
	ELSE
		;丝袜系
		IF TEQUIP:1 == 5 || TEQUIP:1 == 6
		    PRINT 隔着
			SIF TEQUIP:1 == 5
				PRINT 丝袜
			SIF TEQUIP:1 == 6
				PRINT 网袜
			PRINT 摩擦撸动
		ELSE
			IF RAND:3 == 0
				PRINTFORM 淫靡地晃动
			ELSEIF RAND:2 == 0
				PRINTFORM 上下撸动
			ELSE
				PRINTFORM 把玩
		 	ENDIF
		ENDIF
		PRINTFORM %CALLNAME%
        ;情欲のPALAMLV3以上
        IF PALAM:6 >= PALAMLV:3
        	PRINTFORM 已完全挺立
        	IF TALENT:包茎
        		PRINT ，裹着包皮
            	IF TALENT:孩童尺寸
            		SIF TALENT:矮小体型 == 0
            		    PRINT ，与其体型不符
        		    PRINT 的小小
        		ELSEIF TALENT:短小
        		    SIF TALENT:高大体型
        		        PRINT ，与其高大体型不符
        		    PRINT 的短小
        		ELSEIF TALENT:巨根
        		    SIF TALENT:矮小体型
        		        PRINT ，与其体型不符
        		  	PRINT 的粗大
                ELSEIF TALENT:尺寸傲人
        		    SIF TALENT:矮小体型
        		        PRINT ，与其体型不符
        		  	PRINT ，尺寸傲人的
        		ELSE
        	    	PRINT 的
        	    ENDIF
        	ELSE
           		IF TALENT:孩童尺寸
           		    SIF TALENT:矮小体型 == 0
           		        PRINT ，与其体型不符
        	    	PRINT 的小小
        	    ELSEIF TALENT:短小
        	        SIF TALENT:高大体型
        	            PRINT ，与其高大体型不符
        	        PRINT 的短小
        	    ELSEIF TALENT:巨根
        	        SIF TALENT:矮小体型
        	            PRINT ，与其体型不符
        	    	PRINT 的粗大
        	    ELSEIF TALENT:尺寸傲人
        	        SIF TALENT:矮小体型
        	            PRINT ，与其体型不符
        	    	PRINT ，尺寸傲人的
        	    ELSE
        	    	PRINT 的
        	    ENDIF
        	ENDIF
        ELSE
            IF TALENT:包茎
        	    PRINT 裹着包皮
           	    IF TALENT:孩童尺寸
           		    SIF TALENT:矮小体型 == 0
           		        PRINT ，与其体型不符
        	    	PRINT 的小小
        	    ELSEIF TALENT:短小
        	        SIF TALENT:高大体型
        	            PRINT ，与其高大体型不符
        	        PRINT 的短小
        	    ELSEIF TALENT:巨根
        	        SIF TALENT:矮小体型
        	            PRINT ，与其体型不符
        	    	PRINT 的粗大
        	    ELSEIF TALENT:尺寸傲人
        	        SIF TALENT:矮小体型
        	            PRINT ，与其体型不符
        	        PRINT ，尺寸傲人的
        	    ELSE
        	    	PRINT 的
        	    ENDIF
        	ELSE
           		IF TALENT:孩童尺寸
           		    SIF TALENT:矮小体型 == 0
           		       PRINT 与其体型不符
        	    	PRINT 的小小
        	    ELSEIF TALENT:短小
        	        SIF TALENT:高大体型
        	            PRINT 与其高大体型不符
        	        PRINT 的短小
        	    ELSEIF TALENT:巨根
        	        SIF TALENT:矮小体型
        	            PRINT 与其体型不符
        	    	PRINT 的粗大
        	    ELSEIF TALENT:尺寸傲人
        	        SIF TALENT:矮小体型
        	            PRINT 与其体型不符，
        	    	PRINT 尺寸傲人的
        	    ELSE
        	        PRINT 的
        	    ENDIF
        	ENDIF
        ENDIF
		PRINTFORM 阴茎…
	ENDIF
ENDIF
;乱れ牡丹の場合
IF PREVCOM == 504
	;露出癖
	IF ABL:8 >= 5
		PRINTL 
		PRINTFORM %CALLNAME%双目痴迷地看着镜中自己淫荡的身姿，发出了欢喜的呻吟…
	ELSEIF ABL:8 >= 4
		PRINTL 
		PRINTFORM %CALLNAME%凝视着镜中自己臣服于快感，既羞耻又快乐的痴态…
	ELSEIF ABL:8 >= 2
		PRINTL 
		PRINTFORM %CALLNAME%满脸羞红，间或泄露出动听的呻吟…
	ENDIF
ENDIF
;-------------------------------------------------
;600,呼ばせ方变更
;-------------------------------------------------
@MESSAGE_COM600
TFLAG:52 = 1
TFLAG:51 = 1
PRINTL 要改变什么的称呼方式呢…
PRINTL
PRINTFORML [ 0] - 名字的称呼方式（现在是「%MASTERNAME%」）
PRINTFORML [ 1] - 主人的称呼方式（现在是「%CSTR:3%」）
PRINTFORML [10] - 放弃
$INPUT_LOOP_YOBASE
INPUT
IF RESULT == 0
	Y = 1
	$INPUT_LOOP_NAMAE
	PRINTL
	PRINTL
	PRINTL
	PRINTL 要选择哪种称呼方式？
	PRINTL
	SIF MASTERNAME != CSTR:MASTER:4
		PRINTFORML [11] %CSTR:MASTER:4%
	SIF MASTERNAME != CSTR:MASTER:5
		PRINTFORML [12] %CSTR:MASTER:5%
	SIF MASTERNAME != CSTR:MASTER:4 + "桑"
		PRINTFORML [13] %CSTR:MASTER:4%桑
	SIF MASTERNAME != CSTR:MASTER:5 + "桑"
		PRINTFORML [14] %CSTR:MASTER:5%桑
	SIF MASTERNAME != CSTR:MASTER:4 + "君"
		PRINTFORML [15] %CSTR:MASTER:4%君
	SIF MASTERNAME != CSTR:MASTER:5 + "君"
		PRINTFORML [16] %CSTR:MASTER:5%君
	SIF MASTERNAME != CSTR:MASTER:4 + "酱"
		PRINTFORML [17] %CSTR:MASTER:4%酱
	SIF MASTERNAME != CSTR:MASTER:5 + "酱"
		PRINTFORML [18] %CSTR:MASTER:5%酱
	SIF MASTERNAME != CSTR:MASTER:4 + "前辈"
		PRINTFORML [19] %CSTR:MASTER:4%前辈
	SIF MASTERNAME != CSTR:MASTER:5 + "前辈"
		PRINTFORML [20] %CSTR:MASTER:5%前辈
	SIF MASTERNAME != CSTR:MASTER:4 + "大人"
		PRINTFORML [21] %CSTR:MASTER:4%大人
	SIF MASTERNAME != CSTR:MASTER:5 + "大人"
		PRINTFORML [22] %CSTR:MASTER:5%大人
	PRINTFORML [30] --输入其他称呼方式--
	INPUT
	SELECTCASE RESULT
		CASE 11
			MASTERNAME = %CSTR:MASTER:4%
		CASE 12
			MASTERNAME = %CSTR:MASTER:5%
		CASE 13
			MASTERNAME = %CSTR:MASTER:4%桑
		CASE 14
			MASTERNAME = %CSTR:MASTER:5%桑
		CASE 15
			MASTERNAME = %CSTR:MASTER:4%君
		CASE 16
			MASTERNAME = %CSTR:MASTER:5%君
		CASE 17
			MASTERNAME = %CSTR:MASTER:4%酱
		CASE 18
			MASTERNAME = %CSTR:MASTER:5%酱
		CASE 19
			MASTERNAME = %CSTR:MASTER:4%前辈
		CASE 20
			MASTERNAME = %CSTR:MASTER:5%前辈
		CASE 21
			MASTERNAME = %CSTR:MASTER:4%大人
		CASE 22
			MASTERNAME = %CSTR:MASTER:5%大人
		CASE 30
			$INPUT_LOOP_KUURAN
			INPUTS
			MASTERNAME = %RESULTS%
			;空欄はだめ
			SIF MASTERNAME == ""
				GOTO INPUT_LOOP_KUURAN
		CASEELSE
			GOTO INPUT_LOOP_NAMAE
	ENDSELECT
	IF NO:TARGET >= 300 && (CFLAG:2000 == 5 || CFLAG:2000 == 6)
		CSTR:1 = %MASTERNAME%
		CSTR:2 = %MASTERNAME%
	ENDIF
	IF KANRAKU(TARGET) == 1
		PRINTFORMW 要求%CALLNAME%用｢%MASTERNAME%｣来称呼自己
	ELSE
		PRINTFORMW 命令%CALLNAME%用｢%MASTERNAME%｣来称呼自己
	ENDIF
	PRINTFORML %CALLNAME%答应了…
ELSEIF RESULT == 1
Y = 2
	$INPUT_LOOP_TOKUSHU
	PRINTL
	PRINTL
	PRINTL
	PRINTL 要选择哪种称呼方式？
	PRINTL
	SIF CSTR:3 != "主人"
		PRINTFORML [11] 主人
	SIF CSTR:3 != "国王大人"
		PRINTFORML [12] 国王大人
	SIF CSTR:3 != "大人"
		PRINTFORML [13] 大人
	PRINTFORML [30] --输入其他称呼方式--
	INPUT
	SELECTCASE RESULT
		CASE 11
			CSTR:3 = 主人
		CASE 12
			CSTR:3 = 国王大人
		CASE 13
			CSTR:3 = 大人
		CASE 30
			$INPUT_LOOP_KUURAN2
			INPUTS
			CSTR:3 = %RESULTS%
			;空欄はだめ
			SIF CSTR:3 == ""
				GOTO INPUT_LOOP_KUURAN2
		CASEELSE
			GOTO INPUT_LOOP_TOKUSHU
	ENDSELECT
	IF KANRAKU(TARGET) == 1
		PRINTFORMW 要求%CALLNAME%用｢%CSTR:3%｣来称呼自己
	ELSE
		PRINTFORMW 命令%CALLNAME%用｢%CSTR:3%｣来称呼自己
	ENDIF
	PRINTFORML %CALLNAME%答应了…
ELSEIF RESULT == 10
Y = 0
ELSE
	GOTO INPUT_LOOP_YOBASE
ENDIF

