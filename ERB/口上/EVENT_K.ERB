;调教時キャラ別口上を一括管理

;-----------------------------------------------------------------------------------
;口上用系统ファイルの存在判定
;EVENT_K.ERB(つまりこのファイル)が存在しているかを判定
;口上が使用可能で口上機能を能動的にOFFにしていない場合には、自動的にスイッチが入る
;各口上ファイル内でキャラフラグを制御するので、
;EVENT_Kxx.ERB(キャラ別口上ファイル)を途中で削除してもエラーはでない
;ただし、口上をONにした状態でこのファイルを削除するとエラーが発生する
;あっても邪魔にはならないので削除しない方が良いだろう
;-----------------------------------------------------------------------------------
@EVENTSHOP
#PRI
SIF FLAG:7 == 0
	FLAG:7 = 2

@EVENTTRAIN
SIF FLAG:7 <= 0
	RETURN 0
;口上の存在判定
SIF FLAG:(NO:TARGET + 100) == 0 && FLAG:(CFLAG:600 + 100) == 0 && !TALENT:人外
	RETURN 0
;キャラ別
IF FLAG:(NO:TARGET + 100) && !TALENT:人外

    TRYCALLFORM KOJO_EVENTTRAIN_K{NO:TARGET}

ELSEIF CFLAG:600 != 0 && ((NO:TARGET >= 1 && NO:TARGET <= 199) || (NO:TARGET >= 300 && NO:TARGET <= 400))

    TRYCALLFORM KOJO_EVENTTRAIN_K{CFLAG:600}

ELSE
    IF TALENT:人外 && FLAG:(NO:TARGET + 100) == 0
        IF TALENT:天使
            TRYCALLFORM KOJO_EVENTTRAIN_K417
        ELSEIF TALENT:堕天使
            TRYCALLFORM KOJO_EVENTTRAIN_K437
        ELSEIF TALENT:恶魔
            TRYCALLFORM KOJO_EVENTTRAIN_K461
        ELSEIF TALENT:精灵
            TRYCALLFORM KOJO_EVENTTRAIN_K491
        ELSEIF TALENT:走兽
            TRYCALLFORM KOJO_EVENTTRAIN_K550
        ELSEIF TALENT:飞禽
            TRYCALLFORM KOJO_EVENTTRAIN_K580
        ELSEIF TALENT:爬虫
            TRYCALLFORM KOJO_EVENTTRAIN_K600
        ELSEIF TALENT:水生
            TRYCALLFORM KOJO_EVENTTRAIN_K630
        ELSEIF TALENT:植物
            TRYCALLFORM KOJO_EVENTTRAIN_K660
        ELSEIF TALENT:无头骑士
            TRYCALLFORM KOJO_EVENTTRAIN_K661
        ELSEIF TALENT:人偶
            TRYCALLFORM KOJO_EVENTTRAIN_K681
        ELSEIF TALENT:机器人
            TRYCALLFORM KOJO_EVENTTRAIN_K701
        ELSEIF TALENT:吸血鬼 && TALENT:类人
            TRYCALLFORM KOJO_EVENTTRAIN_K721
        ELSEIF TALENT:僵尸
            TRYCALLFORM KOJO_EVENTTRAIN_K741
        ELSEIF TALENT:史莱姆
            TRYCALLFORM KOJO_EVENTTRAIN_K781
        ELSEIF TALENT:幽灵
            TRYCALLFORM KOJO_EVENTTRAIN_K801
        ELSEIF TALENT:数据体
            TRYCALLFORM KOJO_EVENTTRAIN_K841
        ELSEIF TALENT:人工智能
            TRYCALLFORM KOJO_EVENTTRAIN_K861
        ENDIF
    ELSE
		SIF BASE:0 > 0
        TRYCALLFORM KOJO_EVENTTRAIN_K{NO:TARGET}
    ENDIF
ENDIF

@EVENTEND
SIF FLAG:7 <= 0
	RETURN 0
;口上の存在判定
SIF FLAG:(NO:TARGET + 100) == 0 && FLAG:(CFLAG:600 + 100) == 0 && !TALENT:人外
	RETURN 0
;キャラ別
IF FLAG:(NO:TARGET + 100) && !TALENT:人外

    TRYCALLFORM KOJO_EVENTEND_K{NO:TARGET}

ELSEIF CFLAG:600 != 0 && ((NO:TARGET >= 1 && NO:TARGET <= 199) || (NO:TARGET >= 300 && NO:TARGET <= 400))

    TRYCALLFORM KOJO_EVENTEND_K{CFLAG:600}

ELSE
    IF TALENT:人外 && FLAG:(NO:TARGET + 100) == 0
        IF TALENT:天使
            TRYCALLFORM KOJO_EVENTEND_K417
        ELSEIF TALENT:堕天使
            TRYCALLFORM KOJO_EVENTEND_K437
        ELSEIF TALENT:恶魔
            TRYCALLFORM KOJO_EVENTEND_K461
        ELSEIF TALENT:精灵
            TRYCALLFORM KOJO_EVENTEND_K491
        ELSEIF TALENT:走兽
            TRYCALLFORM KOJO_EVENTEND_K550
        ELSEIF TALENT:飞禽
            TRYCALLFORM KOJO_EVENTEND_K580
        ELSEIF TALENT:爬虫
            TRYCALLFORM KOJO_EVENTEND_K600
        ELSEIF TALENT:水生
            TRYCALLFORM KOJO_EVENTEND_K630
        ELSEIF TALENT:植物
            TRYCALLFORM KOJO_EVENTEND_K660
        ELSEIF TALENT:无头骑士
            TRYCALLFORM KOJO_EVENTEND_K661
        ELSEIF TALENT:人偶
            TRYCALLFORM KOJO_EVENTEND_K681
        ELSEIF TALENT:机器人
            TRYCALLFORM KOJO_EVENTEND_K701
        ELSEIF TALENT:吸血鬼 && TALENT:类人
            TRYCALLFORM KOJO_EVENTEND_K721
        ELSEIF TALENT:僵尸
            TRYCALLFORM KOJO_EVENTEND_K741
        ELSEIF TALENT:史莱姆
            TRYCALLFORM KOJO_EVENTEND_K781
        ELSEIF TALENT:幽灵
            TRYCALLFORM KOJO_EVENTEND_K801
        ELSEIF TALENT:数据体
            TRYCALLFORM KOJO_EVENTEND_K841
        ELSEIF TALENT:人工智能
            TRYCALLFORM KOJO_EVENTEND_K861
        ENDIF
    ELSE
        TRYCALLFORM KOJO_EVENTEND_K{NO:TARGET}
    ENDIF
ENDIF


;--------------------------------------------------
;@KOJO_MESSAGE_COM関係
;指令実行時に出力されます
;--------------------------------------------------
@KOJO_MESSAGE_COM
CALL KYOTUU_JYOKYO
IF FLAG:7 <= 0
	IF FLAG:6 > 0
		CALL TRAIN_MESSAGE
		;喘がないフラグが無いなら汎用喘ぎ
		IF TFLAG:51 == 0 && FLAG:10 == 1
			CALL PANT_VOICE2(10)
			PRINTL
			WAIT
		ELSE
			PRINTL
			WAIT
		ENDIF
	ENDIF
	RETURN 0
ENDIF
;口上の存在判定
IF FLAG:(NO:TARGET + 100) == 0 && FLAG:(CFLAG:600 + 100) == 0 && !TALENT:人外
	IF FLAG:6 > 0
		CALL TRAIN_MESSAGE
		;喘がないフラグが無いなら汎用喘ぎ
		IF TFLAG:51 == 0 && FLAG:10 == 1
			CALL PANT_VOICE2(10)
			PRINTL
			WAIT
		ELSE
			PRINTL
			WAIT
		ENDIF
	ENDIF
	RETURN 0
ENDIF
IF FLAG:(NO:TARGET + 100) && !TALENT:人外

    TRYCALLFORM KOJO_MESSAGE_COM_K{NO:TARGET}

ELSEIF CFLAG:600 != 0 && ((NO:TARGET >= 1 && NO:TARGET <= 199) || (NO:TARGET >= 300 && NO:TARGET <= 400))

    TRYCALLFORM KOJO_MESSAGE_COM_K{CFLAG:600}

ELSE
    IF TALENT:人外 && FLAG:(NO:TARGET + 100) == 0
        IF TALENT:天使
            TRYCALLFORM KOJO_MESSAGE_COM_K417
        ELSEIF TALENT:堕天使
            TRYCALLFORM KOJO_MESSAGE_COM_K437
        ELSEIF TALENT:恶魔
            TRYCALLFORM KOJO_MESSAGE_COM_K461
        ELSEIF TALENT:精灵
            TRYCALLFORM KOJO_MESSAGE_COM_K491
        ELSEIF TALENT:走兽
            TRYCALLFORM KOJO_MESSAGE_COM_K550
        ELSEIF TALENT:飞禽
            TRYCALLFORM KOJO_MESSAGE_COM_K580
        ELSEIF TALENT:爬虫
            TRYCALLFORM KOJO_MESSAGE_COM_K600
        ELSEIF TALENT:水生
            TRYCALLFORM KOJO_MESSAGE_COM_K630
        ELSEIF TALENT:植物
            TRYCALLFORM KOJO_MESSAGE_COM_K660
        ELSEIF TALENT:无头骑士
            TRYCALLFORM KOJO_MESSAGE_COM_K661
        ELSEIF TALENT:人偶
            TRYCALLFORM KOJO_MESSAGE_COM_K681
        ELSEIF TALENT:机器人
            TRYCALLFORM KOJO_MESSAGE_COM_K701
        ELSEIF TALENT:吸血鬼 && TALENT:类人
            TRYCALLFORM KOJO_MESSAGE_COM_K721
        ELSEIF TALENT:僵尸
            TRYCALLFORM KOJO_MESSAGE_COM_K741
        ELSEIF TALENT:史莱姆
            TRYCALLFORM KOJO_MESSAGE_COM_K781
        ELSEIF TALENT:幽灵
            TRYCALLFORM KOJO_MESSAGE_COM_K801
        ELSEIF TALENT:数据体
            TRYCALLFORM KOJO_MESSAGE_COM_K841
        ELSEIF TALENT:人工智能
            TRYCALLFORM KOJO_MESSAGE_COM_K861
        ENDIF
    ELSE
        TRYCALLFORM KOJO_MESSAGE_COM_K{NO:TARGET}
    ENDIF
ENDIF
PRINTL
WAIT
;-------------------------------------------------
;@KOJO_MESSAGE_PALAMCNG関係
;パラメータ変動をトリガーにした口上
;パラメータ変動後に口上を発動します
;-------------------------------------------------
@KOJO_MESSAGE_PALAMCNG_A
IF FLAG:7 <= 0
	SIF FLAG:6 > 0
		CALL PALAM_MESSAGE_A
	RETURN 0
ENDIF

;口上の存在判定
IF FLAG:(NO:TARGET + 100) == 0 && FLAG:(CFLAG:600 + 100) == 0 && !TALENT:人外
	SIF FLAG:6 > 0
		CALL PALAM_MESSAGE_A
	RETURN 0
ENDIF
;キャラ別
IF FLAG:(NO:TARGET + 100) && !TALENT:人外

    TRYCALLFORM KOJO_MESSAGE_PALAMCNG_A_K{NO:TARGET}

ELSEIF CFLAG:600 != 0 && ((NO:TARGET >= 1 && NO:TARGET <= 199) || (NO:TARGET >= 300 && NO:TARGET <= 400))

    TRYCALLFORM KOJO_MESSAGE_PALAMCNG_A_K{CFLAG:600}

ELSE
    IF TALENT:人外 && FLAG:(NO:TARGET + 100) == 0
        IF TALENT:天使
            TRYCALLFORM KOJO_MESSAGE_PALAMCNG_A_K417
        ELSEIF TALENT:堕天使
            TRYCALLFORM KOJO_MESSAGE_PALAMCNG_A_K437
        ELSEIF TALENT:恶魔
            TRYCALLFORM KOJO_MESSAGE_PALAMCNG_A_K461
        ELSEIF TALENT:精灵
            TRYCALLFORM KOJO_MESSAGE_PALAMCNG_A_K491
        ELSEIF TALENT:走兽
            TRYCALLFORM KOJO_MESSAGE_PALAMCNG_A_K550
        ELSEIF TALENT:飞禽
            TRYCALLFORM KOJO_MESSAGE_PALAMCNG_A_K580
        ELSEIF TALENT:爬虫
            TRYCALLFORM KOJO_MESSAGE_PALAMCNG_A_K600
        ELSEIF TALENT:水生
            TRYCALLFORM KOJO_MESSAGE_PALAMCNG_A_K630
        ELSEIF TALENT:植物
            TRYCALLFORM KOJO_MESSAGE_PALAMCNG_A_K660
        ELSEIF TALENT:无头骑士
            TRYCALLFORM KOJO_MESSAGE_PALAMCNG_A_K661
        ELSEIF TALENT:人偶
            TRYCALLFORM KOJO_MESSAGE_PALAMCNG_A_K681
        ELSEIF TALENT:机器人
            TRYCALLFORM KOJO_MESSAGE_PALAMCNG_A_K701
        ELSEIF TALENT:吸血鬼 && TALENT:类人
            TRYCALLFORM KOJO_MESSAGE_PALAMCNG_A_K721
        ELSEIF TALENT:僵尸
            TRYCALLFORM KOJO_MESSAGE_PALAMCNG_A_K741
        ELSEIF TALENT:史莱姆
            TRYCALLFORM KOJO_MESSAGE_PALAMCNG_A_K781
        ELSEIF TALENT:幽灵
            TRYCALLFORM KOJO_MESSAGE_PALAMCNG_A_K801
        ELSEIF TALENT:数据体
            TRYCALLFORM KOJO_MESSAGE_PALAMCNG_A_K841
        ELSEIF TALENT:人工智能
            TRYCALLFORM KOJO_MESSAGE_PALAMCNG_A_K861
        ENDIF
    ELSE
        TRYCALLFORM KOJO_MESSAGE_PALAMCNG_A_K{NO:TARGET}
    ENDIF
ENDIF
;-------------------------------------------------
;@KOJO_MESSAGE_PALAMCNG関係
;パラメータ変動をトリガーにした口上
;パラメータ変動後に口上を発動します
;-------------------------------------------------
@KOJO_MESSAGE_PALAMCNG_B
IF FLAG:7 <= 0
	SIF FLAG:6 > 0
		CALL PALAM_MESSAGE_B
	RETURN 0
ENDIF

;口上の存在判定
IF FLAG:(NO:TARGET + 100) == 0 && FLAG:(CFLAG:600 + 100) == 0 && !TALENT:人外
	SIF FLAG:6 > 0
		CALL PALAM_MESSAGE_B
	RETURN 0
ENDIF
;キャラ別
IF FLAG:(NO:TARGET + 100) && !TALENT:人外

    TRYCALLFORM KOJO_MESSAGE_PALAMCNG_B_K{NO:TARGET}

ELSEIF CFLAG:600 != 0 && ((NO:TARGET >= 1 && NO:TARGET <= 199) || (NO:TARGET >= 300 && NO:TARGET <= 400))

    TRYCALLFORM KOJO_MESSAGE_PALAMCNG_B_K{CFLAG:600}

ELSE
    IF TALENT:人外 && FLAG:(NO:TARGET + 100) == 0
        IF TALENT:天使
            TRYCALLFORM KOJO_MESSAGE_PALAMCNG_B_K417
        ELSEIF TALENT:堕天使
            TRYCALLFORM KOJO_MESSAGE_PALAMCNG_B_K437
        ELSEIF TALENT:恶魔
            TRYCALLFORM KOJO_MESSAGE_PALAMCNG_B_K461
        ELSEIF TALENT:精灵
            TRYCALLFORM KOJO_MESSAGE_PALAMCNG_B_K491
        ELSEIF TALENT:走兽
            TRYCALLFORM KOJO_MESSAGE_PALAMCNG_B_K550
        ELSEIF TALENT:飞禽
            TRYCALLFORM KOJO_MESSAGE_PALAMCNG_B_K580
        ELSEIF TALENT:爬虫
            TRYCALLFORM KOJO_MESSAGE_PALAMCNG_B_K600
        ELSEIF TALENT:水生
            TRYCALLFORM KOJO_MESSAGE_PALAMCNG_B_K630
        ELSEIF TALENT:植物
            TRYCALLFORM KOJO_MESSAGE_PALAMCNG_B_K660
        ELSEIF TALENT:无头骑士
            TRYCALLFORM KOJO_MESSAGE_PALAMCNG_B_K661
        ELSEIF TALENT:人偶
            TRYCALLFORM KOJO_MESSAGE_PALAMCNG_B_K681
        ELSEIF TALENT:机器人
            TRYCALLFORM KOJO_MESSAGE_PALAMCNG_B_K701
        ELSEIF TALENT:吸血鬼 && TALENT:类人
            TRYCALLFORM KOJO_MESSAGE_PALAMCNG_B_K721
        ELSEIF TALENT:僵尸
            TRYCALLFORM KOJO_MESSAGE_PALAMCNG_B_K741
        ELSEIF TALENT:史莱姆
            TRYCALLFORM KOJO_MESSAGE_PALAMCNG_B_K781
        ELSEIF TALENT:幽灵
            TRYCALLFORM KOJO_MESSAGE_PALAMCNG_B_K801
        ELSEIF TALENT:数据体
            TRYCALLFORM KOJO_MESSAGE_PALAMCNG_B_K841
        ELSEIF TALENT:人工智能
            TRYCALLFORM KOJO_MESSAGE_PALAMCNG_B_K861
        ENDIF
    ELSE
        TRYCALLFORM KOJO_MESSAGE_PALAMCNG_B_K{NO:TARGET}
    ENDIF
ENDIF

CALL PALAM_MESSAGE_B2
;-------------------------------------------------
;@KOJO_MESSAGE_PALAMCNG関係
;パラメータ変動をトリガーにした口上
;パラメータ変動後に口上を発動します
;-------------------------------------------------
@KOJO_MESSAGE_PALAMCNG_C
IF FLAG:7 <= 0
	SIF FLAG:6 > 0
		CALL PALAM_MESSAGE_C
	RETURN 0
ENDIF

;口上の存在判定
IF FLAG:(NO:TARGET + 100) == 0 && FLAG:(CFLAG:600 + 100) == 0 && !TALENT:人外
	SIF FLAG:6 > 0
		CALL PALAM_MESSAGE_C
	RETURN 0
ENDIF
;キャラ別
IF FLAG:(NO:TARGET + 100) && !TALENT:人外

    TRYCALLFORM KOJO_MESSAGE_PALAMCNG_C_K{NO:TARGET}

ELSEIF CFLAG:600 != 0 && ((NO:TARGET >= 1 && NO:TARGET <= 199) || (NO:TARGET >= 300 && NO:TARGET <= 400))

    TRYCALLFORM KOJO_MESSAGE_PALAMCNG_C_K{CFLAG:600}

ELSE
    IF TALENT:人外 && FLAG:(NO:TARGET + 100) == 0
        IF TALENT:天使
            TRYCALLFORM KOJO_MESSAGE_PALAMCNG_C_K417
        ELSEIF TALENT:堕天使
            TRYCALLFORM KOJO_MESSAGE_PALAMCNG_C_K437
        ELSEIF TALENT:恶魔
            TRYCALLFORM KOJO_MESSAGE_PALAMCNG_C_K461
        ELSEIF TALENT:精灵
            TRYCALLFORM KOJO_MESSAGE_PALAMCNG_C_K491
        ELSEIF TALENT:走兽
            TRYCALLFORM KOJO_MESSAGE_PALAMCNG_C_K550
        ELSEIF TALENT:飞禽
            TRYCALLFORM KOJO_MESSAGE_PALAMCNG_C_K580
        ELSEIF TALENT:爬虫
            TRYCALLFORM KOJO_MESSAGE_PALAMCNG_C_K600
        ELSEIF TALENT:水生
            TRYCALLFORM KOJO_MESSAGE_PALAMCNG_C_K630
        ELSEIF TALENT:植物
            TRYCALLFORM KOJO_MESSAGE_PALAMCNG_C_K660
        ELSEIF TALENT:无头骑士
            TRYCALLFORM KOJO_MESSAGE_PALAMCNG_C_K661
        ELSEIF TALENT:人偶
            TRYCALLFORM KOJO_MESSAGE_PALAMCNG_C_K681
        ELSEIF TALENT:机器人
            TRYCALLFORM KOJO_MESSAGE_PALAMCNG_C_K701
        ELSEIF TALENT:吸血鬼 && TALENT:类人
            TRYCALLFORM KOJO_MESSAGE_PALAMCNG_C_K721
        ELSEIF TALENT:僵尸
            TRYCALLFORM KOJO_MESSAGE_PALAMCNG_C_K741
        ELSEIF TALENT:史莱姆
            TRYCALLFORM KOJO_MESSAGE_PALAMCNG_C_K781
        ELSEIF TALENT:幽灵
            TRYCALLFORM KOJO_MESSAGE_PALAMCNG_C_K801
        ELSEIF TALENT:数据体
            TRYCALLFORM KOJO_MESSAGE_PALAMCNG_C_K841
        ELSEIF TALENT:人工智能
            TRYCALLFORM KOJO_MESSAGE_PALAMCNG_C_K861
        ENDIF
    ELSE
        TRYCALLFORM KOJO_MESSAGE_PALAMCNG_C_K{NO:TARGET}
    ENDIF
ENDIF
PRINTL
;-------------------------------------------------
;@KOJO_MESSAGE_PALAMCNG関係
;パラメータ変動をトリガーにした口上
;パラメータ変動後に口上を発動します
;-------------------------------------------------
@KOJO_MESSAGE_PALAMCNG_D
IF FLAG:7 <= 0
	SIF FLAG:6 > 0
		CALL PALAM_MESSAGE_D
	RETURN 0
ENDIF

;口上の存在判定
IF FLAG:(NO:TARGET + 100) == 0 && FLAG:(CFLAG:600 + 100) == 0 && !TALENT:人外
	SIF FLAG:6 > 0
		CALL PALAM_MESSAGE_D
	RETURN 0
ENDIF
;キャラ別
IF FLAG:(NO:TARGET + 100) && !TALENT:人外

    TRYCALLFORM KOJO_MESSAGE_PALAMCNG_D_K{NO:TARGET}

ELSEIF CFLAG:600 != 0 && ((NO:TARGET >= 1 && NO:TARGET <= 199) || (NO:TARGET >= 300 && NO:TARGET <= 400))

    TRYCALLFORM KOJO_MESSAGE_PALAMCNG_D_K{CFLAG:600}

ELSE
    IF TALENT:人外 && FLAG:(NO:TARGET + 100) == 0
        IF TALENT:天使
            TRYCALLFORM KOJO_MESSAGE_PALAMCNG_D_K417
        ELSEIF TALENT:堕天使
            TRYCALLFORM KOJO_MESSAGE_PALAMCNG_D_K437
        ELSEIF TALENT:恶魔
            TRYCALLFORM KOJO_MESSAGE_PALAMCNG_D_K461
        ELSEIF TALENT:精灵
            TRYCALLFORM KOJO_MESSAGE_PALAMCNG_D_K491
        ELSEIF TALENT:走兽
            TRYCALLFORM KOJO_MESSAGE_PALAMCNG_D_K550
        ELSEIF TALENT:飞禽
            TRYCALLFORM KOJO_MESSAGE_PALAMCNG_D_K580
        ELSEIF TALENT:爬虫
            TRYCALLFORM KOJO_MESSAGE_PALAMCNG_D_K600
        ELSEIF TALENT:水生
            TRYCALLFORM KOJO_MESSAGE_PALAMCNG_D_K630
        ELSEIF TALENT:植物
            TRYCALLFORM KOJO_MESSAGE_PALAMCNG_D_K660
        ELSEIF TALENT:无头骑士
            TRYCALLFORM KOJO_MESSAGE_PALAMCNG_D_K661
        ELSEIF TALENT:人偶
            TRYCALLFORM KOJO_MESSAGE_PALAMCNG_D_K681
        ELSEIF TALENT:机器人
            TRYCALLFORM KOJO_MESSAGE_PALAMCNG_D_K701
        ELSEIF TALENT:吸血鬼 && TALENT:类人
            TRYCALLFORM KOJO_MESSAGE_PALAMCNG_D_K721
        ELSEIF TALENT:僵尸
            TRYCALLFORM KOJO_MESSAGE_PALAMCNG_D_K741
        ELSEIF TALENT:史莱姆
            TRYCALLFORM KOJO_MESSAGE_PALAMCNG_D_K781
        ELSEIF TALENT:幽灵
            TRYCALLFORM KOJO_MESSAGE_PALAMCNG_D_K801
        ELSEIF TALENT:数据体
            TRYCALLFORM KOJO_MESSAGE_PALAMCNG_D_K841
        ELSEIF TALENT:人工智能
            TRYCALLFORM KOJO_MESSAGE_PALAMCNG_D_K861
        ENDIF
    ELSE
        TRYCALLFORM KOJO_MESSAGE_PALAMCNG_D_K{NO:TARGET}
    ENDIF
ENDIF
;-------------------------------------------------
;@KOJO_MESSAGE_PALAMCNG関係
;パラメータ変動をトリガーにした口上
;パラメータ変動後に口上を発動します
;-------------------------------------------------
@KOJO_MESSAGE_PALAMCNG_E
IF FLAG:7 <= 0
	SIF FLAG:6 > 0
		CALL PALAM_MESSAGE_E
	RETURN 0
ENDIF

;口上の存在判定
IF FLAG:(NO:TARGET + 100) == 0 && FLAG:(CFLAG:600 + 100) == 0 && !TALENT:人外
	SIF FLAG:6 > 0
		CALL PALAM_MESSAGE_E
	RETURN 0
ENDIF

;キャラ別
IF FLAG:(NO:TARGET + 100) && !TALENT:人外

    TRYCALLFORM KOJO_MESSAGE_PALAMCNG_E_K{NO:TARGET}

ELSEIF CFLAG:600 != 0 && ((NO:TARGET >= 1 && NO:TARGET <= 199) || (NO:TARGET >= 300 && NO:TARGET <= 400))

    TRYCALLFORM KOJO_MESSAGE_PALAMCNG_E_K{CFLAG:600}

ELSE
    IF TALENT:人外 && FLAG:(NO:TARGET + 100) == 0
        IF TALENT:天使
            TRYCALLFORM KOJO_MESSAGE_PALAMCNG_E_K417
        ELSEIF TALENT:堕天使
            TRYCALLFORM KOJO_MESSAGE_PALAMCNG_E_K437
        ELSEIF TALENT:恶魔
            TRYCALLFORM KOJO_MESSAGE_PALAMCNG_E_K461
        ELSEIF TALENT:精灵
            TRYCALLFORM KOJO_MESSAGE_PALAMCNG_E_K491
        ELSEIF TALENT:走兽
            TRYCALLFORM KOJO_MESSAGE_PALAMCNG_E_K550
        ELSEIF TALENT:飞禽
            TRYCALLFORM KOJO_MESSAGE_PALAMCNG_E_K580
        ELSEIF TALENT:爬虫
            TRYCALLFORM KOJO_MESSAGE_PALAMCNG_E_K600
        ELSEIF TALENT:水生
            TRYCALLFORM KOJO_MESSAGE_PALAMCNG_E_K630
        ELSEIF TALENT:植物
            TRYCALLFORM KOJO_MESSAGE_PALAMCNG_E_K660
        ELSEIF TALENT:无头骑士
            TRYCALLFORM KOJO_MESSAGE_PALAMCNG_E_K661
        ELSEIF TALENT:人偶
            TRYCALLFORM KOJO_MESSAGE_PALAMCNG_E_K681
        ELSEIF TALENT:机器人
            TRYCALLFORM KOJO_MESSAGE_PALAMCNG_E_K701
        ELSEIF TALENT:吸血鬼 && TALENT:类人
            TRYCALLFORM KOJO_MESSAGE_PALAMCNG_E_K721
        ELSEIF TALENT:僵尸
            TRYCALLFORM KOJO_MESSAGE_PALAMCNG_E_K741
        ELSEIF TALENT:史莱姆
            TRYCALLFORM KOJO_MESSAGE_PALAMCNG_E_K781
        ELSEIF TALENT:幽灵
            TRYCALLFORM KOJO_MESSAGE_PALAMCNG_E_K801
        ELSEIF TALENT:数据体
            TRYCALLFORM KOJO_MESSAGE_PALAMCNG_E_K841
        ELSEIF TALENT:人工智能
            TRYCALLFORM KOJO_MESSAGE_PALAMCNG_E_K861
        ENDIF
    ELSE
        TRYCALLFORM KOJO_MESSAGE_PALAMCNG_E_K{NO:TARGET}
    ENDIF
ENDIF


;-------------------------------------------------
;@KOJO_MESSAGE_MARKCNG関係
;刻印変動をトリガーにした口上
;刻印変動後に口上を発動します
;-------------------------------------------------
@KOJO_MESSAGE_MARKCNG
IF FLAG:7 <= 0
	SIF FLAG:6 > 0
		CALL MARK_MESSAGE
	RETURN 0
ENDIF
;口上の存在判定
IF FLAG:(NO:TARGET + 100) == 0 && FLAG:(CFLAG:600 + 100) == 0 && !TALENT:人外
	SIF FLAG:6 > 0
		CALL MARK_MESSAGE
	RETURN 0
ENDIF
;キャラ別
IF FLAG:(NO:TARGET + 100) && !TALENT:人外

    TRYCALLFORM KOJO_MESSAGE_MARKCNG_K{NO:TARGET}

ELSEIF CFLAG:600 != 0 && ((NO:TARGET >= 1 && NO:TARGET <= 199) || (NO:TARGET >= 300 && NO:TARGET <= 400))

    TRYCALLFORM KOJO_MESSAGE_MARKCNG_K{CFLAG:600}

ELSE
    IF TALENT:人外 && FLAG:(NO:TARGET + 100) == 0
        IF TALENT:天使
            TRYCALLFORM KOJO_MESSAGE_MARKCNG_K417
        ELSEIF TALENT:堕天使
            TRYCALLFORM KOJO_MESSAGE_MARKCNG_K437
        ELSEIF TALENT:恶魔
            TRYCALLFORM KOJO_MESSAGE_MARKCNG_K461
        ELSEIF TALENT:精灵
            TRYCALLFORM KOJO_MESSAGE_MARKCNG_K491
        ELSEIF TALENT:走兽
            TRYCALLFORM KOJO_MESSAGE_MARKCNG_K550
        ELSEIF TALENT:飞禽
            TRYCALLFORM KOJO_MESSAGE_MARKCNG_K580
        ELSEIF TALENT:爬虫
            TRYCALLFORM KOJO_MESSAGE_MARKCNG_K600
        ELSEIF TALENT:水生
            TRYCALLFORM KOJO_MESSAGE_MARKCNG_K630
        ELSEIF TALENT:植物
            TRYCALLFORM KOJO_MESSAGE_MARKCNG_K660
        ELSEIF TALENT:无头骑士
            TRYCALLFORM KOJO_MESSAGE_MARKCNG_K661
        ELSEIF TALENT:人偶
            TRYCALLFORM KOJO_MESSAGE_MARKCNG_K681
        ELSEIF TALENT:机器人
            TRYCALLFORM KOJO_MESSAGE_MARKCNG_K701
        ELSEIF TALENT:吸血鬼 && TALENT:类人
            TRYCALLFORM KOJO_MESSAGE_MARKCNG_K721
        ELSEIF TALENT:僵尸
            TRYCALLFORM KOJO_MESSAGE_MARKCNG_K741
        ELSEIF TALENT:史莱姆
            TRYCALLFORM KOJO_MESSAGE_MARKCNG_K781
        ELSEIF TALENT:幽灵
            TRYCALLFORM KOJO_MESSAGE_MARKCNG_K801
        ELSEIF TALENT:数据体
            TRYCALLFORM KOJO_MESSAGE_MARKCNG_K841
        ELSEIF TALENT:人工智能
            TRYCALLFORM KOJO_MESSAGE_MARKCNG_K861
        ENDIF
    ELSE
        TRYCALLFORM KOJO_MESSAGE_MARKCNG_K{NO:TARGET}
    ENDIF
ENDIF
;-------------------------------------------------
;@KOJO_MESSAGE_EVENT関係
;イベント時の口上、イベント開始時に出力します
;-------------------------------------------------
@KOJO_MESSAGE_EVENT
IF FLAG:7 <= 0
	SIF FLAG:6 > 0
		CALL EVENT_MESSAGE
	RETURN 0
ENDIF

;口上の存在判定
IF FLAG:(NO:TARGET + 100) == 0 && FLAG:(CFLAG:600 + 100) == 0 && !TALENT:人外
	SIF FLAG:6 > 0
		CALL EVENT_MESSAGE
	RETURN 0
ENDIF

;キャラ別
IF FLAG:(NO:TARGET + 100) && !TALENT:人外

    TRYCALLFORM KOJO_MESSAGE_EVENT_K{NO:TARGET}

ELSEIF CFLAG:600 != 0 && ((NO:TARGET >= 1 && NO:TARGET <= 199) || (NO:TARGET >= 300 && NO:TARGET <= 400))

    TRYCALLFORM KOJO_MESSAGE_EVENT_K{CFLAG:600}

ELSE
    IF TALENT:人外 && FLAG:(NO:TARGET + 100) == 0
        IF TALENT:天使
            TRYCALLFORM KOJO_MESSAGE_EVENT_K417
        ELSEIF TALENT:堕天使
            TRYCALLFORM KOJO_MESSAGE_EVENT_K437
        ELSEIF TALENT:恶魔
            TRYCALLFORM KOJO_MESSAGE_EVENT_K461
        ELSEIF TALENT:精灵
            TRYCALLFORM KOJO_MESSAGE_EVENT_K491
        ELSEIF TALENT:走兽
            TRYCALLFORM KOJO_MESSAGE_EVENT_K550
        ELSEIF TALENT:飞禽
            TRYCALLFORM KOJO_MESSAGE_EVENT_K580
        ELSEIF TALENT:爬虫
            TRYCALLFORM KOJO_MESSAGE_EVENT_K600
        ELSEIF TALENT:水生
            TRYCALLFORM KOJO_MESSAGE_EVENT_K630
        ELSEIF TALENT:植物
            TRYCALLFORM KOJO_MESSAGE_EVENT_K660
        ELSEIF TALENT:无头骑士
            TRYCALLFORM KOJO_MESSAGE_EVENT_K661
        ELSEIF TALENT:人偶
            TRYCALLFORM KOJO_MESSAGE_EVENT_K681
        ELSEIF TALENT:机器人
            TRYCALLFORM KOJO_MESSAGE_EVENT_K701
        ELSEIF TALENT:吸血鬼 && TALENT:类人
            TRYCALLFORM KOJO_MESSAGE_EVENT_K721
        ELSEIF TALENT:僵尸
            TRYCALLFORM KOJO_MESSAGE_EVENT_K741
        ELSEIF TALENT:史莱姆
            TRYCALLFORM KOJO_MESSAGE_EVENT_K781
        ELSEIF TALENT:幽灵
            TRYCALLFORM KOJO_MESSAGE_EVENT_K801
        ELSEIF TALENT:数据体
            TRYCALLFORM KOJO_MESSAGE_EVENT_K841
        ELSEIF TALENT:人工智能
            TRYCALLFORM KOJO_MESSAGE_EVENT_K861
        ENDIF
    ELSE
        TRYCALLFORM KOJO_MESSAGE_EVENT_K{NO:TARGET}
    ENDIF
ENDIF

CALL EVENT_MESSAGE
;-------------------------------------------------
;@KOJO_MESSAGE_ENDING関係
;イベント時の口上、イベント開始時に出力します
;-------------------------------------------------
@KOJO_MESSAGE_ENDING

;口上の存在判定
SIF FLAG:(NO:TARGET + 100) == 0 && FLAG:(CFLAG:600 + 100) == 0 && !TALENT:人外
	RETURN 0
;キャラ別
IF FLAG:(NO:TARGET + 100) && !TALENT:人外

    TRYCALLFORM KOJO_MESSAGE_ENDING_K{NO:TARGET}

ELSEIF CFLAG:600 != 0 && ((NO:TARGET >= 1 && NO:TARGET <= 199) || (NO:TARGET >= 300 && NO:TARGET <= 400))

    TRYCALLFORM KOJO_MESSAGE_ENDING_K{CFLAG:600}

ELSE
    IF TALENT:人外 && FLAG:(NO:TARGET + 100) == 0
        IF TALENT:天使
            TRYCALLFORM KOJO_MESSAGE_ENDING_K417
        ELSEIF TALENT:堕天使
            TRYCALLFORM KOJO_MESSAGE_ENDING_K437
        ELSEIF TALENT:恶魔
            TRYCALLFORM KOJO_MESSAGE_ENDING_K461
        ELSEIF TALENT:精灵
            TRYCALLFORM KOJO_MESSAGE_ENDING_K491
        ELSEIF TALENT:走兽
            TRYCALLFORM KOJO_MESSAGE_ENDING_K550
        ELSEIF TALENT:飞禽
            TRYCALLFORM KOJO_MESSAGE_ENDING_K580
        ELSEIF TALENT:爬虫
            TRYCALLFORM KOJO_MESSAGE_ENDING_K600
        ELSEIF TALENT:水生
            TRYCALLFORM KOJO_MESSAGE_ENDING_K630
        ELSEIF TALENT:植物
            TRYCALLFORM KOJO_MESSAGE_ENDING_K660
        ELSEIF TALENT:无头骑士
            TRYCALLFORM KOJO_MESSAGE_ENDING_K661
        ELSEIF TALENT:人偶
            TRYCALLFORM KOJO_MESSAGE_ENDING_K681
        ELSEIF TALENT:机器人
            TRYCALLFORM KOJO_MESSAGE_ENDING_K701
        ELSEIF TALENT:吸血鬼 && TALENT:类人
            TRYCALLFORM KOJO_MESSAGE_ENDING_K721
        ELSEIF TALENT:僵尸
            TRYCALLFORM KOJO_MESSAGE_ENDING_K741
        ELSEIF TALENT:史莱姆
            TRYCALLFORM KOJO_MESSAGE_ENDING_K781
        ELSEIF TALENT:幽灵
            TRYCALLFORM KOJO_MESSAGE_ENDING_K801
        ELSEIF TALENT:数据体
            TRYCALLFORM KOJO_MESSAGE_ENDING_K841
        ELSEIF TALENT:人工智能
            TRYCALLFORM KOJO_MESSAGE_ENDING_K861
        ENDIF
    ELSE
        TRYCALLFORM KOJO_MESSAGE_ENDING_K{NO:TARGET}
    ENDIF
ENDIF



;-------------------------------------------------
;キャラ別の拍卖後エピソード
;-------------------------------------------------
;@SET_SLAVE_AFTERWARDS
;TRYCALLFORM KOJO_MESSAGE_ENDING_K{X}

;拍卖後に使うのでNO:TARGETは使えない
@SHOW_SLAVE_AFTERWARDS
TRYCALLFORM SHOW_SLAVE_AFTERWARDS_K{X}

