;-------------------------------------------------
;@KOJO_MESSAGE_PALAMCNG関係（K1の1を性格タイプ番号に置換）
;パラメータ変化などをトリガーにした口上　CFLAG 221～260を使用
;-------------------------------------------------
;口上を書けた部分の「CFLAG:2XX = 1」や「CFLAG:2XX = 2」のコメントアウトは解除してください。
;-------------------------------------------------
;@KOJO_MESSAGE_PALAMCNG_A
;调教者の射精をトリガーにした口上
;-------------------------------------------------
@KOJO_MESSAGE_PALAMCNG_A_K1
;助手が调教した時に口上をスキップする（好みに応じて使う、行頭の;を消すとスキップするようになる）
;SIF ASSI > 0 && ASSIPLAY
;	RETURN 0 
;口塞球着用か触手口辱中、崩溃中は地の文を表示して口上をスキップする
IF (TEQUIP:20 && SELECTCOM != 107) || (TEQUIP:36 && SELECTCOM != 168) || TALENT:崩溃
	CALL PALAM_MESSAGE_A
	RETURN 0
ENDIF


;■ 口上作者们请从这里开始，认准有■标注的地方
;-------------------------------------------------
;■ 调教者射精时的文本
;-------------------------------------------------
;-------------------------------------------------
;■ 射精部位变更時的文本
;■ 也即是说，本来在XX部位，射精时突然变换了位置
;■ 举例：抽插小穴的时候快要射了，就把丁丁抽出来，射在屁股上
;-------------------------------------------------
IF CFLAG:106
	CALL PALAM_MESSAGE_A
	;■ 头发
	IF CFLAG:106 == 1
		;■ 屈服刻印Lv3
		IF MARK:2 == 3 && (CFLAG:221 < 2 || FLAG:7 == 2)
            PRINTFORML 「唔…随便%CSTR:2%怎么做吧！」
            PRINTFORML %CALLNAME:TARGET%屈辱地背过头去，仿佛要避开自己被玷污的头发。
			CFLAG:221 = 2
		;■ 非屈服刻印Lv3
		ELSEIF CFLAG:221 < 1 || FLAG:7 == 2
	        PRINTFORML 「唔…！恶心，恶心透了…像%CSTR:1%这种人」
	        PRINTFORML 头发上浓稠的精液顺着额头滑落到脸上，%CALLNAME:TARGET%用憎恶的眼光看向%PLAYER_NAME()%.
			CFLAG:221 = 1
		ENDIF
	;■ 脸
	ELSEIF CFLAG:106 == 2
		;■ 屈服刻印Lv3
		IF MARK:2 == 3 && (CFLAG:222 < 2 || FLAG:7 == 2)
			PRINTFORML 「唔、呜…」
			PRINTFORML %CALLNAME:TARGET%闭上双眼迎接喷射而来的精液，无意识地舔去射在嘴角上的乳白液体。
			CFLAG:222 = 2
		;■ 非屈服刻印Lv3
		ELSEIF CFLAG:222 < 1 || FLAG:7 == 2
			;PRINTFORMW 
			;CFLAG:222 = 1
		ENDIF
	;■ 胸
	ELSEIF CFLAG:106 == 3
		;■ 屈服刻印Lv3
		IF MARK:2 == 3 && (CFLAG:223 < 2 || FLAG:7 == 2)
			;PRINTFORMW 
			;CFLAG:223 = 2
		;■ 非屈服刻印Lv3
		ELSEIF CFLAG:223 < 1 || FLAG:7 == 2
			;PRINTFORMW 
			;CFLAG:223 = 1
		ENDIF
	;■ 腹
	ELSEIF CFLAG:106 == 4
		;■ 屈服刻印Lv3
		IF MARK:2 == 3 && (CFLAG:224 < 2 || FLAG:7 == 2)
			;PRINTFORMW 
			;CFLAG:224 = 2
		;■ 非屈服刻印Lv3
		ELSEIF CFLAG:224 < 1 || FLAG:7 == 2
			;PRINTFORMW 
			;CFLAG:224 = 1
		ENDIF
	;■ 腋
	ELSEIF CFLAG:106 == 5
		;■ 屈服刻印Lv3
		IF MARK:2 == 3 && (CFLAG:225 < 2 || FLAG:7 == 2)
			;PRINTFORMW 
			;CFLAG:225 = 2
		;■ 非屈服刻印Lv3
		ELSEIF CFLAG:225 < 1 || FLAG:7 == 2
			;PRINTFORMW 
			;CFLAG:225 = 1
		ENDIF
	;■ 大腿
	ELSEIF CFLAG:106 == 6
		;■ 屈服刻印Lv3
		IF MARK:2 == 3 && (CFLAG:226 < 2 || FLAG:7 == 2)
			;PRINTFORMW 
			;CFLAG:226 = 2
		;■ 非屈服刻印Lv3
		ELSEIF CFLAG:226 < 1 || FLAG:7 == 2
			;PRINTFORMW 
			;CFLAG:226 = 1
		ENDIF
	;■ 股间
	ELSEIF CFLAG:106 == 7
		;■ 屈服刻印Lv3
		IF MARK:2 == 3 && (CFLAG:227 < 2 || FLAG:7 == 2)
			;PRINTFORMW 
			;CFLAG:227 = 2
		;■ 非屈服刻印Lv3
		ELSEIF CFLAG:227 < 1 || FLAG:7 == 2
			;PRINTFORMW 
			;CFLAG:227 = 1
		ENDIF
	;■ 屁股
	ELSEIF CFLAG:106 == 8
		;■ 屈服刻印Lv3
		IF MARK:2 == 3 && (CFLAG:228 < 2 || FLAG:7 == 2)
			;PRINTFORMW 
			;CFLAG:228 = 2
		;■ 非屈服刻印Lv3
		ELSEIF CFLAG:228 < 1 || FLAG:7 == 2
			;PRINTFORMW 
			;CFLAG:228 = 1
		ENDIF
	;■ 足
	ELSEIF CFLAG:106 == 10
		;■ 屈服刻印Lv3
		IF MARK:2 == 3 && (CFLAG:228 < 2 || FLAG:7 == 2)
			;PRINTFORMW 
			;CFLAG:228 = 2
		;■ 非屈服刻印Lv3
		ELSEIF CFLAG:228 < 1 || FLAG:7 == 2
			;PRINTFORMW 
			;CFLAG:228 = 1
		ENDIF
	;■ 戴着避孕套
	ELSEIF CFLAG:106 == 9
		;做爱时射精，肛交系以外
		IF TFLAG:2 > 0 && SELECTCOM != 62 && SELECTCOM != 63 && SELECTCOM != 67 && SELECTCOM != 70 && SELECTCOM != 71 && SELECTCOM != 73 && SELECTCOM != 75 && SELECTCOM != 77
			;■ 屈服刻印Lv3
			IF MARK:2 == 3 && (CFLAG:229 < 2 || FLAG:7 == 2)
				;PRINTFORMW 
				;CFLAG:229 = 2
			;■ 非屈服刻印Lv3
			ELSEIF CFLAG:229 < 1 || FLAG:7 == 2
				;PRINTFORMW 
				;CFLAG:229 = 1
			ENDIF
		ELSE
			;■ 屈服刻印Lv3
			IF MARK:2 == 3 && (CFLAG:230 < 2 || FLAG:7 == 2)
				;PRINTFORMW 
				;CFLAG:230 = 2
			;■ 非屈服刻印Lv3
			ELSEIF CFLAG:230 < 1 || FLAG:7 == 2
				;PRINTFORMW 
				;CFLAG:230 = 1
			ENDIF
		ENDIF
	ENDIF
	RETURN 0
ENDIF

;■ 以下是不临时变换射精位置，直接射精的场合
;-------------------------------------------------
;■ 手、口以外的射精
;■ 对应指令是接吻、素股、足交侍奉、肛強制侍奉、Ｗ素股
;-------------------------------------------------
IF TFLAG:9 == 1
	CALL PALAM_MESSAGE_A
	;■ 屈服刻印Lv3
	IF MARK:2 == 3 && (CFLAG:231 < 2 || FLAG:7 == 2)
		;PRINTFORMW 
		;CFLAG:231 = 2
	;■ 非屈服刻印Lv3
	ELSEIF CFLAG:231 < 1 || FLAG:7 == 2
		;PRINTFORMW 
		;CFLAG:231 = 1
	ENDIF
ENDIF

;-------------------------------------------------
;■ 侵犯助手时射精
;-------------------------------------------------
IF TFLAG:7 > 0
	CALL PALAM_MESSAGE_A
	;■ 屈服刻印Lv3
	IF MARK:2 == 3 && (CFLAG:232 < 2 || FLAG:7 == 2)
		;PRINTFORMW 
		;CFLAG:232 = 2
	;■ 非屈服刻印Lv3
	ELSEIF CFLAG:232 < 1 || FLAG:7 == 2
		;PRINTFORMW 
		;CFLAG:232 = 1
	ENDIF
ENDIF

;-------------------------------------------------
;■ 口中射精
;-------------------------------------------------
IF TFLAG:0 > 0
	CALL PALAM_MESSAGE_A
	;■ 屈服刻印Lv3
	IF MARK:2 == 3 && (CFLAG:233 < 2 || FLAG:7 == 2)
		;PRINTFORMW 
		;CFLAG:233 = 2
	;■ 非屈服刻印Lv3
	ELSEIF CFLAG:233 < 1 || FLAG:7 == 2
		;PRINTFORMW 
		;CFLAG:233 = 1
	ENDIF
ENDIF

;-------------------------------------------------
;■ 手上射精
;-------------------------------------------------
IF TFLAG:1 > 0
	CALL PALAM_MESSAGE_A
	;■ 屈服刻印Lv3
	IF MARK:2 == 3 && (CFLAG:234 < 2 || FLAG:7 == 2)
		;PRINTFORMW 
		;CFLAG:234 = 2
	;■ 非屈服刻印Lv3
	ELSEIF CFLAG:234 < 1 || FLAG:7 == 2
		;PRINTFORMW 
		;CFLAG:234 = 1
	ENDIF
ENDIF

;-------------------------------------------------
;■ 胸上射精
;-------------------------------------------------
IF TFLAG:34 > 0
	CALL PALAM_MESSAGE_A
	;■ 屈服刻印Lv3
	IF MARK:2 == 3 && (CFLAG:235 < 2 || FLAG:7 == 2)
		;PRINTFORMW 
		;CFLAG:235 = 2
	;■ 非屈服刻印Lv3
	ELSEIF CFLAG:235 < 1 || FLAG:7 == 2
		;PRINTFORMW 
		;CFLAG:235 = 1
	ENDIF
ENDIF

;-------------------------------------------------
;■ 足上射精
;-------------------------------------------------
IF TFLAG:16 > 0
	CALL PALAM_MESSAGE_A
	;■ 屈服刻印Lv3
	IF MARK:2 == 3 && (CFLAG:236 < 2 || FLAG:7 == 2)
		;PRINTFORMW 
		;CFLAG:236 = 2
	;■ 非屈服刻印Lv3
	ELSEIF CFLAG:236 < 1 || FLAG:7 == 2
		;PRINTFORMW 
		;CFLAG:236 = 1
	ENDIF
ENDIF

;-------------------------------------------------
;■ 做爱时射精
;-------------------------------------------------
IF TFLAG:2 > 0
	CALL PALAM_MESSAGE_A
	;■ 肛做爱系
	IF SELECTCOM == 62 || SELECTCOM == 63 || SELECTCOM == 67 || SELECTCOM == 70 || SELECTCOM == 71 || SELECTCOM == 73 || SELECTCOM == 75 || SELECTCOM == 77
		;■ 屈服刻印Lv3
		IF MARK:2 == 3 && (CFLAG:237 < 2 || FLAG:7 == 2)
            PRINTFORML 「呜、唔……射了…射在里面了……」
            PRINTFORMW 像天鹅一样竭力仰起后颈，%CALLNAME:TARGET%翻出眼白，牙关颤抖着迎接体内一阵阵的热流。
            PRINTFORML 「啊、啊啊…%MASTERNAME%的精液全部射进来了，好烫，好满，嗯……」
            PRINTFORML 抽出阴茎的后穴留下一个空虚的深洞，熟红的穴口不满地一下下翁张。
            PRINTFORMW %CALLNAME:TARGET%无力地喘息，高高翘着屁股倒在床上。
            PRINTFORMW 因为被射的太深，过了好久，白浊的精液才终于被那张小嘴吐出来，顺着会阴流下，色情地积在睾丸上。
            PRINTFORML 「哈，哈……%CSTR:0%的身体，让%MASTERNAME%…满足了吗？」
            PRINTFORML %CALLNAME:TARGET%转过头来看向%PLAYER_NAME()%，头发沾湿了\@ MALE(TARGET) ? 他 # 她 \@的前额，看上去颇为淫荡。
			CFLAG:237 = 2
		;■ 非屈服刻印Lv3
		ELSEIF CFLAG:237 < 1 || FLAG:7 == 2
			PRINTFORML 「难道说……不，不要！不要射在里面啊！只有这个绝对不可以！」
            PRINTFORMW 突然明白了%PLAYER_NAME()%要做什么， %CALLNAME:TARGET%满眼惊惧。
            PRINTFORMW %PLAYER_NAME()%不管不顾地加快了抽插的速度，被%CALLNAME:TARGET%后穴伺候的阴茎变得越来越肿胀。
            PRINTFORML 「%CSTR:0%、%CSTR:0%会听话的，%CSTR:0%给%CSTR:1%口出来好不好？拜托…拔出来、拔出来啊！不要、不——！」
            PRINTFORMW %CALLNAME:TARGET%绝望地仰起后颈，体内被噗哧噗哧地射入一股股浓郁的精液，抽出阴茎后，白浊的液体顺着穴口流出来。
            PRINTFORML 「不会原谅%CSTR:1%，不会原谅%CSTR:1%的…」
            PRINTFORML %CALLNAME:TARGET%眼神灰暗，一遍遍诅咒着%PLAYER_NAME()%的名字。
			CFLAG:237 = 1
		ENDIF
	;■ 阴道做爱系
	ELSE
		;■ 屈服刻印Lv3
		IF MARK:2 == 3 && (CFLAG:238 < 2 || FLAG:7 == 2)
			;PRINTFORMW 
			;CFLAG:238 = 2
		;■ 非屈服刻印Lv3
		ELSEIF CFLAG:238 < 1 || FLAG:7 == 2
			;PRINTFORMW 
			;CFLAG:238 = 1
		ENDIF
	ENDIF
ENDIF

;-------------------------------------------------
;■ 逆做爱时主人被射精
;-------------------------------------------------
IF SEXPLAY(TARGET) >= 3 && TFLAG:10 > 0
	CALL PALAM_MESSAGE_A
	;■ 屈服刻印Lv3
	IF MARK:2 == 3 && (CFLAG:239 < 2 || FLAG:7 == 2)
		;PRINTFORMW 
		;CFLAG:239 = 2
	;■ 非屈服刻印Lv3
	ELSEIF CFLAG:239 < 1 || FLAG:7 == 2
		;PRINTFORMW 
		;CFLAG:239 = 1
	ENDIF
ENDIF
;-------------------------------------------------
;■ 逆做爱时主人被射精　非施虐狂限定の台詞　汎用版
;-------------------------------------------------
IF SEXPLAY(TARGET) >= 3 && TFLAG:10 > 0 && !TALENT:施虐狂 && ABL:受虐属性 > ABL:嗜虐属性 && !RAND:3
	;■ 屈服刻印Lv3
	IF MARK:2 == 3 && (CFLAG:239 < 2 || FLAG:7 == 2)
		;淫乱陷落
		IF KANRAKU(TARGET) == 2
			PRINTFORML 「哈啊…满满地中出了…好棒…」
		;恋慕、服从限定、性保守か贞操重视持ち　肛プ雷ではない
		ELSEIF SEXPLAY(TARGET) == 3 && (KANRAKU(TARGET) == 1 || KANRAKU(TARGET) == 3) && (TALENT:性保守 || TALENT:贞操重视) && !TALENT:PLAYER:怀孕
			IF !RAND:2
				PRINTFORM 「哈、啊…射了好多在里面…
			ELSE
				PRINTFORM 「哈、啊…全部射在里面了…
			ENDIF
			IF !RAND:2
				PRINTFORML 不要紧吗…」
			ELSE
				PRINTFORML 没关系吗…」
			ENDIF
		ENDIF
		CFLAG:239 = 2
	;■ 非屈服刻印Lv3
	ELSEIF CFLAG:239 < 1 || FLAG:7 == 2
		;PRINTFORMW 
		;CFLAG:239 = 1
	ENDIF
ENDIF
;-------------------------------------------------
;■ 调教者空射精 CFLAG:255   就是射不出来了的情况
;-------------------------------------------------
;调教者空射精
IF TFLAG:164
	CALL PALAM_MESSAGE_A
	;■ 屈服刻印Lv3
	;IF MARK:2 == 3 && (CFLAG:255 < 2 || FLAG:7 == 2)
	;	
	;	CFLAG:255 = 2
	;■ 非屈服刻印Lv3
	;ELSEIF CFLAG:255 < 1 || FLAG:7 == 2
	;	
	;	CFLAG:255 = 1
	;ENDIF
	;TFLAG:164 = 0
ENDIF

;-------------------------------------------------
;@KOJO_MESSAGE_PALAMCNG_E（K1の1を性格タイプ番号に置換）
;触手射精
;-------------------------------------------------
@KOJO_MESSAGE_PALAMCNG_E_K1
;助手が调教した時に口上をスキップする（好みに応じて使う、行頭の;を消すとスキップするようになる）
;SIF ASSI > 0 && ASSIPLAY
;	RETURN 0 
;戴着口塞球和被触手口辱中、崩溃中的话是没有口上的
IF (TEQUIP:20 && SELECTCOM != 107) || (TEQUIP:36 && SELECTCOM != 168) || TALENT:崩溃
	CALL PALAM_MESSAGE_E
	RETURN 0
ENDIF
;-------------------------------------------------
;触手の射精時のテ接吻ト
;-------------------------------------------------
;-------------------------------------------------
;■ 触手が射精
;-------------------------------------------------
IF TFLAG:15 >0
	CALL PALAM_MESSAGE_E
	;■ 屈服刻印Lv3
	IF MARK:2 == 3 && (CFLAG:240 < 2 || FLAG:7 == 2)
		;PRINTFORMW 
		;CFLAG:240 = 2
	;■ 非屈服刻印Lv3
	ELSEIF CFLAG:240 < 1 || FLAG:7 == 2
		;PRINTFORMW 
		;CFLAG:240 = 1
	ENDIF
ENDIF


;-------------------------------------------------
;@KOJO_MESSAGE_PALAMCNG_B関係（X1を性格タイプ番号に置換）
;奴隶の绝顶をトリガーにした口上
;-------------------------------------------------
@KOJO_MESSAGE_PALAMCNG_B_K1
;助手が调教した時に口上をスキップする（好みに応じて使う、行頭の;を消すとスキップするようになる）
;SIF ASSI > 0 && ASSIPLAY
;	RETURN 0 
;口塞球着用か触手口辱中、崩溃中は地の文を表示して口上をスキップする
IF (TEQUIP:20 && SELECTCOM != 107) || (TEQUIP:36 && SELECTCOM != 168) || TALENT:崩溃
	CALL PALAM_MESSAGE_B
	RETURN 0
ENDIF
;-------------------------------------------------
;奴隶绝顶時的文本
;-------------------------------------------------
;-------------------------------------------------
;■ 初次C绝顶
;-------------------------------------------------
IF NOWEX:0 > 0 && CFLAG:241 == 0
    ;■ 孩童尺寸的阴茎，通常是小孩子
	IF TALENT:孩童尺寸
		IF EXP:绝顶经验 == 0
			PRINTFORML %CALLNAME%初次射精，对这种陌生的感觉十分困惑
		ELSE
			PRINTFORML %CALLNAME%因为第一次在人前射精，羞得面红耳赤
		ENDIF
	ELSE
		IF !RAND:2
			PRINTFORML 「嗯、啊……嗯…！　唔……这、这只是生理现象…罢了……」
		ELSE
			PRINTFORML 「哈啊……嗯…！　唔……这不过是生、生理现象而已……」
		ENDIF
		IF TALENT:男性 && !TEQUIP:禁止射精
			PRINTFORML 尽管嘴上这么说，%CALLNAME%还是因为第一次在人前射精羞得面红耳赤
		ELSE
			PRINTFORML 尽管嘴上这么说，%CALLNAME%还是因为第一次在人前高潮羞得面红耳赤
		ENDIF
	ENDIF
	CFLAG:241 = 1
;-------------------------------------------------
;■ 初次V绝顶
;-------------------------------------------------
ELSEIF NOWEX:1 > 0 && CFLAG:242 == 0
	;CFLAG:242 = 1
;-------------------------------------------------
;■ 初次A绝顶
;-------------------------------------------------
ELSEIF NOWEX:2 > 0 && CFLAG:243 == 0
	IF !RAND:2
		PRINTFORML 「呜…唔啊啊啊…讨、讨厌…咿…啊啊啊啊啊啊—！」
	ELSE
		PRINTFORML 「呜…唔啊啊啊…不、不要…咿…啊啊啊啊啊啊—！」
	ENDIF
	PRINTFORMW %CALLNAME%因后穴的快感紧绷着身体迎来了高潮
	PRINTFORML 「怎、怎么会……这不可能……%CSTR:0%竟然……呜…！」
	PRINTFORML %CALLNAME%无法接受自己仅仅因后面就去了的事实，神情呆然地楞在那里
	CFLAG:243 = 1
;-------------------------------------------------
;■ 初次B绝顶
;-------------------------------------------------
ELSEIF NOWEX:3 > 0 && CFLAG:244 == 0 && MALE(TARGET)
	PRINTFORML 「呜、啊啊啊！　啊……啊…不、不可能……！」
	PRINTFORML 如同女人一般被玩弄着胸部高潮，%CALLNAME%一副快哭出来的样子…
	CFLAG:244 = 1
;-------------------------------------------------
;■ 禁止射精解除时射精
;-------------------------------------------------
ELSEIF SELECTCOM == 109 && TEQUIP:37 == 0 && TFLAG:10 > 0
	CALL PALAM_MESSAGE_A
;-------------------------------------------------
;■ 奴隶射精
;-------------------------------------------------
;射精している
ELSEIF TFLAG:10 > 0 && TFLAG:29 < 4 && !RAND:4
	CALL PALAM_MESSAGE_B
	;■ 屈服刻印Lv3
	IF MARK:2 == 3 && (CFLAG:246 < 2 || (FLAG:7 == 1 && !RAND:3) || FLAG:7 == 2)
		IF !RAND:4
			PRINTFORML 「咕、唔…呜……」
		ELSEIF !RAND:3
			IF !RAND:2
				PRINTFORML 「啊、不行了…要、要出来了……啊啊！！」
			ELSE
				PRINTFORML 「不、不行了…要、要去……啊啊啊！！」
			ENDIF
		ELSEIF !RAND:2
			PRINTFORML 「啊、呜…出、出来了——！！」
		ELSE
			IF !RAND:2
				PRINTFORML 「……啊……要…射……！」
			ELSE
				PRINTFORML 「……啊……要…射出来…了…啊……」
			ENDIF
		ENDIF
		CFLAG:246 = 2
	;■ 非屈服刻印Lv3
	ELSEIF CFLAG:246 < 1 || (FLAG:7 == 1 && !RAND:3) || FLAG:7 == 2
		IF !RAND:4
			PRINTFORML 「唔、啊…啊……」
		ELSEIF !RAND:3
			IF !RAND:2
				PRINTFORML 「啊、啊啊！…可、恶……」
			ELSE
				PRINTFORML 「啊、啊啊！…呜……」
			ENDIF
		ELSEIF !RAND:2
			PRINTFORML 「嗯…嗯、啊啊！…呜……」
		ELSE
			PRINTFORML 「不行、了…快…射……！」
		ENDIF
		CFLAG:246 = 1
	ENDIF
;-------------------------------------------------
;■ 空射精   什么都射不出来
;-------------------------------------------------
;空射精している
ELSEIF NOWEX:40
	CALL PALAM_MESSAGE_A2
	;■ 孩童尺寸
	IF TALENT:114
        PRINTFORML 「嗯、嗯…！不、不要…不要射了……已经不想、不——！」
        PRINTFORMW %CALLNAME%一边哭泣一边丢脸地摆着腰，豆芽一样小小的阴茎什么都没有吐出来。
        PRINTFORML 「射不出来，射出不来了，阴茎坏掉了，怎么办，呜呜……」
	ELSE
        PRINTFORML 「啊、啊啊…！不…不要、又要射了……不、已经…啊啊啊啊——！」
        PRINTFORMW %CALLNAME%的哭腔带着嘶哑，本能地一下一下向空气挺动着腰身，但肿胀的阴茎里什么都射不出来。
        PRINTFORML 「停下吧，放了%CSTR:0%，放了%CSTR:0%吧……什么都没有了，已经、不行了…」
        PRINTFORML %CALLNAME%无力地斜倒在床上，肮脏的液体粘的浑身都是，像狗一样吐着舌头喘息着。
	ENDIF
;-------------------------------------------------
;■ 绝顶
;-------------------------------------------------
ELSEIF TFLAG:29 > 0
	CALL PALAM_MESSAGE_B
	;■ 屈服刻印Lv3
	IF MARK:2 == 3
		;■ 非常强的绝顶
		IF TFLAG:29 >= 4
			IF !RAND:5
				PRINTFORML 「呜、呜…！　脑、脑子好、好奇怪……呜、不要…啊啊啊啊！！」
			ELSEIF !RAND:4
				IF !RAND:2
					PRINTFORML 「呀、啊…不行了…要、要死…要死掉了…呀、啊啊啊啊！！」
				ELSE
					PRINTFORML 「咿…不要、已经不行…死、死了…要死了…呀、啊啊啊啊啊！！」
				ENDIF
			ELSEIF !RAND:3
				PRINTFORML 「呜、呜…不行、拜托…呜、啊啊啊啊！！！」
			ELSEIF !RAND:2
				IF !RAND:2
					PRINTFORML 「呜…为、为什么…停不下来……呜、啊啊啊啊！！」
				ELSE
					PRINTFORML 「不、不要…快停下来……唔、啊啊啊啊啊！！」
				ENDIF
			ELSE
                PRINTFORML 「已经…慢、慢一点……这样下去、会疯掉……唔、啊啊啊啊啊！」
			ENDIF 
		ELSEIF CFLAG:245 < 2 || (FLAG:7 == 1 && !RAND:3) || FLAG:7 == 2
			IF !RAND:4
                PRINTFORML 「不……行…不行了…要去、去了…啊啊啊啊！」
			ELSEIF !RAND:3
				PRINTFORML 「呜…要射、要射了…唔、呀啊啊啊！！」
			ELSEIF !RAND:2
				IF !RAND:2
					PRINTFORML 「啊、啊嗯…不行、不行了…！　哈啊…去、去了——！」
				ELSE
					PRINTFORML 「啊…已经、极限了…！　唔…射、射了——！」
				ENDIF
			ELSE
				PRINTFORML 「啊…嗯、啊！ 不、不…呜、啊啊啊啊！！！」
			ENDIF
			CFLAG:245 = 2
		ENDIF
	;■ 非屈服刻印Lv3
	ELSEIF CFLAG:245 < 1 || (FLAG:7 == 1 && !RAND:3) || FLAG:7 == 2
		IF TFLAG:29 >= 5
			IF !RAND:2
				PRINTFORML 「咿……不…不行了…绝对、要杀了%CSTR:1%…！」
			ELSE
				PRINTFORML 「要……杀了、%CSTR:1%…绝对要杀了%CSTR:1%…！」
			ENDIF
		ELSE
			IF !RAND:3
				PRINTFORML 「可、恶…像、这种事……呜、啊啊啊啊！」
			ELSEIF !RAND:2
				PRINTFORML 「根、根本就不舒……呜、啊啊啊啊啊！」
			ELSE
                PRINTFORML 「呜……不、要…别看%CSTR:0%…啊、啊啊啊啊啊！」
			ENDIF
			CFLAG:245 = 1
		ENDIF
	ENDIF
ENDIF
;-------------------------------------------------
;■ 喷乳
;-------------------------------------------------
;喷乳している
IF TFLAG:11 > 0
	;■ 屈服刻印Lv3
	IF MARK:2 == 3 && (CFLAG:247 < 2 || FLAG:7 == 2)
		;PRINTFORMW 
		;CFLAG:247 = 2
	;■ 非屈服刻印Lv3
	ELSEIF CFLAG:247 < 1 || FLAG:7 == 2
		;PRINTFORMW 
		;CFLAG:247 = 1
	ENDIF
ENDIF

;-------------------------------------------------
;■ 放尿
;-------------------------------------------------
;放尿している
IF TFLAG:29 > 0 && (TEQUIP:27 || TALENT:57)
	;■ 屈服刻印Lv3
	IF MARK:2 == 3 && (CFLAG:248 < 2 || FLAG:7 == 2)
		;PRINTFORMW 
		;CFLAG:248 = 2
	;■ 非屈服刻印Lv3
	ELSEIF CFLAG:248 < 1 || FLAG:7 == 2
		;PRINTFORMW 
		;CFLAG:248 = 1
	ENDIF
ENDIF


;-------------------------------------------------
;@KOJO_MESSAGE_PALAMCNG_C関係（K1の1を性格タイプ番号に置換）
;调教後の変化をトリガーにした口上
;-------------------------------------------------
@KOJO_MESSAGE_PALAMCNG_C_K1
;助手が调教した時に口上をスキップする（好みに応じて使う、行頭の;を消すとスキップするようになる）
;SIF ASSI > 0 && ASSIPLAY
;	RETURN 0 
;口塞球着用か触手口辱中、崩溃中は地の文を表示して口上をスキップする
IF (TEQUIP:20 && SELECTCOM != 107) || (TEQUIP:36 && SELECTCOM != 168) || TALENT:崩溃
	CALL PALAM_MESSAGE_C
	RETURN 0
ENDIF
;-------------------------------------------------
;■ 处女喪失的场合
;-------------------------------------------------
IF TFLAG:3 > 0
	CALL PALAM_MESSAGE_C
	;■ 屈服刻印Lv3
	IF MARK:2 == 3
		;PRINTFORMW 
	;■ 非屈服刻印Lv3
	ELSE
		;PRINTFORMW 
	ENDIF
ENDIF

;-------------------------------------------------
;■ 童贞喪失的场合
;-------------------------------------------------
IF TFLAG:160 == 1
	CALL PALAM_MESSAGE_C
	;■ 恋慕系台詞(和主人做爱时丧失童贞)
	IF TFLAG:162 && (TALENT:3 || TALENT:6)
		;■ 逆强奸場合
		IF SELECTCOM == 211 || SELECTCOM == 212
            PRINTFORML 「%CSTR:0%、%CSTR:0%还是第一次…所以…稍微手下留情一点…」
		;■ 非逆强奸的場合
		ELSE
            PRINTFORML 「%CSTR:0%…那个，还、还是第一次…掌控不好力度的话先说声抱歉……」
		ENDIF
		;■ 主人处女喪失場合
		IF TFLAG:165
            PRINTFORMW 「欸…%MASTERNAME%也是第一次？…不会吧……哎、是真的？」
            PRINTFORMW 「…怎、怎么说呢……还真是高兴……嘿嘿」
            PRINTFORML 「…哇啊、糟糕。比起那个…不、不痛吗？」
		ENDIF
	ELSE
		;■ 屈服刻印Lv3
		IF MARK:2 == 3
		;■ 非屈服刻印Lv3　主人による童贞喪失のみ
		ELSEIF TFLAG:162
			PRINTFORML 「没想到初夜的对象会是%CSTR:1%，而且还是在这种地方…」
			PRINTFORML %CALLNAME%好像受到了不小的冲击
		ENDIF
	ENDIF
ENDIF

;-------------------------------------------------
;■ 后庭处子喪失的场合
;-------------------------------------------------
IF TFLAG:161 == 1
	CALL PALAM_MESSAGE_C
	;■ 屈服刻印Lv3
	IF MARK:2 == 3
		;PRINTFORMW 
	;■ 非屈服刻印Lv3
	ELSE
		;PRINTFORMW 
	ENDIF
ENDIF

;-------------------------------------------------
;@KOJO_MESSAGE_PALAMCNG_D関係（K1の1を性格タイプ番号に置換）
;パラメータ変化をトリガーにした口上
;-------------------------------------------------
@KOJO_MESSAGE_PALAMCNG_D_K1
;助手が调教した時に口上をスキップする（好みに応じて使う、行頭の;を消すとスキップするようになる）
;SIF ASSI > 0 && ASSIPLAY
;	RETURN 0 
;口塞球着用か触手口辱中、崩溃中は地の文を表示して口上をスキップする
IF (TEQUIP:20 && SELECTCOM != 107) || (TEQUIP:36 && SELECTCOM != 168) || TALENT:崩溃
	CALL PALAM_MESSAGE_D
	RETURN 0
ENDIF
;-------------------------------------------------
;パラメータ変化時のテ接吻ト
;-------------------------------------------------
;-------------------------------------------------
;■ 初次润滑超过Lv3
;-------------------------------------------------
P = PALAM:4 + UP:4
IF P > PALAMLV:3 && CFLAG:245 == 0
	CALL PALAM_MESSAGE_D
	;■ 使用润滑液的场合
	IF SELECTCOM == 180
		;PRINTFORMW 
	;■ 非屈服刻印Lv3
	ELSE
		;PRINTFORMW 
	ENDIF
	;CFLAG:245 = 1
ENDIF

;-------------------------------------------------
;■ 初次情欲超过Lv2
;-------------------------------------------------
P = PALAM:6 + UP:6
IF P > PALAMLV:2 && CFLAG:246 == 0
	CALL PALAM_MESSAGE_D
	;■ 使用媚药的场合
	IF SELECTCOM == 181
		;PRINTFORMW 
	;■ 非屈服刻印Lv3
	ELSE
		;PRINTFORMW 
	ENDIF
	;CFLAG:246 = 1
ENDIF

;-------------------------------------------------
;■ 初次屈服超过Lv3
;-------------------------------------------------
P = PALAM:7 + UP:7
IF P > PALAMLV:3 && CFLAG:247 == 0
	CALL PALAM_MESSAGE_D
	;PRINTFORMW 
	;CFLAG:247 = 1
ENDIF

;-------------------------------------------------
;■ 初次恭顺超过Lv3
;-------------------------------------------------
P = PALAM:5 + UP:5
IF P > PALAMLV:3 && CFLAG:248 == 0
	CALL PALAM_MESSAGE_D
	;PRINTFORMW 
	;CFLAG:248 = 1
ENDIF

;-------------------------------------------------
;■ 初次恐怖超过LV3
;-------------------------------------------------
P = PALAM:11 + UP:11
IF P > PALAMLV:3 && CFLAG:249 == 0
	CALL PALAM_MESSAGE_D
	;PRINTFORMW 
	;CFLAG:249 = 1
ENDIF


;-------------------------------------------------
;@KOJO_MESSAGE_MARKCNG関係（K1の1を性格タイプ番号に置換）
;刻印変動をトリガーにした口上
;刻印変動後に口上を発動します
;-------------------------------------------------
@KOJO_MESSAGE_MARKCNG_K1
;助手が调教した時に口上をスキップする（好みに応じて使う、行頭の;を消すとスキップするようになる）
;SIF ASSI > 0 && ASSIPLAY
;	RETURN 0 
;口塞球着用か触手口辱中、崩溃中は地の文を表示して口上をスキップする
IF (TEQUIP:20 && SELECTCOM != 107) || (TEQUIP:36 && SELECTCOM != 168) || TALENT:崩溃
	CALL MARK_MESSAGE
	RETURN 0
ENDIF
;--------------------------------------------------
;特定助手、主人時に特殊助手口上を呼び出す(Y1を特定助手、X1を主人の性格タイプ番号に置換）
;--------------------------------------------------
;IF NO:MASTER == Y1
;	CALL MESSAGE_MARKCNG_PY1_K1
;	RETURN 0
;ENDIF
;IF ASSI > 0 && ASSIPLAY
;	IF NO:ASSI == Y1
;		CALL MESSAGE_MARKCNG_PY1_K1
;		RETURN 0
;	ENDIF
;ENDIF
IF TFLAG:22 != 3 && TFLAG:23 != 3 && TFLAG:24 != 13 && TFLAG:24 != 23 && TFLAG:24 != 33 && TFLAG:21 != 3
	CALL MARK_MESSAGE
	RETURN 0
ENDIF
;-------------------------------------------------
;■ 刚获得痛苦刻印Lv3时的口上
;-------------------------------------------------
CALL MARK_MESSAGE_A
IF TFLAG:22 == 3
	;PRINTFORMW 
ENDIF

;-------------------------------------------------
;■ 刚获得快乐刻印Lv3时的口上
;-------------------------------------------------
CALL MARK_MESSAGE_P
IF TFLAG:23 == 3
	;PRINTFORMW 
ENDIF

;-------------------------------------------------
;■ 刚获得屈服刻印Lv3时的口上
;-------------------------------------------------
CALL MARK_MESSAGE_S
IF TFLAG:24 == 13 || TFLAG:24 == 23 || TFLAG:24 == 33
	;■ 因快乐而屈服
	IF TFLAG:24 == 13
		;PRINTFORMW 
	;■ 因恐怖和痛苦屈服
	ELSEIF TFLAG:24 == 23
		;PRINTFORMW 
	;■ 因屈服和恭顺而屈服
	ELSEIF TFLAG:24 == 33
		;PRINTFORMW 
	ENDIF
ENDIF

;-------------------------------------------------
;■ 刚获得反抗刻印Lv3时的口上
;-------------------------------------------------
CALL MARK_MESSAGE_R
IF TFLAG:21 == 3
	;PRINTFORMW 
ENDIF